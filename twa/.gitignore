# Von Bubblewrap generiertes Android-Projekt + Build-Artefakte — NICHT committen.
# Quelle der Wahrheit sind die Manifest-Dateien (prod + dev);
# `bubblewrap update` regeneriert den Rest.
*
!.gitignore
!twa-manifest.json
!twa-manifest.dev.json
!README.md
