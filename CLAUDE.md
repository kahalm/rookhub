# RookHub

Zentrales Webportal für schachrelevante Funktionen: PGN-Repertoire-Verwaltung, Turnierdaten, Benutzerprofile mit FIDE/ChessResults-Verlinkung, Freundeslisten, Puzzle-/Endless-/Kurs-Training, Wochenpost. Gehört zusammen mit dem **ChessResults Crawler** (`C:/git/chessresults_crawler`) und dem **Schach-Bot** (separates Repo) – bei Änderungen immer alle betroffenen Projekte berücksichtigen.

## ⚠️ Parallel-Arbeit: Agenten-Koordination (ZUERST LESEN)

Es gibt **zwei gleichwertige, funktionierende Arbeitskopien** des gesamten Stacks:

| Kopie | Pfad |
|-------|------|
| 1 (primär) | `/home/kahalm/claude/rookhubstack` |
| 2 | `/home/kahalm/claude/rookhubstack-2` |

**Damit sich zwei gleichzeitig laufende Agenten nicht ins Gehege kommen, gilt ein Lock-Protokoll. Jede Instanz führt das BEVOR sie zu arbeiten beginnt aus:**

1. **Lock prüfen/claimen** — Lock-Datei ist `<stack-root>/.agent-lock` (liegt im Stack-Root, **außerhalb** aller Git-Repos → wird nie committet).
   - Existiert `rookhubstack/.agent-lock` **nicht** → diese Kopie ist frei: Lock anlegen (Inhalt: Zeitstempel + kurze Aufgabenbeschreibung) und **hier** in `rookhubstack` arbeiten.
   - Existiert `rookhubstack/.agent-lock` schon → Kopie 1 ist belegt: **direkt nach `rookhubstack-2` wechseln**, dort dasselbe prüfen und `rookhubstack-2/.agent-lock` anlegen, und dort arbeiten.
   - Sind **beide** gelockt → nicht parallel weiterarbeiten; nachfragen (vermutlich Stale-Lock).
2. **Stale-Locks**: Ein Lock älter als ~24 h darf als verwaist betrachtet und überschrieben werden (Zeitstempel im Lock prüfen).
3. **Lock über den GANZEN Zyklus halten — NICHT direkt nach dem Push freigeben.** Der Lock gilt bis **Commit → Push → CI-Build GRÜN**. Erst wenn der eigene Push in GitHub Actions grün durchgelaufen ist (`gh run list`), den **eigenen** Lock entfernen (`rm <stack-root>/.agent-lock`). Grund: gibst du sofort nach dem Push frei, claimt ein anderer Agent dieselbe Kopie und pusht obendrauf, während dein Build noch läuft — scheitert dein Build, kannst du ihn nicht mehr sauber fixen, ohne fremde Arbeit zu treffen.

**⚠️ Der Lock schützt NUR innerhalb einer Kopie — beide Kopien pushen auf DASSELBE Remote (`master`).** Ein Lock in Kopie 1 hindert Kopie 2 NICHT am Pushen. Daraus folgen Pflichten bei JEDEM Push:
- **Unmittelbar vor dem Push**: `git fetch` + `git pull --rebase`. Kamen fremde Commits rein → **danach neu bauen UND Tests laufen lassen** (der fremde Stand kann deinen Code brechen — z. B. ein Feature, das über mehrere Dateien geht und nur halb gemergt ankam). Niemals blind auf „Already up to date" von vor den Edits vertrauen.
- **Nie auf einen roten `master` pushen und `master` nie rot hinterlassen.** Vor dem Push prüfen, ob origin/master baut (bei Zweifel: `gh run list` des letzten master-Runs ansehen). Ist master fremdverschuldet rot, erst mit dem anderen Agenten/Stand klären — nicht einfach obendrauf pushen (dein Build erbt die Rotfärbung).
- **Mehrdatei-Änderungen atomar committen** (alle zusammengehörigen Dateien in EINEM Commit) — nie einen Commit pushen, der auf noch nicht committete Symbole (DTO-Property, neue Methode) verweist. Genau so entsteht ein „Service nutzt X, DTO kennt X nicht"-Compile-Fehler auf master.
- **Nach dem eigenen Push den CI-Run beobachten** (`gh run list --workflow "Build & Push Docker Images"`). Rot → sofort fixen (Lock noch halten!), nicht liegen lassen.

Die beiden Kopien werden NICHT automatisch synchronisiert — jede committet/pusht für sich. Nach Merges ggf. per `git pull` abgleichen.

## Zusammenspiel der Projekte

```
RookHub Frontend (Angular :8085)
    |  /api/* via nginx proxy
RookHub API (.NET :5001)  -- Crawler__BaseUrl -->  Crawler API (.NET :8080)  -- crawl -->  chess-results.com
    |                                                   |
    v                                                   v
  rookhub DB (MariaDB)                            chessresults DB (MariaDB)
    \                                                 /
     '------> Elasticsearch :9200 <------------------'
                    |
              Kibana :5601
```

- **chessresults_crawler**: Backend-Crawler der Turnierdaten von chess-results.com extrahiert. Reine REST-API, kein Frontend. Eigene MariaDB-Datenbank `chessresults`.
- **RookHub** (dieses Projekt): Webportal mit Angular-Frontend + .NET API. Leitet Turnier-Anfragen als Proxy an den Crawler weiter. Eigene MariaDB-Datenbank `rookhub`.
- **Schach-Bot** (separates Repo): Discord-Bot, der Tagespuzzle-/Wochenpost-Embeds postet und Motivations-DMs schickt. Konsumiert RookHub-Webhooks + `GET /api/bot/player-progress/{discordId}` (HMAC-signiert).

### Kritische Abhängigkeiten zwischen den Projekten
- `Services/CrawlerProxyService.cs` – HTTP-Client zum Crawler, muss Crawler-Routen kennen
- `Controllers/TournamentProxyController.cs` – Mappt RookHub-Routen auf Crawler-Routen (RookHub-`/api/tournaments/crawl*` → Crawler-`/api/crawl*`)
- `Services/SchachBotWebhookService.cs` – HMAC-signierte Webhooks an den Bot (Tagespuzzle + Wochenpost-Progress)
- Crawler-Endpoint-Änderungen müssen in den beiden ersten Dateien nachgezogen werden
- Crawler-Response-Strukturen werden als `JsonElement` durchgereicht (kein festes DTO-Mapping)

## Tech Stack

| Komponente | Technologie | Version |
|-----------|-------------|---------|
| Backend Runtime | .NET | 9.0 |
| Web Framework | ASP.NET Core Web API | 9.0 |
| ORM | EF Core + Pomelo | 9.0.0 (Pomelo) / 9.0.5 (EF Design) |
| Datenbank | MariaDB | 11 |
| Auth | JWT Bearer + BCrypt.Net-Next | 9.0.5 / 4.2.0 |
| API Docs | Swashbuckle (Swagger) | 6.9.0 |
| Frontend | Angular | 20.3 |
| UI Library | Angular Material | 20.2.14 |
| Frontend Webserver | nginx (alpine) | latest |
| Logging | Serilog + Elasticsearch Sink | 9.0.0 / 10.0.0 |
| Log-Speicher | Elasticsearch | 8.17.0 |
| Log-Visualisierung | Kibana | 8.17.0 |
| Tests | xUnit + InMemory DB | - |

**Hinweis**: RookHub nutzt Swashbuckle 6.9.0 (nicht 10.x) wegen Kompatibilität mit .NET 9's OpenAPI-Namespace.

## REST API

### Auth (offen, kein JWT nötig)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| POST | `/api/auth/register` | Registrierung `{ username, email?, password }` — E-Mail optional (`null` erlaubt, Unique-Index toleriert NULL-Duplikate) |
| POST | `/api/auth/login` | Login, gibt JWT zurück (`rememberMe` → 30 Tage statt 1 Tag) |
| POST | `/api/auth/forgot-password` | „Passwort vergessen" `{ email }` — schickt (falls die Adresse zu einem aktiven Konto gehört) einen einmaligen Reset-Link (TTL 1 h) per Mail. Antwortet IMMER 200 (keine User-Enumeration). Versand via `PasswordResetService` + `IEmailSender` (SMTP/MailKit); ohne `Email:SmtpHost` wird die Mail nur geloggt. Link-Basis = `App:BaseUrl` |
| POST | `/api/auth/reset-password` | Neues Passwort setzen `{ token, newPassword }` — 204 bei Erfolg, 400 bei ungültigem/abgelaufenem/verbrauchtem Token. Token ist einmalig (`UsedAt`) |

### Profil (auth)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/profile` | Eigenes Profil |
| PUT | `/api/profile` | Profil bearbeiten |
| DELETE | `/api/profile/account` | Konto löschen (DSGVO: anonymisiert Identität+PII, behält Statistik) |
| GET | `/api/profile/{username}` | Öffentliches Profil (reduziertes `PublicProfileDto` ohne Klarnamen/ChessResultsId/Discord) |
| GET | `/api/profile/player-search?lastName=&firstName=` | Spielersuche (ChessResults + FIDE) |
| POST | `/api/profile/discord/link` | Discord verknüpfen via bot-signiertem Token `{ token }` (400 ungültig/abgelaufen, 409 Discord-ID schon vergeben) |
| DELETE | `/api/profile/discord` | Discord-Verknüpfung trennen |
| GET | `/api/profile/tokens` | Eigene API-Tokens (ohne Raw-Token) |
| POST | `/api/profile/tokens` | Neuen Token anlegen `{ name, expiresInDays?, scope? }` — Raw-Token nur einmalig im Response |
| DELETE | `/api/profile/tokens/{id}` | Token widerrufen |

### Freunde (auth)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/friends` | Freundesliste |
| GET | `/api/friends/requests` | Offene (eingehende) Anfragen |
| GET | `/api/friends/requests/sent` | Von mir gesendete, noch nicht angenommene (Pending) Anfragen — für „wartet auf Bestätigung" in der Freundesliste. Literal-Route vor `{...}` |
| POST | `/api/friends/request/{userId}` | Anfrage senden |
| POST | `/api/friends/accept/{friendshipId}` | Annehmen |
| POST | `/api/friends/decline/{friendshipId}` | Ablehnen |
| DELETE | `/api/friends/{friendshipId}` | Entfernen |
| GET | `/api/friends/search?q={query}` | User suchen (min. 2 Zeichen) |
| GET | `/api/friends/{userId}/stats` | Puzzle-Statistik eines Freundes (Vergleich „Du vs. Freund": Elo/Gelöst/Versuche/Genauigkeit/Serien + Themen-Aufschlüsselung). Nur zwischen akzeptierten Freunden (sonst 403); reused `PuzzleService.GetStatsAsync`/`GetBreakdownAsync` |
| GET | `/api/friends/{userId}/revenge` | „Revenge a Friend": Standard-Puzzles, an denen der Freund gescheitert ist und die er nie gelöst hat (`PuzzleService.GetUnsolvedFailuresAsync(targetId, viewerId)`, sortiert nach jüngstem Fehlversuch). Pro Puzzle `solvedByViewer` (hat der Aufrufer es schon gelöst → erledigte Revanche). Nur zwischen akzeptierten Freunden (sonst 403) |

### Puzzle-Challenges (auth) — „schick dieses Puzzle an Freunde"
Nach dem Lösen kann ein User ein konkretes Puzzle an **einen oder mehrere** Freunde schicken (Multi-Select im Solver-Menü, alle Modi außer Wochenpost). Die Challenge ist **polymorph**: `Source` (`Standard` = `Puzzles`-Tabelle, Standard/Endless; `Book` = `BookPuzzles`-Tabelle, Buch/Kurs/Tagespuzzle). Der Empfänger löst sie über den quellen-passenden Deep-Link (`/puzzles/:id?challengeId=…` bzw. `/puzzles/book/:id?challengeId=…`, meldet das Ergebnis nach dem Versuch via Resolve zurück), der Status (Pending→Solved/Failed) erscheint beim Absender. Logik in `ChallengeService` (nutzt `FriendService.AreFriendsAsync`); Existenz wird je Quelle geprüft (kein FK). Frontend: wiederverwendbare `ChallengeFriendsComponent`.

| Methode | Endpoint | Zweck |
|---------|----------|-------|
| POST | `/api/challenges` | Batch-Challenge anlegen `{ toUserIds[], puzzleId, source }` — antwortet `{ sent, skipped[] }` (übersprungene Empfänger mit Grund `self`/`not_friends`/`duplicate`); 404 nur wenn das Puzzle in der zur `source` passenden Tabelle fehlt |
| GET | `/api/challenges/incoming` | Offene eingehende Challenges (Posteingang) inkl. Absender + Puzzle-Rating |
| GET | `/api/challenges/outgoing` | Gesendete Challenges inkl. Ergebnis-Status + Lösezeit |
| GET | `/api/challenges/incoming/count` | Anzahl offener eingehender Challenges (Navbar-Badge) |
| GET | `/api/challenges/outgoing/pending-counts` | Pro Freund (Map `toUserId`→Count) die von mir geschickten, noch OFFENEN (Pending) Challenges — für die „Freund (n)"-Klammer im „An Freund schicken"-Menü. Nur Freunde mit n > 0. Literal-Route vor `{id}` |
| POST | `/api/challenges/{id}/resolve` | Ergebnis melden `{ solved, timeSpentSeconds }` — nur der Empfänger (403), 409 wenn schon aufgelöst |

### Revenge-Benachrichtigungen (auth) — Ziel-User über Revanche informieren
Geht ein Freund (Avenger) eines gescheiterten Puzzles eines Users (Target) im Revenge-Modus an, wird der Target informiert (gelöst ODER gescheitert). Frontend: `/puzzles/:id?revengeUserId=…` meldet das Ergebnis nach dem Versuch (fire-and-forget). `RevengeNotificationService` legt nur an, wenn die beiden befreundet sind UND der Target an dem Puzzle tatsächlich gescheitert ist.

| Methode | Endpoint | Zweck |
|---------|----------|-------|
| POST | `/api/revenge/result` | Revanche-Ergebnis melden `{ targetUserId, puzzleId, solved }` — legt Benachrichtigung an (still ignoriert, wenn keine Freunde / Target nie gescheitert) |
| GET | `/api/revenge/notifications` | Eigene Revanche-Benachrichtigungen (neueste zuerst) |
| GET | `/api/revenge/notifications/count` | Anzahl ungelesener (Navbar-Badge, kombiniert mit Challenges) |
| POST | `/api/revenge/notifications/seen` | Alle als gelesen markieren |

### Benachrichtigungen / Glocke (auth) — generischer In-App-Strom
Eine zentrale Navbar-Glocke mit „!"-Indikator. `Notifications`-Tabelle (`UserId`, `Type`, `DataJson` = i18n-Parameter, `Link`, `SeenAt?`), Text wird im Frontend über `notifications.type.<type>` lokalisiert. `NotificationService.CreateAsync` wird per fire-and-forget von den Domänen-Services aufgerufen. Trigger-Typen: `chessable_import_completed`/`_failed` (ChessableImportService), `friend_request_received`/`friend_request_accepted` (FriendService), `challenge_received`/`challenge_resolved` (ChallengeService), `revenge_performed` (RevengeNotificationService, Dual-Write). Frontend: `InAppNotificationService` + Glocke in der Navbar (löste den Freunde-Badge ab); 60-s-Poll für den Zähler; Browser-`NotificationService` (Web-Notification-API) bleibt separat für späteres Push. Mail/Push sind Phase 2/3.

| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/notifications?take=20` | Letzte Benachrichtigungen (neueste zuerst) |
| GET | `/api/notifications/history?page=&pageSize=` | Vollständige History (paginiert, neueste zuerst) + Gesamtzahl — für die `/notifications`-Seite |
| GET | `/api/notifications/count` | Anzahl ungelesener (Glocken-Badge) |
| POST | `/api/notifications/seen` | Alle als gelesen markieren (beim Öffnen der Glocke) |

### Direktnachrichten Admin↔User (auth)
Beide Seiten können eine Konversation **starten**: der Admin schreibt einem User, ODER der User kontaktiert von sich aus das Admin-Team. Danach beliebig oft hin und her (durchgehende Konversation). Ein „Thread" = alle `AdminMessages` mit derselben `UserId` (Nicht-Admin-Teilnehmer); Metadaten/Zuweisung in `MessageThreads` (1 Zeile je User). Jede neue Nachricht legt eine In-App-Benachrichtigung bei der Gegenseite an: Admin→User `admin_message_received` (Link `/messages`), User→Admin `user_message_received` an **alle** Admins (Link `/admin`). **Claim/Übernahme**: ein Admin kann einen Thread übernehmen (`ClaimedByAdminId`) — alle Admins sehen, wer welchen bearbeitet; eine Admin-Antwort auf einen offenen Thread übernimmt ihn automatisch. Read-Receipts getrennt je Seite (`SeenByUserAt`/`SeenByAdminAt`). Logik in `AdminMessageService`; User-Seite `/api/messages`, Admin-Seite `/api/admin/messages`. Frontend: User-Seite `/messages` (Navbar-Mail-Icon, immer sichtbar, mit Badge), Admin-Tab „Nachrichten" (Thread-Liste mit Claim-Status + Übernehmen/Freigeben).

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/messages` | Auth | Eigener Thread (chronologisch); leer, solange niemand schrieb |
| GET | `/api/messages/unread-count` | Auth | Ungelesene Admin-Nachrichten (Navbar-Badge) |
| POST | `/api/messages/reply` | Auth | User schreibt dem Admin-Team `{ body }` — startet die Konversation selbst oder antwortet (400 nur bei leerem Text) |
| POST | `/api/messages/seen` | Auth | Eigene Admin-Nachrichten als gelesen markieren |
| GET | `/api/admin/messages/threads` | Admin | Alle Konversationen (je User: letzte Nachricht, ungelesene User-Antworten, Claim-Status `ClaimedByAdminId`/`-Name`) |
| GET | `/api/admin/messages/unread-count` | Admin | Ungelesene User-Antworten über alle Threads (Tab-Badge) |
| GET | `/api/admin/messages/threads/{userId}` | Admin | Vollständiger Thread mit einem User |
| POST | `/api/admin/messages/threads/{userId}` | Admin | Schickt/antwortet dem User `{ body }` (legt Thread an + übernimmt offenen Thread automatisch; 404 wenn User fehlt) |
| POST | `/api/admin/messages/threads/{userId}/seen` | Admin | User-Antworten des Threads als gelesen markieren |
| POST | `/api/admin/messages/threads/{userId}/claim` | Admin | Thread übernehmen (Zuweisung an den aufrufenden Admin) |
| POST | `/api/admin/messages/threads/{userId}/release` | Admin | Thread wieder freigeben |

### Repertoires (auth)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/repertoires` | Alle eigenen Repertoires |
| POST | `/api/repertoires` | Neues Repertoire (`kind`: none/opening/middlegame/endgame) |
| GET | `/api/repertoires/{id}` | Repertoire mit Dateien |
| PUT | `/api/repertoires/{id}` | Metadaten ändern |
| DELETE | `/api/repertoires/{id}` | Löschen |
| POST | `/api/repertoires/{id}/files` | PGN hochladen (multipart, max 10 MB) |
| GET | `/api/repertoires/{id}/files/{fileId}` | PGN herunterladen |
| DELETE | `/api/repertoires/{id}/files/{fileId}` | Datei löschen |
| GET | `/api/repertoires/{id}/pgn` | Alle PGNs kombiniert |
| POST | `/api/repertoires/{id}/convert-to-course` | „Repertoire → Kurs umwandeln": legt aus dem kombinierten Repertoire-PGN einen persönlichen Kurs an (`CourseService.UploadPersonalCourseAsync`). Nur bei Puzzle-PGN im Chessable-Stil (FEN + Trainingsmarker); reines Eröffnungs-Repertoire → 400. Nur der Besitzer (verschiebt/löscht das Original) |
| POST | `/api/repertoires/{id}/share` | „Repertoire mit ausgewählten Personen teilen" (Batch) `{ recipientUserIds[] }` — nur der Besitzer; Empfänger müssen befreundet sein (Admin an alle). Antwort `{ shared, skipped[] }` (Gründe `self`/`not_found`/`not_friends`/`duplicate`); Notification `repertoire_shared`. Empfänger sehen/öffnen/downloaden/trainieren es (eigener SR-Fortschritt), können es NICHT bearbeiten/löschen/weiterteilen. 403 wenn nicht Besitzer |
| GET | `/api/repertoires/{id}/shares` | Mit welchen Nutzern ist dieses eigene Repertoire geteilt (für den Teilen-Dialog); 403 wenn nicht Besitzer |
| DELETE | `/api/repertoires/{id}/share/{recipientId}` | Freigabe für einen Empfänger zurücknehmen (idempotent); 403 wenn nicht Besitzer |
| GET | `/api/repertoires/reprocess/status` | Aufbereitungs-Status der eigenen Repertoires (heute meist 0; live ausgewertet). Literal-Route vor `{id}` |
| POST | `/api/repertoires/reprocess` | Markiert veraltete eigene Repertoires auf die aktuelle Pipeline-Version (heute No-op für abgeleitete Daten) |

### Extension API (auth, CORS für chess.com)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/extension/repertoires?kind=opening` | Leichtgewichtige Liste (id, name, fileCount, kind, totalSizeBytes); `kind` filtert auf `none|opening|middlegame|endgame`. Nur Repertoires mit `UseForExtension=true` (Default true, im Bearbeiten-Dialog abwählbar); gilt ebenso für das Positions-Set der Abweichungsanalyse (`RepertoireAnalyzeService`) |
| GET | `/api/extension/repertoires/{id}/pgn` | Kombinierter PGN-Text |
| POST | `/api/extension/training-activity` | Meldet ein Häppchen AKTIVER Chessable-Trainingszeit `{ secondsActive (1–3600), movesTrained? }` (von RepCheck auf chessable.com gemessen). Append-only → `ChessableActivities`; fließt in die Kategorie „Chessable" des Trainingsziele-Trackers. Zeitstempel serverseitig |
| POST | `/api/extension/remember-line` | Merkt eine auf chessable.com angezeigte Stellung `{ fen, courseId?, courseName?, sourceUrl? }` → `RememberedPositions` (append-only, Verwendungszweck offen). **Kursname**: die Extension liefert ihn (über den erfassten Chessable-Bearer aus der Chessable-API) mit; fehlt er, löst der Server ihn aus dem gespeicherten Bearer des Users auf — cache-first aus `ChessableCredential.CachedCoursesJson`, sonst best-effort Live-Abruf (`ChessableProxyService.GetCoursesAsync`). `GET /remembered-lines` trägt bei Alt-Einträgen ohne Namen den Cache-Namen nach |
| GET | `/api/extension/remembered-lines?take=200` | Gemerkte Stellungen des Users (neueste zuerst) |
| POST | `/api/extension/games` | Speichert die aktuell auf chess.com/lichess angeschaute Partie (Button „Partie speichern") `{ source, moves[], externalId?, white?, black?, result?, sourceUrl?, playedAt? }` → `SavedGames`. Server baut das PGN aus der SAN-Zugliste + Headern und vergibt ein `ShareToken`. Dedup über (UserId, Source, ExternalId). Sichtbar im Bereich „Partien" (`/api/games`) |

### Gespeicherte Partien (auth + öffentlicher Teilen-Link)
Bereich „Partien" (`/games`): zeigt die über die RepCheck-Extension von chess.com/lichess gespeicherten Partien. Nachspielen (PGN-Viewer-Dialog), „In Analyse öffnen" (PGN via Router-State an `/analysis`), Löschen, und Teilen über einen eindeutigen öffentlichen Link `/g/{shareToken}` (kein Login). Logik in `SavedGameService`; Menü-Key `games` (Default `Registered`).

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/games?take=200` | Auth | Eigene gespeicherte Partien (neueste zuerst, ohne PGN) |
| GET | `/api/games/shared/{token}` | AllowAnonymous | Öffentliche Sicht einer geteilten Partie inkl. PGN (ohne Besitzer-Daten). Literal-Route VOR `{id}` |
| GET | `/api/games/{id}` | Auth | Detail einer eigenen Partie inkl. PGN (Nachspielen/Analysieren) |
| DELETE | `/api/games/{id}` | Auth | Eigene Partie löschen |

Akzeptiert sowohl JWT (User-Login) als auch ApiToken (`Authorization: Bearer rkh_…`). Bei ApiToken muss `scope=extension` sein (sonst 403). Policy-Scheme im Auth-Stack routet das Bearer-Format automatisch zum passenden Handler.

CORS (`ExtensionPolicy`, nur für `ExtensionController`): erlaubt `https://www.chess.com`, `https://lichess.org`, `https://www.chessable.com`, `https://chessable.com` mit `GET`+`POST`, ohne `AllowCredentials` (Auth strikt über Bearer-Header). Gilt für den Userscript-`fetch`-Pfad; die Extension-Variante geht ohnehin CORS-frei über ihren Background-Worker. Die Default-CORS-Policy (Frontend) erlaubt `http://localhost:4200` + `http://localhost:8085`.

### Turnier-Proxy (auth, leitet an Crawler weiter)
| Methode | Endpoint | Crawler-Route |
|---------|----------|---------------|
| GET | `/api/tournaments` | `/api/tournaments` |
| GET | `/api/tournaments/{id}` | `/api/tournaments/{id}` |
| GET | `/api/tournaments/{id}/players?team=&sortBy=` | `/api/tournaments/{id}/players` |
| GET | `/api/tournaments/{id}/teams` | `/api/tournaments/{id}/teams` |
| GET | `/api/tournaments/{id}/pairings?round=` | `/api/tournaments/{id}/pairings` |
| GET | `/api/tournaments/{id}/players/{snr}/results` | `/api/tournaments/{id}/players/{snr}/results` |
| GET | `/api/tournaments/{id}/rounds/check` | `/api/tournaments/{id}/rounds/check` |
| POST | `/api/tournaments/crawl` | `/api/crawl` |
| POST | `/api/tournaments/crawl/player-details` | `/api/crawl/player-details` |

### Chessable-Integration (auth, leitet an piratechess-API weiter)
RookHub speichert nur den per-User Chessable-Bearer (AES-verschlüsselt via `EncryptionService` → `ChessableCredentials.EncryptedBearer`). Alle Chessable-HTTP-Calls (curl-impersonate gegen Cloudflare) liegen im piratechess-Stack; `ChessableProxyService` reicht den Bearer pro Request an `POST /api/chessable/direct/*` durch und authentifiziert sich mit dem `X-Service-Key`-Header (`Chessable:ServiceKey` ↔ piratechess `Service:ApiKey`). Netzwerk: externes Docker-Netz `chessable-bridge` (von piratechess_docker bereitgestellt). **Admin-Download „im Namen eines Users"**: `ChessableImport.BearerUserId` (nullable) entkoppelt Bearer-Quelle von Besitzer — der Service lädt den Bearer von `BearerUserId ?? UserId`. Admin-Import setzt `UserId`=Admin (Repertoire + Notification beim Admin), `BearerUserId`=Ziel-User; piratechess ist stateless, der gespeicherte Bearer des Ziel-Users genügt.

| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET | `/api/chessable/credentials` | Status + maskierter Bearer (`{ hasCredentials, maskedBearer }`) |
| POST | `/api/chessable/credentials` | Bearer setzen/überschreiben `{ bearer }` |
| DELETE | `/api/chessable/credentials` | Bearer löschen |
| POST | `/api/chessable/test` | Bearer-Validität + Kursanzahl (`{ uid, courseCount }`) |
| GET | `/api/chessable/courses` | Liste der Kurse des Users (`[{ bid, name }]`) |
| GET | `/api/chessable/admin/imports` | **Admin**: alle Importe ALLER User (Verlauf, max. 200, neueste zuerst) inkl. `username`/`createdAt`/`completedAt` + globaler Queue-Position |
| GET | `/api/chessable/admin/active` | **Admin**: nur aktive (laufende/pausierte) Importe aller User — fürs Dashboard-Widget |
| GET | `/api/chessable/admin/credentialed-users` | **Admin**: User mit hinterlegtem Bearer (Auswahl für „Kurse von Usern holen") |
| GET | `/api/chessable/admin/users/{userId}/courses?refresh=` | **Admin**: Kursliste eines Users (mit dessen Bearer; Import-Status gegen die eigenen Admin-Importe markiert) |
| POST | `/api/chessable/admin/users/{userId}/import/{bid}` | **Admin**: lädt Kurs `{bid}` eines Users ins EIGENE Admin-Konto — als Repertoire ODER Buch (`{ name?, target? }`; `target` "repertoire"/"book", Default "repertoire"). Import-Besitzer = Admin (`UserId`), Bearer vom Ziel-User (`BearerUserId`). 404 unbek. User, 400 wenn Ziel-User keinen Bearer hat / `target` ungültig |

### Turnier-Abos + Favoriten + Monitor (auth)
| Methode | Endpoint | Zweck |
|---------|----------|-------|
| GET/POST/DELETE | `/api/subscriptions[/{id}]` | Abonnierte Turniere verwalten |
| GET/POST/DELETE | `/api/tournament-favorites[/{id}]` | Favoriten verwalten |
| GET/POST | `/api/tournament-monitor[/{id}]` | Per-Turnier-User-Einstellungen + Runden-Monitor (Round-Watch, Auto-Subscribe) |

### Book-Puzzles (offen + Admin)
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/book-puzzles/{id}` | AllowAnonymous | Puzzle by ID |
| GET | `/api/book-puzzles/{id}/next` | AllowAnonymous | Nächstes Puzzle im selben Buch (Loop am Ende) |
| GET | `/api/book-puzzles/{id}/random` | AllowAnonymous | Zufälliges Puzzle aus demselben Buch |
| POST | `/api/book-puzzles/{id}/attempt` | Auth | Lösungsversuch erfassen `{ solved, timeSeconds }` (Tagespuzzle) |
| POST | `/api/book-puzzles/{id}/flag-hints` | Auth | Tipps als „dumm/schlecht" markieren/aufheben `{ flagged }` — jeder eingeloggte User (Review-Flag `BookPuzzle.HintsFlagged`; 404 wenn Puzzle fehlt) |
| POST | `/api/book-puzzles/{id}/attempt/anonymous` | Anon | Anonymer Versuch (Session-ID, je Session/Puzzle dedupliziert) |
| GET | `/api/book-puzzles/{id}/results?since=` | AllowAnonymous | Solver-Liste (je User, inkl. Discord) + Versuchs-/Lösungszähler + `anonymousSolvedCount`. Löser-Status: nur wer im **ersten** Versuch löste, gilt als Löser |
| POST | `/api/book-puzzles/{id}/track` | AllowAnonymous | „Track solves" eines per Link geteilten Puzzles: erfasst den **Erstversuch** des Besuchers (eingeloggt via Token, sonst `{ solved, sessionId }`) in `SharedPuzzleAttempts` (Unique `(BookPuzzleId, IdentityKey)` → nur 1. Versuch zählt; `solved=false` = Fehlzug/Aufgeben/Reset) und liefert `{ solved, failed }` |
| GET | `/api/book-puzzles/{id}/track-counts` | AllowAnonymous | Aktuelle „Track solves"-Zähler `{ solved, failed }` |
| GET | `/api/book-puzzles/daily/leaderboard?month=yyyy-MM` | AllowAnonymous | Monats-Wertung des Tagespuzzles (für den Bot): je User Punkte (10 je Erstversuch-Lösung + Tages-Rang-Bonus 5/3/1), `solved`, `golds`; absteigend nach Punkten. Default = laufender UTC-Monat. Literal-Route **vor** `daily/{date}` |
| GET | `/api/book-puzzles/daily/hall-of-fame?top=5` | AllowAnonymous | All-time-Bestenlisten: meiste gelöste Dailies, meiste 🥇 (Tage als schnellster Erstversuch-Löser), schnellste je gelöste Lösung. `top` 1–25 |
| GET | `/api/book-puzzles/daily/{date}` | AllowAnonymous | Tagespuzzle für UTC-Datum (`yyyyMMdd` oder `today`); legt on-demand eine persistierte Zuordnung in `DailyPuzzles` an — aber NUR für heute/gestern (ältere Daten: gespeicherte Zuordnung oder 404; verhindert anonyme Write-Amplification per Datums-Enumeration) |
| GET | `/api/book-puzzles/by-line-id?lineId=xxx` | AllowAnonymous | Lookup für schach-bot |
| GET | `/api/book-puzzles/books` | AllowAnonymous | Buch-Liste mit Counts |
| POST | `/api/admin/book-puzzles/import` | Admin | Bulk-Import aus JSON |
| POST | `/api/admin/book-puzzles/daily/{date}/regenerate` | Admin | Tagespuzzle eines UTC-Datums neu generieren: Datum/Link bleibt, bisheriges Puzzle wird `Retired=true` gesetzt (nie wieder in Daily/Random/Blind), neues aus dem forDaily-Pool zugeordnet |
| POST | `/api/admin/book-puzzles/{id}/regenerate-hints` | Admin | Tipps eines einzelnen Buch-Puzzles synchron (neu) generieren (force). 400 ohne `Anthropic:ApiKey`, 404 wenn Puzzle/keine Tipps; sonst die generierten Tipps |
| POST | `/api/admin/books/{bookId}/generate-hints?force=` | Admin | Tipps für ein ganzes Buch im Hintergrund erzeugen (Queue); `force` regeneriert auch vorhandene, sonst nur fehlende/veraltete. Antwort `{ queued }` |

### Gruppen (Admin + auth)
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/admin/groups` | Admin | Alle Gruppen inkl. MemberCount |
| POST | `/api/admin/groups` | Admin | Gruppe anlegen (name, description) |
| PUT | `/api/admin/groups/{id}` | Admin | Gruppe umbenennen / Beschreibung |
| DELETE | `/api/admin/groups/{id}` | Admin | Gruppe + Mitgliedschaften löschen |
| GET | `/api/admin/groups/{id}/members` | Admin | Mitglieder einer Gruppe |
| POST | `/api/admin/groups/{id}/members/{userId}` | Admin | User zur Gruppe hinzufügen (idempotent) |
| DELETE | `/api/admin/groups/{id}/members/{userId}` | Admin | User aus Gruppe entfernen |
| GET | `/api/admin/groups/{id}/training-goal` | Admin | Trainingsziel-Vorlage der Gruppe (Source "none" wenn keine) |
| PUT | `/api/admin/groups/{id}/training-goal` | Admin | Vorlage setzen/aktualisieren (PuzzleMinutes/BookMinutes 0–600, PlayGames 0–200 Partien/Woche, WeeklyDaysTarget 0–7) |
| DELETE | `/api/admin/groups/{id}/training-goal` | Admin | Vorlage entfernen |
| GET | `/api/my-groups` | Auth | Gruppen-Namen des eingeloggten Users (gruppenabhängige Anzeige) |

### Menü-Sichtbarkeit (Admin konfiguriert, je Nutzer aufgelöst)
Admin legt pro Menüeintrag eine Sichtbarkeitsstufe fest: `All` (jeder, auch anonym) / `Registered` (eingeloggt) / `Groups` (Mitglieder bestimmter Gruppen, Admins immer) / `Admin`. Defaults in `Services/MenuRegistry.cs` (bilden das bisherige Verhalten ab); nur Overrides landen in der DB. `MenuVisibilityService` löst die effektive Sichtbarkeit auf. Frontend: `MenuService` (Navbar-Snapshot + frischer Guard-Check) + `menuGuard('<key>')` sperrt auch den direkten URL-Aufruf. „courses" bleibt zusätzlich content-gegated (courseAccessGuard).

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/menu` | AllowAnonymous | Sichtbare Menü-Keys für den (ggf. anonymen) Aufrufer |
| GET | `/api/admin/menu` | Admin | Vollständige Konfiguration (Defaults + Overrides) |
| PUT | `/api/admin/menu` | Admin | Konfiguration setzen (Liste `{ key, level, groupIds }`; unbekannte Keys ignoriert) |

### Endless Puzzle Sync (auth + anon)
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/endless/progress` | Auth | Progress + Sessions laden (single call) |
| GET | `/api/endless/history?page=&pageSize=&archived=` | Auth | Paginierte Session-History (archived: bool-Filter) |
| GET | `/api/endless/sessions/{id}` | Auth | Lauf-Detail inkl. einzelner Puzzle-Versuche (History-Detailansicht) |
| PUT | `/api/endless/progress` | Auth | Config + Highscore + Active Game upsert |
| POST | `/api/endless/archive` | Auth | Sessions archivieren/unarchivieren |
| GET | `/api/endless/progress/anonymous?sessionId=` | Anon+RL | Anonymer Progress |
| PUT | `/api/endless/progress/anonymous` | Anon+RL | Anonymer Progress speichern |
| POST | `/api/endless/sessions` | Auth | Session aufzeichnen |
| POST | `/api/endless/sessions/anonymous` | Anon+RL | Anonyme Session aufzeichnen |
| POST | `/api/endless/sessions/bulk` | Auth | Bulk-Import (localStorage-Migration) |
| POST | `/api/endless/sessions/bulk/anonymous` | Anon+RL | Bulk-Import anonym |
| POST | `/api/endless/claim-session` | Auth | Anonyme Daten auf User übertragen |

### Kurse (auth, gruppen-/admin-gated)
„Kurse" = importierte Bücher, die ein User puzzleweise durcharbeitet. Fortschritt pro Buch (gelöste Puzzles / gesamt), geteilt über beide Modi; der Modus bestimmt nur die Reihenfolge. Alles user-bezogen in der DB. **Sichtbarkeit**: Admins sehen alle Bücher; Nicht-Admins nur Bücher, die einer ihrer Gruppen via `BookGroupAccess` freigegeben sind. Zugriff wird je Buch in jedem Endpoint erzwungen (kein Zugriff → 404).

Der `mode`-Parameter bei `/next` akzeptiert `sequential` (Buchreihenfolge, `after` = überspringen) oder `random` (zufällig, `exclude` vermeidet Wiederholung); `completed` wenn alle gelöst. **Random-Pool: jedes Puzzle nur EINMAL pro Durchgang** — neben den gelösten (CoursePuzzleResults) werden auch die seit dem letzten Reset GESCHEITERTEN ausgeschlossen (CourseAttempt mit `AttemptedAt >= CourseProgress.ResetAt`; `ResetAt==null` ⇒ alle bisherigen Versuche zählen). Erst `POST /reset` (rückt `ResetAt` vor + leert die gelöste Menge) bringt sie zurück. Im Solver-„abgeschlossen"-Panel gibt es dafür im Random-Modus einen „Von vorn"-Knopf. Sequential bleibt unverändert (nur gelöste raus).

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/courses` | Auth | Sichtbare Bücher als Kurse inkl. Fortschritt des Users (Admin: alle) |
| GET | `/api/courses/access` | Auth | `{ hasAccess }` — Basis für die Menü-Sichtbarkeit (Admin: true wenn Bücher existieren) |
| GET | `/api/courses/{bookId}/chapters` | Auth | Kapitel des Buchs in Lesereihenfolge inkl. Fortschritt je Kapitel (`index`/`name`/`puzzleCount`/`solvedCount`/`progressPercent`); `name=null` = Sammel-„ohne Kapitel" |
| GET | `/api/courses/{bookId}/next?mode=&after=&exclude=&chapterIndex=` | Auth | Nächstes ungelöstes Puzzle (siehe `mode` oben); mit `chapterIndex` auf das Kapitel beschränkt (Pool + Fortschritt) |
| POST | `/api/courses/{bookId}/results` | Auth | Lösungsversuch aufzeichnen (idempotent); validiert Puzzle↔Buch |
| GET | `/api/courses/{bookId}/puzzles` | Auth | Alle Puzzles eines (zugänglichen) Buchs am Stück — für Offline-Speichern |
| GET | `/api/courses/stats` | Auth | Aggregierte Kurs-Puzzle-Statistik des Users (TotalAttempts/Solved/Accuracy/Streaks; **ohne Elo** — Kurs-Puzzles haben kein User-Elo). Quelle: `CourseAttempt`. Literal-Route vor `{bookId}` |
| GET | `/api/courses/history?page=&pageSize=` | Auth | Paginierte Kurs-Versuchs-History (neueste zuerst) inkl. Buch-Puzzle-Infos (LineId/Title/BookRating/Difficulty). Literal-Route vor `{bookId}` |
| GET | `/api/courses/stats/breakdown` | Auth | Aufschlüsselung der Kurs-Versuche nach Tag/Thema (aus `BookPuzzle.Tags`), Rating-Band (aus `BookPuzzle.BookRating`) und Aktivität (`PuzzleBreakdownDto`). Literal-Route vor `{bookId}` |
| POST | `/api/courses/{bookId}/reset` | Auth | Fortschritt des Kurses zurücksetzen |
| POST | `/api/courses/{bookId}/convert-to-repertoire` | Auth | „Kurs → Repertoire umwandeln": legt aus dem Kurs-PGN (`CourseService.ConvertToRepertoireAsync` → `RepertoireService.CreateFromPgnAsync`, `UseForExtension=false`) ein neues Repertoire an; Original-Kurs bleibt. Zugriff wie andere Kurs-Endpoints (kein Zugriff → 404) |
| GET | `/api/courses/reprocess/status` | Auth | Aufbereitungs-Status der verwaltbaren Kurse (Admin: alle; sonst eigene): `{ currentVersion, total, stale, reprocessableLocally, refetchable, needsReimport }` — Basis fürs „Aktualisieren (N)"-Banner. Literal-Route vor `{bookId}` |
| POST | `/api/courses/reprocess` | Auth | Bereitet alle veralteten verwaltbaren Kurse neu auf: lokal in-place aus `Book.SourcePgn` (Fortschritt/IDs bleiben), Chessable-Altbestand ohne Quelle wird als Re-Fetch-Job eingereiht; sonst übersprungen. Antwort `{ reprocessed, updatedLines, enqueued, skipped }` |
| POST | `/api/courses/{bookId}/share` | Auth | „Kurs mit ausgewählten Personen teilen" (Batch) `{ recipientUserIds[] }` — nur der Besitzer eines persönlichen Kurses; Empfänger müssen befreundet sein (Admin an alle). Antwort `{ shared, skipped[] }` (übersprungen mit Grund `self`/`not_found`/`not_friends`/`duplicate`); legt je neuem Empfänger die Notification `course_shared` an. 403 wenn nicht Besitzer |
| GET | `/api/courses/{bookId}/shares` | Auth | Mit welchen Nutzern ist dieser eigene Kurs geteilt (für den Teilen-Dialog); 403 wenn nicht Besitzer |
| DELETE | `/api/courses/{bookId}/share/{recipientId}` | Auth | Freigabe des eigenen Kurses für einen Empfänger zurücknehmen (idempotent); 403 wenn nicht Besitzer |
| POST | `/api/courses/{bookId}/link` | Auth | Kurs mit einem anderen (zugänglichen) Kurs verknüpfen (Buch↔Workbook) `{ linkedBookId }` — persönlich, symmetrisch, je Buch max. 1 Partner (ersetzt bestehende). 400 self-link, 404 unzugänglich |
| GET | `/api/courses/{bookId}/link` | Auth | Aktuell verknüpfter Partner-Kurs `{ linkedBookId, linkedDisplayName }` (leer wenn keiner) — für den Schnellwechsel im Solver. Literal-Route |
| DELETE | `/api/courses/{bookId}/link` | Auth | Verknüpfung dieses Kurses lösen (beide Richtungen, idempotent) |

Buch↔Gruppe-Freigabe verwaltet der Admin:
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/admin/books/{id}/groups` | Admin | Gruppen-Ids mit Kurs-Zugriff auf das Buch |
| PUT | `/api/admin/books/{id}/groups` | Admin | Vollständige Gruppen-Freigabe setzen (ersetzt; ungültige Ids ignoriert) |

### Wochenpost (öffentlich lesbar, durchspielbar mit Login, Admin verwaltet)
Bildet die wöchentlichen schach-bot-Posts auf RookHub ab: ein PGN + Termin (Datum + Uhrzeit). PGN-Validierung via `RepertoireService.LooksLikePgn`. Puzzles werden on-the-fly aus dem PGN geparst (`PgnImportService.ParsePgn`) — Progress ist index-basiert.

**Per-User-Fortschritt**: idempotenter erster Versuch je `(WeeklyPostId, UserId, PuzzleIndex)`. „Erledigt" = **alle Puzzles gespielt** (gelöst egal). Aufgeben und Reset nach mindestens einem Zug zählen als ✗. Nach jedem **neuen** Versuch fire-and-forget Webhook (`SchachBotWebhookService.NotifyWeeklyAsync`, HMAC-signiert) an den Bot → Discord-Embed mit Live-Bestenliste.

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/weekly-posts` | AllowAnonymous | Liste (ohne PGN), nach Termin absteigend |
| GET | `/api/weekly-posts/progress` | Authorize | Batch-Fortschritt für die Übersicht (`List<WeeklyPostProgressDto>`, nur Posts mit Versuchen) — literal-Route MUSS vor `{id}` stehen |
| GET | `/api/weekly-posts/{id}` | AllowAnonymous | Detail inkl. PGN |
| GET | `/api/weekly-posts/{id}/puzzles` | AllowAnonymous | Puzzle-Sequenz zum Durchspielen |
| POST | `/api/weekly-posts/{id}/attempt` | Authorize | Versuch erfassen `{ puzzleIndex, solved, timeSeconds }` (idempotent je Index) |
| GET | `/api/weekly-posts/{id}/progress` | Authorize | Eigener Fortschritt `{ total, playedCount, solvedCount, totalSeconds, playedIndices[], completed }` |
| GET | `/api/weekly-posts/{id}/results` | AllowAnonymous | Bestenliste (alle Spieler mit ≥1 Versuch): `playedCount`, `solvedCount`, `totalSeconds`, `completed`; Sortierung erledigt→gelöst→Name |
| POST | `/api/admin/weekly-posts` | Admin | Upload (multipart: file + scheduledAt + optional title) |
| PUT | `/api/admin/weekly-posts/{id}` | Admin | Termin/Titel ändern |
| DELETE | `/api/admin/weekly-posts/{id}` | Admin | Löschen |

### Bot-Stats (Bot-intern, HMAC-signiert)
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/bot/player-progress/{discordId}` | AllowAnonymous + HMAC | Heutiger Trainingsziel-Fortschritt + Puzzle-Stats + jüngster Wochenpost-Status für eine verknüpfte Discord-ID. Signaturheader `X-Bot-Signature: sha256=…` mit `SchachBot:StatsSecret` (== Bot-`ROOKHUB_STATS_SECRET`); 401 bei falscher Signatur, 404 bei nicht verknüpfter Discord-ID |

### Client-Diagnostik (offen)
| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| POST | `/api/client-log` | AllowAnonymous + RL | Client-seitiges Diagnose-Event `{ kind, detail?, url? }` (v. a. Browser-Engine-Crash/Hänger) — wird strukturiert mit Marker „ClientLog" geloggt (→ ES/Kibana), nichts in der DB. `heartbeat*`-Kinds auf Information, sonst Warning. Frontend: `ClientLogService` (gedrosselt), Engine-Services melden via `reportEngineEvent`-Hook |

### Bestenlisten (auth)
Ranglisten über vier Kategorien je Periode (`weekly`/`monthly`/`alltime`, UTC-Grenzen). `weekly`/`monthly` sind **rollierende Fenster** = die letzten **7** bzw. **31** Tage (taggenau inkl. heute, `WindowStart` = `today.AddDays(-6)`/`-30`), NICHT Kalenderwoche/-monat. Nur eingeloggte Nutzer (Menü-Key `leaderboards`, Stufe `Registered`); anonyme Versuche (`UserId == null`) zählen nicht. Logik in `LeaderboardService` (rein lesend, keine neue Tabelle). Kategorien: **Puzzles** = einzigartige gelöste Standard-Puzzles (distinct `PuzzleAttempts.PuzzleId` mit `Solved`, im Fenster), **DailyPuzzles** = einzigartige gelöste Tagespuzzles (gelöste `BookPuzzleAttempts`, deren `BookPuzzleId` in `DailyPuzzles` vorkommt, distinct), **EndlessRuns** = abgeschlossene `EndlessSessions` (je Lauf), **CourseLines** = gelöste Kurs-Linien (`CoursePuzzleResults`, idempotent = einzigartig). Sortierung Count desc → Name asc; jeder Eintrag trägt seinen echten 1-basierten `rank` + ein `isMe`-Flag. Geliefert wird je Kategorie nur **Top-`top`** (1–500, Default **5**) **PLUS das Fenster ±`around`** (0–25, Default **2**) um den eigenen Platz — die Liste kann also eine Lücke zwischen Top-Block und eigenem Fenster haben. Frontend: `/leaderboards` (Perioden-Umschalter + 4 Karten; eigene Zeile hervorgehoben, „⋯"-Trenner bei Lücke).

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/leaderboards?period=&top=&around=` | Auth | Alle vier Bestenlisten für die Periode (`{ period, puzzles[], dailyPuzzles[], endlessRuns[], courseLines[] }`, je Eintrag `{ name, discordId?, discordUsername?, count, rank, isMe }`). Je Kategorie nur Top-`top` (Default 5) + Fenster ±`around` (Default 2) um den eigenen Platz |

### Trainingsziele (auth)
Tagesziele Puzzles/Buch-Kurs/**Chessable** (in Minuten) + wöchentliches Spielen-Ziel (Anzahl Rapid-/Classical-Partien pro ISO-Woche) + Wochenziel (volle Tage); effektives Ziel = persönlicher Override > zuletzt aktualisierte Gruppen-Vorlage > keins. Tracker aggregiert je UTC-Tag die verbrachte Zeit (Pro-Einzelpuzzle-Clamp 1800 s, Chessable-Häppchen-Clamp 3600 s) für Puzzles/Buch/Chessable + die Partienzahl für Spielen und markiert Tage none/partial/full (**Tagesstatus aus Puzzles + Buch + Chessable** — Spielen ist ein Wochenziel). Kategorien-Quellen: Puzzles = PuzzleAttempt + EndlessSession + BookPuzzleAttempt + **CourseAttempt aus Büchern der Art Puzzle**; Buch/Kurs = **CourseAttempt aus Büchern der Art Study** (`Book.Kind` steuert das Routing; **jeder** Kurs-Versuch zählt, nicht nur die Erstlösung); **Chessable = ChessableActivity** (aktive Trainingszeit, von der RepCheck-Extension via `POST /api/extension/training-activity` gemeldet). Logik in `TrainingGoalService`; Admin-Vorlage je Gruppe siehe Gruppen-Tabelle.

**Manuelle Offline-Aktivitäten** (selbst gemeldet, korrigierbar): `ManualActivities` (`/api/training-goals/manual` GET/POST/PUT/DELETE) speist **dieselben bestehenden Kategorien** — kein neues Ziel-Feld. Mapping je `ManualActivityKind`: **OtbGame** → Spielen (+Amount Partien/Tag, Cap 50), **OfflinePuzzle** → Puzzles (Amount Min), **OfflineStudy** + **Coaching** → Buch/Kurs (Amount Min); Minuten-Arten via `PerSessionCapSeconds` (4 h) gedeckelt. Tage mit ≥1 manuellem Eintrag liefern `TrackerDayDto.HasManual=true` (Tracker-Marker „manuell").

Spielen-Tracking: `PlayTimeService` (typed HttpClient) holt Lichess exakt (createdAt/lastMoveAt) + chess.com Best-Effort (PGN-Header UTCDate/UTCTime↔EndDate/EndTime) öffentlich ohne Login; `PlayTimeSyncService` (BackgroundService, `PlayTime:IntervalHours`=6) + manueller `/sync-play`-Button. Gezählt: Lichess `speed` rapid+classical, chess.com `time_class` rapid (keine eigene classical-Live-Klasse); Bullet/Blitz/Korrespondenz zählen nicht.

| Methode | Endpoint | Auth | Zweck |
|---------|----------|------|-------|
| GET | `/api/training-goals` | Auth | Effektives Ziel (`source` personal/group/none, ggf. `groupName`) |
| PUT | `/api/training-goals` | Auth | Persönlichen Override setzen (PuzzleMinutes/BookMinutes 0–600, PlayGames 0–200 Partien/Woche, WeeklyDaysTarget 0–7) |
| DELETE | `/api/training-goals` | Auth | Override entfernen → Rückfall auf Gruppen-Vorlage |
| GET | `/api/training-goals/today` | Auth | Heutiger Fortschritt Puzzles/Buch (Tag) + Spielen-Partien (Woche) + Tagesstatus + Wochenstand (X/Y Tage) |
| GET | `/api/training-goals/tracker?weeks=27` | Auth | Tagesreihe (nur Tage mit Aktivität) für die Tracker-Heatmap; je Tag auch PlayGames (informativ) |
| GET | `/api/training-goals/daily-series` | Auth | Vollständige Tagesreihe (ganze Historie, **ungedeckelt** durch das 53-Wochen-Fenster), je Tag bySource+byTheme — Basis für die client-seitig umschaltbare Perioden-Aufschlüsselung (Tag/Woche/Monat/Jahr/Gesamt mit Durchschalten) |
| POST | `/api/training-goals/sync-play` | Auth | Gespielte Rapid-/Classical-Partien (Lichess/chess.com) des eigenen Users sofort synchronisieren |
| GET | `/api/training-goals/manual?take=200` | Auth | Eigene manuell eingetragene Offline-Aktivitäten (neueste zuerst) |
| POST | `/api/training-goals/manual` | Auth | Manuelle Offline-Aktivität anlegen `{ date (yyyy-MM-dd, nicht Zukunft), kind, amount, note? }` — `kind` ∈ OtbGame/OfflinePuzzle/OfflineStudy/Coaching; `amount` = Partienzahl (OtbGame, 1–50) bzw. Minuten (sonst, 1–600), serverseitig geklemmt. 400 bei ungültigem/Zukunfts-Datum |
| PUT | `/api/training-goals/manual/{id}` | Auth | Eigene manuelle Aktivität ändern (404 wenn nicht vorhanden/nicht eigene) |
| DELETE | `/api/training-goals/manual/{id}` | Auth | Eigene manuelle Aktivität löschen (404 wenn nicht vorhanden/nicht eigene) |

## Datenbank-Schema (eigene DB `rookhub`, nicht geteilt mit Crawler)

| Tabelle | Zweck | Wichtige Felder / Constraints |
|---------|-------|-------------------------------|
| AppUsers | Auth | Username (unique), Email (unique, **nullable**), PasswordHash, CreatedAt |
| UserProfiles | Schach-Identität | UserId (1:1 zu AppUser), FideId, ChessResultsId, ChessComUsername, LichessUsername, DisplayName, DiscordId (unique, nullable) + DiscordUsername |
| Friendships | Freundesliste | RequesterId, AddresseeId (unique pair), Status (Pending/Accepted/Declined) |
| PuzzleChallenges | Puzzle an Freund(e) schicken | FromUserId, ToUserId (beide Restrict-FK auf AppUser), **Source (Enum Standard/Book)** + PuzzleId (polymorph, **kein FK** — je nach Source `Puzzles.Id` oder `BookPuzzles.Id`), Status (Pending/Solved/Failed), CreatedAt, ResolvedAt?, TimeSpentSeconds?; Index (ToUserId, Status) + (FromUserId) + (Source, PuzzleId) |
| RevengeNotifications | Revanche an gescheitertem Puzzle | AvengerUserId, TargetUserId, PuzzleId (alle Restrict), Solved, CreatedAt, SeenAt?; Index (TargetUserId, SeenAt) |
| Repertoires | PGN-Sammlungen | UserId, Name, Description, Kind (Enum None/Opening/Middlegame/Endgame), IsPublic, CreatedAt, UpdatedAt, **ImportVersion (Pipeline-Version; < CurrentVersion ⇒ veraltet/reprozessierbar — heute meist No-op, da live ausgewertet)** |
| RepertoireFiles | Einzelne PGNs | RepertoireId, FileName, PgnContent (LONGTEXT), FileSize |
| TournamentSubscriptions | Turnier-Abo | UserId + CrawlerTournamentId (unique pair), TournamentName, EventDate (`DateOnly?`, Turniertermin — steuert Refresh-Crawl + Bot-Turnier-Einordnung) |
| TournamentFavorites | Markierte Turniere | UserId + CrawlerTournamentId |
| TournamentUserSettings | Per-Turnier-User-Einstellungen | UserId + TournamentId, Highlights/Notes/Pinning |
| TournamentMonitors | Runden-Monitor | TournamentId, RoundsCount, LastSeenRound, AutoSubscribed; `RoundMonitorService` checkt periodisch |
| Puzzles + PuzzleAttempts | Standard-Puzzle-Pool + Versuche | klassische Lichess-Puzzles + Pro-User-Versuche (UserId Cascade) |
| Tags + PuzzleTags | Normalisierte Puzzle-Themen für schnellen Themen-Filter | Tag.Name (unique); PuzzleTag composite PK (PuzzleId, TagId) + denormalisiertes Rating, Index **(TagId, Rating)** → indexgestützter Themen-Filter statt LIKE-Scan. Import pflegt automatisch; **einmaliger Backfill bestehender Puzzles via `POST /api/admin/puzzles/backfill-tags`** (Hintergrund-Job). Bis Backfill: Fallback auf LIKE |
| BookPuzzles | Buch-Puzzles | LineId (unique), BookFileName (indexed), Round, Fen, Moves, Title, Chapter, Comment, **MoveComments (LONGTEXT, JSON `{plyIndex:text}`; Pro-Zug-Kommentare der Hauptlinie, Schlüssel = 0-basierter Halbzug NACH dem Zug, -1 = Einleitung; beim Durchspielen/Review angezeigt)**, Difficulty, BookRating, Tags, **HintsJson (LONGTEXT, JSON `{lang:[h1,h2,h3]}`; vorberechnete gestufte Tipps de/en/hr, per LLM erzeugt) + HintsVersion (int, 0=keine; entkoppelt von Book.ImportVersion) + HintsFlagged (bool; Admin-Review-Flag „dumme Tipps", per Solver-Button)**, **Retired (indexed; ausgemustert → nicht mehr in Daily/Random/Blind-Pools)** |
| SharedPuzzleAttempts | „Track solves" geteilter Einzel-Puzzles (opt-in per Teilen-Link `?track=1`) — Erstversuch je Besucher | BookPuzzleId (indexed), **IdentityKey** (`u:{userId}` eingeloggt / `s:{sessionId}` anonym), Solved (true nur saubere Erstlösung; Fehlzug/Aufgeben/Reset = false), **HintsUsed (höchste angesehene Tipp-Stufe 0–3 beim Erstversuch)**, CreatedAt; **UNIQUE (BookPuzzleId, IdentityKey)** = nur 1. Versuch zählt. Kein harter FK (Index genügt) |
| BookPuzzleAttempts | Buch-/Tagespuzzle-Versuche | BookPuzzleId (Restrict) + UserId (Cascade, nullable für Anon) + AnonymousSessionId, Solved, TimeSeconds, AttemptedAt, **HintsUsed (höchste angesehene Tipp-Stufe 0–3)**; Index (BookPuzzleId, AttemptedAt) + (BookPuzzleId, UserId) + **UNIQUE (BookPuzzleId, AnonymousSessionId)** (eine anonyme Lösung je Session; auth. Versuche = NULL-Session → mehrfach erlaubt) |
| Books | Buch-Metadaten | FileName (unique), Title, Author, **Kind** (Enum Puzzle/Study, Default Puzzle; steuert das Trainingsziel-Routing der Kurszeit), **SourcePgn (LONGTEXT, nullable; Roh-PGN als Reprocessing-Quelle, null bei Altbestand/JSON-Import)**, **ImportVersion (Pipeline-Version; < CurrentVersion ⇒ veraltet → Reprocess-Knopf)** |
| DailyPuzzles | Persistierte Tagespuzzle-Zuordnung je UTC-Datum | Date (PK, DATE), BookPuzzleId (Restrict), CreatedAt; vom `DailyPuzzleScheduler` (00:00 UTC) gesetzt oder on-demand bei `/daily/{date}` (nur heute/gestern); Admin-Regenerate ändert nur `BookPuzzleId` (Datum bleibt) |
| Groups | Benutzergruppen | Name (unique), Description, CreatedAt |
| UserGroups | User<->Gruppe (n:m) | Composite PK (UserId, GroupId), Cascade von AppUser + Group |
| EndlessProgresses | Endless Config+Highscore | UserId (unique, nullable), AnonymousSessionId, StartElo, Themes, FasttrackThreshold1/2, StockfishDepth, Highscore, ActiveGameState (LONGTEXT) |
| EndlessSessions | Abgeschlossene Endless Sessions | UserId (nullable), AnonymousSessionId, Timestamp, TotalSolved, MaxRating, DurationSeconds, ConfigJson (TEXT), MistakeAtRatings |
| CourseProgresses | Per-Kurs-Zustand (Buch) | UserId + BookId (unique pair), LastMode ("sequential"/"random"), CreatedAt, UpdatedAt |
| CoursePuzzleResults | Gelöste Buch-Puzzles im Kurs (idempotente „gelöst"-Menge für Fortschritt) | UserId + BookPuzzleId (unique pair), BookId (denormalisiert, indexed mit UserId), SolvedAt, TimeSeconds (nur Erstlösung; **nicht mehr Aggregations-Quelle**) |
| CourseAttempts | Append-only Zeit-Log JEDES Kurs-Versuchs (gelöst/fehlgeschlagen/Wiederholung) für die akkumulierte Kurs-/Studienzeit im Trainingsziele-Tracker | UserId (Cascade) + BookId (denormalisiert für Kind-Join, Cascade) + BookPuzzleId (Restrict), Solved, TimeSeconds, AttemptedAt, **HintsUsed (höchste angesehene Tipp-Stufe 0–3)**; Index (UserId, AttemptedAt) |
| BookGroupAccesses | Welche Gruppe darf welches Buch als Kurs sehen | Composite PK (BookId, GroupId), Cascade von Book + Group, Index GroupId |
| CourseShares | Persönlichen Kurs (Book.OwnerUserId) person-zu-person mit ausgewählten Nutzern teilen (Empfänger sieht/löst mit eigenem Fortschritt, kann nicht verwalten) | BookId (Cascade von Book), OwnerId + RecipientId (beide Restrict-FK auf AppUser, analog Friendship — vermeidet doppelte Cascade-Pfade), SharedAt; **UNIQUE (BookId, RecipientId)** + Index (RecipientId). Nur mit Freunden teilbar (Admins an alle); DeleteBook räumt Freigaben explizit ab |
| CourseLinks | Persönliche Kurs-Verknüpfung (Buch↔Workbook) für den Schnellwechsel — SYMMETRISCH in 2 Zeilen (A→B, B→A) | UserId (Cascade), BookId (Cascade von Book), LinkedBookId (**kein FK** → vermeidet 2. Cascade-Pfad von Book; Gegenzeile + DeleteBook-Cleanup halten Konsistenz), CreatedAt; **UNIQUE (UserId, BookId)** = je Buch max. 1 Partner. Beide Kurse müssen zugänglich sein; DeleteBook räumt beide Richtungen ab |
| RepertoireShares | Persönliches Repertoire person-zu-person teilen (Empfänger sieht/öffnet/downloadet/trainiert mit eigenem SR-Fortschritt, kann nicht bearbeiten/löschen/weiterteilen) | RepertoireId (Cascade von Repertoire), OwnerId + RecipientId (beide Restrict-FK auf AppUser, analog CourseShare), SharedAt; **UNIQUE (RepertoireId, RecipientId)** + Index (RecipientId). Nur mit Freunden teilbar (Admins an alle); RepertoireService.DeleteAsync räumt Freigaben explizit ab. Training-Zugriff via `RepertoireTrainingService.CanTrainAsync` (Besitzer ODER Empfänger); Repertoire-SR-Intervall-Override bleibt owner-only |
| WeeklyPosts | Wochenpost (terminiertes PGN) | Title, FileName, PgnContent (LONGTEXT), FileSize, **PuzzleCount (beim Upload gecachte Puzzle-Anzahl; 0=Alt → Lazy-Backfill)**, ScheduledAt (indexed), CreatedAt, UpdatedAt |
| WeeklyPostAttempts | Per-User-Fortschritt Wochenpost | WeeklyPostId + UserId + PuzzleIndex (unique triple), Solved, TimeSeconds, AttemptedAt; beide FKs Cascade |
| GroupTrainingGoals | Coach-Vorlage Trainingsziel je Gruppe | GroupId (unique, Cascade von Group), PuzzleMinutes, BookMinutes, ChessableMinutes, PlayGames (Partien/Woche), WeeklyDaysTarget, CreatedAt, UpdatedAt |
| UserTrainingGoals | Persönlicher Trainingsziel-Override | UserId (unique, Cascade), PuzzleMinutes, BookMinutes, ChessableMinutes, PlayGames (Partien/Woche), WeeklyDaysTarget, CreatedAt, UpdatedAt |
| ChessableActivities | Append-only Zeit-Log aktiver Chessable-Trainingszeit (von RepCheck-Extension gemeldet) für die Kategorie „Chessable" im Trainingsziele-Tracker | UserId (Cascade), TimeSeconds, MovesTrained, AttemptedAt; Index (UserId, AttemptedAt) |
| ManualActivities | Manuell (selbst) eingetragene Offline-Trainingsaktivität — speist bestehende Tracker-Kategorien, editier-/löschbar | UserId (Cascade), Date (DateOnly), Kind (Enum OtbGame/OfflinePuzzle/OfflineStudy/Coaching), Amount (Partien bzw. Minuten), Note? (≤200), CreatedAt; Index (UserId, Date) |
| RememberedPositions | Auf chessable.com „gemerkte" Stellungen (RepCheck „Remember line") — append-only, Verwendungszweck offen | UserId (Cascade), Fen (≤120), CourseId? (≤32), **CourseName? (≤200; über den Chessable-Bearer aufgelöst — Extension-mitgeliefert oder serverseitig aus der gecachten Kursliste)**, SourceUrl? (≤1000), CreatedAt; Index (UserId, CreatedAt) |
| SavedGames | Von chess.com/lichess (über RepCheck) gespeicherte Partien — Bereich „Partien" | UserId (Cascade), Source (≤20: chess.com/lichess), ExternalId? (≤120, Dedup), Pgn (LONGTEXT, serverseitig gebaut), White?/Black? (≤120), Result? (≤12), PlayedAt?, SourceUrl? (≤1000), ShareToken (≤32, UNIQUE; öffentlicher Link `/g/{token}`), CreatedAt; Index (UserId, CreatedAt) + **UNIQUE (UserId, Source, ExternalId)** (Dedup hart erzwungen; NULL-ExternalId = mehrfach erlaubt) |
| PlayTimeDailies | Gespielte Rapid-/Classical-Partien je UTC-Tag/Plattform | UserId + Date + Platform (unique, Cascade), Games (Anzahl Partien), UpdatedAt; befüllt vom `PlayTimeSyncService` |
| PlayTimeSyncs | Sync-Cursor externe Spielzeit | UserId + Platform (unique, Cascade), LastGameTimestamp (ms), LastSyncedAt, LastError |
| UserApiTokens | Personal-Access-Tokens für Maschinen-Clients (chess.com-Extension) | UserId (Cascade), Name, TokenHash (SHA-256, UNIQUE), Prefix (12 char), Scope ("extension"), CreatedAt, LastUsedAt, ExpiresAt (nullable); Index (UserId, Name) |
| PasswordResetTokens | „Passwort vergessen"-Einmal-Token | UserId (Cascade), TokenHash (SHA-256-Hex, UNIQUE), CreatedAt, ExpiresAt, UsedAt (nullable); Roh-Token nur per Mail, nie gespeichert. Beim Anfordern werden ältere offene Tokens des Users entwertet |
| MenuItemSettings | Admin-Override der Menü-Sichtbarkeit | ItemKey (PK, string), Level (Enum All/Registered/Groups/Admin); fehlt eine Zeile → Default aus `MenuRegistry` |
| MenuItemGroupAccesses | Welche Gruppe sieht einen gruppen-gegateten Menüeintrag | Composite PK (ItemKey, GroupId), Cascade von MenuItemSetting + Group, Index GroupId |
| ChessableCredentials | Per-User Chessable-Bearer (1:1) | UserId (unique, Cascade), EncryptedBearer (TEXT, AES via `EncryptionService`), CreatedAt, UpdatedAt; Plaintext nie persistiert. Wird vom `ChessableProxyService` an piratechess durchgereicht |
| AdminMessages | Admin↔User-Direktnachrichten (Thread je User) | UserId (Cascade, = Thread-Schlüssel/Nicht-Admin-Teilnehmer), SenderId (Audit), FromAdmin (bool, Richtung), Body (max 4000), CreatedAt, SeenByUserAt?, SeenByAdminAt?; Index (UserId, CreatedAt) + (FromAdmin, SeenByAdminAt) |
| MessageThreads | Metadaten/Zuweisung einer Konversation (1 Zeile je User) | UserId (PK + FK AppUser Cascade), ClaimedByAdminId? (welcher Admin übernommen hat, **ohne FK** → vermeidet doppelte Cascade-Pfade; Name wird beim Abruf aufgelöst), ClaimedAt?; entsteht mit der ersten Nachricht |
| CiBuildReports | Per-Push gemeldete laufende Build-SHA/Ref eines Stacks, den rookhub nicht per HTTP erreichen kann (z. B. log-watcher; `POST /api/ci/build-report`). PERSISTENT statt nur In-Memory → Admin-CI kennt die laufende Version auch nach rookhub-api-Neustart sofort | Repo (PK, ≤100), Sha? (≤64), Ref? (≤200), ReportedAt; Upsert je Repo via `GithubActionsService.ReportBuildAsync`, gelesen in `ResolveRunningBuildsAsync` |

Cascade Deletes: AppUser → Profile, Repertoires, Subscriptions, EndlessProgresses, EndlessSessions, UserGroups, CourseProgresses, CoursePuzzleResults, CourseAttempts, UserTrainingGoals, PlayTimeDailies, PlayTimeSyncs, WeeklyPostAttempts, SavedGames, ManualActivities; Repertoire → Files, RepertoireShares (RepertoireShare.Owner/Recipient Restrict); Group → UserGroups, BookGroupAccesses, GroupTrainingGoals; Book → BookPuzzles, CourseProgresses, CoursePuzzleResults, CourseAttempts, BookGroupAccesses, CourseShares, CourseLinks (CoursePuzzleResult.BookPuzzle + CourseAttempt.BookPuzzle = Restrict, um doppelte Cascade-Pfade zu vermeiden; CourseShare.Owner/Recipient ebenfalls Restrict; CourseLink.LinkedBookId ohne FK → DeleteBook räumt beide Richtungen explizit ab); WeeklyPost → WeeklyPostAttempts; AppUser → AdminMessages + MessageThreads (über UserId, der Nicht-Admin-Teilnehmer; MessageThread.ClaimedByAdminId hat bewusst keinen FK). Admin-DeleteBook und GroupController.Delete räumen die abhängigen Kurs-/Freigabe-/Ziel-Vorlagen-Daten zusätzlich explizit ab (InMemory-Tests cascaden nicht).
Friendships nutzen Restrict (kein Cascade) wegen zwei FKs zur selben Tabelle.

## Projektstruktur

```
compose.dev.yml             Dev-Stack ohne VPN (MariaDB + Crawler + API + Frontend)
compose.vpn.yml             Prod-Stack mit Gluetun VPN (WireGuard)
init-db.sh                  Erstellt beide DBs + User beim ersten MariaDB-Start
.env.dev.example            Umgebungsvariablen-Template (Development)
.env.vpn.example            Umgebungsvariablen-Template (VPN/Production)
twa/                        Android-TWA-Build-Gerüst (Bubblewrap, GH-Action — prod + dev-Variante)
src/
  api/RookHub.Api/
    Controllers/            Auth, Profile, Friend, Repertoire, Extension, TournamentProxy,
                            TournamentFavorite, TournamentMonitor, Subscription, BookPuzzle,
                            Course, Endless, Group, WeeklyPost, TrainingGoal, ClientLog,
                            Puzzle, Admin, Me, BotStats, BaseApiController
    Services/               Auth, Profile, Friend, Repertoire, CrawlerProxy, PlayerSearch,
                            BookPuzzle, Course, Puzzle, EndlessProgress, TrainingGoal,
                            PlayTime, PlayTimeSync, WeeklyPost, BotStats,
                            ApiToken+ApiTokenAuthenticationHandler, DiscordLink, PgnImport,
                            SchachBotWebhook, BackgroundTaskQueue, Admin, BookAdmin,
                            AdminSeeder, AutoSubscription, RoundMonitor,
                            DailyPuzzleScheduler, Heartbeat
    Models/                 EF-Entities (1:1 zum Schema oben)
    DTOs/                   Request/Response-Typen je Endpoint-Familie
    Data/                   AppDbContext, DesignTimeDbContextFactory, Migrations/
    Program.cs              Startup: DB, JWT+ApiToken Policy-Scheme, CORS, Swagger,
                            Auto-Migration, Health-Endpoint, BackgroundServices,
                            ForwardedHeaders (private Peers only)
    Dockerfile              Multi-stage .NET Build
  frontend/
    app/                    Angular 19 CLI-Projekt (siehe src/frontend/CLAUDE.md)
    nginx.conf              Proxy /api/ → api:8080, SPA-Fallback
    Dockerfile              Multi-stage Node Build + nginx
tests/
  RookHub.Api.Tests/        xUnit, eine Testklasse je Controller/Service
                            (Helpers: CapturingLogger, TestLogger, NoOpTaskQueue,
                             DiscordTokenTestHelper)
```

## Lokales Development

### Kompletter Stack via Docker
```bash
# Development (ohne VPN):
docker compose -f compose.dev.yml --env-file .env.dev up --build

# Production (mit Gluetun VPN):
docker compose -f compose.vpn.yml --env-file .env.vpn up --build
```

| Port | Dienst | URL |
|------|--------|-----|
| 8085 | Frontend (nginx) | http://localhost:8085 |
| 5001 | RookHub API | http://localhost:5001/swagger |
| 8080 | Crawler API | http://localhost:8080/swagger/ui/index.html |
| 3306 | MariaDB | Host: localhost, DBs: `chessresults` + `rookhub` |
| 9200 | Elasticsearch | http://localhost:9200 |
| 5601 | Kibana | http://localhost:5601 |

### Angular standalone (ohne Docker)
```bash
cd src/frontend/app
npm install
npx ng serve    # http://localhost:4200, braucht API auf :5001
```

### API standalone (ohne Docker, braucht MariaDB auf :3306)
```bash
cd src/api/RookHub.Api
dotnet run
```

### Tests

**Pflicht**: Jedes neue Feature, jeder neue Endpoint und jeder Bugfix MUSS mit mindestens einem Test abgedeckt werden. Kein PR/Commit ohne passenden Test.

> **`dotnet` ist installiert, aber NICHT im PATH** — liegt unter `/home/kahalm/.dotnet/dotnet`.
> Vor `dotnet`-Befehlen daher: `export PATH="$HOME/.dotnet:$PATH"` (ggf. `DOTNET_CLI_TELEMETRY_OPTOUT=1 DOTNET_NOLOGO=1`).
> **Achtung Test-Lücke:** Tests laufen gegen die EF **InMemory-DB** (LINQ-to-Objects) und stellen die
> **MySQL/Pomelo-SQL-Übersetzung NICHT nach**. Übersetzungsfehler (z. B. `EF.Functions.Like` in
> handgebauten Expression-Trees, raw SQL, provider-spezifische Funktionen) fallen erst gegen echtes
> MariaDB auf — solche Änderungen zusätzlich auf Dev verifizieren.

```bash
export PATH="$HOME/.dotnet:$PATH"
cd tests/RookHub.Api.Tests
dotnet test
```

### Test-Pattern
- **InMemory DB** pro Testklasse via `UseInMemoryDatabase(Guid.NewGuid().ToString())`
- **IDisposable** für DB-Cleanup
- **xUnit `[Fact]`** Attribute
- **Namenskonvention**: `MethodName_Scenario_ExpectedResult`
- **Service-Tests** testen direkt gegen InMemory-DB
- **Controller-Tests** instanziieren den Controller direkt; `BaseApiController.GetUserId()` wird via `ControllerContext` mit `ClaimsPrincipal` + `ClaimTypes.NameIdentifier` gemockt
- **Helper-Methode** `CreateUserAsync()` pro Testklasse für Test-Daten
- **InMemory cascaded nicht** — Admin-Delete-Pfade räumen abhängige Daten explizit ab; Tests entsprechend prüfen

## EF Core Migrations

```bash
cd src/api/RookHub.Api
dotnet ef migrations add <MigrationName>    # Nutzt DesignTimeDbContextFactory
dotnet ef database update                   # Braucht laufende MariaDB
```
Auto-Migration ist in `Program.cs` aktiv – beim Start werden Migrations automatisch angewendet.

## Offene Aufgaben

Nicht direkt angegangene Bugs, geparkte Features, Refactoring-Ideen und periodische Aufgaben (Code Review, Security Review etc.) werden in **`rookhub/TODO.md`** geführt. Neue Punkte dort eintragen, nicht separat als Markdown-Datei anlegen.

## Arbeitsweise

- **PFLICHT: `git pull` vor jedem Edit** — sobald du anfängst, Dateien auf der Platte zu ändern, MUSS unmittelbar davor ein `git pull` (bzw. `git pull --rebase`) laufen. Beide Stack-Kopien + diese Windows-Workstation arbeiten parallel am selben Remote; ein Edit auf einem N Versionen alten Stand führt unweigerlich zu Merge-Konflikten und verlorener Arbeit (passiert vor v0.95.2 mit 10 verpassten Commits). Lesen/Recherchieren ohne Pull ist OK; sobald du `Edit`/`Write` greifst → vorher pullen.
- **Commit early, commit often** – nach jedem abgeschlossenen Feature, Fix oder logischen Schritt committen. Kleine, atomare Commits sind besser als ein großer Sammel-Commit.
- **Tags NUR auf Zuruf** – NIEMALS automatisch Git-Tags erstellen. Der User muss vorher testen und explizit nach einem Tag fragen.
- **IMMER erst `git fetch`/`pull` vor jedem Tag** – ein Tag zeigt auf einen konkreten Commit; wegen der zwei Stack-Kopien am selben Remote ist der lokale HEAD oft veraltet. Vor dem Taggen `git fetch` und den AKTUELLEN `origin/master`-HEAD taggen (dessen `APP_VERSION` aus `changelog.ts` = Tag-Name), sonst zeigt der Tag auf einen alten Stand OHNE die zwischenzeitlich von der anderen Kopie gepushten Features → das `:latest`-Prod-Image ist dann unvollständig (passiert 2026-07-06: v0.266.0 getaggt, während master schon auf 0.270.0 mit dem Chapter-Feature stand).
- **CI/CD**: Docker-Images werden nach Push automatisch gebaut (GitHub Actions). Kein manueller Build nötig.
- **NIEMALS automatisch deployen** — weder auf Dev noch auf Prod. Der User startet Deploys immer selbst explizit.

## Versionierung

- **Aktuelle Version**: `0.292.6` - 0.292.6 (Frontend-Framework-Upgrade Angular 19.2→20.3 [dediziert]: alle @angular 20.3.26, CLI/build-angular 20.3.32, CDK/Material 20.2.14, angularx-qrcode 20, TypeScript 5.7→5.8. Via package.json-Pinning + Frisch-Install [neues Lockfile]; Prod-Build sauber, 1081 Tests grün, keine Code-Änderung. Nächster Schritt 20→21→22 schrittweise); 0.292.5 (Upgrade-Durchlauf 1 [Framework/Deps stack-weit]: piratechess .NET 8→9 [EF9-Test-Factory-Fix, 285 grün], crawler AngleSharp 1.4→1.5.2 + xunit 2.9.3 [201], rookhub-API Anthropic 12.29→12.35 [1438]. Zurückgestellt: .NET 10 [Pomelo ohne EF10-Release], Angular 19→20 [ng-update-Peer-Konflikte, Frontend bleibt 19.2]; Python floatet. Details in TODO.md „## Periodisch"); 0.292.4 (Nur Doku: RBAC-Punkt in TODO.md als erledigt markiert [Phasen 1–4 = v0.292.0–0.292.3]; IsAdmin-Spalten-Entfernung bewusst zurückgestellt. Keine Code-Änderung); 0.292.3 (RBAC Phase 4 [Admin-UI Rollen & Berechtigungen]: neuer Admin-Tab „Rollen" — Rollen anlegen/bearbeiten/löschen + Permission-Checkboxen [aus Code-Konstanten] + Rollen-Zuweisung je Nutzer. BE: RolesManage-Permission + RoleAdminService + Endpoints /api/admin/roles [Liste/permissions/POST/PUT/DELETE] + /api/admin/users/{id}/roles [GET/PUT]. admin-Rolle = Superuser [alle Rechte, nicht editier-/zuweisbar, folgt IsAdmin-Toggle], member nicht löschbar. +4 BE-Tests, +6 FE-Tests, i18n en/de/hr [Permission-Keys mit „." → „_"-i18n-Keys wegen ngx-translate-Pfadtrenner]. IsAdmin bleibt Sync-Quelle der admin-Rollenmitgliedschaft. RBAC-Feature abgeschlossen); 0.292.2 (RBAC Phase 3 [JWT-Permissions + Frontend has()]: AuthService.GenerateJwt trägt perm-Claims [ResolvePermissionClaimsAsync über UserRoles→RolePermissions] bei Login+Impersonation; Frontend AuthService.permissions + has(p) [isAdmin ODER perm-Claim] + permissionGuard(p). Bestehende isAdmin-Checks unangetastet [schrittweise Migration]. +4 FE-Tests. RBAC Ende-zu-Ende funktional; Phase 4 = Admin-UI für Trainer/Moderator-Zuweisung + IsAdmin-Spalte entfernen); 0.292.1 (RBAC Phase 2 [Permission-Durchsetzung, keine Verhaltensänderung für Admins]: Authorization/-Ordner mit HasPermissionAttribute + PermissionPolicyProvider [dynamische perm:<key>-Policies] + PermissionAuthorizationHandler [Admin-Rolle ODER perm-Claim]; in Program.cs registriert. Alle [Authorize(Roles=Admin)] → [HasPermission(...)] [8 Controller; AdminController Klassen-Gate → [Authorize] + Permission je Methode]. +4 BE-Tests. Phase 2 ✓); 0.292.0 (RBAC Phase 1 [Backend-Grundgerüst, keine Verhaltensänderung]: Modelle Role/UserRole/RolePermission + Migration AddRbacRoles + Permissions-Konstanten [1 Key je Admin-Bereich]; RoleSeeder [System-Rollen admin=alle Permissions + member, IsAdmin→admin-Rolle gespiegelt], in Program.cs nach AdminSeeder. IsAdmin bleibt Quelle der Wahrheit. +2 BE-Tests. TODO „RBAC-Ausbau" Phase 1 ✓); 0.291.34 (i18n: 2 fehlende hr-Keys ergänzt [repertoire.dialog.chessableCourseId + Hint]; hr wieder vollständig, alle 25 Dateien JSON-valide. Periodischer Übersetzungs-Review); 0.291.33 (Perf E1/E2/E3/E5: vier Statistik-Queries server-seitig statt In-Memory-Materialisierung ganzer Attempt-Historien. E1 LeaderboardService: GROUP BY über DISTINCT-Subquery statt alle (User,Puzzle)-Paare. E2/E3 PuzzleStatsService: Themen über PuzzleTags/Tags-Join, Rating-Bänder [GroupBy Rating/200] + Aktivität [GroupBy Date] server-seitig. E5 CourseStatsService: Bänder+Aktivität server-seitig, Themen string-basiert [keine Buch-Tag-Tabelle] mit Lean-Projektion. Gegen echte Dev-MariaDB Pomelo-übersetzbarkeit verifiziert [Wegwerf-Test]; Test-Helper CreatePuzzleAsync legt PuzzleTags mit an [syncTags-Flag]. Keine Verhaltensänderung); 0.291.32 (Konvention Routing: WeeklyPostController.GetAllProgress [„progress"] + PuzzleController-Literal-GETs [stats/history/…] jetzt VOR der {id}-Route deklariert [CLAUDE.md Literal-vor-Parameter]; Puzzle-{id} mit :int-Constraint. Course/Friend/TournamentProxy: keine echte einsegmentige Kollision. Keine Verhaltensänderung); 0.291.31 (Fix TrainingGoalService.PhaseFromText: es/hr-Schlüsselwörter [finales/apertura/medio juego, završnica/otvaranj/srednjica] ergänzt — hr/es-Kursnamen fielen sonst still auf Taktik zurück; strukturiertes Phasen-Feld als Tiefen-Fix im Code vermerkt. +1 BE-Test); 0.291.30 (Härtung ChessableBearerBreaker.IsBearerFatal: Fatal-Keywords zweisprachig [expired/invalid token/re-enter/unauthorized + de], VPN/Cloudflare-Ausschlüsse laufen weiter zuerst; strukturierter Proxy-Fehlercode als Tiefen-Fix im Code vermerkt. +3 BE-Tests); 0.291.29 (Fix Observability: ChessableProxyService.IsCourseCachedAsync/GetCachedBidsAsync loggen Fehler jetzt als Warnung statt nacktem catch [Proxy down = Fast-Lane starb undiagnostizierbar]; OperationCanceledException wird nicht mehr als „ungecacht" maskiert. +2 BE-Tests); 0.291.28 (Refactor TrainingGoalService: MapDay-Helper statt doppelter TrackerDayDto-Projektion [GetTracker/GetDailySeries]; StopTimerAsync nutzt ToDto(manual) statt Inline-Mapping); 0.291.27 (Refactor: ReplaceRepertoireFileAsync-Helper ersetzt die 2 wortgleichen Replace-PGN-Sequenzen in ImportAsRepertoireAsync + BumpSiblingRepertoiresAsync [„nie 0 Dateien"-Invariante nur noch an EINER Stelle]); 0.291.26 (Refactor: Buch-Themen-Semantik zentral in BookThemeTags [ValidKeys/ParseKeys/Default-Taktik] — ersetzt die 2 Parser-Kopien in CourseService + TrainingGoalService [Drift = Zeit im falschen Tracker-Topf]. +3 BE-Tests); 0.291.25 (Refactor: BookPuzzleService.PickRandomAsync-Helper ersetzt 3 kopierte Zufallszieh-Blöcke [GetRandom/GetOrAssignDaily/RegenerateDaily]; FirstOrDefault-Race-Absicherung nur noch an EINER Stelle); 0.291.24 (Fix+Refactor Endless: EINE BuildSession-Factory statt 4 Initialisierer-Kopien [BulkImport ließ PuzzleAttemptsJson still weg → Detail-Ansicht importierter Läufe leer]; tote TrimSessions(userId)-Aufrufe entfernt, Trim jetzt explizit TrimAnonymousSessionsAsync. +1 BE-Test); 0.291.23 (Refactor: Paging.Normalize [Services/Paging.cs] ersetzt 5 kopierte page/pageSize-Klemmungen [Notification/Endless/PuzzleStats/Admin/CourseStats]. +2 BE-Tests); 0.291.22 (Refactor: RecordAnonymousAttemptAsync nutzt MapAttemptToDto statt handgebautem PuzzleAttemptDto [neue DTO-Felder fehlten sonst still nur in der anonymen Antwort]); 0.291.21 (Refactor: CiController.VerifyGithubSignature nutzt SchachBotWebhookService.ComputeHmacHex statt eigener HMAC-Kopie [CLAUDE.md-Konvention, wie BotStatsController]; FixedTimeEquals bleibt für den Shared-Key-Check); 0.291.20 (Refactor: Inline-Claim-Parsing in PuzzleController [random/random-batch], MenuController und ClientLogController durch BaseApiController.GetUserIdOrNull ersetzt [4 Kopien, Drift-Risiko bei Claim-Auflösungs-Änderungen]); 0.291.19 (Refactor: Repertoire-Lesezugriffsregel [Besitzer ODER Share-Empfänger] zentral in RepertoireAccess [ReadableBy/CanReadAsync] — ersetzt 4 Kopien in RepertoireService/CanAccess, RepertoireTrainingService/CanTrain, DownloadFileAsync, RepertoirePositionLookupService [Autorisierungs-Drift-Risiko]. +1 BE-Test); 0.291.18 (Perf Admin-CI: MergeRunningAsync je Repo parallel via Task.WhenAll statt sequenziellem await [je Cache-Miss-Repo ~0,3–0,8 s GitHub-Roundtrip in Serie]); 0.291.17 (Härtung flag-hints [Standard+Buch]: neue Pro-User-RateLimit-Policy „user-flag" [10/min, Partition = UserId] + Audit-Log je Flag-Änderung — vorher ungedrosseltes, nicht zuordenbares Massen-Umflaggen des Review-Bestands möglich. +1 BE-Test); 0.291.16 (Fix Bot-Webhooks: Sibling-URL-Ableitung [weekly-progress/daily-regenerate] robust via SiblingWebhookUrl-Helper — Trailing-Slash/Host-only-WebhookUrl bauten vorher stille Müll-URLs [z. B. „http://weekly-progress", falscher Host]. +1 BE-Test); 0.291.15 (Fix Admin-CI: gemeldete Build-SHA wird validiert [IsPlausibleSha, 7–64 Hex, sonst verworfen] + in der GitHub-Query escaped — vorher Query-Injection/ungültige URI über build-info/build-report möglich. +2 BE-Tests); 0.291.14 (Fix Registrierung: nur echte Unique-Verletzungen [MySqlErrorCode 1062 + Fallback] werden als „already exists" [409] gemappt — transiente DB-Fehler [Deadlock/Timeout/Abriss] erschienen sonst fälschlich als vergebener Name statt als retrybarer Serverfehler. Neuer Helper AuthService.IsUniqueViolation. +1 BE-Test); 0.291.13 (Fix Wochenpost-Übersicht: Kapitel-basierte Posts zählen ihr Total jetzt auch in GetAllProgressAsync LIVE [wie GetTotalAsync/RecordAttempt] statt über den beim Anlegen gecachten PuzzleCount — nach Kapitel-Re-Fetch divergierte sonst „completed" zwischen Übersicht und Post-Ansicht. +1 BE-Test); 0.291.12 (Fix StockfishAnalyzer-Timeout-Race: Token bis in ReadLineAsync + Reader vor Parse/Dispose quiesziert [vorher: nebenläufige List-Mutation während ParseUciOutput + Read aus disposedem stdout]; TimeoutMs test-überschreibbar. +1 BE-Test mit Fake-Engine); 0.291.11 (Härtung Tagespuzzle: GetOrAssignDailyAsync legt nur noch für heute/gestern on-demand an — anonyme /daily/{date}- und OG-Bild-Aufrufe konnten per Datums-Enumeration DailyPuzzles-Zeilen für beliebige Vergangenheit massenhaft anlegen und den forDaily-Pool „verbrauchen"; ältere Daten: gespeicherte Zuordnung oder 404. +2 BE-Tests); 0.291.10 (Fix Tagespuzzle-Wertung: Daily-Ladder/Hall-of-Fame werten den ersten Versuch AM UTC-Daily-Tag [statt erster Versuch überhaupt — Alt-Fails/Buch-Solves außerhalb des Tags entschieden Credit/Zeit/🥇]; ebenso zählt die „Tagespuzzles"-Leaderboard-Kategorie nur Solves am Zuordnungs-Datum [Buch-Grinder bekamen sonst Daily-Credit]. +2 BE-Tests); 0.291.9 (Fix Web-Push: Sends hart auf 15 s gedeckelt [WebPushClient-Default ~100 s] + CancellationToken bis zum HTTP-Send durchgereicht — toter Push-Endpoint blockierte sonst den EINEN sequenziellen WebhookTaskWorker minutenlang je Subscription [Starvation der schach-bot-Webhooks] und Shutdown konnte nicht abbrechen. +1 BE-Test); 0.291.8 (Fix RoundMonitorService: gescheiterte Monitor-Entität wird im catch detacht — ein dirty Eintrag [z. B. Concurrency nach parallelem Unsubscribe] ließ sonst die Saves ALLER Folge-Monitore des Durchlaufs scheitern [LastCheckedAt nie persistiert → doppelte „neue Runde"-Glocken]. +1 BE-Test); 0.291.7 (Fix BackgroundTaskQueue: DropOldest→Wait [nichts wird mehr still verdrängt; TryWrite gelang unter DropOldest IMMER → Warn-Zweig war toter Code, Hint-Gen/Auto-Subscription/Backfills konnten bei Import-Stürmen lautlos verloren gehen], Kapazität 100→2048 als Runaway-Schranke. +1 geschärfter BE-Test); 0.291.6 (Fix Passwort-Reset: Mail wird VOR dem Entwerten/Persistieren verschickt — SMTP-Ausfall beim erneuten Anfordern entwertete bisher den bereits zugestellten alten Link, ohne dass der neue ankam [null gültige Links]. +1 BE-Test); 0.291.5 (Fix PGN-Import/SplitGames: [1] '['-beginnende Kommentar-Fortsetzungszeilen wurden verworfen [ggf. samt schließendem '}' → Scanner fraß Folgezüge]; Klammertiefe wird jetzt über Zeilen geführt, Kommentar-Inhalt bleibt, header-artige Zeilen im Kommentar splitten nicht. [2] Header-only-Spiele mischten ihre Header [FEN!] still ins nächste Spiel → werden separat geflusht. ImportPipeline 11→12 [lokal reprozessierbar]. +3 BE-Tests); 0.291.4 (Fix Kurse: Kapitel-Index-Auflösung [?chapterIndex] nutzt jetzt denselben Quiz-only-Index-Raum wie die Frontend-Kapitelliste — reine Info-/Intro-Kapitel verschoben vorher jedes spätere Kapitel um eins [falsches Kapitel trainiert / leeres „completed"]. Geteilte Logik in neuem ChapterOrder-Helper [CourseService + WeeklyPostService, ersetzt 3 Kopien]; rohes NUL-Byte in CourseService.cs entfernt [Datei galt für grep als binär]. +1 BE-Test); 0.291.3 (Fix Standard-Puzzles: 30-s-Idempotenz-Fenster in PuzzleService.RecordAttemptAsync matcht jetzt auch dto.Solved — Fail→Solve innerhalb 30 s wurde als Doppel-Submit verworfen [Solve nie gespeichert, Puzzle blieb ungelöst für excludeSolved/Leaderboards/Streaks]. +2 BE-Tests); 0.291.2 (Fix Chessable-Fast-Lane: Cache-Abdeckung wird beim Claim RE-geprüft — nicht mehr gecachte Jobs [Enqueue-Klassifikation veraltet, z. B. nach Tagen bearer-blocked] werden in die Download-Lane zurückgestuft [FullyCached=false, Phase queued], statt parallel und am _downloadLaneGate/Bearer-Breaker vorbei echte Chessable-Abrufe zu starten [IP-Block-Risiko]; Re-Check nur solange kein FetchedPgn-Checkpoint existiert. +1 BE-Test); 0.291.1 (Fix: Tagespuzzle-Zuweisung + Regenerieren schließen jetzt IsInfoOnly aus [wie Retired] — Daily konnte auf eine Chessable-Info-Linie fallen); 0.291.0 (Kurslösen: geschätzte Restzeit bis zum Abschluss neben der aufgewendeten Zeit — für BUCH und KAPITEL [⏳ ~1 h 20 min], extrapoliert aus dem bisherigen Tempo [totalSeconds ÷ attemptedCount × offene Puzzles]; verschwindet bei Abschluss. Nur FE, nutzt CourseScopeStats; neue book-nav.util formatEtaShort/estimateRemainingSeconds + courseEta; i18n book.course.etaHint. +Util-Tests); 0.290.0 („In welchen Repertoires?" bezieht jetzt auch mit dem User GETEILTE Repertoires ein [nicht nur eigene]; geteilte mit „geteilt"-Badge. RepertoirePositionLookupService-Index = eigene + via RepertoireShares geteilte [Owned-Flag→DTO.Shared]; RepertoireService invalidiert den Stellungssuche-Cache ALLER Empfänger bei Upload/Delete-Datei, Rename/Delete/Share/Unshare eines geteilten Repertoires. +1 BE-Test, i18n positionInReps.sharedBadge/sharedHint); 0.290.0 (Neue Seite „Gemerkt" /remembered: zeigt via RepCheck „Remember line" gemerkte Chessable-Stellungen — Brett-Vorschau, Kurs-Link, In-Analyse-öffnen/FEN-kopieren/Löschen; Menüpunkt remembered, DELETE /api/extension/remembered-lines/{id}); 0.289.0 (Knopf „In welchen Repertoires?“ im Analysemodus, Nachspiel-/Recap-Dialog und geteilte-Partie-Seite: listet zur aktuellen Stellung alle eigenen Repertoires, in denen sie vorkommt [Repertoire → Kapitel → Linie], Zugumstellungen inkl.; pro Linie „Trainieren“ [Trainer ?chapter=&line=<lineKey>] + „Ansehen“ [Detail ?line=&ply=]. Neu: POST /api/repertoires/position-lookup + RepertoirePositionLookupService [pro-User gecachter Index normFen→Linien, Key=Brett+Seite+Rochade ohne ep/Zähler → chess.js↔Gera.Chess-konsistent], PositionRepertoiresComponent [self-gated auf Login, lineKey aus Client-Parse] in analysis/pgn-viewer/shared-game, repertoire-detail ?line=&ply=-Deep-Link. +5 BE-Tests, i18n positionInReps.* en/de/hr); 0.288.0 (RepCheck-Extension v1.25.0: Sharebar oben im Popup mit öffentlichem /l/{token}-Link zur aktuell gespielten Line; Backend POST /api/extension/share-line baut PGN aus SAN-Liste, Dedup über Zugfolge); 0.287.0 (Repertoire: einzelne Linie als öffentlichen Nur-Ansehen-Link teilen [/l/{token}], serverseitiger PGN-Snapshot + SharedLine-Modell/Migration, POST {id}/share-line + anon GET shared-line/{token}, QR-Teilen-Dialog + /l/:token-Viewer); 0.285.0 (Admin-Tab Bücher/Kurse: Filterleiste pro Spalte über dem Grid — Name/Art/Schwierigkeit/Puzzle-Anzahl/Elo/Täglich/Zufall/Blind/Öffentlich/Sichtbar-für; UND-verknüpft, Reset-Knopf; clientseitig; i18n admin.books.filters.* en/de/hr); 0.275.0 (Elo für lichess + Heal-on-resave gespeicherter Partien: (1) `SaveAsync`-Dedup heilt beim Re-Save einer bereits gespeicherten Partie [gleiche Source+ExternalId] den Datensatz IN-PLACE [gleiches ShareToken/Id, `TryHeal`], wenn der neue Save mehr Züge ODER erstmals Elo bringt — kürzt nie; so repariert erneutes Speichern eine alt gespeicherte, lückenhafte/Elo-lose Partie ohne Link-Wechsel. `result` vor die Dedup-Prüfung gezogen; Pre-Check jetzt tracked. +2 BE-Tests. (2) RepCheck v1.24.0: lichess-Partien via offizieller Export-API [`https://lichess.org/game/export/{id}`, same-origin] statt og:title/DOM — `fetchLichessGame` liefert korrekte Namen/Ergebnis/WhiteElo/BlackElo/Datum + saubere SAN-Zugliste [`parsePgnText`]; behebt „…"-Zug-Lücken UND falsche lichess-Namen. getGameMeta bevorzugt `meta.moves`, Fallback DOM; lichess-externalId-Regex exakt 8 Zeichen. chess.com unverändert. Beide Artefakte synchron. NICHT deployed); 0.274.1 (Fix zur v0.274.0-Link-Vorschau: lieferte in Prod „frontend unavailable". OG-Renderer holt die echte SPA-index.html vom Frontend-Container, dessen DNS-Name je Deploy abweicht (Compose-Service `frontend` vs container_name `rookhub-frontend`). OgIndexHtmlProvider probiert jetzt mehrere Kandidaten [Frontend:InternalUrl -> frontend -> rookhub-frontend] und merkt sich den funktionierenden. nginx degradiert zudem sauber: `proxy_intercept_errors` + `error_page ... = @og_fallback` [try_files /index.html] -> bei Renderer-Fehler normale SPA statt Fehlerseite. NICHT deployed); 0.274.0 (Link-Vorschau/Open-Graph beim Teilen oeffentlicher Links [Reddit/Teams/Signal/Discord/WhatsApp/Twitter/Telegram]: geteilte Partie /g/ [Endstellung+Spieler/Ergebnis], Puzzles /puzzles/ inkl. Tagespuzzle [Ausgangsstellung, kein Spoiler], Turnier /t/ [Marken-Karte mit Turniername]. Crawler fuehren kein JS -> nginx leitet /g//t//puzzles/ an neuen API-Renderer GET /api/og/render, der die echte SPA-index.html [OgIndexHtmlProvider, 60s-Cache vom frontend-Container] mit OG-/Twitter-Meta-Tags anreichert [Mensch=SPA, Crawler=Tags]; Pfad via X-Original-URI. Brett-Bild = schriftfreies PNG 1200x630, SkiaSharp+Svg.Skia + eingebettetes cburnett-Set, GET /api/og/img/{kind}/{id}.png [game/puzzle/book/daily/tournament], gecacht. Neu: OgController/OgMetaService [ParsePath, End-FEN via Gera.Chess.TryLoadFromPgn, Turniername best-effort via CrawlerProxy]/OgImageService/OgIndexHtmlProvider; NuGet SkiaSharp 2.88.8 + NativeAssets.Linux.NoDependencies + Svg.Skia 1.0.0.18; Config Frontend:InternalUrl [http://frontend:8080]. Keine FE-Code-Aenderung. +17 BE-Tests. NICHT deployed); 0.273.0 (Elo pro Plattform auf der geteilten Partie-Seite /g/…: neben jedem Spielernamen dessen lichess/chess.com-Rating in Klammern [z. B. „Alice (1832) – Bob (2011)"], sofern vorhanden. Erfasst über die RepCheck-Extension [≥1.23.0]: chess.com aus den `pgnHeaders` [WhiteElo/BlackElo] der Callback-API, lichess aus dem og:title [„Name (1234)"]; RookHub speichert sie als `[WhiteElo]`/`[BlackElo]`-PGN-Header [`SaveGameInputDto.WhiteElo/BlackElo` → `BuildPgn`, plausibel 100–4000] und liest sie in `SharedGameDto`/`SavedGameDetailDto` per `ParseEloHeader` aus dem gespeicherten PGN zurück [KEINE Migration]. FE shared-game.component zeigt `whiteElo`/`blackElo` neben den Namen. Nur für NEU gespeicherte Partien; bestehende ohne Rating. +2 BE-Tests. RepCheck v1.23.0 [content.js + userscript: parseElo/splitNameElo, fetchChessComHeaders + parsePlayersFromMeta liefern Elo, Payload whiteElo/blackElo]. NICHT deployed); 0.272.0 (Off-Path-Hinweis [alle Puzzle-Modi]: zeigt jetzt die aktuelle Eval aus Spieler-Sicht in Klammern [z. B. „(-1.2)"/„(#-3)"]; war die Ausgangsstellung ~ausgeglichen/remis, lautet der Text „nicht mehr ausgeglichen" statt „nicht mehr klar für dich" UND es wird erst gewarnt, wenn der Spieler KLAR schlechter steht [< -1] statt schon bei „nicht mehr gewonnen". Start-Eval einmal im Hintergrund via Stockfish beim Puzzle-Start ermittelt [startEvalPawns, captureStartEval in beginSolving, nur wenn offPathWarnThreshold>0]; maybeWarnOffPath-Limit startWasBalanced?-1:+2; neue Helfer playerEvalDisplay/whiteEvalToPlayerPawns/startWasBalanced in BasePuzzleSolver; i18n puzzles.offPathWarningEqual en/de/hr. +3 base-Solver-Tests. NICHT deployed); 0.271.3 (Fix: Kurs-/Puzzle-Solver mit Visualisierungsstufe ≥1 ließ keine Rechtsklick-Pfeile/Kreise aufs Brett zeichnen. Im Viz-Modus fingen die eigenen pointerdown/pointerup-Capture-Listener [puzzle-board.component] JEDE Maustaste ab und riefen preventDefault/stopPropagation → das unterdrückte den kompatiblen mousedown, auf dem chessgrounds drawable hört. Fix: Sekundärtaste [ev.button > 0] wird in beiden Handlern VOR preventDefault durchgereicht → Rechtsklick landet bei Chessground, primäre Taste/Touch übernimmt weiter die Viz-Feld-Erfassung. Pfeile/Kreise funktionieren jetzt auf jeder Visualisierungsstufe. +1 Brett-Test [13 grün]. NICHT deployed); 0.271.2 (Weekly-Admin-Bestenliste mobil: der (i)-Detailknopf je Spieler verschwand bei langen Usernamen aus dem Bildschirm; Namens-Zelle bricht lange Namen jetzt um statt die Zeile zu verbreitern. NICHT deployed); 0.271.1 (Fix klickbare Kommentar-Züge: Alternativen-Aufzählung wurde als eine Linie gespielt — bei „…remis nach einem Zug wie c5, oder a5" spielte der Parser sie als Folge [c5 Weiß, dann a5 Schwarz], a5 also in falscher Farbe. comment-variation.util: branches(text) trennt an Zugnummer-Rücksprüngen UND Alternativ-Signalen [or/oder/bzw./resp./Slash bzw. Komma vor numerlosem Zug; Komma vor NUMMERiertem Zug = Fortsetzung]; resolveVariation bekam optionalen startPly → verankert Zweige an der zur Zugnummer passenden Basis [baseStartPly-Offset aus FEN-Vollzug/Farbe], sonst früheste Stellung zuerst statt „späteste bei Gleichstand" → numerlose Einleitungs-Alternativen lösen aus der Ausgangsstellung [beide Weiß]. +3 Util-Tests [10 grün]. NICHT deployed); 0.271.0 (Wochenpost-Dialog „Aus Buch-Kapitel": Buch- und Kapitel-Auswahl suchbar gemacht — beim Öffnen der Buch-Auswahl erscheint oben ein auto-fokussiertes Suchfeld [In-Panel-Input, stopPropagation gegen mat-select-Typeahead], Live-Filter nach displayName; Kapitel-Auswahl bekommt dasselbe Suchfeld ab >6 Kapiteln [Filter nach name, null-tolerant]. „Keine Treffer"-Hinweis, Filter-Reset bei jedem erneuten Öffnen [openedChange] + chapterFilter-Reset bei Buchwechsel. i18n weekly.fromChapter.searchBook/searchChapter/noMatch en/de/hr. FE-only, +5 FE-Specs. NICHT deployed) - 0.270.0 (Wochenpost aus Buch-Kapitel: Admin legt einen Wochenpost aus einem Kapitel eines Buchs/Kurses an [Knopf „Aus Buch-Kapitel" neben dem PGN-Upload, WeeklyFromChapterDialog: Buch->Kapitel wählen]; Puzzles kommen live aus den BookPuzzles des Kapitels [neue WeeklyPost.SourceBookId+SourceChapter, Migration AddWeeklyPostBookChapter, Endpoint POST /api/admin/weekly-posts/from-chapter]. WeeklyPostService.GetPlayPuzzlesAsync baut die Sequenz [BookPuzzleService.MapToDto, Id=Index] für Durchspielen+Breakdown, GetTotalAsync zählt Quiz-Puzzles des Kapitels live; Info-/Erklärlinien ausgenommen, Fortschritt bleibt index-basiert. FE: weekly.service.createFromChapter + weekly-from-chapter-dialog. i18n weekly.fromChapter.* en/de/hr. +4 BE +1 FE Tests. NICHT deployed) - 0.267.0 (Kurs-/Buch-Puzzle: das Auto-Weiterrücken bei Abschluss-Zitaten [v0.262.0] überspringt jetzt auch kurze Ergebnis-/Ausgangskommentare nach dem letzten Zug — „White wins."/„Black wins."/„White resigns."/„Stalemate!"/„It is game over." etc., Ergebnis+angehängte Quelle [„White wins. Rinck (1928)."] und Einzelnamen-Studien-/Komponisten-Zitate [„Kubbel (1916).", „This exercise was based on a study, composed by Troitzky in 1914."]. `game-citation.util.ts` erweitert: YEAR/NAME/BODY-Bausteine + `TRAILING_CITATION` [Quellenangabe am Textende, verankert nach ^ oder [.!], citeLike-Guard=Klammern/Komma/Namenspaarung], `RESULTS`-Set + `resultCore()`, Cap 100→200. Lehrreiche Kommentare halten weiter an. Datengetrieben validiert [379 Treffer, 0 FPs]. +Util-Spec [7]. FE-only. NICHT deployed); `0.266.0` - 0.266.0 (Web-Push für Benachrichtigungen: pro Bereich [courses/friends/puzzles/messages/tournaments/admin/other] ein-/ausschaltbar auf /notifications, Default AUS, „admin" nur für Admins. Neu: `UserPushSubscription` + `NotificationPushSetting` [+Migration `AddPushNotifications`], `PushNotificationService` [IsConfigured via VAPID WebPush:Subject/PublicKey/PrivateKey; Subscribe/Unsubscribe/Get+SetEnabledCategories/SendToUserAsync; CategoryOf spiegelt FE notificationCategory], `IWebPushSender`/`WebPushSender` [NuGet WebPush 1.0.13]. NotificationService reiht nach CreateMany best-effort Push je User über die `IWebhookTaskQueue` ein. Endpoints `/api/notifications/push/*` [config/subscribe/unsubscribe/preferences]. FE: `PushService` [SwPush/ngsw], Push-Panel auf /notifications, i18n notifications.push.* en/de/hr. VAPID-Keys serverseitig nötig [WebPush__* in .env], sonst Panel „nicht verfügbar". +9 BE-Tests +2 FE-Specs. NICHT deployed); `0.265.0` - 0.265.0 (Kurs-Buch-Themen-Tags: neues `Book.Themes` [CSV opening/middlegame/endgame/tactics/other, +Migration `AddBookThemes`; null/leer→Default Taktik]. `PUT /api/courses/{bookId}/themes` [CourseService.SetBookThemesAsync, Admin ODER Besitzer; 404/403/400], `CourseListItemDto.Themes`. TrainingGoalService verbucht Kurs-Versuchszeit auf die Buch-Themen [gleichmäßig gesplittet bei mehreren] statt PhaseFromText; unset→Taktik. FE: Theme-Chips je Kurs-Karte + „Themen bearbeiten" [CourseThemesDialogComponent, Multi-Select, gated isAdmin||isOwned]. i18n courses.themes.* en/de/hr. +7 BE +3 FE-Tests. NICHT deployed); `0.264.0` - 0.264.0 (Zuege in Kurs-/Buch-Puzzle-Kommentaren klickbar -> Variante als Brett-Vorschau [book-puzzle]. Neue comment-variation.util: extractSanTokens + resolveVariation sucht unter den Hauptlinien-Stellungen [Puzzle-FEN + nach jedem Zug] die, aus der die SAN-Folge legal ist [laengster Praefix, spaeteste bei Gleichstand] -> nur validierte Zuege werden klickbare Chips [buildCommentSegments], Rest bleibt Text. Klick -> variationPreview ueberlagert boardFen/lastMove [view-only] + Zurueck-Knopf; reset in setupPuzzle/reviewGoTo/solutionReviewGoTo. commentBlocks gecacht. i18n book.variation.previewing/back en/de/hr. +1 Util-Spec. NICHT deployed); 0.263.0 (Wochenpost optionale Kurzbeschreibung: neues nullbares WeeklyPost.Description [max 500, Migration AddWeeklyPostDescription], durch List-/Detail-DTOs + Create[multipart]/Update-Endpoints gereicht; Admin gibt sie beim Upload ein + inline editierbar, Anzeige unter dem Titel in der Wochenpost-Liste. schach-bot [v2.78.0] nimmt sie ins Discord-Embed auf [_post_announcement, via bestehendem Poll GET /api/weekly-posts - kein Webhook]. i18n weekly.fields.descriptionOptional/upload.descriptionPlaceholder en/de/hr. +2 BE-Tests +1 Bot-Test. NICHT deployed); 0.262.1 (Fix Buch-/Kurs-Puzzle: nach einem Fehlzug tauchte das Einleitungs-Kommentar wieder auf. commentLines fiel off-path [onSolutionPath=false] aus dem Live-Stapel-Zweig und lieferte erneut puzzle.comment. Fix: Intro nur noch on-path [vor dem 1. Zug]; off-path -> leer. +1 Regressionstest, bestehender Test [der das alte Verhalten festschrieb] korrigiert. NICHT deployed); 0.262.0 (Puzzle: Tipp-Knopf bleibt off-path sichtbar und gibt 3-stufig zu verstehen, dass man falsch abgebogen ist [Stufe 3 nennt die eigene Zug-Nr]. BasePuzzleSolver: neuer offPathHints-Getter [Komponenten uebersetzen], effectiveHints liefert ihn wenn !onSolutionPath -> hasHints bleibt true; offPathUserMoveNumber via markOffPathDeviation bei der ersten Abzweigung; showNextHint zaehlt off-path NICHT in maxHintLevel; resetOffPathTracking setzt hintLevel+Nr zurueck. Templates: Nenner availableHints.length->totalHints, Flag-Knopf off-path aus. i18n puzzles.offPath.h1-3/h3any en/de/hr. +2 base-solver-Specs. NICHT deployed); 0.261.0 (Kurs-/Buch-Puzzle: ist der Abschlusskommentar nach dem letzten Zug nur eine Partie-Angabe [z. B. „Bayer - Kuenitz, Wiesbaden, 2015.“], rueckt der Solver automatisch weiter statt anzuhalten. Neue Util isGameCitationComment [game-citation.util.ts: Namenspaarung Kapital-Bindestrich-Kapital + keine .!? + endet auf 4-stellige Jahreszahl], genutzt in hasTrailingSolutionComment [book-puzzle.component]. Lehrreiche Kommentare halten weiter an. Validiert gegen Buch 252 [40 Zitate, keine False Positives]. +Util-Spec. FE-only. NICHT deployed); 0.260.4 (Kurs/Buch: Aufgeben eines Puzzles laesst es jetzt bis zum Reset aus dem Pool fallen — AUCH im sequenziellen Modus. Vorher schloss der seq-Pool [GetNextAsync] nur geloeste [CoursePuzzleResults] aus, nicht die seit ResetAt gescheiterten [CourseAttempt]; ein aufgegebenes Puzzle kam beim Neustart sofort wieder als erstes. Fix: resetAt vor die Modus-Verzweigung gehoben + derselbe CourseAttempt>=resetAt-Filter im seq-Quiz-Zweig [Random hatte ihn schon]; leerer Pool trotz nicht-alle-geloest -> completed=true [Runde-durch-Panel]. +3 BE-Tests [CourseServiceGiveUpTests]. NICHT deployed); 0.260.3 (Fix: Kurs-/Tagespuzzle-Zugkommentar erschien beim Loesen einer Mid-Line-Aufgabe [startPly>=1] zu frueh - commentLines nahm `startPly + moveIndex`, aber moveIndex ist bereits absolut [nach Setup startPly+1], das doppelte startPly verschob Kommentare nach vorne; Daily 2026-07-05 Zug-31-Kommentar erschien schon nach De7. Fix `moveIndex - 1`, +1 Regressionstest. NICHT deployed); 0.260.2 (Observability, nur Backend: Request-Logs tragen bei ApiToken-Requests [z. B. RepCheck-Extension `rkh_`-Token] wieder die UserId. ApiToken wird erst in UseAuthorization authentifiziert — nach der LogContext-Anreicherungs-Middleware —, daher fehlte UserId am Request-Completion-Log. Neu: Serilog EnrichDiagnosticContext [nach dem Endpoint] setzt UserId/UserName. Ermoeglicht distinct Extension-Nutzerzahl in Kibana [unique metadata.UserId ueber url.path /api/extension/*]. Wirkt ab Deploy. NICHT deployed); 0.260.1 (Crawler VPN-Rotation haelt den Crawl-Rate-Limiter nicht mehr waehrend der Public-IP-Ermittlung; Kopie 2). 0.260.0 (Wochenpost-Admin-Detailaufschluesselung: (i) je Spieler in der Wochenpost-Bestenliste [nur Admin, weekly-list.component] oeffnet WeeklyBreakdownDialogComponent = eine Zeile je Puzzle mit Zeit/Tipps/Fehlzuegen/Mausrutschern + Summenzeile. Fehlzuege+Mausrutscher werden pro Wochenpost-Puzzle NEU erfasst: WeeklyPostAttempt.WrongAttempts/Mouseslips +Migration AddWeeklyAttemptWrongMouseslip [Snapshot zog dabei den nachhinkenden AltMoves-Eintrag mit nach], RecordWeeklyAttemptDto erweitert, BasePuzzleSolver.wrongMoveCount [Fehlzug-Zaehler] + mouseslipUsed→book-puzzle.recordWeeklyAttempt. GetResultsAsync liefert UserId; neuer Admin-Endpoint GET /api/weekly-posts/{id}/players/{userId}/breakdown [WeeklyPostService.GetPlayerBreakdownAsync, Titel aus PGN]. Greift nur fuer ab jetzt gespielte Weeklies [Altdaten 0]. +5 BE-Tests [1289], +3 FE-Service-Tests [935]. NICHT deployed) - 0.259.0 (Kurs-/Tagespuzzle akzeptieren Chessables geduldete Alternativzuege (softFail → [%alt]): Import behaelt sie je Zug (neues BookPuzzle.AltMoves, SAN→UCI via PgnParser.ExtractAltMoves, +Migration AddBookPuzzleAltMoves, ImportPipeline.CurrentVersion 9→10). Kurs-/Tagespuzzle-Solver (BasePuzzleSolver.onMoveMade) wertet einen solchen Zug nicht mehr als Fehler: matchesAlternative/handleAlternativeMove zeigt ihn ~1,4s auf dem Brett, Snackbar „Auch ein guter Zug“ (book.alternativeMove en/de/hr), nimmt ihn zurueck (altHoldTimer→updateBoard), Hauptzug bleibt erwartet, kein moveIndex-Fortschritt. FE: altMovesByPly in Base geleert je setupSolver, book-puzzle setzt es via parseAltMoves(puzzle.altMoves) danach; neue alt-moves.util + Spec, +2 base-solver-Specs, +2 BE-ParsePgn-Tests (Qd3-Alt + Promotion). Repertoire-Trainer nutzt [%alt] aus dem PGN-Baum schon laenger. Greift fuer Kurse erst nach Aktualisieren/Re-Fetch. NICHT deployed) - 0.258.0 (Observability Mobile-vs-PC: Request-Logging um DeviceType (mobile/tablet/desktop/bot/unknown, neue Logging/DeviceClassifier aus User-Agent) + rohen UserAgent erweitert (Program.cs Enrichment-Middleware, labels.DeviceType/UserAgent). Kein Area-Feld noetig - Kibana segmentiert per url.path-Filter (wie feature-usage-pie). Neues Kibana-Dashboard „RookHub Geraete" (init-kibana.sh import_devices_dashboard: Donut Geraete-Anteil + Balken Geraete je Bereich + ueber Zeit, Filter RequestKind=user, prod+dev) LIVE via _import angelegt. +17 BE-Tests (DeviceClassifierTests). Daten fuellen sich erst nach API-Deploy (ES hatte 0 user_agent-Docs, nicht rueckwirkend). NICHT deployed. - **Aktuelle Version**: `0.257.0` - 0.257.0 (Repertoire-Trainer Lern-Modus: Kommentar an einem GEGNERZUG blitzt nicht mehr kurz auf — neue COMMENT-Phase haelt nach einem kommentierten Gegnerzug an [enterCommentHold], zeigt holdComment dauerhaft + Weiter-Knopf/Tap/Leertaste/Enter [continueFromComment]. +1 Spec. FE-only. NICHT deployed); 0.256.0 (Repertoire-Trainer farbgemischte Repertoires: globaler Weiss/Schwarz-Toggle ENTFERNT, Trainingsfarbe jetzt PRO KAPITEL - automatisch erkannt (Mehrheit der Seite des LETZTEN Zugs je Kapitel, neue repertoire-color.util) + pro Kapitel manuell ueberschreibbar in der Linienliste (Weiss/Schwarz-Badge + Menue, localStorage rookhub_rep_train_chaptercolor_{id}). Jede Linie wird aus ihrer EIGENEN Seite gespielt (French-black vs Sizilianisch nicht mehr durcheinander). RepertoireLine +startFen/+lastMoveSide; Trainer nutzt chapterColors/colorOf(line) statt globalem this.color; hasUserMove/countUserMoves/startCurrentLine per-Linie. +repertoire-color.util.spec +2 Trainer- +2 Lines-Tests; Trainer-Spec make() setzt Kapitel-Overrides deterministisch. FE-only, kein BE. NICHT deployed); - **Aktuelle Version**: `0.249.1` - 0.249.1 (Learn-Modus: 0,8s-Pause zwischen automatischem Gegnerzug und dem Zeigen des naechsten eigenen Zugs [LEARN_GAP_MS], vorher quasi gleichzeitig. FE-only. NICHT deployed); - **Aktuelle Version**: `0.249.0` - 0.249.0 (Repertoire-Trainer Mausrutscher: falscher Zug im Abfragen-Modus bietet neben „Loesung zeigen" jetzt „Mausrutscher" [i18n repertoireTrainer.mouseslip existierte schon]. Fehler wird nicht sofort gezaehlt (pendingWrong); Mausrutscher verzeiht [pendingWrong=false, Stellung wieder spielbar, beliebig oft], echter Fehler [lineHadWrong] erst wenn ohne Mausrutscher korrekt/geduldet weitergespielt ODER Loesung gezeigt. +1 Spec [14 trainer]. FE-only, kein BE. NICHT deployed); - **Aktuelle Version**: `0.247.0` - 0.247.0 (Repertoire-SR Teil 2: Learn-Modus + Pool-Management je Linie/Kapitel/Kurs. Trainer: Abfragen/Lernen-Umschalter (`mode`, ?mode=learn, ?line=<key>). Learn spielt ungelernte Linien der Reihe nach: erwarteten Zug 2s zeigen (LEARN_SHOW) -> zuruecknehmen -> User spielt nach; hat der Zug einen Kommentar (line.comments[ply]), NICHT auto-zuruecknehmen (tap weiter); fertige Linie -> promote() in Pool. FE-Fix: korrekten User-Zug in this.chess nachfuehren war schon in Teil 1; hier Learn analog. RepertoireLine +lineKey (RepertoireViewerService). RepertoireLinesComponent: SR-Status-Badge (neu/faellig/geplant/pausiert) + Menue je Linie/Kapitel + Kurs-Bar (Learn/Review/InPool/Faellig/Pause) via getLineStates/promote/makeDue/setPaused. i18n repertoireTrainer.mode*/learn* + repertoire.lines.sr.* en/de/hr. Kein BE-Change (Endpoints aus Teil 1). +Specs (trainer learn, lines status/promote). FE 902 gruen. NICHT deployed); - **Aktuelle Version**: `0.246.0` - 0.246.0 (Repertoire-Spaced-Repetition Teil 1: feste 9-Stufen-Leiter ersetzt SM-2. SR-Einheit = ganze Linie (RepertoireCardState repurposed: +Level/InPool/Paused, CardKey=stabiler Linien-Hash lineKeyFromSans; +Repertoire.SrIntervalsJson Override + neue RepertoireSrSettings global; EINE Migration AddRepertoireSrLevels). Richtig durchgespielt=+1 Stufe, Fehler irgendwo=Stufe 1; Intervall der Stufe = Faelligkeit. Defaults 4h/10h/24h/2.5d/1W/2.5W/1.5M/3M/6M, pro Stufe konfigurierbar (SrConfigDialogComponent, Zahnrad im Trainer) global + pro-Repertoire-Override. Trainer uebt NUR faellige Pool-Linien (strict); EMPTY zeigt naechste Faelligkeit + 'Alle faellig machen'/'Alle in Pool'. RepertoireTrainingService neu: GetLineStates/ReviewLine/Promote/SetPaused/MakeDue/GetConfig/SetUser|RepertoireConfig (+17 BE-Tests). FE-Trainer auf Line-Review umgebaut (+chess-Nachfuehrung bei Mehrzug-Linien, Bugfix). Learn-Modus + granulare Pro-Linie/Kapitel-UI (Pause/Faellig/In-Pool) = Teil 2 (Backend steht schon). FE 894 / BE 1236 gruen. NICHT deployed); - **Aktuelle Version**: `0.244.3` - 0.244.3 (Repertoire-Detail-Fix: Linien-Liste blieb beim ERSTEN Öffnen leer — Kapitel erschienen erst nach Wechsel Baum↔Linien. `RepertoireLinesComponent.chapterGroups` ist ein `computed()`, las aber das plain `@Input() lines`; da Linien async via loadPgn nachgeladen werden und plain Inputs kein Signal sind, invalidierte das computed nie (Template rief `chapterGroups()` zwar jeden Tick, bekam aber den gecachten leeren Wert). Fix: `lines` hinter Backing-Signal (`linesSig`, get/set) → computed reagiert. +2 Specs. NICHT deployed); - **Aktuelle Version**: `0.244.2` - 0.244.2 (Repertoire-Trainer-Fix: Regression behoben, bei der ein geduldeter (optionaler) Zug — per `[%alt]` akzeptiert, aber nicht der Hauptzug — den erwarteten Hauptzug AUTOMATISCH für den User spielte und weiterschaltete (`runAdvance` im v0.244.0-Line-Mode). Jetzt wie das etablierte Verhalten (v0.196.0/0.201.0/0.219.1): geduldeter Zug wird kurz gezeigt (ADVANCE_MS.tolerated 1500ms), dann via neuem `retryCurrentPly()` zurückgenommen und DIESELBE Stellung bleibt spielbar (`currentPly` bleibt), der User zieht den Hauptzug selbst. +1 Trainer-Spec (10 grün), Prod-Build clean. NICHT deployed); - **Aktuelle Version**: `0.255.3` - 0.255.3 (Wochenpost-Puzzle-Zeit zaehlt jetzt als Taktik-Trainingszeit im Trainingsziele-Tracker: TrainingGoalService.AggregateAsync liest zusaetzlich WeeklyPostAttempts und verbucht sie als Quelle randomPuzzle / Thema tactics (fest, kein Tag-Routing) mit Pro-Puzzle-Cap 1800s. +1 BE-Test. NICHT deployed); 0.255.2` - 0.255.2 (Repertoire-Trainer Lern-Modus: LEARN_GAP_MS 800->400 halbiert — Pause zwischen Gegnerzug und dem naechsten eigenen Zug fuehlte sich beim Wiederholen zu lang an. FE-only. NICHT deployed); 0.255.1 (Repertoire-Detailseite: Top-Knopf heisst jetzt „Abfragen" [Hantel-Icon, i18n repertoireTrainer.modeQuiz] statt „Trainieren" [repertoireTrainer.train] — er oeffnet den Trainer im Standard-Abfragen-Modus, Label passt jetzt zum Modus. FE-only. NICHT deployed); 0.255.0 (Repertoire-Trainer Lern-Modus: nur der ERSTE Durchlauf einer neuen Linie zeigt die Zuege vor; Wiederholungs-Durchlaeufe 2/3 verlangen den Zug aus dem Gedaechtnis [enterLearnPlay statt enterLearnShow bei learnPass>=1], falscher Zug blendet ihn kurz als Erinnerung ein [onLearnMove->enterLearnShow]. Vorher zeigte jeder Durchlauf vor, nur der Kommentar fiel weg. FE-only. NICHT deployed); 0.254.0 (Groesseres Brett in ALLEN Puzzle-Modi: die viewport-basierte Chessable-Kappung aus 0.253.0 (Repertoire-Trainer) jetzt auch in Standard-Puzzle, Endlos und Buch-/Kurs-/Wochenpost-/Tagespuzzle-Solver. Brett waechst bis min(60vw, 820 px, vh-180 px) statt hart 560 px; Seiten-Container 1200 -> min(1400 px, 96 vw). Reine CSS-Aenderung, keine Verhaltensaenderung. NICHT deployed); 0.253.2 (Repertoire-Trainer Pool-Training-Desync gefixt: nach Ueberspringen des Richtig-Badge per Tipp/Leertaste feuerte der geplante 1,5-s-Advance-Timer trotzdem spaeter noch, ein zweites runAdvance zaehlte den Zug-Cursor hoch OHNE dass der Gegnerzug gespielt wurde -> Trainer verlangte den Gegnerzug (Schwarz sollte Sf3 spielen). runAdvance raeumt jetzt beim Betreten seinen eigenen Timer weg, startCurrentLine cleart uebrig gebliebene Advance/Opp/Learn-Timer aus der Vorgaenger-Linie. NICHT deployed); 0.253.1 (Repertoire-Trainer: das gruene Richtig-Badge nach korrektem Zug bleibt 1,5 s stehen statt 3 s (0.250.0 fuehlte sich beim Durchspielen zu langsam an); Leertaste/Enter/Tippen ueberspringt weiter, Geduldet unveraendert 1,5 s. NICHT deployed); 0.253.0 (Repertoire-Trainer Layout Teil 2: Brett wächst bis min(820 px, vh − 180 px) statt harter 560-Grenze, Trainer weitet sich auf min(1400 px, 96 vw); Züge-Liste rechts zeigt nur gespielte + aktuellen Zug (keine Zukunft mehr); Lern-Modus-Kommentare erscheinen als eigener Prosa-Block mit Zug-Label (z. B. „2… d6“) + Absätzen unter der Züge-Liste, Abfragen bleibt kommentarfrei. +i18n moveComment en/de/hr, +2 Trainer-Specs. NICHT deployed); 0.252.0 (Repertoire-Trainer, rechte Spalte gefuellt: Zuege-Liste der aktuellen Linie mit hervorgehobenem naechsten Halbzug (Marker + fett) + gedimmten bereits gespielten; Streak-Chip in der Stats-Zeile ab 2 richtigen/geduldeten Zuegen in Folge (Session-Best im Tooltip; Mausrutscher bricht nicht, echter Fehler + Loesung-zeigen brechen); Brett max 480->560 px + Trainer max-width 920->1080 px. +i18n en/de/hr. NICHT deployed); 0.244.1 - 0.244.1 (Defensiver Fix fürs /games-Nachspiel-Brett: `chess-board.component` erzwingt jetzt `aspect-ratio: 1/1` auf dem inneren Div (Klasse `.cg-square`) zusaetzlich zur JS-`el.style.width/height`-Berechnung. Wenn Container-Width und -Height aus irgendeinem Grund auseinanderlaufen (z. B. waehrend der Dialog-Oeffnungs-Animation im PGN-Viewer-Dialog oder ResizeObserver-Race), kann chessground kein gestrecktes Rechteck mehr rendern — Screenshot vom User zeigte Figuren an Ober-/Unterkante zusammengequetscht, Rang-Labels 8/6/2 fehlten. Kein Verhaltens-/Layoutwechsel bei bereits korrekt gesizten Instanzen. NICHT deployed); 0.244.0 - 0.244.0 (Repertoire-Trainer + Endless-Kleinigkeiten: (1) Trainer auf LINE-Modus umgebaut - statt Karten-fuer-Karten Positionen jetzt PGN-Linien komplett vom Anfang durchspielen. RepertoireTrainerComponent nutzt parsePgnText(pgn) fuer die Linien, spielt Gegnerzuege automatisch, wartet an eigenen Zuegen; Zug-Vergleich gegen line.moves[currentPly].san + tolerierte Alts aus RepertoireGraph.moves[cardKey].alts. SM-2-Reviews weiterhin per Position (normFen als cardKey). ?chapter=Name-Query filtert die Linien (Black-Header-Match). (2) Reset: DELETE /api/repertoires/{id}/training/reset neu (RepertoireTrainingService.ResetAsync loescht alle RepertoireCardStates des Users; +3 BE-Tests); Reset-Knopf in der Trainer-Leiste mit Bestaetigung. (3) Chapter-Gruppierung in RepertoireLinesComponent: RepertoireLine.chapter (Black-Header) neu; chapterGroups-computed-Signal buckets nach erstem Auftreten, kollabierbar via toggleChapter; „Kapitel trainieren"-Icon-Knopf pro Kapitel routet auf /repertoires/:id/train?chapter=…. (4) Endless: applyThemePreset toggle-off bei erneutem Klick, neuer clearThemes() + „Alle löschen"-Knopf neben dem Themen-Feld. i18n en/de/hr. FE 878/BE 1228 gruen. NICHT deployed); 0.243.0 (Analysemodus „Stellung aufbauen“-Brett-Editor: neue eigenständige `PositionSetupComponent` (features/analysis) mit chessground im Frei-Modus + Figuren-Palette (schwarze Reihe über / weiße unter dem Brett): Figur antippen wählt einen Stempel, Feld antippen setzt sie (Touch), Maus-`dragNewPiece` zieht sie direkt aufs Brett; Radierer-Tool, vorhandene Figuren per Drag verschieben bzw. per `deleteOnDropOff` vom Brett ziehen = löschen; Zugrecht-Umschalter (Weiß/Schwarz), Drehen, Grundstellung, Brett leeren. „Übernehmen“ baut die volle FEN (`composeFen` = Brett-FEN + Zugrecht + `deriveCastling` aus König/Turm-Grundfeldern, kein e.p.), validiert via chess.js (König-Pflicht etc., Fehler inline) und lädt sie ins Analysebrett. Einstieg über Knopf in der FEN-Karte; +4 FE-Specs, i18n `analysis.setup.*` en/de/hr, 880 FE-Tests grün. NICHT deployed); 0.242.18 (Repertoire-Liste in zwei Sektionen aufgeteilt: die fuer die RepCheck-Extension aktiven Repertoires (`useForExtension=true`) stehen jetzt oben in „In RepCheck“ (mit `extension`-Icon + Hinweistext); der Rest darunter unter „Weitere Repertoires“. `repertoire-list.component` bekam zwei neue Getter `extensionRepertoires`/`otherRepertoires` (bauen auf `filteredRepertoires` auf, Suchfeld wirkt weiter ueber beide Sektionen); die Karten wurden in ein `<ng-template #repCard>` extrahiert und beide Sektionen instanziieren sie per `*ngTemplateOutlet`. Karten-Titel bekam kleines `extension`-Badge (mit Tooltip) bei `useForExtension=true`. i18n `repertoire.list.sectionExtension`/`sectionExtensionHint`/`sectionOther`/`sectionOtherHint`/`extensionBadge` en/de/hr. FE 876 gruen, Prod-Build clean. NICHT deployed); 0.242.17 - 0.242.17 (Phase-D-Start Solver-Trio: (1) DATENFIX — Endless sendete als einziger Modus kein `hintsUsed` ans Backend [`recordAttempt`-Body + Service-Calls], Endless-Tipp-Nutzung ging still verloren; jetzt `this.maxHintLevel` mitgesendet [+Regressions-Spec]. (2) Wartbarkeits-Refactor [keine Verhaltensänderung]: das in allen 3 Solvern identische `opponentArrowMode`-Override [crazy→persist] + das `themeMode`-Feld in `BasePuzzleSolver` hochgezogen [Basis-Default um Crazy-Zweig erweitert, `ThemeMode`-Import ergänzt]; tote `showIntendedSolution()` aus endless gelöscht. `epForcedHints` bewusst NICHT gehoben [bräuchte `translate` in der Basis = Ctor-Eingriff]. 876 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.16` - 0.242.16 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: die umschaltbare Perioden-Aufschlüsselung [Tag/Woche/Monat/Jahr/Gesamt + Durchschalten] unter der Tracker-Heatmap aus `TrainingGoalsComponent` in eine eigene, self-computing `PeriodBreakdownCardComponent` ausgelagert [nimmt `series` als Input, besitzt period/anchor-State + Navigation selbst, ngOnChanges rechnet neu]; die reine Perioden-/Aufschlüsselungs-Mathematik [BreakRow/BreakdownPeriod/BREAKDOWN_PERIODS/ymd/parseYmd/periodBounds/shiftAnchor/sumBreakdown/breakdownRows] in eine geteilte `breakdown.util` gezogen [aus der Komponente re-exportiert für Specs/Importe], MatButtonToggleModule aus dem Parent raus. TrainingGoalsComponent ~701→~530 Z. +1 Spec [5 Fälle]. NICHT deployed); - **Aktuelle Version**: `0.242.15` - 0.242.15 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: die Karte „Chessable-Kurse: Verlauf + manuelle Themen-Zuordnung“ aus `TrainingGoalsComponent` in eine eigene, self-loading `ChessableThemesCardComponent` ausgelagert [lädt Kursliste selbst via ngOnInit, meldet Themen-Änderungen über `(changed)`→Eltern-`reload()`]; Chessable-Themen-State [chessableThemes/chessableCourses/chessableUnassignedOnly/loadingCourses/savingCourseId] + Methoden [loadChessableCourses/toggleUnassignedFilter/selectedTheme/assignTheme] + `loadChessableCourses()`-Aufruf aus reload() raus, MatSelectModule + Chessable-Typ-Imports aus dem Parent entfernt. TrainingGoalsComponent ~817→~725 Z. +1 Spec [6 Fälle]. NICHT deployed); - **Aktuelle Version**: `0.242.14` - 0.242.14 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: Presets-Karte [Timer-Vorlagen] aus `TrainingGoalsComponent` in eigene `ActivityPresetsCardComponent` [lädt/verwaltet Vorlagen selbst via ngOnInit — kein Input/Output]; aus dem reload-forkJoin entfernt, `timerKinds`/`activityThemes`/`presets`-State + Preset-Methoden + `activityKindIcon`-Import raus. TrainingGoalsComponent ~1071→~820 Z. +3 Specs, 866 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.13` - 0.242.13 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: Phase C Start — die Karte „Manuelle Offline-Aktivität“ [Formular + Liste + CRUD] aus der großen `TrainingGoalsComponent` [1071 Z.] in eine eigene `ManualActivitiesCardComponent` ausgelagert [`@Input() manualList` + `@Output() changed`→Eltern-`reload()`]; geteilte `MANUAL_KINDS`/`isMinutesKind` in neue `manual-activity.util` [Component re-exportiert für Specs]. `reload()` public. +3 Specs, 863 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.12` - 0.242.12 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: der Admin-Live-Feed aller Chessable-Importe [alle User, rein lesend] aus `chessable.component` in eine eigene self-polling `ChessableAdminImportsFeedComponent` ausgelagert [Template `@if (isAdmin)`]; `adminImports`/`adminPollSub`/`startAdminPolling`/`loadAdminImports`/`adminStatusLabel` raus, dazu die toten `statusLabel`/`importDurationLabel`. Der produktionskritische EIGENE Import-Polling-Flow [`activeImports`/`pollActive`] bleibt bewusst in der Komponente. 860 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.11` - 0.242.11 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: Admin-Tab „Nachrichten“ aus der `AdminComponent` in eigene, lazy geladene `AdminMessagesComponent` [features/admin/tabs/] ausgelagert — Thread-Liste/Detail/Claim + reaktive User-Suche; behandelt den `?thread=`-Deep-Link selbst [eigenes queryParamMap-Abo], der `?tab=`-Teil bleibt im Parent. MessageService-Injektion + Nachrichten-State/Methoden + rxjs-Such-Pipe-Imports aus AdminComponent raus. Damit sind alle 5 unabhängigen Admin-Tabs [courseDl/daily/puzzles/menu/messages] ausgegliedert [users/groups/books bleiben bewusst gekoppelt]. +3 Specs, 860 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.10` - 0.242.10 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: drei weitere Admin-Tabs — Tagespuzzle [`AdminDailyPuzzleComponent`], Puzzle-Tags [`AdminPuzzleTagsComponent`], Menü-Sichtbarkeit [`AdminMenuVisibilityComponent`, lädt Config+Gruppen selbst] — aus der `AdminComponent` in eigene, lazy geladene Kind-Komponenten unter features/admin/tabs/ ausgelagert [je matTabContent]; State/Methoden + ngOnInit-Calls + ungenutzte Typ-Imports raus. Nebenbei einen build-brechenden geraden `"`-Tippfehler im 0.242.9-Changelog des Parallel-Agenten gefixt. 857 FE-Tests grün. NICHT deployed); - **Aktuelle Version**: `0.242.9` - 0.242.9 (Drei Kurs-Puzzle-Fixes im book-puzzle.component: (1) Nach dem Loesen bleibt die Review-Ansicht in der ENDstellung — enterSolutionReview springt jetzt zu solutionReviewGoTo(reviewTotal) statt zum ersten annotierten Zug (0.240.0-Verhalten war ein „Sprung nach hinten“ fuer den User). (2) Kein Intro-Rueckfall im Review mehr: displayComment/commentLines zeigen die Einleitung nur bei reviewIndex===0; solution-/reviewGoTo suchen ab hier per neuer latestCommentUpTo(start,plyPlayed) rueckwaerts den zuletzt nicht-leeren Kommentar — Zuege ohne Kommentar (typisch: Gegner-Auto-Antwort, unkommentierte Endstellung) behalten den vorherigen Text oder zeigen nichts. (3) Angepinnte Kurse erscheinen zusaetzlich in der „In Arbeit"-Sektion der Kurse-Seite: inProgressCourses filtert nun (isPinned || solvedCount>0) && solvedCount<puzzleCount && puzzleCount>0; angepinnte zuerst, gefolgt vom bereits begonnenen Rest. +1 course-list-Spec (12 gruen), 857 FE gesamt. NICHT deployed); 0.242.8 - 0.242.8 (FE-Wartbarkeits-Refactor, keine Verhaltensänderung: Admin-Tab „Kurse von Usern holen“ aus der großen `AdminComponent` in eigene, lazy geladene `AdminChessableDownloadComponent` [features/admin/tabs/] ausgelagert — alle `dl*`-State/Methoden + `pollDlImport`/`estMinFromLines`/`dlEtaMin` dorthin, Template-Slice als `<ng-template matTabContent>`. `AdminComponent` ~140 Z. TS + ~115 Z. HTML kleiner, `OnDestroy`/ChessableService-Injektion + courseDl-Imports raus; 4 dl-Tests in neue Spec verschoben. 856 FE-Tests grün. NICHT deployed); 0.242.7 (Wartbarkeits-Refactor, keine Verhaltensänderung: die übergreifende Kurs-Statistik [`GetStatsAsync`/`GetHistoryAsync`/`GetBreakdownAsync`, alle self-contained auf `CourseAttempt`] aus dem großen `CourseService` in einen eigenen scoped `CourseStatsService` gezogen [analog PuzzleStatsService]. Die Pro-Kurs-Fortschritts-/`ComputeStatsAsync`/`BuildProgressAsync`/Reading-Order-Logik bleibt bewusst im `CourseService` [eng mit GetNext/Record/Reset verwoben]. `CourseService` 885→760 Zeilen. CourseController bekam den Service [+2 Test-Konstruktionen], CourseServiceStatsTests auf `_stats` umgestellt, in Program.cs registriert. 1225 BE-Tests grün. NICHT deployed); 0.242.6 (Wartbarkeits-Refactor, keine Verhaltensänderung: die Tagespuzzle-Bestenlisten [`GetDailyLadderAsync`/`GetDailyHallOfFameAsync` + private Helfer `LoadDailyFirstAttemptsAsync`/`AggregateScores`/`ResolveUsersAsync`/`ResolveIdentity`/`RankBonus`/`DailyScoreRow`] aus dem großen `BookPuzzleService` in einen eigenen scoped `DailyLeaderboardService` gezogen [nur lesend auf `_db`, keine Rück-Abhängigkeit]. `BookPuzzleService` 894→717 Zeilen. BookPuzzleController bekam den Service [+3 Test-Konstruktionen], in Program.cs registriert. 1225 BE-Tests grün. NICHT deployed); 0.242.5 (Wartbarkeits-Refactor, keine Verhaltensänderung: `PuzzleService` weiter aufgeteilt — lesende Statistik [`GetStatsAsync`/`GetAnonymousStatsAsync`/`GetEloHistoryAsync`/`GetHistoryAsync`/`GetBreakdownAsync`/`GetWorstThemesAsync`/`GetAllThemesAsync`/`GetOpenRevengeCountsAsync`/`GetUnsolvedFailuresAsync`/`ClaimSessionAsync` + privates `GetPrimaryLevelAsync`] in neuen scoped `PuzzleStatsService`, geteilte Elo-Mathematik [`GetDefaultElo`/`GetEloForLevel`/`SetEloForLevel`/`BuildEloDict`/`ProvisionalKFactor`/`CalculateElo`/`BaseKFactor`] in statischen `PuzzleElo`. `PuzzleService` 1090→~640 Zeilen [Recording/Random/Import bleiben, Elo-Aufrufe → `PuzzleElo.*`]. Aufrufer PuzzleController [+_stats], FriendController/BotStatsService [Dependency auf PuzzleStatsService getauscht] + 5 Test-Dateien nachgezogen [Elo-Statics → `PuzzleElo`]. Beide in Program.cs registriert. 1225 BE-Tests grün. NICHT deployed); 0.242.4 (Admin-Kursdownload: Checkbox „Auch abgelaufene anzeigen" neben der Userbox — Default aus → nur User mit gültigem Chessable-Token; angehakt auch gesperrte/tote (🔒). dlShowExpired+dlVisibleUsers()-Filter, onDlShowExpiredChange() leert ausgeblendete gesperrte Auswahl. +MatCheckboxModule, i18n admin.courseDl.showExpired en/de/hr, +3 FE-Tests. NICHT deployed); - 0.242.3 (Wartbarkeits-Refactor, keine Verhaltensänderung: die Puzzle-Themen-Tagging-Logik aus dem großen `PuzzleService` in einen eigenen `PuzzleTaggingService` gezogen [`SyncPuzzleTagsAsync` (jetzt public, von `PuzzleService.ImportFromCsvAsync` injiziert genutzt), `BackfillPuzzleTagsAsync`, `TagEnPassantPossibleAsync`, `HasUnplayedEnPassant`/`MoveToUci`, `EnPassantPossibleTheme`; alle `Chess`-Nutzung wandert mit]. AdminController-Hintergrundjobs + En-passant-/Backfill-Tests hängen an `PuzzleTaggingService`; `PuzzleService`-Ctor bekam den Service [in Program.cs registriert], 10 Test-Ctor-Aufrufe nachgezogen. 1225 BE-Tests grün. NICHT deployed); 0.242.2 (Wartbarkeits-Refactor, keine Verhaltens-/Routenänderung: die 7 Chessable-Admin-Endpoints [`admin/imports`, `admin/credentialed-users`, `admin/users/{id}/courses`, `.../estimate`, `.../test`, `.../import/{bid}`, `admin/active`] aus dem großen `ChessableController` in einen eigenen `ChessableAdminController` [Basis-Route `api/chessable`, Klassen-`[Authorize(Roles=Admin)]`] ausgegliedert; geteilte Queue-/Import-Helfer [`EnrichImportStateAsync`, `UserOwnsCourseAsync`, `FairQueuePositionsAsync`/`QueuedAheadAsync`, `EnqueueNextAsync`, `BlockedMessage`, `ToDto`/`ToAdminDto`, `JsonOpts`] in neuen scoped `ChessableImportQueueService` [in Program.cs registriert]. `ChessableController` = nur noch User-Endpoints. 1222 BE-Tests grün. NICHT deployed); 0.242.1 (Wartbarkeits-Refactor, keine Verhaltensänderung: die reine PGN-Parsing-Engine aus `PgnImportService` in eine eigene, unabhängig testbare Klasse `PgnParser` gezogen [Spiel-Splitting, SAN→UCI-Hauptvariante, Kommentar-Extraktion, `[%tqu]`/`[%cal]`/`[%csl]`-Marker; alle GeneratedRegex + `MoveShape`-Record dorthin]. `PgnImportService` = nur noch Orchestrator [`ParsePgn` delegiert an `PgnParser.*`, `ImportFileAsync` = DB-Persistenz]. Öffentliche API [`ParsePgn`/`CleanDisplayName`/`StripChapterSpoiler`] unverändert. 1222 BE-Tests grün. NICHT deployed); 0.242.0 (Offline-Aktivitaeten nach Thema klassifizierbar (Opening/Middlegame/Endgame/Tactics/Other): `ChessableTheme` um `Other=4` erweitert; `ManualActivity.Theme` + `ActivityPreset.Theme` + `ActivityTimer.Theme` (alle nullable ChessableTheme?), +Migration `AddActivityThemeClassification`. `TrainingGoalService.BuildAccumulatorAsync` nutzt User-Theme (via `ThemeFromChessable`) statt der bisherigen Kind-Defaults (OfflinePuzzle->Tactics/OfflineStudy+Coaching->Other) — null = alter Default bleibt. Timer-Start uebernimmt Preset-Theme; StopTimerAsync akzeptiert Theme-Override + StartedAt-Override (Client haelt Start/Ende/Dauer selbst konsistent, Server validiert Start<=Ende<=jetzt). Dashboard-Kachel `activityTimer` aus DEFAULT_VISIBLE entfernt (nur im Anpassen-Modus zuschaltbar) — DEFAULT_ORDER platziert sie hinter leaderboards. Stop-Dialog um Start-, Dauer- und Ende-Felder erweitert; Konsistenz-Regel: Aendern eines Feldes rechnet die anderen nach (Aendere Dauer -> wuerde Start+Dauer in Zukunft: Start nach vorn schieben (=jetzt-Dauer), Ende=jetzt; sonst Ende=Start+Dauer). Frontend-Service um `ActivityTheme`+`ACTIVITY_THEMES`; Trainingsziele-Seite mit Theme-Select in Preset- und Manual-Aktivitaets-Form (letzterer nur bei Minuten-Arten, OtbGame zeitunwirksam). +3 BE-Tests (52 gesamt), FE 853/BE 1222 gruen. i18n `trainingGoals.theme.*` + `.timer.startedAt`/`.startEndHint` en/de/hr. NICHT deployed); 0.241.2 (Offline-Trainings-Timer mit wiederverwendbaren Vorlagen: neue Modelle ActivityPreset (per-User: Label + Kind, +Migration AddActivityPresetsAndTimers) und ActivityTimer (PK=UserId, hoechstens 1 laufend, Label/Kind gesnapshotet). Nur Minuten-Arten (OfflinePuzzle/OfflineStudy/Coaching), OtbGame server-abgelehnt. TrainingGoalService+Controller um List/Add/Update/Delete-Presets + GetTimer/StartTimer/StopTimer/DiscardTimer (Endpoints unter /api/training-goals/presets und /timer). Stop erzeugt ManualActivity mit Dauer (Rundung + Clamp 1-600 min); optionales endedAt (ISO) fuer Backdating, Zukunft geklammert auf jetzt, vor Start = 400. Trainingsziele-Seite bekam Vorlagen-Card (Name + Kategorie + Liste, id=presets fuer Deep-Link). Dashboard-Kachel activityTimer (default-sichtbar in DEFAULT_VISIBLE nach trainingGoals) mit neuer ActivityTimerTileComponent: Chips fuer Presets/kein-Timer, laufender Timer mit Live-mm:ss-Ticker + Stop/Verwerfen, ActivityTimerStopDialogComponent fuer Backdate. Fuer Nicht-Admins Menue-gated ueber training-goals. +11 BE-Tests (49 TrainingGoalService gruen, 1219 gesamt), FE 847 gruen. i18n dashboard.activityTimer.*+trainingGoals.presets.*+trainingGoals.timer.* en/de/hr. NICHT deployed); - **Aktuelle Version**: `0.241.1` - 0.241.1 (Stale-Repertoires ohne Bearer hingen ewig im Update-Banner (Re-Fetch → EnqueueReimportAsync null → still Skipped). Fix: (1) gecachter Kurs wird OHNE Bearer aus dem piratechess-Cache geholt — EnqueueReimportAsync erlaubt cred==null wenn trustOwnership(Admin)+FullyCached; RunAsync fetcht mit leerem Bearer; piratechess course/start akzeptiert leeren Bearer wenn _rawCache den bid hat (v1.0.35). (2) ReprocessRepertoiresAsync: Re-Fetch wenn Owner-Bearer ODER (Admin && gecacht); sonst Versions-Mark statt ewig stale. +5 Tests. NICHT deployed); - 0.240.7 (Kurs-/Wochenpost-Puzzle: nach Abschluss steht „Naechstes Puzzle" statt „Ueberspringen" — der FAILED-Zustand nutzte bisher book.actions.skip, jetzt wie SOLVED book.actions.nextPuzzle als raised/primary-Button; „Ueberspringen" nur noch waehrend ungeloest. Reiner Template-Change book-puzzle.component.html. NICHT deployed); 0.240.6 (Live-Zug-Kommentare beim Lösen STAPELN statt ersetzen + kein Intro-Rückfall: `book-puzzle` neuer Getter `commentLines: string[]` [Template rendert je Absatz ein `<p>` in `.ctx-comment-body`], außerhalb des Reviews auf dem Lösungspfad die Kommentare ALLER bereits gespielten Lösungszüge in Reihenfolge [`commentForPlyPlayed(start..start+moveIndex−1)`, nur nicht-leere]; Züge ohne Kommentar fügen nichts hinzu, leere Liste → Block aus [KEIN Rückfall auf `puzzle.comment`, außer vor dem 1. Zug]. `displayComment` bleibt Einzel-Getter für Review/Ply-Nav. +2 FE-Tests [45]. NICHT deployed); auf dem Lösungspfad, ab dem 1. gespielten Zug, state AWAITING_USER_MOVE/THINKING — den Kommentar zum zuletzt gespielten Halbzug live zurück [`commentForPlyPlayed(startPly+moveIndex−1)`, gleiche Ply-Konvention wie Review], Fallback = Puzzle-Einleitung. Kein Template-Change [Kontext-Block ist state-unabhängig sichtbar]. Nur Buch-Puzzles [Standard/Endless haben keine moveComments]. +1 FE-Test. NICHT deployed); 0.240.2 (Reprocess-Banner (Kurse/Repertoires) auf EINEN Knopf reduziert: statt „Alle aktualisieren" + „Aus Cache" jetzt nur `reprocess.update` [voller Reprocess, localOnly-Pfad entfernt]. `ReprocessBannerComponent`: `run()` ohne Param [POST ohne ?localOnly], `working` boolean statt 'all'|'cached'|null, `cachedCount`-Getter raus; i18n `reprocess.update` mit Count + `updateTip` en/de/hr [updateAll/updateCached-Keys ungenutzt gelassen]. Spec angepasst [5 grün]. NICHT deployed); 0.240.1 (Notifications-Filter-Leiste (aus 0.238.6) erscheint jetzt auch bei nur EINER vorhandenen Kategorie: Guard in `notifications.component.ts` von `availableCategories.length > 1` auf `> 0` entschaerft. Vorher war die Leiste erst ab >=2 Kategorien sichtbar, sodass ein User mit ausschliesslich Chessable-Import-Benachrichtigungen nie einen Chip sah. Jetzt zeigt jede vorhandene Kategorie ihren Chip; Ausblenden funktioniert also immer. NICHT deployed); 0.240.0 (Chessable-Board-Annotationen + Pro-Zug-Kommentare im Kurs-Review: Import extrahiert die farbigen Pfeile [%cal] / Feld-Markierungen [%csl] je Halbzug nach `BookPuzzle.MoveShapes` [JSON {ply:[{o,d?,b}]}, +Migration `AddBookPuzzleMoveShapes`, +DTO roher JSON-String, `ExtractMoveShapes`/`ParseShapes`/`CalCslRegex` in PgnImportService], `ImportPipeline.CurrentVersion` 7->8. FE: neue `move-shapes.util.parseMoveShapes` -> `DrawShape[]`; `puzzle-board` bekam Input `reviewShapes`, das gemeinsam mit dem Gegnerzug-Pfeil in EINEM setAutoShapes gerendert wird [applyAutoShapes]; `book-puzzle` setzt `reviewShapes` je Review-Ply in solutionReviewGoTo/reviewGoTo. Zusätzlich landet `enterSolutionReview` nach dem Lösen am ERSTEN annotierten Zug [`firstAnnotatedSolutionIndex`, Kommentar ODER Pfeile] statt am Ende -> frühe Kommentare/Pfeile sofort sichtbar. +1 BE-Test [ParsePgn shapes] +1 FE-Spec [parseMoveShapes]. HINWEIS: 725d148 [0.239.0] hatte Model-Feld+Extraktion+MapToDto versehentlich mit-committet [Lock-Race]; Rest hier nachgezogen. Wirkt erst nach Kurs-Aktualisieren. NICHT deployed); 0.239.0 (Schnellere Massen-Kursimporte: die Fast-Lane (voll-gecachte Chessable-Kurse, netzfrei) läuft jetzt PARALLEL statt seriell — bis zu `MaxParallel` gleichzeitig (Config `Chessable:FastLaneParallelism`, Default 3, clamp 1–8). `ChessableImportFastLaneService`: `IsFastLaneReadyAsync` (bool, seriell) → `FreeSlotsAsync` (int = `min(MaxParallel−inflight, wartende)`); `TickAsync` stößt `slots` Drains GLEICHZEITIG an, jeder in EIGENEM DI-Scope/DbContext, Claim atomar (`ExecuteUpdate` queued→claimed) → verschiedene Jobs, keine Doppelverarbeitung; kein Gate im fastLane-Pfad. Download-Lane unverändert gedrosselt. +5 BE-Tests (FreeSlotsAsync); 1202 grün. NICHT deployed); 0.238.6 (Benachrichtigungsseite gruppiert und filtert: neue `notificationCategory(type)`-Util + `NOTIFICATION_CATEGORIES` in `core/notification-text.ts` bucketet alle Types in Kurse [chessable_*]/Freunde [friend_*]/Puzzles [challenge_*+revenge_performed]/Nachrichten [admin|user_message_received]/Turniere [tournament_new_round]/Admin [new_user_registered]/Sonstiges. `NotificationsComponent` bekam eine Filter-Chip-Leiste ueber der Liste [mat-chip-set, nur vorhandene Kategorien mit Zaehler in Klammern], Klick auf Chip schaltet Kategorie aus [durchgestrichen+dimmed] bzw. wieder an, "Alle einblenden"-Knopf sobald mindestens eine ausgeblendet ist; alle-ausgeblendet-Zustand zeigt Hinweis statt leerer Liste. Getter `availableCategories`/`visibleItems`/`counts` filtern in-memory ueber die bereits geladenen Items. Ausgeblendete Kategorien in localStorage `rookhub_notifications_hidden_categories` persistiert [`readHiddenCategories`/`persistHiddenCategories`, still bei Storage-Fehler]. +8 FE-Specs. i18n `notifications.filter.*`/`notifications.category.*` en/de/hr. NICHT deployed); 0.238.5 (Kurs<->Repertoire-Umwandlung VERSCHIEBT jetzt statt zu kopieren: das Original verschwindet nach Erfolg. `CourseService.ConvertToRepertoireAsync` loescht den Original-Kurs via `BookAdminService.DeleteBookAsync` NUR wenn eigener [`Book.OwnerUserId==userId`; geteilte Gruppen-/Admin-Kurse bleiben]; `RepertoireController.ConvertToCourse` loescht das Repertoire per `RepertoireService.DeleteAsync` NACH erfolgreicher Kurs-Erstellung [400 = kein Puzzle-PGN -> nichts geloescht]. FE: course-list/repertoire-list entfernen das Original aus der Liste [Kurs nur bei isOwned]. +3 BE-Tests [CourseConversionTests owned-deleted/shared-kept, RepertoireControllerTests convert-deletes/400-keeps]. NICHT deployed); 0.238.4 (Chessable-Bearer-Feld akzeptiert jetzt auch den kompletten "Bearer eyJ..."-String, nicht nur den reinen JWT: neue exportierte Util stripBearerPrefix() strippt ein fuehrendes Bearer-Praefix (case-insensitiv, ``-verankert, beliebige Whitespace-Menge) und trimmt den Rest; chessable.component.save() nutzt es vor saveCredentials(), und der Speichern-Knopf haengt an einem neuen Getter hasBearerInput = stripBearerPrefix(bearerInput).length > 0 statt an bearerInput.trim(). +5 FE-Specs im chessable.component.spec. NICHT deployed); 0.238.3 (Eigenes PGN als persoenlichen Kurs hochladen laeuft jetzt wie das Repertoire-Anlegen: kleiner "PGN hochladen"-Knopf oben rechts auf der Kurseite (.header analog repertoire-list) oeffnet einen neuen UploadCourseDialogComponent (Datei-Picker + optionaler Kursname + Discord-Hinweis, dialogRef.close({file,name})), statt das inline Panel oben in der Liste. CourseListComponent bekam openUploadDialog() (oeffnet Dialog, ruft bei Bestaetigung uploadCourseFile(file, name) - die Upload-Logik ist aus onFileSelected in einen Public-Helper extrahiert, weiter idempotent + notifyAccessChanged); uploadName/discordUrl/onFileSelected/upload-panel-CSS entfernt, MatDialog injiziert. i18n courses.upload.choosePgn en/de/hr. +2 FE-Specs (Dialog-Flow + Abbrechen), bestehender uploadCourseFile-Test angepasst. NICHT deployed); 0.238.2 (Fehlende Kurs-Linien behoben: Chessable-Kapitel-Einleitungen als NULL-Zug [`{[%info]} 1. -- {Text}`] wurden beim Import verworfen [`--` ergibt keine UCI-Züge, und die Info-Behalten-Logik verlangte einen nicht-leeren ERSTEN Kommentar — der ist hier nur der leere `[%info]`-Marker, der Text folgt im Zug-Kommentar]. Fix in `PgnImportService.ParsePgn`: `isInfoOnly` vor die uci-Prüfung, Keep-Zweig greift auch bei `[%info]`-Linien, Text = erster NICHT-leerer Kommentar [neuer Helfer `FirstNonEmptyComment`]. `ImportPipeline.CurrentVersion` 6→7. Linien stehen bereits im `Book.SourcePgn` → lokal reprozessierbar; Chessable-Kurse via Re-Fetch. Beispiel Buch 16: 583 PGN- vs. 562 importierte Linien = 21 verworfene NULL-Zug-Intros. +2 BE-Tests; 1200 grün. NICHT deployed); 0.238.1 (Lange Kurs-Kommentare nicht mehr gekappt + Kommentar-Kasten scrollbar: `BookPuzzle.Comment` varchar(5000)->LONGTEXT [Migration `WidenBookPuzzleComment`], Import-Kappung 5000->100.000 [`PgnImportService.MaxCommentLength`, ExtractFirstComment+ExtractMoveComments]. Lange Chessable-Erklaer-/Intro-Texte [z. B. Introduction #2 in 100 Tactical Patterns, ~6500 Zeichen] wurden bei 5000 abgeschnitten. `ImportPipeline.CurrentVersion` 5->6 [Reprocess-Banner; voller Text steht im SourcePgn -> lokal aufbereitbar, Chessable-Buecher via Re-Fetch]. CSS `.ctx-comment p`: max-height 40vh + overflow-y:auto + min-width:0/overflow-wrap [laeuft nicht mehr ueber die Seite]. +1 BE-Test [ParsePgn_KeepsLongComment, 169 Import/Course gruen] +Migration. NICHT deployed [zum Fuellen des Alt-Textes Kurs aktualisieren]); 0.238.0 (Kurs ↔ Repertoire umwandeln: `POST /api/courses/{bookId}/convert-to-repertoire` [`CourseService.ConvertToRepertoireAsync` → neues `RepertoireService.CreateFromPgnAsync`, legt Repertoire aus dem Kurs-PGN an, `UseForExtension=false`, Original-Kurs bleibt] + `POST /api/repertoires/{id}/convert-to-course` [reused `UploadPersonalCourseAsync`; nur Puzzle-PGN im Chessable-Stil, reines Eröffnungs-Repertoire → 400]. `CourseService` bekam `RepertoireService`, `RepertoireController` bekam `CourseService` [beide azyklisch]. FE: 📚-Icon-Knopf in `course-list` + „In Kurs"-Knopf in `repertoire-list` [400→klarer Hinweis], Service-Methoden + i18n en/de/hr. +4 BE-Tests [`CourseConversionTests`], Ctor-Anpassungen in 5 Testklassen; 1197 grün. NICHT deployed); 0.237.6 (Dashboard-Snapshot lokal cachen [neuer `DashboardCacheService`, per-User localStorage-Key `rookhub_dashboard_cache_v1_u{userId}`]: `dashboard.component.ngOnInit` liest den Snapshot [Repertoire-/Kurs-Zaehler, angepinnte Kurse, Abos, Freundeszahl, Favoriten, Puzzle-Solved/Accuracy/Elo] SYNCHRON und wendet ihn sofort an, danach ueberschreibt der bestehende `forkJoin`-Fetch mit frischen Werten UND schreibt den Snapshot zurueck. Beim Neuladen keine leeren Kacheln mehr. Fremde User-Snapshots werden nicht angezeigt [Auth-`userId` im Key]; kaputter/leerer Cache = still `null`. +5 FE-Tests [`dashboard-cache.service.spec` 5 cases + 2 neue dashboard cache-Behavior-Tests, 30 gruen]. NICHT deployed); 0.237.5 (Kurs-/Buch-Puzzles zeigen den Kommentar NACH dem letzten Zug (Abschlusstext) + kein Auto-Weiter dabei: `BookPuzzleComponent.enterSolutionReview` springt jetzt via `solutionReviewGoTo(reviewTotal)` ans Ende UND setzt `moveComment` auf den Kommentar des letzten Zugs [vorher nur reviewIndex → displayComment fiel auf die Einleitung zurueck, Schlusstext verborgen]; `finalizeSolve` bricht bei vorhandenem Abschlusstext [neuer Getter `hasTrailingSolutionComment` = commentForPlyPlayed(letzter Ply)] den Auto-Advance-Countdown ab, der manuelle Weiter-Knopf bleibt. Beispiel: Dziuba-Erwich-Linie in „100 Tactical Patterns (Round 002.003, MoveComments key 0). +5 FE-Tests [43 book-puzzle gruen]. NICHT deployed); 0.237.4 (Admin-CI: die per Push gemeldete laufende Version eines Stacks [z. B. log-watcher via `POST /api/ci/build-report`] ist jetzt PERSISTENT in der DB [neue Tabelle `CiBuildReports`, +Migration `AddCiBuildReport`; `GithubActionsService.ReportBuildAsync` upsertet, `ResolveRunningBuildsAsync` liest daraus; `GithubActionsService` bekam `AppDbContext`, `CiBuildReportController.Report` async]. Vorher war es ein prozessweiter statischer `_reportedBuilds`-Cache → jeder rookhub-api-Neustart/Deploy löschte ihn, log-watchers Version verschwand bis zum nächsten Push [bis zu 10 min] aus der Übersicht. Der GitHub-`workflow_run`-Push-Cache [`_pushedRuns`] bleibt bewusst In-Memory [self-healend]. +1 BE-Test [`ReportedBuild_PersistsAcrossServiceInstances`, geteilte InMemory-DB simuliert Neustart], Ctor-Anpassungen in 2 Testklassen. NICHT deployed); 0.237.0 (Kurse fürs Dashboard anpinnbar + direkt startbar: neue per-User-Tabelle `CoursePin` [UserId+BookId unique, +Migration `AddCoursePins`, Cascade User/Book, Cleanup in `BookAdminService.DeleteBookAsync`], `CourseListItemDto.IsPinned` [in `GetCoursesAsync` via Pin-Set gesetzt], Endpoints `POST/DELETE /api/courses/{bookId}/pin` [`CourseService.PinCourseAsync`/`UnpinCourseAsync`, idempotent, EnsureAccess]. FE: 📌-Toggle je Kurs in der Kursliste [optimistisch+Rollback], neue Dashboard-Kachel `pinnedCourses` [in DEFAULT_VISIBLE vor `courses`; eligible nur wenn ≥1 Pin bzw. im Edit-Modus] mit je Kurs zwei Start-Buttons Sequenziell/Zufällig; Dashboard nutzt das ohnehin geladene `/api/courses` [jetzt typisiert `DashboardCourse` inkl. `isPinned`] mit. i18n `courses.pin*`/`dashboard.pinnedCourses.*` en/de/hr. NICHT deployed); 0.236.18 (Admin-CI Push statt Pull: neuer Endpoint `POST /api/ci/gh-webhook` [`CiBuildReportController.GithubWebhook`, AllowAnonymous, verifiziert GitHubs `X-Hub-Signature-256` HMAC gegen `CI:GithubWebhookSecret` ODER Fallback `CI:BuildReportSecret`] empfängt GitHub-`workflow_run`-Events → `GithubActionsService.ReportRun` [statischer `_pushedRuns`-Cache je Repo, ~20 jüngste]. `OverlayPushedRuns`/`OverlayPushedRepo` legt die Push-Läufe bei JEDEM Overview-Abruf LIVE über die (gecachten) GitHub-Daten [Merge per Run-Id, Push gewinnt, Workflow-Filter, neueste 5] — auch im Einzel-Repo-Abruf [👁]. FE `NORMAL_MS` 2min→20s [RookHub-Cache statt GitHub]. Webhook je Repo unter Settings→Webhooks eintragen [Event „Workflow runs", Secret = CI_BUILD_REPORT_SECRET-Wert]. +5 BE-Tests [Overlay + Webhook-Sig; GithubActionsServiceTests+CiBuildReportControllerTests in Collection „CiStaticState" serialisiert wg. statischer Caches]. NICHT deployed); 0.236.17 (Perf: `CourseService.GetCoursesAsync` — die Puzzle-Anzahl je Buch [`PuzzleCount`] lief als korrelierte Unterabfrage pro Buch [`b.Puzzles.Count(!IsInfoOnly)` im Select] → bei Admins mit ~260 Büchern sekundenlang. Jetzt EINE gruppierte Query [`BookPuzzles.Where(!IsInfoOnly).GroupBy(BookId)` → Dict]; Mapping zieht die Zahl aus dem Dict. Verhalten/Zahlen unverändert [67 Course-Tests grün]. NICHT deployed); 0.236.16 (Chessable-Importe sind jetzt standardmäßig NICHT von der RepCheck-Extension nutzbar: `ChessableImportService.ImportAsRepertoireAsync` setzt beim Anlegen `UseForExtension=false` [`CreateRepertoireDto`]. Manuell angelegte Repertoires unberührt [Create-Dialog-Default bleibt true]. +1 BE-Test-Assert. Prod-Datenfix separat: kahalms 133 Repertoires alle auf `UseForExtension=0`. NICHT deployed); 0.236.15 (Admin-CI Poll-Umbau: normaler Voll-Abruf jetzt alle 2 min statt 5 s [`NORMAL_MS`], gegen GitHub-Rate-Limit. Neuer per-Repo-👁-Knopf [`toggleWatch`/`isWatching`, `.watch-btn`]: schnell-pollt NUR dieses eine Repo alle 10 s [`WATCH_MS`] für 1 min [`WATCH_WINDOW_MS`], bei laufender Aktion rollend bis zu deren Ende [`WATCH_RUNNING_EXTEND_MS`, `watchTick`]. Neuer BE-Endpoint `GET /api/admin/ci/runs/{repo}` [`GithubActionsService.GetRepoAsync`, ungecacht, Repo-Whitelist; Merge-Logik in `MergeRunningAsync` extrahiert, von Voll- + Einzel-Abruf geteilt]; FE `loadRepo` mergt das Einzel-Ergebnis in die Übersicht. i18n `admin.ci.watchStart/watchStop` + Subtitle en/de/hr. +2 BE-Tests. NICHT deployed); 0.236.14 (Admin-CI-Seite: die „Deployt: …"-Zusammenfassungszeile im Kopf [gelbe Chips je Stack, `.ci-deployed`] entfernt — inkl. `deployed`-Feld + dessen Berechnung in `recomputeEta` + CSS; die gelbe Zeilen-Hervorhebung des laufenden Builds [`.run-deployed`/`isRunningBuild`] bleibt. NICHT deployed); 0.236.13 (CI-Fix: flakiger `GithubActionsServiceTests.LogWatcher_FiltersOutTestRuns`-Test [aus dem 0.236.11-Feature], der je nach Testreihenfolge den ganzen `test-rookhub`-Job rot machte → kein `:latest`-Image. Ursache: prozessweiter **statischer** `_reportedBuilds`-Cache leckte über Testklassen [`CiBuildReportControllerTests` meldet „log-watcher" → im Overview-Test feuerte der „6.-Zeile-nachladen"-Pfad und der naive Stub lieferte den Run doppelt → `Assert.Single` sah 2]. Fix: interner `ResetReportedBuildsForTests()`-Hook, im `GithubActionsServiceTests`-Ctor vor jedem Test geleert. Produktcode unverändert [`FetchRunByShaAsync` filtert in Prod echt nach `head_sha`]. NICHT deployed); 0.236.12 (RepCheck „Remember line": echter Kursname über den Chessable-Bearer. Neues `RememberedPosition.CourseName` [+Migration `AddRememberedPositionCourseName`], `RememberLineInputDto`/`RememberedPositionDto` um `CourseName`. `RememberedPositionService` löst den Namen auf: von der Extension mitgeliefert [Vorrang] → sonst cache-first aus `ChessableCredential.CachedCoursesJson` → sonst best-effort Live-`GetCoursesAsync` [try/catch, Merken scheitert nie am Kursnamen]. `ListAsync` backfillt Alt-Einträge ohne Namen aus dem Cache [rein in-memory]. +7 BE-Tests [RememberedPositionServiceTests] + ExtensionControllerTests-Ctor angepasst. Extension-Seite [repcheck ≥1.19.0] löst den Namen via erfasstem Bearer + Chessable-getHomeData auf. NICHT deployed); 0.236.11 (Admin-CI: Workflow-Filter je Repo [`GithubActionsService`]: `DefaultWorkflowFilter` [log-watcher→„Build & Push"], überschreibbar via `GitHub:WorkflowFilter:<repo>`; `FetchRepoAsync` lädt `RawRunsPerRepo`=20 statt 5 und filtert per `MatchesWorkflowFilter` [Substring, case-insensitiv] vor `Take(5)`, `FetchRunByShaAsync` ebenso [dieselbe SHA triggert bei log-watcher „Tests" UND „Build & Push"] → log-watcher zeigt nur noch Build-Läufe, keine Test-Läufe. +1 BE-Test [GithubActionsServiceTests]. NICHT deployed); 0.236.10 (Admin-CI: der deployte Build-Lauf ist wieder mit GELBEM Zeilen-Hintergrund markiert [`.run.run-deployed`, `[class.run-deployed]="isRunningBuild(run,repo)"` + gelber Border-Left], nicht nur dem Icon-Badge — 0.236.5 hatte die Zeilenfarbe entfernt, User wollte aber nur ein anderes Icon, nicht den Wegfall des Highlights. NICHT deployed); 0.236.9 (Admin-CI, drei Ergänzungen: (1) laufender Build zeigt prominenten Live-Countdown im Panel-Kopf [`.ci-eta`, BLAUE Plakette `timer`-Icon, z. B. „schach-bot · fertig in ~2m 10s"]; ETA = `avgDurationSec` [Mittel der abgeschlossenen der letzten 5 Läufe je Repo, created→updated] − verstrichene Zeit; 1-s-Ticker [`nowMs`] treibt den Countdown, `recomputeEta` [ETA+deployed] bei 1-s-Tick + 5-s-Refresh. (2) prominente „Deployt"-Übersicht im Kopf [`.ci-deployed`, GELBE Chips je Stack = Repo + Ref des via `isRunningBuild` erkannten laufenden Runs]. (3) Farbschema: BLAU=baut [`.run`-Badge + ETA], GRÜN=fertig gebaut [`.ok`], GELB=deployed [`.run-live-tag` + `.ci-deployed`]; Repo-Icon `folder_open`→`code`. i18n `admin.ci.etaIn/etaSoon/etaUnknown/etaTooltip/deployed/deployedTooltip` en/de/hr. NICHT deployed); 0.236.8 (Dashboard-Kurse-Kachel zeigt jetzt die Kurs-Anzahl [dashboard.courses.count "{{count}} Kurse"] wie die Repertoires-Kachel: neuer courseCount, DashboardService.getCourses [GET /api/courses], im forkJoin geladen. +2 FE-Tests, i18n en/de/hr. NICHT deployed); 0.236.7 (Admin-CI: „live"-Badge des laufenden Builds klar vom grünen Erfolgs-Häkchen abgehoben — `.run-live-tag` jetzt BLAU (#1565c0) + Icon `fiber_manual_record` (live-Punkt) statt grün/`play_circle` [`admin-github-actions.component`]. Rein visuell. FALLE: zeigt die CI-Seite bei ALLEN Repos „HTTP 403", liegt das am GitHub-PAT [`GITHUB_TOKEN` in der rookhub-.env] — abgelaufen oder ohne `Actions: Read-only`-Permission —, NICHT am Code [UA/Accept/Api-Version im HttpClient sind gesetzt]. NICHT deployed); 0.236.6 (Repertoire-Reprocess: (a) nach erfolgreichem Chessable-Re-Fetch werden ALLE Geschwister-Repertoires desselben Users mit derselben bid mit-aktualisiert [ChessableImportService.BumpSiblingRepertoiresAsync in ImportAsRepertoireAsync] — vorher blieben Kurs-Duplikate veraltet, weil der (Owner,bid)-Dedup den Kurs nur 1x holt (Banner ging nie weg). (b) GetRepertoireStatusAsync/ReprocessRepertoiresAsync sind jetzt admin-weit [isAdmin -> alle User statt nur eigene; RefetchCandidate.OwnerId = r.UserId -> je Owner-Bearer]; RepertoireController reicht IsAdmin durch. +2 BE-Tests. NICHT deployed); 0.236.5 (0.236.5 (Admin-CI: laufender Build nicht mehr per Zeilenfarbe markiert, nur noch eigenes live-Icon-Badge [run-live-tag; .run-live-Fill entfernt]; faellt der laufende Build aus den Top-5 raus, wird er per GitHub ?head_sha= nachgeladen + als Zusatzzeile angehaengt [GithubActionsService.FetchRunByShaAsync + RunMatchesBuild + MapRun extrahiert]. Server ermittelt jetzt auch rookhubs eigene laufende SHA aus der Frontend-build-info [http://rookhub:8080/build-info.json, Frontend:BuildInfoUrl], isRunningBuild bevorzugt repo.runningSha [client-buildSha nur Fallback]. + Push-Modell-Empfangsseite: POST /api/ci/build-report [CiBuildReportController, AllowAnonymous + X-Build-Report-Key==CI:BuildReportSecret] -> GithubActionsService.ReportBuild [statischer _reportedBuilds-Cache, in ResolveRunningBuilds gemergt]. +3 BE-Tests. OFFEN: log-watcher-Client (Push). NICHT deployed); 0.236.4 (0.236.4 (Repertoire-Trainer falscher Zug: (1) "Losung zeigen" spielt den erwarteten Zug jetzt auf dem Brett [fen nach expected + lastMove-Markierung], statt ihn nur im Text zu nennen; (2) nach falschem Zug SOFORT erneut ziehbar - onMove akzeptiert einen Retry im wrong-FEEDBACK, Zug wird sofort auf startFen zurueckgenommen, Brett bleibt spielbar [viewOnly nur wenn nicht wrong-unrevealed], kein Mausrutscher-Klick noetig; Mouseslip-Button entfernt [Retry ersetzt ihn]. +Specs angepasst. NICHT deployed); 0.236.3 (0.236.3 (Admin-Kursdownload: Schalter "Geladene Kurse ausblenden" [dlHideLoaded + Getter dlVisibleCourses] blendet Kurse aus, die schon als Repertoire/Buch importiert sind [ChessableCourseDto.ImportedRepertoire/ImportedBook, bereits vorhanden]; mat-slide-toggle + i18n admin.courseDl.hideLoaded/allLoaded en/de/hr. +1 FE-Test. NICHT deployed); 0.236.2 (0.236.2 (Frontend erkennt neue Versionen schnell: app.component sucht aktiv beim Start, alle 15 min und bei visibilitychange->sichtbar via swUpdate.checkForUpdate() [neue Methode checkForAppUpdate, fehlertolerant] - lange offener Tab/PWA haengt nicht mehr auf altem Build bis manuellem Reload; VERSION_READY-Neu-laden-Snackbar wie gehabt. Spec: SwUpdate-Mock um checkForUpdate ergaenzt. + Bot v2.77.0: Monats-Endstand am 1. im Tagespuzzle-Thread statt Standalone-Post. NICHT deployed); 0.236.1 (0.236.1 (Zwei Prod-Log-Fixes nach Watcher-Alerts: (1) Weniger Fehlalarme: der anonyme Endless-Progress-Upsert [`EndlessProgressService.SaveAnonymousProgressAsync`] läuft bei gleichzeitigen Saves in eine `IX_EndlessProgresses_AnonymousSessionId`-Unique-Race — war bereits abgefangen+retried, aber EF Core loggte `SaveChangesFailed` trotzdem auf Error → triggerte den Log-Watcher grundlos. Jetzt `ConfigureWarnings` in Program.cs: `CoreEventId.SaveChangesFailed`→Information [genuine, NICHT abgefangene Fehler bleiben übers HTTP-500-Request-Log sichtbar; betrifft alle bewusst abgefangenen DbUpdateException-Races: CoursePuzzleResult/CourseInfoView/CourseProgress/…]. (2) `POST /api/courses|repertoires/reprocess` lief bei vielen Chessable-Kursen über das ~60-s-Request-Timeout → 500 „operation was canceled". Neu: `IReprocessLauncher`/`ReprocessLauncher` [Singleton, eigener DI-Scope, `Task.Run` mit `CancellationToken.None`, Fehler nur geloggt] startet den Reprocess im Hintergrund; beide Endpoints geben sofort `202 Accepted {started:true}` zurück [nicht mehr synchron/`ct`-gebunden]. FE `ReprocessBannerComponent.run` erwartet keine Counts mehr → Snackbar `reprocess.started` [en/de/hr] + Status-Refresh nach 2,5 s. +Test-Doppel `RecordingReprocessLauncher`, reprocess-banner-Spec auf fakeAsync/202 umgestellt. NICHT deployed); 0.236.0 (Admin-CI markiert das laufende Image jetzt auch für crawler/piratechess/bot, nicht nur rookhub-Frontend: jeder Dienst hat einen build-info-Endpoint [crawler `GET /api/health/build-info`, piratechess `GET /api/chessable/direct/build-info` (ServiceKey), bot `GET /webhook/build-info`], gespeist aus CI-Build-Args `GIT_SHA`/`GIT_REF`→ENV. `GithubActionsService.ResolveRunningBuildsAsync` pollt sie parallel [best-effort, 4s-Timeout, im 4s-Overview-Cache], `CiRepoDto.RunningSha/RunningRef`; FE `isRunningBuild(run, repo)` nutzt für Fremd-Stacks repo.runningSha/ref [rookhub weiter client-seitig], Match per SHA+Ref → :dev=master-Run, :prod=Tag-Run. Nicht erreichbar/altes Image = kein Marker. +2 BE-Tests [GithubActionsService], +1 FE-Test [Fremd-Stack], +Crawler-/piratechess-/Bot-Endpoint-Tests. NICHT deployed); 0.235.1 (0.235.1 (Admin-CI markiert genau den EINEN Run, der das laufende Image baute: build-info.json trägt jetzt zusätzlich den Build-Ref [Branch bei :dev, Tag-Name bei :prod; docker.yml `GIT_REF=github.ref_name`, Dockerfile `ARG GIT_REF`, `{sha,ref}`]. master-Push + gleichnamiger Tag haben dieselbe SHA → reines SHA-Matching markierte beide; `isRunningBuild` verlangt jetzt SHA UND [sofern gemeldet] Ref-Gleichheit → :dev hebt master-Run, :prod den Tag-Run hervor. Altes Image ohne Ref = SHA-only-Fallback. +4 FE-Tests [neues admin-github-actions.component.spec]. NICHT deployed); 0.235.0 (Zwei Kurs-/Buch-Verbesserungen: (1) Sequenziell durchgeklickte Info-/Erklärlinien werden gemerkt [neue Tabelle `CourseInfoView` + Migration `AddCourseInfoView`, Endpoint `POST /api/courses/{bookId}/info-seen`, `CourseService.MarkInfoSeenAsync`; seqPool überspringt gesehene Info-Linien via `CourseInfoViews` statt sie IMMER zu zeigen; FE `courseNext` meldet die aktuelle Info-Linie + merkt sie offline in `offlineCourseSolvedIds`; beim Reset geleert; Cleanup in `BookAdminService.DeleteBookAsync`]. (2) Doppelte Kommentar-Anzeige entdoppelt: `puzzle.comment`-Kopfzeile [Template Z.88] UND `move-comment-card` [unten] ersetzt durch EINEN `ctx-comment`-Block in der Kontext-Card ÜBER der Fortschrittsanzeige, gespeist vom neuen Getter `displayComment` [Review/Info→moveComment mit Fallback auf puzzle.comment, sonst puzzle.comment] — gilt für alle Modi, nicht nur Info-Linien. +4 BE-Tests [1156] +5 FE-Tests. NICHT deployed); 0.234.1 (piratechess-Fix [Repo bb74686]: TryGetChessableErrorMessage fällt bei leerem error.message auf userFriendlyErrorMessage zurück → nicht besessene Kurse melden ehrlich 'You do not own this course.' (BOOK_NOT_OWNED) statt fälschlich 'Course has no chapters'. +1 Regressionstest. [Analyse: die 4 'no chapters'-Kurse des Re-Fetchs gehören dem aktuellen Bearer-Account uid 790927 nicht]); 0.234.0 (Navigation läuft jetzt IMMER über das Hamburger-Menü — auf allen Auflösungen, nicht nur mobil [`navbar.component`: `.nav-links` generell `display:none`, `.mobile-menu-btn` generell `inline-flex`; der 768px-Media-Query blendet nur noch die `.nav-extra`-Sekundär-Icons aus]. Bei den vielen Menüpunkten lief die Toolbar sonst auch auf mittleren/großen Screens über. NICHT deployed); 0.233.2 (Zwei Anarchy-Politur-Punkte: (1) Setup-Zug-Pfeil-Fix — der erste (automatische) Gegnerzug, der die Puzzle-Stellung aufbaut, bekommt jetzt auch den Gegnerzug-Pfeil [`showVizOpponentArrow()` im Setup-Timeout von `BasePuzzleSolver`, nach `playMove(setupMove)`]; vorher nur die Antwortzüge [advanceAfterCorrectMove/opponentRespond], sodass genau der stellungserzeugende Zug unmarkiert blieb — besonders auf dem Crazy-Brett störend. Gilt für alle Modi [+1 base-Solver-Test]. (2) Anarchy-Hints [`puzzles.anarchyHint1-3` en/de/hr] memefiziert [Holy hell / Google en passant / New response just dropped]. NICHT deployed); 0.233.1 (Mobil-Fix: Dashboard beim Anpassen wieder scrollbar — im Bearbeitungsmodus war die GANZE Kachel die Drag-Fläche [`.tile-shield` = `cdkDragHandle` über `inset:0`], sodass auf dem Touchscreen jede Berührung einen Drag auslöste statt zu scrollen. Gezogen wird jetzt nur noch am dedizierten Griff [`drag_indicator`-Icon je Kachel als `cdkDragHandle` in `.tile-controls`], der Kachel-Rumpf scrollt normal; Sortieren via ↑/↓-Pfeile bleibt. `.tile-shield` ist kein `cdkDragHandle` mehr [nur noch Klick-Fänger]. i18n `dashboard.edit.dragAria` wiederverwendet. NICHT deployed); 0.233.0 (Admin-CI-Run-Zeile aussagekräftiger: Ref (Branch/Tag-Name mit Icon; Tag-Läufe haben kein head_branch → GithubActionsService löst Tags per /tags sha→Name-Map auf, CiRunDto.Ref/IsTag), Startzeit (createdAt), Laufzeit (created→updated bzw. live bis jetzt), Titel bleibt Link zum Run (keine sichtbare URL). i18n admin.ci.started/duration. Tags-Abruf = +1 GitHub-Call/Repo (gecacht); Cache-Test angepasst); 0.232.0 (Admin-CI-Seite markiert den GitHub-Actions-Run des LAUFENDEN Frontend-Builds: Frontend-Dockerfile schreibt Commit-SHA (ARG GIT_SHA aus docker.yml --build-arg github.sha) nach /build-info.json; CI-Seite holt das + markiert den Run mit passendem head_sha grün ('live'). CiRunDto/GithubActionsService um HeadSha erweitert. Greift erst nach Deploy eines so gebauten Images; nur rookhub-Frontend meldet Build-SHA. +1 BE-Test-Assert); 0.231.0 (Repertoire-Reprocess auf denselben Stand wie Kurse gebracht + Zentralisierung: die gemeinsame Re-Fetch-Logik [Batch-Cache-Abruf 1×, kein Abbruch am Request-ct, Backoff für nicht-cachebare Kurse, Dedup, Admin-`trustOwnership`] liegt jetzt in EINER Methode `ImportReprocessService.EnqueueRefetchesAsync(candidates, isAdmin, result)` [+ `RefetchCandidate`-Record]; `ReprocessCoursesAsync`/`ReprocessRepertoiresAsync` sammeln nur noch Kandidaten. `ReprocessRepertoiresAsync` bekam `isAdmin`-Param [Default false], `IsAdmin` in `BaseApiController` hochgezogen [aus CourseController], RepertoireController reicht es durch. Vorher hing „Alle aktualisieren" bei Repertoiren [alter langsamer/blockierender Pfad]. +1 BE-Test [1152 grün]. NICHT deployed); 0.230.0 (Crazy-Modus Gegnerzug-Pfeil: der Gegnerzug wird auf dem Crazy-Brett mit einem GRÜNEN Pfeil markiert, der bis zum nächsten User-Zug stehen bleibt. BasePuzzleSolver: neuer overridebarer opponentArrowMode ['off'|'timed'|'persist']; showVizOpponentArrow setzt bei 'persist' keinen 1s-Timer, onMoveMade löscht den Pfeil beim User-Zug. Komponenten: crazy→'persist'. Board: applyVizOpponentArrow ohne visualization>0-Guard + neuer Input vizOpponentArrowBrush [crazy 'green', Viz 'red']. +2 base-Solver-Tests); 0.229.0 (Anarchy-Tipps: bei e.p.-Zwang liefern alle 3 Puzzle-Tipps drei unterschiedlich formulierte En-passant-ist-Pflicht-Hinweise statt der normalen Tipps. Zentral in BasePuzzleSolver [effectiveHints = enPassantForced && epForcedHints ? epForcedHints : availableHints; hasHints/shownHints/canShowMoreHints darauf], epForcedHints je Solver überschrieben [i18n puzzles.anarchyHint1-3 en/de/hr]. +2 base-Solver-Tests); 0.228.0 (1) Off-Path-Warnung in allen Puzzle-Modi: BasePuzzleSolver zählt off-path-Züge [offPathUserPlies], berechnet in opponentRespond aus currentEval [Weiß-Sicht] playerEvalPawns() [Spieler-Sicht, Matt=±100] und warnt EINMALIG [onOffPathWarning()-Hook → Snackbar in allen 3 Solvern] ab offPathWarnThreshold Zügen, wenn Eval < +2. Reset in setupSolver + Mouseslip-zurück-auf-Pfad. Schwelle = Pref offPathWarnMoves [lokal, Default 3, 0=nie], im Settings-Dialog. (2) Anarchy: Settings-Toggle En-passant-forciert [Pref enPassantForced, Default an, nur bei themeMode=crazy sichtbar]; enPassantForced in onSetupStart = anarchyForcedByUrl [URL ?anarchy=max] ODER (crazy && Pref). +7 base-Solver-Tests; 0.227.0 (Logging-Flag `RequestKind` [system/user]: automatische, nicht nutzer-initiierte Requests werden als `system` markiert → in Kibana filterbar. Neuer `Logging/SystemCallClassifier` [Health/Swagger, /api/client-log, /api/menu, `/count`|`-count`|`-counts`-Polls, chessable/admin/active|imports]; gesetzt via `LogContext.PushProperty("RequestKind", …)` in der Enrichment-Middleware [neben UserId/VisitorId] + im HeartbeatService [`system`]. +14 Classifier-Tests. Reine Observability. NICHT deployed); 0.226.0 (Fix „Alle aktualisieren" für Kurse tat bei Admins NICHTS: `OwnerHasCourseAsync` prüfte jeden bid gegen Chessables getHomeData, das nur einen TEIL der Bibliothek listet [User 5: 32 statt 259] → 262 übersprungen/0 eingereiht [+142s/500er]. Neu: `EnqueueReimportAsync` bekam `trustOwnership`-Param; `ReprocessCoursesAsync` setzt ihn = `isAdmin` → Admin-Reprocess überspringt die Eigentumsprüfung [Admins dürfen ohnehin alles holen]; Nicht-Admin + Repertoire-Reprocess unverändert geprüft. +3 BE-Tests. 259/262 sind gecacht → Fast-Lane. NICHT deployed); 0.225.0 (Endless-Deep-Link-Query-Parameter [`endless-puzzle.component` ngOnInit]: `?start=1`/`?autostart=1` [Auto-Start ohne Config via `maybeAutoStart()` nach Rating-Range-Load], `?themes=…`/`?tags=…` [Komma/Leerzeichen→`config.themes`, worstTags off, ODER-Filter], `?elo=…` [startElo], `?anarchy=max|max+1` [reused `parseShareViewParams` → themeMode='crazy'+enPassantForced, max+1 crazyPieceMode='square'; `[crazyPieceMode]` ans Endless-Board gebunden]. +6 FE-Tests); 0.224.1 (CSP-Fix: eigenes Inline-Script in `index.html` [PWA-`beforeinstallprompt`-Hook] wurde von der strikten CSP [`script-src 'self'`] blockiert → in externe `public/pwa-install-hook.js` ausgelagert [von `'self'` gedeckt, kein `'unsafe-inline'`/Hash]. CSP-Warnungen aus `content.js` = Browser-Extensions, korrekt geblockt, kein RookHub-Bug); 0.224.0 (Admin-Tab „CI / Actions": letzte 5 GitHub-Actions-Läufe je beteiligtem Repo [kahalm/rookhub,-chessresults_crawler,-schach-bot,-piratechess_docker,-log-watcher], Auto-Refresh alle 5 s, Status-Badge + Link. Backend: `GithubActionsService` [getypter HttpClient→api.github.com, Token aus `GitHub:Token`/Env `GitHub__Token` (PAT actions:read) pro Request, Repos `GitHub:Repos` Default=die 5, Owner `GitHub:Owner`=kahalm] + IMemoryCache-Aggregat-Cache `GitHub:CacheSeconds`=4 → 5-s-Polls rate-limit-schonend; ohne Token `Configured=false`→UI-Hinweis. `CiController` `GET /api/admin/ci/runs` [Admin]. FE: eigenständiger `AdminGithubActionsComponent` [timer(0,5000), lazy `*matTabContent`→nur bei offenem Tab], neuer Tab 'ci' [admin-tabs.ts+spec+HTML]. +4 BE-Tests. i18n en/de/hr); 0.223.0 (Neues Puzzle-Theme `enPassantPossible`: `PuzzleService.HasUnplayedEnPassant(fen, uci, firstSolverPly=1)` [pure, Gera.Chess: FEN laden, UCI-Lösung nachspielen, NUR an Löser-Zügen (Indizes 1,3,5… — moves[0]=Gegner-Setup) `board.Moves()` auf `MoveEnPassant` prüfen; e.p. verfügbar + gespielter Zug ≠ e.p. → true] + `TagEnPassantPossibleAsync` [batchweise, idempotent] taggt betroffene Standard-Puzzles im `Themes`-String UND der normalisierten `PuzzleTags`-Tabelle [get-or-create Tag]; Admin-Endpoint `POST /api/admin/puzzles/tag-en-passant` [Hintergrund-Job]. Abgegrenzt von Lichess-`enPassant` [Lösung IST e.p.]. Nur Standard-Puzzles. Kein neues DB-Feld/Migration. +7 BE-Tests); 0.222.0 (Chessable-Flut durch nicht-cachebare Kurse gestoppt — zwei rookhub-only Schutzmechanismen (keine Migration): (1) DEDUP: `ChessableImportService.EnqueueReimportAsync` legt keinen zweiten Import an, wenn für (Owner, bid) bereits einer läuft/pausiert → ein erneuter „Update all"-Klick/Retry löst keine doppelten Kurs-Downloads mehr aus (beobachtet: bid 116242 4× in Folge). (2) BACKOFF: `ImportReprocessService.ReprocessCoursesAsync` überspringt einen stale, NICHT gecachten Kurs, dessen letzter Import <24h her ist (`IncompleteRefetchBackoff`) — ein truncated Kurs wird von piratechess nie gecacht und würde sonst bei JEDEM Massen-Reprocess komplett neu von Chessable geholt (Hunderte Line-Fetches); nutzt die bereits geholte `cachedBids`-Menge + `ChessableImports.CompletedAt`. Gezielter Admin-Re-Import umgeht den Backoff. +3 BE-Tests [1114 grün]. NICHT deployed); 0.221.0 (Reprocess „Alle aktualisieren“ robust+schnell + gecachte Kurse trotz gesperrtem Bearer: (1) `ImportReprocessService.ReprocessCoursesAsync` holt den Cache-Status ALLER Kurse EINMAL per Batch [`ICourseReimporter.GetCachedBidsAsync` → piratechess `/courses/cached`] statt je Kurs den teuren `IsCourseCachedAsync` [lädt+entpackt den ganzen Blob]; `EnqueueReimportAsync` bekam `knownCached`-Param [`FullyCached = knownCached ?? IsCourseCachedAsync`]. (2) Die Einreih-Schleife läuft mit `CancellationToken.None` statt dem Request-ct → Wegnavigieren [Browser schließt Verbindung] bricht das Einreihen NICHT mehr ab [vorher nur ~20 von 268 in der Queue]. (3) `RunAsync`-Bearer-Breaker-Check `&& import.FullyCached != true` → voll-gecachte Kurse kommen aus dem Cache [kein Chessable-Abruf] und laufen trotz totem/gesperrtem Bearer durch. +4 BE-Tests [1097 grün]. NICHT deployed); 0.220.1 (Kurs-Upload-Hinweis: neuer i18n-Text `courses.upload.restriction` [en/de/hr] unter dem Upload-Panel — funktioniert nur mit Puzzle-PGNs im Chessable-Stil, sonst Admin ansprechen oder im Discord fragen [klickbarer `DISCORD_INVITE_URL`-Link aus `core/community.ts`]. Reiner Text/UI, keine Logik); 0.220.0 (Eigenes PGN als persönlicher Kurs hochladen: Kurse-Seite ist jetzt für JEDEN eingeloggten Nutzer erreichbar [`courseAccessGuard` lässt alle Eingeloggten durch, Einstieg über die Dashboard-Kachel — Navbar bleibt content-gated via `checkAccess`], auch bei leerer Liste; oben ein Upload-Panel [PGN-Datei + optionaler Name]. Backend: `CourseService.UploadPersonalCourseAsync` [reused `PgnImportService.ImportFileAsync`, pro-User-eindeutiger Dateiname `user-u{id}-{guid}.pgn`, setzt `Book.OwnerUserId`+`Kind=Study`+Tags `personal`; leeres Ergebnis → Buch wieder entfernt+400] + `DeletePersonalCourseAsync` [nur Besitzer, reused `BookAdminService.DeleteBookAsync`]; Endpoints `POST /api/courses/upload` [multipart] + `DELETE /api/courses/{bookId}`. NUR Chessable-Stil-Puzzle-PGNs [FEN+Round+Trainingsmarker] erzeugen Puzzles — normale Partie-PGNs werden verworfen. Löschen-Icon je eigenem Kurs. Kein neues DB-Feld/Migration. +6 BE-Tests [1100] +5 FE-Tests; i18n `courses.upload.*`/`courses.delete*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.219.1 (Repertoire-Trainer: alternativer (geduldeter) ODER falscher Zug bleibt jetzt ~1 s sichtbar auf dem Brett stehen, bevor er zurückgenommen wird, statt sofort zurückzuspringen. `onMove` setzt `this.fen = fenAfterPlayerMove` auch für tolerated/wrong [vorher tolerated sofort `lastMove=undefined`/wrong nur Markierung]; neue `startFen`-Erfassung am `onMove`-Anfang. Geduldet: ~1,8 s gehalten [`ADVANCE_MS.tolerated`], dann `retryCurrentCard` nimmt zurück [`fen=startFen`] + dieselbe Karte wieder spielbar. Falsch: neuer `WRONG_HOLD_MS`=1000-Timer [`scheduleWrongRevert`/`revertWrongHold`] nimmt zurück, bleibt im FEEDBACK [Mausrutscher/Lösung-zeigen-Buttons]; `clearWrongRevert` in `clearAdvance`+`mouseslip`+`showSolution`. 7 Trainer-Specs grün, PROD-Build grün); 0.219.0 (Zug-lose Kurs-Erklär-/Intro-Seiten [Kommentar, keine Züge] werden beim Buch-/Kurs-Import nicht mehr verworfen: `PgnImportService.ParsePgn(..., keepCommentOnlyAsInfo:true)` [von `ImportFileAsync` gesetzt; Wochenpost-Pfad bleibt Default false] behält sie als Info-Linie [synthetischer Fake-Zug e4 ab Grundstellung, `IsInfoOnly`, Text als Kommentar] → nur sequenziell zum Durchklicken, kein Quiz, nicht in Random/Daily, nicht im Fortschritt. `ImportPipeline.CurrentVersion` 4→5; die Seiten stehen schon im `Book.SourcePgn` → Reprocess ist hier ein reiner LOKALER Re-Import, KEIN Chessable-Re-Fetch. +4 BE-Tests [1094]); 0.218.1 (Repertoire-Trainer-Zug-Feedback gefixt: `onMove` setzte `this.fen` nie auf die Stellung NACH dem Zug → bei jedem Zug Brett-Take-back + Replay über die Folgekarte [Flackern]. Jetzt: richtig → `this.fen = fenAfterPlayerMove` [Zug bleibt stehen, kein Flackern]; geduldet/spielbar → `lastMove=undefined` + `this.fen` unverändert [Zug zurückgenommen] und neue `runAdvance`/`retryCurrentCard`: geduldet springt NICHT weiter [kein Vorspielen des Hauptzugs], sondern macht dieselbe Karte erneut spielbar, sodass der User den Hauptzug selbst zieht. `onPlayClick`+Timer rufen `runAdvance`. Spec: tolerated-Test auf Retry+Take-back umgestellt + neuer correct-„kein Flackern"-Test, 780 FE grün); 0.218.0 (Anarchy-Stufe `?anarchy=max+1`: wie max [Crazy-Brett + erzwungenes En passant] PLUS das FELD bestimmt den Figurenstil [`crazyPieceMode='square'`] — Figur nimmt das Zufallsset ihres Feldes an, beim Ziehen wechselt das Aussehen zum Zielfeld. `board-theme.util`: `paintCrazyPieces(root, mode, orientation)` + `squareOfPiece` [Feld aus `transform: translate`], 64er-Feld→Set-Map; `parseShareViewParams` matcht `max+1`/`max 1` [+-Dekodierung]. `BasePuzzleSolver.crazyPieceMode` → `@Input` am `app-puzzle-board`, `repaintCrazyPieces` mit Settle-Repaint [~230ms] nach der Zug-Animation. +5 board-theme-Specs [25]); 0.217.1 (Fix: „Letztes Puzzle"-Favoriten-Knopf zeigte zwei Herzen [mat-icon + ♥ im Label-Text der i18n `favorites.loveLast`]; ♥-Präfix aus en/de/hr entfernt, nur das Icon bleibt); 0.217.0 — 0.217.0 (Standard-Puzzle-Links beachten jetzt `?single=1` [wie Buch-Puzzles]: `puzzle.component` liest `single` aus der Query → nach dem Lösen KEIN Auto-Weiter [`handleSolved` überspringt `startSolvedCountdown`/`loadNext` bei `singlePuzzle`], der „Weiter"-Knopf [`showNext` default true] bleibt aber. +1 Spec. Der Standard-Teilen-Dialog hängt `single=1` NICHT automatisch an [nur Buch] — Link wird bei Bedarf von Hand erweitert); 0.216.0 (Geliebte Puzzles ♥: nach Lösen/Aufgeben jedes Puzzles [Standard/Endless/Kurs-Buch/Tagespuzzle] ein Herz-Button in der Status-Card [`PuzzleStatusCardComponent` `canFavorite`/`isFavorite`/`favoriteToggled`]. Der „Letztes analysieren"-Knopf ist in zwei geteilt: „Letztes analysieren" + „♥ Letztes Puzzle". Backend polymorph wie `PuzzleChallenge` [`FavoritePuzzle` Standard/Book via `PuzzleSource`, Unique (UserId,Source,PuzzleId), Migration `AddFavoritePuzzles`], `FavoriteService` [add/remove/contains idempotent, list mit Fen+Moves+Rating+Themes+Title-Enrichment + Skip verwaister Einträge], `FavoriteController` `api/favorites` [GET/`count`/`contains`/POST/DELETE], Menü-Key `favorites`. Neue Seite `/favorites` [`FavoritesComponent`, Nachspielen/Analysieren/Entlieben] + Dashboard-Kachel `favorites` [default-hidden] + Navbar. FE-Logik gekapselt in `FavoriteTracker` [optimistisch + Rollback, current/last synchron], in alle 3 Solver eingehängt. Wochenpost-Puzzles NICHT favorisierbar [keine echte BookPuzzle-Id]. +8 BE-Tests [1090] +15 FE-Tests [773]. i18n `favorites.*`/`nav.favorites`/`dashboard.favorites.*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.215.0 (Crazy-Brett würfelt jetzt JEDE Figur einzeln aus statt pro Rolle+Farbe [vorher „alle Bauern gleich"]: `board-theme.util` ersetzt das per-Rolle-`!important`-CSS [`applyCrazyPieces` entfernt] durch `paintCrazyPieces(root)` — pro `<piece>`-DOM-Element ein zufälliges Set via Inline-`background-image`, stabil über `WeakMap<Element,set>` [Figur behält ihr Set beim Ziehen; neues Puzzle/`redrawAll` würfelt neu]. `puzzle-board.component` ruft `repaintCrazyPieces()` [rAF] nach Init, nach jedem `ground.set` und nach Resize-`redrawAll`; sonst `clearCrazyPieces`. Figuren-CSS in `styles.scss` ist `.piece-set-<set>`-skopiert ohne `!important` → für `_crazy` greift nur das Inline. +5 board-theme-Specs [20]. Gilt für Crazy-Theme + `?anarchy=max`); 0.214.0 (Anarchy-Modus `?anarchy=max` an jedem Puzzle-Link [Standard + Kurs/Buch]: Crazy-Brett UND en passant verpflichtend [„en passant is forced"]. `parseShareViewParams` parst `anarchy=max`→`{themeMode:'crazy', enPassantForced:true}`; `BasePuzzleSolver.enPassantForced` an `calcDests(chess, enPassantForced)` durchgereicht → ist ein En-passant-Schlag verfügbar [chess.js-Flag `e`], enthält die dests-Map NUR diese Züge, sonst alle. `puzzle.component`+`book-puzzle.component` setzen das Flag aus dem Query. Rein transient, ändert keine gespeicherten Einstellungen. +2 Specs `puzzle-move.util` +2 `board-theme.util`. Hinweis: paralleler 0.213.1-Fix [Reprocess→Re-Fetch] liegt uncommittet in Kopie 1; daher Sprung auf 0.214.0); 0.213.1 (Reprocess-Banner-Fix für die Info-Linien-Änderung: bestehende Chessable-Kurse haben ein gecachtes Alt-PGN OHNE `[%info]`-Marker; `ImportReprocessService.GetCourseStatusAsync`/`ReprocessCoursesAsync` stuften sie als „lokal aufbereitbar" ein → lokales Reprocess hätte `ImportVersion` auf 4 hochgesetzt OHNE `IsInfoOnly` zu setzen [still falsch, Banner verschwindet]. Neu: Chessable-Kurse [`CanRefetch` = `IsChessable` + parsebare bid] werden IMMER als `Refetchable` eingestuft und per vollständigem Re-Fetch aktualisiert [Chessable-Zweig VOR dem SourcePgn-Pfad], nur Nicht-Chessable-Bücher mit Quelle laufen lokal. Matcht den v4-Doc-Kommentar [„für Chessable-Bücher ist der Reprocess ein Re-Fetch"], der vorher nur für Repertoires zutraf. 3 bestehende Tests auf Nicht-Chessable-Lokalbuch umgestellt + 1 neuer Test [`ReprocessCourses_ChessableWithSource_RefetchesInsteadOfLocalReprocess`]; 12 Reprocess-Tests/1082 BE grün. NICHT gepusht/getaggt/deployed); 0.213.0 (Frontend-Durchklick-UI für Chessable-Info-/Erklärlinien [Backend-Seite war 0.212.0]: `book-puzzle.component` bekam den Zustand `'INFO'` — `setupPuzzle` zweigt bei `puzzle.isInfoOnly` in `enterInfoReview()` ab [kein `setupSolver`, Brett view-only, `reviewMode`, ◀/▶ über die ganze Linie + Zug-Kommentare], statt Status-Card eine „Erklärlinie"-Card, Kontext-Nav zeigt „Weiter" [`courseNext`/`nextInBook`]. Board-`[visualization]` im Review/Info auf 0. i18n `book.info.title/subtitle` + `book.actions.prev/next` en/de/hr. +2 FE-Specs. HINWEIS: greift nur für aus Chessable neu importierte/abgerufene Kurse); 0.212.0 (Chessable-Info-/Erklärvarianten [von Chessable als IsInfo markiert] werden nicht mehr als Quiz abgefragt: piratechess emittiert `[%info]` im PGN [Commit `6f5ed38`], rookhub-Import setzt daraus `BookPuzzle.IsInfoOnly` [+Migration `AddBookPuzzleIsInfoOnly`, +`ImportPipeline.CurrentVersion` 3→4]. Solche Linien fallen aus ALLEN Zufalls-/Tagespuzzle-Töpfen [`BookPuzzleService`-Pool + `GetRandomInBookAsync`], zählen NICHT zum Kurs-Fortschritt [`CourseService`: `quizScope` ohne `IsInfoOnly` in `GetNextAsync`/`GetChaptersAsync`/`GetCoursesAsync`/`BuildProgressAsync`/`ComputeStatsAsync`] und sind im sequenziellen Kurs-Modus nur zum Durchklicken [seqPool nimmt Info-Linien mit, Completion bleibt quiz-basiert]. `BookPuzzleDto.IsInfoOnly` durchgereicht. HINWEIS: Marker entsteht erst beim frischen Chessable-Abruf → bestehende Kurse erst nach Re-Import/Re-Fetch. Frontend-Durchklick-UI folgt [0.213.0]. +7 BE-Tests [1081] +2 piratechess-Lib-Tests [16]); 0.211.0 (Dashboard-Customize „Standard"-Knopf + kuratierter Default: `applyDefault()` [ersetzt `resetLayout`] setzt `DEFAULT_VISIBLE` = Puzzles/Weekly/Repertoires/Kurse/Trainingsziele/Bestenlisten [in dieser Reihenfolge] sichtbar, alle übrigen Kacheln ausgeblendet [`DEFAULT_HIDDEN`], und persistiert. Dieser Default ist auch das Initial-Layout [ngOnInit: leeres gespeichertes Layout → kuratierter Default]. `DashboardLayoutService`-Key auf `rookhub_dashboard_layout_v2` erhöht → alle User starten einmalig frisch auf dem Default. Button-i18n `dashboard.edit.default` [Standard/Default/Zadano] en/de/hr. Spec-Anpassungen [Default-Layout, chessableQueue jetzt default-hidden → Edit-Modus-Checks]. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.210.0 (Visualisierungs-Umschalter unter dem Brett (0.207.0) WIEDER ENTFERNT — gefiel nicht. `VizModeSelectorComponent` + Spec gelöscht, viz-0..4-SVG-Icon-Registrierung aus `app.component` raus, Import/Imports-Array in allen drei Solvern entfernt. Stattdessen der alte Aufdecken-Knopf zurück in `.board-hint` (`@if vizPiecesHidden || visualizationMode===1`, ruft `onVizShow()`), aber als **Auge-Icon** statt Text: `vizShowPressed ? 'visibility' : 'visibility_off'` (durchgestrichen solange verdeckt, offen wenn aufgedeckt) + `.board-hint-eye`-Style; Tooltip/aria via vorhandene `*.viz.show`/`showing`-Keys. Modus-Wahl wieder nur im Einstellungsdialog. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.209.1 (Build-Fix: gerades Anführungszeichen im de-0.209.0-Changelog [`„keine Treffer"`] beendete den TS-String vorzeitig → Production-Build [CI] rot; auf typografisches `„…“` korrigiert. FALLE [[rookhub-changelog-build-gotcha]]: nach Changelog-Edit IMMER `npx ng build` [Production], nicht nur `--configuration development` — der Dev-Build lief vor dem Changelog-Edit und übersah es); 0.209.0 (Suchfeld für Kurse + Repertoires: oben in `course-list`/`repertoire-list.component` ein `mat-form-field`-Suchfeld [`[(ngModel)]="search"`, Such-Icon + Leeren-Knopf, `subscriptSizing="dynamic"`]; clientseitige Filterung über die bereits geladene Liste [Kurse nach `displayName`, Repertoires nach `name`+`description`, case-insensitive]. Kurse: neuer `filtered`-Getter, die Sektions-Getter [inProgress/public/chessable] bauen darauf auf → Filter wirkt über alle Sektionen; `courses.noMatch`-Hinweis. Repertoires: `filteredRepertoires`-Getter, `@empty`→`repertoire.list.noMatch` vs `empty`. FormsModule+MatFormField/Input ergänzt. i18n `common.clear`, `courses.searchPlaceholder`/`noMatch`, `repertoire.list.searchPlaceholder`/`noMatch` en/de/hr. +1 course-Spec +4 repertoire-Specs [756 grün]. NICHT deployed); 0.208.0 (Dashboard-Bearbeitungsmodus ändert die Ansicht NICHT mehr: das Kachelraster behält sein Layout [kein flex-column-Umbau mehr], es werden nur alle geeigneten Kacheln gezeigt [deaktivierte `tile-off` ausgegraut]. Je Kachel Overlay oben rechts: ↑/↓-Pfeile [`moveUp`/`moveDown` → `shift`/`commitOrder`] + Auge-Toggle; transparentes `.tile-shield` [zugleich `cdkDragHandle`] fängt Klicks auf den Kachel-Buttons ab [keine versehentliche Navigation] und ist die Drag-Fläche. Drag&Drop bleibt, jetzt `cdkDropListOrientation="mixed"` [rastertauglich, CDK 19]. i18n `dashboard.edit.moveUp`/`moveDown` + hint-Text en/de/hr. +2 FE-Specs [751 grün]. NICHT deployed); 0.207.0 (Visualisierungsmodi als Inline-Icon-Umschalter in der Solver-Hinweiszeile [ersetzt den „Aufdecken"-Knopf]: neue `VizModeSelectorComponent` [5 Buttons, Output `modeChange`→ruft das vorhandene `setVisualizationLevel($event)`] in `.board-hint` aller drei Solver [puzzle/book-puzzle/endless-puzzle.component.html]; aktiver Modus hervorgehoben. 5 SVG-Icons global via `MatIconRegistry.addSvgIconLiteral` in `app.component` [`viz-0`…`viz-4`, wie Discord-Logo]: 0=Bauer, 1=Bauer gedimmt+Kette, 2=schwarz/weiß-Token [fix], 3=weißes Token [fix], 4=durchgestrichenes Auge [gefüllt, Iris+Pupille via Maske]. 0/1/4 `currentColor`, 2/3 feste Farben. Aufdecken = Modus 0 wählen [`board-hint-show`+`onVizShow` aus Templates raus; Base-Logik bleibt]. i18n `puzzles.viz.modeSelectAria` en/de/hr. +3 FE-Specs [748 grün]. Versionssprung 0.206.0→0.207.0 [paralleler 0.206.0-Kollision mit Revenge-Icon ausgewichen]. NICHT deployed); 0.206.0 (Freundesliste: Revenge-a-Friend-Icon wird ROT [+ Anzahl-Badge], wenn offene Revanchen gegen den Freund existieren [Freund gescheitert+nie gelöst, ich noch nicht gelöst]. `GET /api/friends` reichert je Freund `FriendDto.OpenRevengeCount` an [`PuzzleService.GetOpenRevengeCountsAsync`, provider-sicher in-memory aggregiert]; FE `friends.component` bindet `[color]`+`matBadge`, i18n `friends.revenge.menuOpen` en/de/hr. +1 BE-Test [25] +1 FE-Test [8]); 0.205.3 (Observability: Admin-Impersonation pro Request sichtbar — die Request-Logging-Middleware [`Program.cs`] schreibt den `imp`-Claim [Admin-Id] jetzt als `LogContext`-Property `ImpersonatorId` [→ ECS `labels.ImpersonatorId`], sodass impersonierte Requests in Kibana filterbar sind; vorher nur das einmalige Impersonation-Start-Event geloggt. Reines Logging-Plumbing analog `UserId`/`UserName`; `imp`-Claim-Quelle bereits in `AuthServiceTests` getestet); 0.205.2 (Chessable-Bearer-Circuit-Breaker GEMERGT [vorher lokales v0.204.0, Commit `d6f0995`] und mit der entfernten 0.203.x-Chessable-Security-Linie versöhnt: weist Chessable einen Bearer endgültig ab [Account gesperrt/gelöscht oder Token tot — `ChessableBearerBreaker.IsBearerFatal`, NICHT bei IP-/Cloudflare-/VPN-Block], sperrt ihn [`ChessableCredential.BlockedAt`/`BlockedReason` + Migration `AddChessableCredentialBreaker`] und verwendet ihn für KEINE weitere Anfrage. Trip in `ChessableImportService.FailAsync` + `ChessableController` [Test/Courses/Admin-Courses/Estimate]; laufende Importe PAUSIEREN [`Phase="bearer-blocked"`], Lese-Abrufe/neue Importe → 400. Reset = erfolgreicher „Testen"-Klick [`ClearAndResumeAsync`]; neuer Bearer-Save schließt den Breaker. Admin-Test-Endpoint `POST admin/users/{id}/test`. FE: Sperr-Banner + 🔒/„Bearer testen" im Admin-Kursdownload. Merge-Konflikt: in `StartImportForUserAdmin` sitzt die Bearer-Sperr-Prüfung VOR der neuen `UserOwnsCourseAsync`-Eigentumsprüfung [0.203.8], damit ein toter Bearer keinen Fetch auslöst. +12 BE-Tests. Versionssprung 0.205.1→0.205.2 [0.204.0 in 0.205.2 aufgegangen]); 0.205.0 (Anpassbare Dashboard-Kacheln: Dashboard ist ein konfigurierbares Kachelraster über alle sinnvollen Module [Puzzles/Trainingsziele/Kurse/Bestenlisten/Turniere/Freunde/Repertoires/Partien/Wochenpost/Statistik/Analyse/Nachrichten + Admin-Chessable-Queue]. „Anpassen"-Knopf → Bearbeitungsmodus: Kacheln per CDK-Drag&Drop sortieren + per Augen-Knopf ein-/ausblenden, „Zurücksetzen" → Default. Persistenz pro Gerät via `DashboardLayoutService` [localStorage `rookhub_dashboard_layout`, `{order,hidden}`]; Eignung je Kachel über `MenuService.visible$` [+ `auth.isAdmin` für Chessable]; neue Kacheln werden hinten an ein bestehendes Layout angehängt. Datengetriebene `TileDef`-Definitionen [`eligible`/`subtitle`/`buttons`] + generisches Card-Template; Detail-Listen bleiben darunter. i18n en/de/hr. +5 Layout-Service-Specs +7 Dashboard-Tile-Specs. HINWEIS: Das lokale v0.204.0 [Chessable-Bearer-Circuit-Breaker, Commit `d6f0995`] ist auf diesem gepushten Stand NICHT enthalten — es kollidiert mit der entfernten 0.203.x-Chessable-Linie und braucht einen bewussten manuellen Merge; gesichert im Branch `backup-local-master`. Daher Versionssprung 0.203.13→0.205.0); 0.203.12 (piratechess VPN-Concurrency [piratechess `53e90b1`]: `VpnIpHealth._byIp`-Eviction [Cap `Vpn:IpHealthMaxEntries`]; `VpnTunnel.RequestCompleted` zählt Ausgang VOR `_inFlight`-Decrement [Block-Fehlbuchung bei Drain-Race behoben]; `_currentIp` volatile; `VpnRotationService.RotateNowAsync` sequenziell statt `Task.WhenAll` [kein Total-Stall bei manueller Rotation]. 253 Tests grün. NICHT getaggt/deployed); 0.203.11 (piratechess Parsing-Fix [piratechess `1fe1eda`, Batch 2]: `SanToMove` konstruiert gerade Bauern-Push-Umwandlungen [`e8=Q`] direkt — ChessDotNet 1.0.0 listet sie nicht in `GetValidMoves` → vorher leerer `[%tqu]`-Trainings-UCI + Abbruch der Folgezüge; relevant für Endspiel-Kurse. +10 Lib-Fixtures [SAN + softFail]. Geprüft als Nicht-Probleme: useLocalData-Oid [positionell korrekt, Oid beim Cachen nie gesetzt] + softFail-Indizierung [relativ korrekt]. 252 Tests grün. NICHT getaggt/deployed); 0.203.10 (piratechess Code-Review Resilienz+Hygiene [piratechess-Repo, Commit `5074a72`, 242 Tests grün]: ExportBackgroundService überlebt Reader-Fehler [äußere while+Backoff]; curl stdout/stderr parallel [kein Pipe-Deadlock]; ExportJobQueue bounded; Startup-Fail-fast für ConnString/Jwt; globaler ProblemDetails-Exception-Handler; Health 503 bei DB-Ausfall; Dictionary-Indexer statt Add bei doppelter Bid; bid-Format-Validierung [numerisch] → schließt BidLock-Leck. NICHT getaggt/deployed); 0.203.9 (**Security-Fix (2. Review-Pass)**: zweite Tür zum Cached-Content-Bypass geschlossen — `EnqueueReimportAsync` (Reprocess-/Re-Fetch-Choke-Point) prüft jetzt via `OwnerHasCourseAsync`, dass der `bid` in der Bibliothek des Owners liegt, sonst Skip [null]. Vorher konnte ein User ein Repertoire mit beliebigem `ChessableCourseId`/präpariertem Dateinamen anlegen [ImportVersion 0 = stale] und per `/reprocess` fremden gecachten Kursinhalt holen. +2 Tests/1051 grün); 0.203.8 (Nachzieher Import-Security: Admin-`StartImportForUserAdmin` macht denselben `UserOwnsCourseAsync`-Check gegen den Ziel-User; anonyme Tagespuzzle-Lösungen DB-seitig dedupliziert — Unique-Index `(BookPuzzleId, AnonymousSessionId)` [Migration `AddAnonBookAttemptUniqueDedup` inkl. Alt-Duplikat-Bereinigung; authentifizierte Versuche unberührt via NULL-Session], `RecordAnonymousAttemptAsync` fängt `DbUpdateException` ab → kein Doppel-Count/Webhook); 0.203.7 (**Security-Fix**: `ChessableController.StartImport` prüft via `UserOwnsCourseAsync` jetzt, dass der `bid` in der Chessable-Bibliothek des Users liegt [gecachte Liste, sonst 1× frisch laden], sonst 403. Vorher konnte jeder eingeloggte User mit Bearer JEDEN gecachten Kurs importieren — piratechess `POST direct/course` liefert gecachte Kurse aus dem geteilten Cache OHNE Eigentumsprüfung, und Chessable-Kurs-IDs sind öffentlich. Closes den Content-Bypass für alle Lanes [cached+fetch]; +1 Test/1049 grün); 0.203.6 (Doku: API-Code-Review 2026-06-30 in TODO.md festgehalten — Gefixtes [v0.202.0–0.203.5] + bewusst gegen reale Prod-Größe [49 User] zurückgestellte „unbounded read"-Funde [laden real 50–1900 Zeilen → SQL-Umbau = Pomelo-Risiko ohne Nutzen] + Import-Concurrency [bereits durch Claim+Watchdog+Lane-Gate abgedeckt]; keine Code-Änderung); 0.203.5 (Code-Review-Robustheit: `CreateChallengeBatchDto.ToUserIds` `[MaxLength(50)]` [DoS-Schutz]; BookPuzzle-Routen `{id:int}`; `RepertoireTrainingService.ReviewAsync` `DbUpdateException`-Race-Catch [Unique CardKey]; `TournamentMonitorController` `TryGetInt32` statt `GetInt32` [kein 500 bei Crawler-Typänderung]. Bereits erledigt im aktuellen Code: Buch-Versuch klemmt Zeit/Hints serverseitig + validiert SessionId; Listen-Endpoints klemmen take/page); 0.203.4 (Code Review: SavedGame-Dedup hart auf DB-Ebene — Unique-Index `(UserId, Source, ExternalId)` [Migration `AddSavedGameUniqueDedup` inkl. Alt-Duplikat-Bereinigung; NULL-ExternalId weiter erlaubt], `SaveAsync` fängt `DbUpdateException` ab und liefert die bestehende Partie); 0.203.3 (Performance [Code Review]: `AutoSubscriptionService` — frischer DbContext/Scope pro User [`CheckAllUsersAsync`, kein unbeschränktes ChangeTracker-Wachstum/Leakage über den ganzen Lauf] + Freundes-/Profil-Set je User EINMAL geladen [`LoadFavoriteProfilesAsync`, an `AutoFavoritePlayersAsync` durchgereicht] statt je Turnier neu; reine Effizienz, Verhalten unverändert); 0.203.2 (Korrektheit [Code Review]: (1) Daily-Leaderboard `AggregateScores` Competition-Ranking nach Lösezeit [zeitgleiche Löser = gleicher Rang/Bonus + alle 🥇, statt Submit-Reihenfolge bei TimeSeconds==0]; (2) `AdminMessageService.GetThreadsAsync` lädt jüngste Nachricht je Thread über `Max(Id)` statt `CreatedAt`-IN-Match [kein Cross-Thread-Mismatch bei Zeitgleichheit]; +2 Tests); 0.203.1 (Performance [Code Review]: Wochenpost-Übersicht ohne N+1 — `WeeklyPost.PuzzleCount` gecacht [Migration `AddWeeklyPostPuzzleCount`, beim Upload berechnet], `GetAllProgressAsync` lädt alle Posts in EINER Query + nutzt gecachte Anzahl statt PGN-Parse je Post; `GetTotalAsync`-Helper mit Lazy-Backfill für Alt-Datensätze auf allen Pfaden); 0.203.0 (Puzzle-Link-Query-Parameter `?crazy=1`/`?visualmode=0..4` [anderer Agent]); 0.202.2 (Security-Härtung [Code Review]: benannte Rate-Limiter `auth`/`anonymous-puzzle`/`anonymous-tournament` partitionieren jetzt pro Client-IP [`AddPolicy` mit `RateLimitPartition` statt einzelnem `AddFixedWindowLimiter`-Bucket] → kein site-weiter Login-DoS / pro-IP-Brute-Force-Drossel; Default-CORS ohne `AllowCredentials` [Bearer-only]; „Eingeloggt bleiben"-JWT 365→90 Tage); 0.202.1 (Viz-/Blind-Puzzle-Gesten gehärtet [anderer Agent]); 0.202.0 (Track-solves erfasst Tipp-Nutzung: `SharedPuzzleAttempt.HintsUsed` (0–3, Migration `AddSharedPuzzleAttemptHints`) wird beim Erstversuch mitgespeichert; `RecordSharedAttemptDto.HintsUsed` + `SharedPuzzleCountsDto.SolvedByHints[0..3]` (gelöste je Tipp-Stufe) — `GetSharedCountsAsync` aggregiert in einer GroupBy-Query; Buch-Puzzle-Solver sendet `hintLevel`. +2 BE-Tests); 0.201.0 (Kurs „Ganzes Buch" + Repertoire-Trainer Politur: (1) Kurs-Abschluss differenziert: `book-puzzle.component` neuer Getter `courseFullyDone` (= `courseSolved >= courseTotal`) + `courseRemaining`; Template zeigt Trophäe+„Kurs abgeschlossen!" nur wenn wirklich alle gelöst, sonst „Zufalls-Runde durch — noch N Aufgaben" mit „Mit den restlichen weitermachen"-Knopf. Backend bleibt unverändert (`Completed=puzzle==null` = Pool leer; war im Random-Modus immer schon irreführend für die UI). i18n `book.course.roundFinished`/`continueRemaining` en/de/hr. (2) Repertoire-Trainer `tolerated`-Feedback verrät den Repertoire-Zug nicht mehr — neuer Key `toleratedPlayable` ohne `{{move}}`. (3) `wrong`-Feedback zeigt nicht mehr sofort die Lösung: zwei Buttons „Mausrutscher" (kein Penalty, kein Server-Review, kein Re-Queue, zurück zu PLAYING; Brett unverändert) und „Lösung zeigen" (zählt erst dann als wrong, sendet `grade=0` an `RepertoireTrainingService.review`, re-queued die Karte, enthüllt `expectedDisplay`). State-Felder: `wrongRevealed`, `wrongMove`, `pendingWrongReview`. (4) Bei `wrong` Stockfish-18-Eval-Vergleich im Hintergrund: `StockfishService.getEval` (Depth 14) auf FEN nach Spielerzug + FEN nach Repertoire-Zug, Differenz aus Spieler-Sicht in Bauern; Mate-Sonderfälle (`evalMateNote` = `missed`/`allowed`). `kickOffEvalCompare` mit `evalEpoch` gegen Karten-/Zugwechsel-Race; Stockfish-Init im `ngOnInit` (Warmup). i18n `repertoireTrainer.toleratedPlayable`/`wrongNoHint`/`mouseslip`/`showSolution`/`evalLoading`/`evalWorse`/`evalEqual`/`evalMateMissed`/`evalMateAllowed` en/de/hr. +2 Spec-Cases (mouseslip kein Penalty, showSolution zählt+re-queued); 6/6 trainer-spec grün); 0.200.4 (Mobile-Fix Status-Karten: bei `max-width: 768px` setzte `puzzle.component.scss`+`book-puzzle.component.scss` zwar `flex-direction: column`, aber nur `.board-section: 100%` — `.info-section` blieb auf Content-Width, daher waren die Status-/Korrekt-Karten unter dem Brett schmaler als das Brett selbst. Beide bekommen jetzt `.board-section, .info-section { width: 100% }` wie endless-puzzle bereits seit längerem. Gilt für Standard-Puzzles und Buch-/Kurs-/Tages-/Wochen-/Geteilte-Puzzles); 0.200.3 (Dark Mode als Default: `ThemeService._preference` Default `system`→`dark`; gespeicherte Nutzerwahl [localStorage `rookhub_app_theme`] hat weiter Vorrang, Toggle unverändert); 0.200.2 (Leaderboard „Diese Woche"/„Dieser Monat" = rollierende Fenster: `LeaderboardService.WindowStart` weekly→`today.AddDays(-6)` [letzte 7 Tage], monthly→`today.AddDays(-30)` [letzte 31 Tage] statt ISO-Montag/Monatserster; reine Backend-Änderung, FE-Labels unverändert. Boundary-Test angepasst, 9 LeaderboardService-Tests grün); 0.200.1 (Track solves immer an: das Opt-in [Teilen-Dialog-Checkbox + `?track=1`-Param] entfernt; `BookPuzzleComponent.trackSolves = singlePuzzle` [also für jeden geteilten `?single=1`-Link aktiv]. SharePuzzleDialog: Checkbox/`canTrack`/`setTrack`/`&track=1`-Anhängen + MatCheckbox-Import raus, `activeUrl` liefert nur die Basis-URL; ungenutzte i18n `puzzles.share.trackSolves(+Hint)` entfernt. share-dialog-Spec umgeschrieben); 0.200.0 (Geteilte Puzzles „Track solves": Teilen-Dialog bekam Checkbox „Versuche zählen" [nur bei Buch-Einzel-Link mit `?single=1`; hängt `&track=1` an `activeUrl`]. Empfänger-Link zeigt unter dem Puzzle Gelöst-/Fehlversuch-Zähler. Neue Tabelle `SharedPuzzleAttempts` [Unique `(BookPuzzleId, IdentityKey)` → nur Erstversuch je Besucher; `u:{userId}`/`s:{sessionId}`] + Migration `AddSharedPuzzleAttempts`; Endpoints `POST/GET /api/book-puzzles/{id}/track[-counts]` [AllowAnonymous, optionale Auth via `GetUserIdOrNull`]. `BookPuzzleComponent`: Flag `trackSolves` [Query `track=1`], `recordTrack(solved)` [guard + serverseitig erstversuch-dedupliziert] aus finalizeSolve[true]/handleFailed/giveUp/resetPuzzle[false] → Reset/Aufgeben = failed; Anzeige `sharedCounts`. i18n `puzzles.share.trackSolves(+Hint)`/`book.track.solved|failed` en/de/hr. +5 BE-Tests [1036], +5 FE-Tests [neues share-dialog-Spec + book-puzzle]); 0.199.1 (Direkt geteiltes Einzel-Puzzle bleibt am Ende stehen: Teilen-Link aus dem Buch-Puzzle-Solver bekommt `?single=1` [`sharePuzzle()` url+previousUrl]; `BookPuzzleComponent` liest das in Flag `singlePuzzle` → `finalizeSolve` überspringt den Auto-Advance-Countdown, `solvedAutoNext` ist no-op, neuer Getter `browseInBook`=`standalone && !isDaily && !singlePuzzle` blendet die Nächstes/Zufällig-im-Buch-Buttons aus, und `PuzzleStatusCard` bekam `@Input() showNext` [=`!singlePuzzle`] um den „Weiter"-Knopf im Gelöst-Zustand zu verstecken. Normales Buch-Durchblättern unverändert. +3 FE-Tests [28 grün im book-puzzle-Spec; share-URL-Test auf `?single=1` angepasst]); 0.199.0 (Freundesliste zeigt ausstehende GESENDETE Anfragen: Anfragen-Tab bekam neben „Eingehend" einen Abschnitt „Gesendet (wartet auf Bestätigung)" [bisher nirgends sichtbar], je Zeile Zurückziehen-Knopf [reused `DELETE /api/friends/{id}` → `RemoveFriendAsync` erlaubt auch dem Requester], Tab-Zähler = eingehend + gesendet. BE: `FriendService.GetSentPendingRequestsAsync` [RequesterId==me && Pending, inkl. Addressee+Profile] + DTO `SentFriendRequestDto` + Endpoint `GET /api/friends/requests/sent`. FE: `FriendsService.getSentRequests` + `friends.component.sentRequests`/`withdrawRequest`; i18n `friends.requests.*`/`friends.aria.withdraw`/`friends.errors.withdrawRequest` en/de/hr [22 weitere fallen für friends-Detailkeys ohnehin auf en zurück]. +1 BE-Test [24 FriendController grün] +2 FE-Tests [7 grün]); 0.198.0 (Puzzle-an-Freund-Menü: hinter jedem Freund in Klammern, wie viele der von mir geschickten Puzzle noch OFFEN sind [Pending = noch nicht versucht], z. B. „Max (3)"; gelöste/gescheiterte zählen NICHT [erledigt], Freunde ohne offene zeigen keine Zahl. BE: `ChallengeService.GetPendingOutgoingCountsAsync` [GroupBy ToUserId, nur FromUserId==me && Pending] + Endpoint `GET /api/challenges/outgoing/pending-counts`. FE: `ChallengeService.getPendingCounts` + `ChallengeFriendsComponent.pendingCounts` [lädt beim Menü-Öffnen mit der Freundesliste, frischt nach `send()` nach]; i18n `puzzles.challenge.unsolvedTitle` en/de/hr [andere 22 Sprachen fallen für den ganzen challenge-Block ohnehin auf en zurück]. +1 BE-Test [18 ChallengeController grün], +1 FE-Test [7 grün gesamt]); 0.197.1 (Freundeszahl reaktiv: Nimmt ein Freund meine Anfrage an, aktualisierte sich die Freundeszahl bei mir erst nach Seiten-Refresh [Dashboard-`friendCount` nur in `ngOnInit`-`forkJoin` geladen, keine Reaktivität]. Fix: `InAppNotificationService` bekam ein `arrived$`-Subject [feuert, wenn `refreshCount` einen GESTIEGENEN Ungelesen-Zähler erkennt = neue Notification]; Dashboard lädt darauf die Freundeszahl nach, `friends.component` macht einen STILLEN `loadData(true)` [kein Spinner-Flackern]. Hängt am vorhandenen 60-s-Glocken-Poll → kein zusätzlicher Timer. +3 Tests [service `arrived$`, dashboard friendCount, friends quiet reload] + neue `dashboard.component.spec`); 0.197.0 (Chessable-Import-Fortschritt auf der Kursseite: neues schreibgeschütztes `ChessableImportsBannerComponent` [self-polling `getImports`, 8s] oben in `CourseListComponent` zeigt laufende/pausierte Importe mit derselben Visualisierung wie der Chessable-Tab [„hole Kurs… Kapitel 7/36 · 82/1000 Linien · noch ca. 23 Min"], `(importCompleted)`→`loadCourses()` lädt nach Abschluss nach. Fortschritts-/ETA-/Label-Logik aus `chessable.component` in geteilte `chessable-progress.util` extrahiert [`chessableStatusLabel`/`chessableQueueLabel`/`compareImportsByQueue` + `CHESSABLE_LINES_PER_MIN`/`formatDuration`/`effectiveTotalLines`/`estimateRemainingMinutes`, von der Komponente rückwärtskompatibel re-exportiert]. Chessable-Tab-Queue `activeList()` jetzt nach „#" sortiert [queuedAhead asc, dann createdAt]. +2 Spec-Dateien); 0.196.2 (Daily-💡-Badge-Fix: Tagespuzzle in Discord zeigte kein 💡 hinter Lösern mit Tipps, weil das Daily-Webhook-Payload [`SchachBotWebhookService.NotifyAttemptAsync`] pro Solver nur `name/discordId/discordUsername/timeSeconds` sendete — **`hintsUsed` fehlte** [war NIE drin, Git-Historie bestätigt; nur das Wochenpost-Payload `NotifyWeeklyAsync` sendet es]. Bot rendert es längst [`puzzle/daily_results.py:141` `s.get('hintsUsed',0)>0`]. Fix = 1 Zeile `hintsUsed = s.HintsUsed` in der Solver-Projektion [Daten lagen in `BookSolverDto.HintsUsed` bereit]. Test erweitert/6 Webhook-Tests grün. Kein Bot-Update nötig. NICHT deployed); 0.196.1 (Discord-Solver-Webhook eigene Queue: Daily-Solve [book-puzzle 19207, User 5/kahalm] erschien nicht in Discord, weil der schach-bot-Webhook-Push [`BookPuzzleService.NotifySchachBotAsync` Z.99/188, `WeeklyPostService`] sich DIESELBE `IBackgroundTaskQueue` mit dem Chessable-Import teilte — bounded cap 100 + `DropOldest` + 1 serieller Consumer [[prod-chessable-import-stall-restart]]. Nach Redeploy hatte der ResumeService ~263 minutenlange Import-Jobs in die 100er-Queue geworfen → das Webhook-Ticket wurde verdrängt, bevor es lief [heute KEIN `Notify`-Log; gestern feuerte derselbe Webhook ohne Import problemlos]. Fix: neue `IWebhookTaskQueue`/`WebhookTaskQueue` [cap 256] + eigener `WebhookTaskWorker`, registriert in Program.cs; `BookPuzzleService`+`WeeklyPostService` enqueuen Webhooks jetzt dort [`BookPuzzleController`-Hint-Gen bleibt auf der allgemeinen Queue]. Test-Stubs [NoOp/Counting/Immediate] auf `IWebhookTaskQueue` gehoben. One-Shot-Webhook für das heutige Solve manuell signiert+gefeuert [HTTP 200]. +2 Tests/1029 grün. NICHT deployed); 0.196.0 (Repertoire-Trainer ohne „Weiter"-Knopf: nach richtigem/geduldetem Zug läuft der Trainer automatisch zur nächsten Stellung weiter [`ADVANCE_MS` correct 700 ms / tolerated 1800 ms, Timer in `onMove`, abgeräumt in `ngOnDestroy`/`setColor`/`buildQueue`/`next`]; geduldet zeigt die Repertoirezug-Visualisierung länger; Tippen aufs `.play` [`onPlayClick`] überspringt die Wartezeit; FALSCH behält den expliziten „Weiter"-Knopf. Neuer i18n-Key `repertoireTrainer.tapToContinue` en/de/hr + neuer Spec `repertoire-trainer.component.spec.ts` [4 fakeAsync-Cases]); 0.195.5 (VpnIpHealth Fehlalarm-Fix [piratechess v1.0.25]: Prod-Live-Daten zeigten gesunde Exit-IPs [37.46.199.54/.70/.86, alle DE-Tunnel, ≈2–5 % Gesamt-Block-Rate] als „WIEDERHOLT SCHLECHT" geflaggt — weil einzelne kurze Stints [1 Block von 2 Requests = 50 %] über `Vpn:BadStintRate`=0.4 lagen und sich ≥2 davon ansammelten. Neu: eine Phase zählt nur ab `Vpn:BadStintMinRequests`=5 Requests als „schlecht", und „wiederholt schlecht" hängt jetzt an der KUMULATIVEN Per-IP-Block-Rate [`Vpn:BadIpBlockRate`=0.15 über `Vpn:BadIpMinRequests`=50 Requests] statt an gezählten Mini-Phasen; WARN gedrosselt [1× bei Überschreiten, danach frühestens alle 20 Stints]; `IpStat.RecurringBad`-Verdikt, Snapshot sortiert wiederholt-schlechte zuerst dann nach Block-Rate. Prod fährt seit 2026-06-29 v0.195.4+v1.0.24 [Watchdog+Resume+Cooldown live, Import-Drain verifiziert]. +3 Testfälle/214 piratechess grün. NICHT deployed); 0.195.4 (Download-Lane-Nebenläufigkeits-Fix: Chessable-Importe luden gelegentlich ZWEI Kurse gleichzeitig, weil die „serielle" Download-Lane von zwei unabhängigen Treibern bedient wird — dem einzelnen `BackgroundTaskWorker` (Queue-Tickets) UND dem `ChessableImportWatchdogService` (ruft `RunNextAsync` an der bounded Queue VORBEI direkt auf). Der atomare Claim [[prod-chessable-import-stall-restart]] verhindert nur Doppel-Verarbeitung DESSELBEN Jobs, nicht, dass jeder Treiber einen ANDEREN wartenden Job claimt und parallel zieht (beobachtet 2026-06-29: Watchdog-Drive + Queue-Worker zogen Kurs 583468e8… und 69cc61bc… gleichzeitig). Neu: prozessweites `static SemaphoreSlim(1) _downloadLaneGate` in `ChessableImportService`; `RunNextAsync` (Download-Lane) acquired per `WaitAsync(0)` → ein zweiter Drive kehrt SOFORT zurück statt parallel zu laden, Body in `DrainNextAsync` ausgelagert; Fast-Lane bleibt ungated/nebenläufig. +1 Test [`RunNextAsync_DownloadLane_GateBlocksSecondConcurrentDrive`, deterministisch via blockierten Fetch], 1027 grün. NICHT gepusht/getaggt/deployed); 0.195.3 (Per-IP-Request/Block-Tracking [piratechess]: neuer Singleton `VpnIpHealth` akkumuliert je VPN-Ausgangs-IP Requests/Blocks über alle Rotationen [„Stint" = IP-Lebensdauer zwischen Rotationen, gemeldet via `VpnTunnel.FlushIpStint` beim Rotieren; `_currentIp`/`_ipRequests`/`_ipBlocks`]; eine IP mit ≥2 schlechten Phasen [Block-Rate ≥ `Vpn:BadStintRate`=0.4] wird als „WIEDERHOLT SCHLECHT" geloggt [WARN, strukturiert→Kibana je IP gruppierbar]; Debug-Endpoint `GET /api/chessable/direct/debug/ip-health` liefert die Per-IP-Tabelle [schlechteste zuerst]. +3 Tests/211 grün. NICHT deployed); 0.195.2 (Import-Robustheit [piratechess]: (1) hartes `--max-time` je Request [`Chessable:RequestMaxTimeSec`, Default 20s] in `BuildGetArgs` → hängende IP scheitert nach ~20s statt 75–120s, leerer Body → Soft-Block → retire/rotate; DrainTimeout 60→25s. (2) Per-Tunnel-Health+Cooldown in `VpnTunnel`: gleitendes Fenster der letzten N Ausgänge [`Vpn:HealthWindow`=8], blockt ein Tunnel ≥`Vpn:HealthBlockThreshold`=5 → `Vpn:CooldownSec`=120 aus dem Pool [`TryAcquire(respectCooldown)`, `RecordOutcome` via `RequestCompleted(bool)`, `VpnLease.onComplete` jetzt `Action<bool>`]; `AcquireAsync` Zwei-Pass [gesund zuerst, Notfall ohne Cooldown → kein Verhungern]. +2 Tests/208 grün. NICHT deployed [Prod läuft noch v1.0.23]); 0.195.1 (ETA-Durchsatz `CHESSABLE_LINES_PER_MIN` 25→40 [nach Rotate-on-Block-Speedup]; admin.component nutzt jetzt die Konstante statt hartem /25 in `dlEtaMin`/`estMinFromLines`); 0.195.0 (Admin-Kursdownload „Größe schätzen": on-demand pro Kurs Gesamt-Linienzahl + grobe Zeit vor dem Import. piratechess `POST /api/chessable/direct/course/info` {bearer,bid} → `DirectCourseInfoResponse` [gecacht=Cache-Summe ohne Chessable-Call, sonst `GetCourseLineCountAsync`=1× getCourse?includeVariations]. rookhub `ChessableProxyService.GetCourseInfoAsync` + Admin-Endpoint `GET admin/users/{userId}/courses/{bid}/estimate` [Bearer des Ziel-Users]. FE: `estimateCourseForUser`, `dlEstimate`/`dlEstimates`/`estMinFromLines` [~25/min], `straighten`-Knopf je Zeile → „≈N Linien · ~M Min/gecacht", i18n estimate/estimateError/cachedInstant/min. +2 Tests; FE 630/BE 1026/piratechess 206 grün); 0.194.0 (Chessable-Import: echte Gesamt-Linienzahl + ETA. piratechess getCourse `&includeVariations=true` → `Chapter.Total`/`Variations`, `FetchCourseDataAsync` meldet Variantensumme via `onTotalLines` → `CourseFetchJob.LinesTotal`/Snapshot → `DirectCourseProgressResponse.LinesTotal`. rookhub `ChessableImport.LinesTotal` [+Migration `AddChessableImportLinesTotal`] durch alle Progress-DTOs; FE `effectiveTotalLines` bevorzugt exakten Wert, `CHESSABLE_LINES_PER_MIN` 16,7→25, i18n `chessable.fetchProgressTotal`, Linien-X/Gesamt + ETA im User-Import + admin-Kursdownload [`dlEtaMin`]; FE 630/BE 1024/piratechess 206 grün); 0.193.2 (Chessable-Line-Fetch Rotate-on-Block [piratechess-Repo]: größter Importzeit-Fresher war der FIXE 30-s-Backoff bei Soft-Block [leere `{}`-Antwort, ~6 % der Zeilen → amortisiert ~1,8 s/Zeile ≈ 77 % der Wandzeit, bei nur 187 ms echtem curl]. Neu: `VpnRotationService` hält GENAU EINEN aktiven Tunnel sticky [statt round-robin pro Request]; `VpnLease.ReportBlocked()` [aufgerufen von `ChessableHttpService.CurlGetAsync` via `IsSoftBlockedBody`] retired die IP sofort → `VpnTunnel.RetireNow()` rotiert sie drain-aware im Hintergrund, Pool wechselt auf den nächsten, bereits ausgeruhten Tunnel [Ping-Pong; Rotation dauert lt. Prod-Log nur ~4 s ≈ Abarbeiten von 10 Zeilen]; Line-Retry-Backoff 30 s→`Chessable:BlockRetryDelayMs` [Default 1500 ms]; Inter-Request-Delay-Default 1000–2000→0–200 ms [Block ist requests-pro-IP-, nicht timing-getrieben]. Erwartung ~26→~100–150 Zeilen/min. piratechess-Commit 9071793, 206 Tests grün. OFFEN: Live-ENV in /opt/stacks setzen [ParallelLineFetches=1, InterRequestDelay 0/200] + Dev-Verifikation; NICHT deployed); 0.193.1 (Chessable-Import Fast-Lane: voll-gecachte Kurse [`ChessableImport.FullyCached`, gesetzt via `IsCourseCachedAsync` beim Anlegen in allen Pfaden — StartImport/Admin/`EnqueueReimportAsync`] laufen in eigener SERIELLER, netzfreier Lane [`ChessableImportFastLaneService`, parallel zur Download-Queue] statt hinter den langsamen Downloads; `RunNextAsync(ct, fastLane)` filtert fast=`FullyCached==true` / download=`!=true` [null→Download, kann nie hängen]; Watchdog download-lane-spezifisch; altes `RunDetached` entfällt; +Migration `AddChessableImportFullyCached`, +8 Tests/1024 grün); 0.193.0 (Trainingsziele-Zeitanzeige gestuft: neue pure `formatDuration(seconds, lang)` in `training-goals.component` + Methoden `durValue`/`durUnit` → < 120 min in Minuten, < 48 h in Stunden, sonst Tage [1 Nachkommastelle, locale-Dezimaltrenner via `Intl.NumberFormat`]; ersetzt `mins()`/`minutes()` in Breakdown-Rows [heute+Periode], Tageshistory-Tabelle und Chessable-Kurs-Summen [Daily-Goal-Paarung „done/target min" bleibt bewusst in Minuten]; neue i18n-Keys `trainingGoals.hours`/`.days` in ALLEN 25 Sprachen; +6 Spec-Cases); 0.192.1 (Admin-Kursdownload zeigt „Bereits in Warteschlange": `ChessableCourseDto.Queued` + `EnrichImportStateAsync` markiert Kurse mit laufendem Import [Status=running], FE-Branch im admin.component.html `@else if (c.queued)`, i18n `admin.courseDl.alreadyQueued` en/de/hr; +1 Test); 0.192.0 (Chessable-Import-Watchdog: neuer `ChessableImportWatchdogService` (BackgroundService) draint hängende Import-Queue ohne API-Neustart. Root-Cause des „eingeschlafen"-Vorfalls: `BackgroundTaskQueue` ist bounded (cap 100, DropOldest) + single-consumer + Abschluss reiht nicht nach → großer Import-Schwung verwirft Tickets, Jobs bleiben `running/queued` liegen. Watchdog prüft periodisch `IsDrainStalledAsync` (queued>0 && kein claimed/fetching/importing) und ruft dann `RunNextAsync` DIREKT (umgeht die bounded Queue); Startverzögerung 1min/Ruhe-Takt 2min/Busy-2s; +6 Tests/1015 grün. Siehe Memory [[prod-chessable-import-stall-restart]]); 0.191.0 (Reprocess-Banner zwei Knöpfe „Alle"/„Aus Cache": `ImportReprocessService.Reprocess{Courses,Repertoires}Async` + beide POST-Endpoints bekamen `localOnly`-Flag [`[FromQuery] bool localOnly`]; localOnly=true überspringt den Chessable-Re-Fetch [`continue`], bereitet nur aus serverseitig gespeicherter Quelle auf [Courses: SourcePgn; Repertoires: Nicht-Chessable-Versions-Mark]; Banner `allCount`=reprocessableLocally+refetchable, `cachedCount`=reprocessableLocally, Cache-Knopf nur wenn cachedCount<allCount; i18n reprocess.updateAll/updateCached/+Tips en/de/hr; +4 Tests); 0.190.1 (Discord-Invite-Link auf den richtigen Server korrigiert: `discord.gg/wczc4BJtMf` statt `nKQCdC7Xff` in `core/community.ts` [Konstante → Navbar/Footer/Mobil-Menü], README + Hilfetext en/de/hr + Changelog-0.187.0-Eintrag; beide Invites resolven aktuell auf Guild „Rookhub", neuer hat kein Ablaufdatum); 0.190.0 (Offline-Auto-Cache: Kurs öffnen (online) lädt das ganze Buch im Hintergrund offline [`autoCacheCourse` in book-puzzle.component, kein manuelles ☁ nötig]; Tagespuzzle wird beim Online-Abruf automatisch gecacht [`saveDailyOffline`/`getDailyOffline`, Key `rookhub_daily_offline`, letzte 14 Tage] + Offline-Read in `loadDaily`; Default-Offline-Pool 10→30 [`DEFAULTS.puzzleCount`]; +4 Specs/621 grün); 0.189.2 (Offline-Fix: `MenuService` cacht die Menü-Sichtbarkeit in localStorage [`rookhub_menu_keys`] + seedt das `visibleSubject` daraus → Flugmodus-Kaltstart zeigt nicht mehr nur Admin+Discord, sondern das zuletzt bekannte Menü; `fetch()`-catchError gibt Cache statt leerem Set); 0.189.1 (Mobile-Navbar-Fix: Sekundär-Icons [Discord/Theme/Sprache] wandern auf Mobil ins Hamburger-Menü `nav-extra`+navMenu, damit Toolbar nicht überläuft & Profil-Icon sichtbar bleibt); 0.189.0 (Repertoire-Trainer/geduldete Züge: `ImportPipeline.CurrentVersion` 2→3 [softFail-`[%alt]` jetzt im piratechess-Export]; Reprocess-Banner auf der Repertoire-Seite bietet für Chessable-Repertoires jetzt einen echten **Re-Fetch** an statt No-op-Versions-Mark — `ImportReprocessService.GetRepertoireStatusAsync`/`ReprocessRepertoiresAsync` melden Chessable-Repertoires [bid aus `ChessableCourseId` ODER Dateiname `chessable-{bid}.pgn`] als `Refetchable` und reihen `EnqueueReimportAsync(..., target:"repertoire", targetRepertoireId)` ein; neuer `ChessableImport.TargetRepertoireId` + Migration → `ImportAsRepertoireAsync` ersetzt das PGN **in-place** im bestehenden Repertoire [Id/Trainings-Fortschritt bleiben], Nicht-Chessable nur Versions-Mark; +3 Tests/1007 grün); 0.188.0 (Discord-Link prominent in der Fußzeile [Discord-Logo via `MatIconRegistry` + Markenfarbe #5865F2, neben Hilfe/Feedback, Konstante `core/community.ts`; Footer nur Desktop, mobil im Nav-Menü]); 0.187.0 (Discord-Community-Link überall eingebunden: Discord-Button in Navbar [eingeloggt + ausgeloggt] + Mobil-Menü [SVG-Icon via `MatIconRegistry`, Link `https://discord.gg/nKQCdC7Xff`, zentrale Konstante `core/community.ts`], Einladungs-Satz im Discord-Abschnitt der Hilfeseite [en/de/hr], `nav.discord`-Tooltip in allen 25 i18n-Sprachen, READMEs aller Repos); 0.186.1 (Chessable-Rate-Limit-Fix); 0.186.0 (Repertoire-Trainer/Spaced Repetition); 0.185.0 (Admin-Benachrichtigung bei Neu-Registrierung: neuer Notification-Typ `new_user_registered` → Glocke aller Admins [`CreateManyAsync`, Link „/admin", Daten `{username}`]; `AuthService` injiziert optional `NotificationService` und benachrichtigt best-effort nach erfolgreichem Register; Icon `group_add`; i18n en/de/hr); 0.184.39 (Offline-Fix: Kurse offline startbar — erstes Puzzle aus lokalem Cache statt Server [offline gespeicherter Kurs via ☁-Knopf, sequenziell/zufällig], Versuche werden gequeued/synchronisiert; Nicht-Admins offline nicht mehr von der Kursseite ausgesperrt); 0.184.38 (Chessable-Diagnose [piratechess-Repo]: `ClassifyBlockedResponse` unterscheidet jetzt abgelaufenen/ungültigen Token [lokal am JWT-`exp`-Claim erkannt → „Bearer neu hinterlegen"] von einem Cloudflare-403-Block bei noch gültigem Token [→ „VPN-Ausgangs-IP gesperrt, IP rotieren/Server wechseln"]; `IsCloudflareBlockPage` erkennt Block-Marker; Hintergrund: M247-IPs [AS9009] werden von Chessable geblockt, Netrouting [AS6206] nicht; piratechess-Commit 32a2f83, +7 Tests/175 grün); 0.184.37 (Chessable-Fix [piratechess-Repo]: HTML-statt-JSON-Antwort [abgelaufener/ungültiger Bearer bzw. Cloudflare-Block/Proxy-Gateway → Chessable liefert eine HTML-Seite] wird in `ChessableHttpService.GetCoursesAsync`/`FetchCourseDataAsync` jetzt sauber erkannt [`LooksLikeHtml`] und als sprechender Token-Hinweis gemeldet, statt den rohen JSON-Parser-Text „'<' is an invalid start of a value" bis in die rookhub-UI durchzureichen; +JsonException-Catch ohne Leak; piratechess-Commit c1cc507, +7 Tests/168 grün); 0.184.36 (Crawler-Härtung [Crawler-Repo]: `ApiKeyMiddleware` fail-closed bei leerem `API_KEY` in Production [503 statt offen], Dev-Fallback + Liveness bleiben; Crawler-Commit 4ca4feb); 0.184.35 (BotStats-Endpoint Replay-Schutz: `GET /api/bot/player-progress` akzeptiert optionalen `X-Bot-Timestamp` [±300 s, HMAC über `<ts>.<discordId>`]; rückwärtskompatibel zur alten body-only-Signatur; Gegenstück zu Bot v2.73.0); 0.184.34 (Webhook-Timestamp-Replay-Schutz auf der rookhub-Sendeseite: alle drei Bot-Webhooks [Tagespuzzle/Wochenpost/Daily-Regenerate] signieren jetzt zusätzlich einen Zeitstempel [`X-Webhook-Timestamp`, HMAC über `<ts>.<body>`], ±300 s; Gegenstück zu Bot v2.70.0, rückwärtskompatibel); 0.184.33 (Crawler-Robustheit [Crawler-Repo]: Freilos/spielfrei nur informativ statt Warnung, defensives Response-Größenlimit `Crawler:MaxResponseBytes`, Hidden-Field-Parsing Regex→AngleSharp, Player-/Team-Upsert in DB-Transaktion; Crawler-Commits c518e74/cf6b5a9/7522f3a/bc59f31); 0.184.32 (A11y tournament-favoriten: Favoriten-Sterne [Spieler+Team, Tabelle+Mobil-Karte] in tournament-detail+public-tournament tastaturbedienbar — `role=button`/`tabindex=0`/`keydown.enter`+`space`/`aria-label`+`aria-pressed`/`:focus-visible`; i18n `tournaments.favorites.toggleAria` en/de/hr); 0.184.31 (BasePuzzleSolver-Dedup: `formatTime` → gemeinsame `puzzle-format.util.ts` [`formatPuzzleTime`], und Einzel-Stoppuhr-Timer [`elapsedSeconds`/`stopwatch`/`startTimer`/`stopTimer`] aus puzzle+book in die Basis hochgezogen; Endless erbt `elapsedSeconds`+`formatTime`, behält seine Doppel-Stoppuhren; +2 Specs, Verhalten unverändert); 0.184.30 (OnPush für 4 weitere präsentationale Puzzle-Karten: puzzle-your-turn/-status-card/-rating-card/viz-card — alle nehmen nur primitive Inputs [Eltern rebinden je CD] + EventEmitter-Outputs, in-place-Mutation geprüft = keine; +Spec); 0.184.29 (Admin-Kleinkram: chessable-Bookmarklet-`bypassSecurityTrustUrl` mit Origin-Guard+Sicherheits-Kommentar [Code rein app-konstruiert]; Admin-Mitglieder-Dropdown warnt bei `totalCount > 500` statt still abzuschneiden; `availableUsers()` war bereits memoisiert [v0.184.19]; +2 Specs); 0.184.28 (chessable.component: `activeImports`-Zeilen cachen ihr `queueLabelText` jetzt einmal je Update [`setActiveImport`] statt `queueLabel(imp)`-`translate.instant` je CD-Zyklus während des Pollings; +2 Specs); 0.184.27 (Anon-Session-IDOR-Härtung: `ValidationConstants.SessionIdPattern` Mindestlänge 1→32 [UUID-Form 32–36], erratbare Kurz-Ids können fremde anonyme Puzzle-/Endless-Stats nicht mehr claimen/überschreiben; Clients nutzen ohnehin `crypto.randomUUID()` → rückwärtskompatibel); 0.184.26 (FriendService.SearchUsersAsync: Identitäts-/Konto-Felder [Username/chess.com/Lichess/FIDE/ChessResults] präfix-anker [`StartsWith`, Username-Index nutzbar], nur DisplayName bleibt Teilstring; Länge+Take service-seitig hart gekappt); 0.184.25 (Chessable-Import: atomarer Claim beim Job-Picking via `ExecuteUpdate` „queued"→„claimed", InMemory-Re-Check-Fallback → keine Doppelverarbeitung bei Resume-Sturm/Skalierung); 0.184.24 (Schnellstart-Popup nach Register erklärt jetzt die Puzzle-Modi statt Turnier-Tipps: Zufalls-Puzzle/Endlos/Tagespuzzle/Wochenpost; `app.qs.*`-Keys in de/en/hr ersetzt, Icons 🎲/♾/📅/📰); 0.184.23 (Crawler-Robustheit gegen Redeploy/VPN-Aussetzer [Crawler-Repo]: `VpnReadinessGate` wartet vor dem ersten Crawl auf den wiederhergestellten gluetun-Tunnel [`Gluetun__WaitForReady=true` in beiden VPN-Compose-Dateien], und `ExecuteCrawlAsync` versucht reine Verbindungsfehler [`IsTransientConnectionError`, z. B. „Resource temporarily unavailable"] mit gestuftem Backoff erneut statt sofort `Failed`; Retry-Parameter via `Crawler:CrawlMaxAttempts`/`RetryDelayMs`/`CrawlRetryBackoffSeconds` konfigurierbar; behebt die Fehler-Häufung direkt nach Deploys); 0.184.22 (Frontend Service-Layer-Extraktion: `FriendsService`/`PublicTournamentService`/`ProfileService` + `RepertoireService`-Erweiterung, 9 Komponenten ohne direkten `HttpClient`; `AuthService.changePassword`; OnPush für review-nav/promotion-picker; +5 Service-Specs); 0.184.21 (Crawler-Härtung: `/api/health/ip` API-Key-pflichtig + Phantom-Runden-Clamp gegen fremde `rd=`-Links; Crawler-Repo-Commits f5071aa/052007b); 0.184.14–0.184.20 (TODO-Abarbeitung Runde 3: DataProtection-Keys konfigurierbar/anlegen/SetApplicationName · In-App-Benachrichtigung bei neuer Turnierrunde [`NotificationType.TournamentNewRound`] · Service-Extraktion Repertoire/Tournament-List/Dashboard [+Specs] · Admin-Tab in URL [`?tab=`] + availableUsers gecacht + admin.component-Spec · A11y puzzle-tags/repertoire-tree/-lines + OnPush für präsentationale Komponenten); 0.184.9–0.184.13 (Runde 2: JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source) JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source); 0.237.3 (Dashboard-Edit-Steuerpille [Ziehen/Hoch/Runter/Auge] richtig dimensioniert: die sichtbaren Buttons waren auf 30 px geklemmt, aber Angular Material 19s mat-icon-button hält intern via MDC einen 48-px-State-Layer/Touch-Target — die Klick-Flächen lagen über die Icons hinaus und überlappten die Nachbarknöpfe. Neu: die Material-3-MDC-Custom-Properties --mdc-icon-button-state-layer-size:32px + --mdc-icon-button-icon-size:18px gesetzt statt nur width/height [Pille 30→32 px, padding:3px, dezenter 1-px-Border]; deaktivierte Pfeile bekommen opacity:.35, Griff-Icon opacity:.7. NICHT deployed); 0.237.2 (Kurs-Lesereihenfolge nach `Round` statt `Id`: Intro-/Info-Linien, die durch einen spaeteren Re-Fetch ergaenzt wurden [hohe DB-Id, aber kleines Chessable-Round, z. B. Intro von 100 Tactical Patterns Round 002.002], erschienen hinter den ersten Puzzles statt ganz vorne. `CourseService` sortiert Kapitel [`GetOrderedChapterNamesAsync` MinRound], Sequenz-Cursor [`GetNextAsync` cursor-sicher in-memory ueber (Round,Id)], `GetChaptersAsync`, `GetAllPuzzlesAsync` und den PGN-Export-Fallback jetzt nach `Round` [dann Id]; `BookPuzzleService.GetNextInBookAsync` ebenso. SQL macht nur ORDER BY/MIN(Round) [Standard], String-Cursor-Vergleich in-memory. Kein Migration/Re-Import noetig, wirkt fuer alle Kurse. +4 BE-Tests [CourseServiceReadingOrderTests, 189 Course/BookPuzzle gruen]. NICHT deployed [Round-SQL-Sortierung auf Dev gegen MariaDB verifizieren]); 0.237.1 (Dashboard-Kachel „Angepinnte Kurse" aufgehübscht: je Kurs `mat-progress-bar` [wie Kurse-Seite], Sequenziell = `mat-flat-button color=primary` [primär], Zufällig = `mat-stroked-button` [sekundär, Rahmen], beide `flex:1` volle Breite; bei 100 % gelöst 🏆-Icon neben `progressPercent`; größere Zeilen-Trennlinien+Gaps [0.6→0.9rem/0.25→0.5rem]. `MatProgressBarModule` importiert. Nur Template+Styles+kleine i18n-lose Umbenennung `.pc-info`→`.pc-header`. NICHT deployed); 0.237.0 (Kurse fürs Dashboard anpinnbar + direkt startbar: neue per-User-Tabelle `CoursePin` [UserId+BookId unique, +Migration `AddCoursePins`, Cascade User/Book, Cleanup in `BookAdminService.DeleteBookAsync`], `CourseListItemDto.IsPinned` [in `GetCoursesAsync` via Pin-Set gesetzt], Endpoints `POST/DELETE /api/courses/{bookId}/pin` [`CourseService.PinCourseAsync`/`UnpinCourseAsync`, idempotent, EnsureAccess]. FE: 📌-Toggle je Kurs in der Kursliste [optimistisch+Rollback], neue Dashboard-Kachel `pinnedCourses` [in DEFAULT_VISIBLE vor `courses`; eligible nur wenn ≥1 Pin bzw. im Edit-Modus] mit je Kurs zwei Start-Buttons Sequenziell/Zufällig; Dashboard nutzt das ohnehin geladene `/api/courses` [jetzt typisiert `DashboardCourse` inkl. `isPinned`] mit. i18n `courses.pin*`/`dashboard.pinnedCourses.*` en/de/hr. NICHT deployed); 0.236.18 (Admin-CI Push statt Pull: neuer Endpoint `POST /api/ci/gh-webhook` [`CiBuildReportController.GithubWebhook`, AllowAnonymous, verifiziert GitHubs `X-Hub-Signature-256` HMAC gegen `CI:GithubWebhookSecret` ODER Fallback `CI:BuildReportSecret`] empfängt GitHub-`workflow_run`-Events → `GithubActionsService.ReportRun` [statischer `_pushedRuns`-Cache je Repo, ~20 jüngste]. `OverlayPushedRuns`/`OverlayPushedRepo` legt die Push-Läufe bei JEDEM Overview-Abruf LIVE über die (gecachten) GitHub-Daten [Merge per Run-Id, Push gewinnt, Workflow-Filter, neueste 5] — auch im Einzel-Repo-Abruf [👁]. FE `NORMAL_MS` 2min→20s [RookHub-Cache statt GitHub]. Webhook je Repo unter Settings→Webhooks eintragen [Event „Workflow runs", Secret = CI_BUILD_REPORT_SECRET-Wert]. +5 BE-Tests [Overlay + Webhook-Sig; GithubActionsServiceTests+CiBuildReportControllerTests in Collection „CiStaticState" serialisiert wg. statischer Caches]. NICHT deployed); 0.236.17 (Perf: `CourseService.GetCoursesAsync` — die Puzzle-Anzahl je Buch [`PuzzleCount`] lief als korrelierte Unterabfrage pro Buch [`b.Puzzles.Count(!IsInfoOnly)` im Select] → bei Admins mit ~260 Büchern sekundenlang. Jetzt EINE gruppierte Query [`BookPuzzles.Where(!IsInfoOnly).GroupBy(BookId)` → Dict]; Mapping zieht die Zahl aus dem Dict. Verhalten/Zahlen unverändert [67 Course-Tests grün]. NICHT deployed); 0.236.16 (Chessable-Importe sind jetzt standardmäßig NICHT von der RepCheck-Extension nutzbar: `ChessableImportService.ImportAsRepertoireAsync` setzt beim Anlegen `UseForExtension=false` [`CreateRepertoireDto`]. Manuell angelegte Repertoires unberührt [Create-Dialog-Default bleibt true]. +1 BE-Test-Assert. Prod-Datenfix separat: kahalms 133 Repertoires alle auf `UseForExtension=0`. NICHT deployed); 0.236.15 (Admin-CI Poll-Umbau: normaler Voll-Abruf jetzt alle 2 min statt 5 s [`NORMAL_MS`], gegen GitHub-Rate-Limit. Neuer per-Repo-👁-Knopf [`toggleWatch`/`isWatching`, `.watch-btn`]: schnell-pollt NUR dieses eine Repo alle 10 s [`WATCH_MS`] für 1 min [`WATCH_WINDOW_MS`], bei laufender Aktion rollend bis zu deren Ende [`WATCH_RUNNING_EXTEND_MS`, `watchTick`]. Neuer BE-Endpoint `GET /api/admin/ci/runs/{repo}` [`GithubActionsService.GetRepoAsync`, ungecacht, Repo-Whitelist; Merge-Logik in `MergeRunningAsync` extrahiert, von Voll- + Einzel-Abruf geteilt]; FE `loadRepo` mergt das Einzel-Ergebnis in die Übersicht. i18n `admin.ci.watchStart/watchStop` + Subtitle en/de/hr. +2 BE-Tests. NICHT deployed); 0.236.14 (Admin-CI-Seite: die „Deployt: …"-Zusammenfassungszeile im Kopf [gelbe Chips je Stack, `.ci-deployed`] entfernt — inkl. `deployed`-Feld + dessen Berechnung in `recomputeEta` + CSS; die gelbe Zeilen-Hervorhebung des laufenden Builds [`.run-deployed`/`isRunningBuild`] bleibt. NICHT deployed); 0.236.13 (CI-Fix: flakiger `GithubActionsServiceTests.LogWatcher_FiltersOutTestRuns`-Test [aus dem 0.236.11-Feature], der je nach Testreihenfolge den ganzen `test-rookhub`-Job rot machte → kein `:latest`-Image. Ursache: prozessweiter **statischer** `_reportedBuilds`-Cache leckte über Testklassen [`CiBuildReportControllerTests` meldet „log-watcher" → im Overview-Test feuerte der „6.-Zeile-nachladen"-Pfad und der naive Stub lieferte den Run doppelt → `Assert.Single` sah 2]. Fix: interner `ResetReportedBuildsForTests()`-Hook, im `GithubActionsServiceTests`-Ctor vor jedem Test geleert. Produktcode unverändert [`FetchRunByShaAsync` filtert in Prod echt nach `head_sha`]. NICHT deployed); 0.236.12 (RepCheck „Remember line": echter Kursname über den Chessable-Bearer. Neues `RememberedPosition.CourseName` [+Migration `AddRememberedPositionCourseName`], `RememberLineInputDto`/`RememberedPositionDto` um `CourseName`. `RememberedPositionService` löst den Namen auf: von der Extension mitgeliefert [Vorrang] → sonst cache-first aus `ChessableCredential.CachedCoursesJson` → sonst best-effort Live-`GetCoursesAsync` [try/catch, Merken scheitert nie am Kursnamen]. `ListAsync` backfillt Alt-Einträge ohne Namen aus dem Cache [rein in-memory]. +7 BE-Tests [RememberedPositionServiceTests] + ExtensionControllerTests-Ctor angepasst. Extension-Seite [repcheck ≥1.19.0] löst den Namen via erfasstem Bearer + Chessable-getHomeData auf. NICHT deployed); 0.236.11 (Admin-CI: Workflow-Filter je Repo [`GithubActionsService`]: `DefaultWorkflowFilter` [log-watcher→„Build & Push"], überschreibbar via `GitHub:WorkflowFilter:<repo>`; `FetchRepoAsync` lädt `RawRunsPerRepo`=20 statt 5 und filtert per `MatchesWorkflowFilter` [Substring, case-insensitiv] vor `Take(5)`, `FetchRunByShaAsync` ebenso [dieselbe SHA triggert bei log-watcher „Tests" UND „Build & Push"] → log-watcher zeigt nur noch Build-Läufe, keine Test-Läufe. +1 BE-Test [GithubActionsServiceTests]. NICHT deployed); 0.236.10 (Admin-CI: der deployte Build-Lauf ist wieder mit GELBEM Zeilen-Hintergrund markiert [`.run.run-deployed`, `[class.run-deployed]="isRunningBuild(run,repo)"` + gelber Border-Left], nicht nur dem Icon-Badge — 0.236.5 hatte die Zeilenfarbe entfernt, User wollte aber nur ein anderes Icon, nicht den Wegfall des Highlights. NICHT deployed); 0.236.9 (Admin-CI, drei Ergänzungen: (1) laufender Build zeigt prominenten Live-Countdown im Panel-Kopf [`.ci-eta`, BLAUE Plakette `timer`-Icon, z. B. „schach-bot · fertig in ~2m 10s"]; ETA = `avgDurationSec` [Mittel der abgeschlossenen der letzten 5 Läufe je Repo, created→updated] − verstrichene Zeit; 1-s-Ticker [`nowMs`] treibt den Countdown, `recomputeEta` [ETA+deployed] bei 1-s-Tick + 5-s-Refresh. (2) prominente „Deployt"-Übersicht im Kopf [`.ci-deployed`, GELBE Chips je Stack = Repo + Ref des via `isRunningBuild` erkannten laufenden Runs]. (3) Farbschema: BLAU=baut [`.run`-Badge + ETA], GRÜN=fertig gebaut [`.ok`], GELB=deployed [`.run-live-tag` + `.ci-deployed`]; Repo-Icon `folder_open`→`code`. i18n `admin.ci.etaIn/etaSoon/etaUnknown/etaTooltip/deployed/deployedTooltip` en/de/hr. NICHT deployed); 0.236.8 (Dashboard-Kurse-Kachel zeigt jetzt die Kurs-Anzahl [dashboard.courses.count "{{count}} Kurse"] wie die Repertoires-Kachel: neuer courseCount, DashboardService.getCourses [GET /api/courses], im forkJoin geladen. +2 FE-Tests, i18n en/de/hr. NICHT deployed); 0.236.7 (Admin-CI: „live"-Badge des laufenden Builds klar vom grünen Erfolgs-Häkchen abgehoben — `.run-live-tag` jetzt BLAU (#1565c0) + Icon `fiber_manual_record` (live-Punkt) statt grün/`play_circle` [`admin-github-actions.component`]. Rein visuell. FALLE: zeigt die CI-Seite bei ALLEN Repos „HTTP 403", liegt das am GitHub-PAT [`GITHUB_TOKEN` in der rookhub-.env] — abgelaufen oder ohne `Actions: Read-only`-Permission —, NICHT am Code [UA/Accept/Api-Version im HttpClient sind gesetzt]. NICHT deployed); 0.236.6 (Repertoire-Reprocess: (a) nach erfolgreichem Chessable-Re-Fetch werden ALLE Geschwister-Repertoires desselben Users mit derselben bid mit-aktualisiert [ChessableImportService.BumpSiblingRepertoiresAsync in ImportAsRepertoireAsync] — vorher blieben Kurs-Duplikate veraltet, weil der (Owner,bid)-Dedup den Kurs nur 1x holt (Banner ging nie weg). (b) GetRepertoireStatusAsync/ReprocessRepertoiresAsync sind jetzt admin-weit [isAdmin -> alle User statt nur eigene; RefetchCandidate.OwnerId = r.UserId -> je Owner-Bearer]; RepertoireController reicht IsAdmin durch. +2 BE-Tests. NICHT deployed); 0.236.5 (0.236.5 (Admin-CI: laufender Build nicht mehr per Zeilenfarbe markiert, nur noch eigenes live-Icon-Badge [run-live-tag; .run-live-Fill entfernt]; faellt der laufende Build aus den Top-5 raus, wird er per GitHub ?head_sha= nachgeladen + als Zusatzzeile angehaengt [GithubActionsService.FetchRunByShaAsync + RunMatchesBuild + MapRun extrahiert]. Server ermittelt jetzt auch rookhubs eigene laufende SHA aus der Frontend-build-info [http://rookhub:8080/build-info.json, Frontend:BuildInfoUrl], isRunningBuild bevorzugt repo.runningSha [client-buildSha nur Fallback]. + Push-Modell-Empfangsseite: POST /api/ci/build-report [CiBuildReportController, AllowAnonymous + X-Build-Report-Key==CI:BuildReportSecret] -> GithubActionsService.ReportBuild [statischer _reportedBuilds-Cache, in ResolveRunningBuilds gemergt]. +3 BE-Tests. OFFEN: log-watcher-Client (Push). NICHT deployed); 0.236.4 (0.236.4 (Repertoire-Trainer falscher Zug: (1) "Losung zeigen" spielt den erwarteten Zug jetzt auf dem Brett [fen nach expected + lastMove-Markierung], statt ihn nur im Text zu nennen; (2) nach falschem Zug SOFORT erneut ziehbar - onMove akzeptiert einen Retry im wrong-FEEDBACK, Zug wird sofort auf startFen zurueckgenommen, Brett bleibt spielbar [viewOnly nur wenn nicht wrong-unrevealed], kein Mausrutscher-Klick noetig; Mouseslip-Button entfernt [Retry ersetzt ihn]. +Specs angepasst. NICHT deployed); 0.236.3 (0.236.3 (Admin-Kursdownload: Schalter "Geladene Kurse ausblenden" [dlHideLoaded + Getter dlVisibleCourses] blendet Kurse aus, die schon als Repertoire/Buch importiert sind [ChessableCourseDto.ImportedRepertoire/ImportedBook, bereits vorhanden]; mat-slide-toggle + i18n admin.courseDl.hideLoaded/allLoaded en/de/hr. +1 FE-Test. NICHT deployed); 0.236.2 (0.236.2 (Frontend erkennt neue Versionen schnell: app.component sucht aktiv beim Start, alle 15 min und bei visibilitychange->sichtbar via swUpdate.checkForUpdate() [neue Methode checkForAppUpdate, fehlertolerant] - lange offener Tab/PWA haengt nicht mehr auf altem Build bis manuellem Reload; VERSION_READY-Neu-laden-Snackbar wie gehabt. Spec: SwUpdate-Mock um checkForUpdate ergaenzt. + Bot v2.77.0: Monats-Endstand am 1. im Tagespuzzle-Thread statt Standalone-Post. NICHT deployed); 0.236.1 (0.236.1 (Zwei Prod-Log-Fixes nach Watcher-Alerts: (1) Weniger Fehlalarme: der anonyme Endless-Progress-Upsert [`EndlessProgressService.SaveAnonymousProgressAsync`] läuft bei gleichzeitigen Saves in eine `IX_EndlessProgresses_AnonymousSessionId`-Unique-Race — war bereits abgefangen+retried, aber EF Core loggte `SaveChangesFailed` trotzdem auf Error → triggerte den Log-Watcher grundlos. Jetzt `ConfigureWarnings` in Program.cs: `CoreEventId.SaveChangesFailed`→Information [genuine, NICHT abgefangene Fehler bleiben übers HTTP-500-Request-Log sichtbar; betrifft alle bewusst abgefangenen DbUpdateException-Races: CoursePuzzleResult/CourseInfoView/CourseProgress/…]. (2) `POST /api/courses|repertoires/reprocess` lief bei vielen Chessable-Kursen über das ~60-s-Request-Timeout → 500 „operation was canceled". Neu: `IReprocessLauncher`/`ReprocessLauncher` [Singleton, eigener DI-Scope, `Task.Run` mit `CancellationToken.None`, Fehler nur geloggt] startet den Reprocess im Hintergrund; beide Endpoints geben sofort `202 Accepted {started:true}` zurück [nicht mehr synchron/`ct`-gebunden]. FE `ReprocessBannerComponent.run` erwartet keine Counts mehr → Snackbar `reprocess.started` [en/de/hr] + Status-Refresh nach 2,5 s. +Test-Doppel `RecordingReprocessLauncher`, reprocess-banner-Spec auf fakeAsync/202 umgestellt. NICHT deployed); 0.236.0 (Admin-CI markiert das laufende Image jetzt auch für crawler/piratechess/bot, nicht nur rookhub-Frontend: jeder Dienst hat einen build-info-Endpoint [crawler `GET /api/health/build-info`, piratechess `GET /api/chessable/direct/build-info` (ServiceKey), bot `GET /webhook/build-info`], gespeist aus CI-Build-Args `GIT_SHA`/`GIT_REF`→ENV. `GithubActionsService.ResolveRunningBuildsAsync` pollt sie parallel [best-effort, 4s-Timeout, im 4s-Overview-Cache], `CiRepoDto.RunningSha/RunningRef`; FE `isRunningBuild(run, repo)` nutzt für Fremd-Stacks repo.runningSha/ref [rookhub weiter client-seitig], Match per SHA+Ref → :dev=master-Run, :prod=Tag-Run. Nicht erreichbar/altes Image = kein Marker. +2 BE-Tests [GithubActionsService], +1 FE-Test [Fremd-Stack], +Crawler-/piratechess-/Bot-Endpoint-Tests. NICHT deployed); 0.235.1 (0.235.1 (Admin-CI markiert genau den EINEN Run, der das laufende Image baute: build-info.json trägt jetzt zusätzlich den Build-Ref [Branch bei :dev, Tag-Name bei :prod; docker.yml `GIT_REF=github.ref_name`, Dockerfile `ARG GIT_REF`, `{sha,ref}`]. master-Push + gleichnamiger Tag haben dieselbe SHA → reines SHA-Matching markierte beide; `isRunningBuild` verlangt jetzt SHA UND [sofern gemeldet] Ref-Gleichheit → :dev hebt master-Run, :prod den Tag-Run hervor. Altes Image ohne Ref = SHA-only-Fallback. +4 FE-Tests [neues admin-github-actions.component.spec]. NICHT deployed); 0.235.0 (Zwei Kurs-/Buch-Verbesserungen: (1) Sequenziell durchgeklickte Info-/Erklärlinien werden gemerkt [neue Tabelle `CourseInfoView` + Migration `AddCourseInfoView`, Endpoint `POST /api/courses/{bookId}/info-seen`, `CourseService.MarkInfoSeenAsync`; seqPool überspringt gesehene Info-Linien via `CourseInfoViews` statt sie IMMER zu zeigen; FE `courseNext` meldet die aktuelle Info-Linie + merkt sie offline in `offlineCourseSolvedIds`; beim Reset geleert; Cleanup in `BookAdminService.DeleteBookAsync`]. (2) Doppelte Kommentar-Anzeige entdoppelt: `puzzle.comment`-Kopfzeile [Template Z.88] UND `move-comment-card` [unten] ersetzt durch EINEN `ctx-comment`-Block in der Kontext-Card ÜBER der Fortschrittsanzeige, gespeist vom neuen Getter `displayComment` [Review/Info→moveComment mit Fallback auf puzzle.comment, sonst puzzle.comment] — gilt für alle Modi, nicht nur Info-Linien. +4 BE-Tests [1156] +5 FE-Tests. NICHT deployed); 0.234.1 (piratechess-Fix [Repo bb74686]: TryGetChessableErrorMessage fällt bei leerem error.message auf userFriendlyErrorMessage zurück → nicht besessene Kurse melden ehrlich 'You do not own this course.' (BOOK_NOT_OWNED) statt fälschlich 'Course has no chapters'. +1 Regressionstest. [Analyse: die 4 'no chapters'-Kurse des Re-Fetchs gehören dem aktuellen Bearer-Account uid 790927 nicht]); 0.234.0 (Navigation läuft jetzt IMMER über das Hamburger-Menü — auf allen Auflösungen, nicht nur mobil [`navbar.component`: `.nav-links` generell `display:none`, `.mobile-menu-btn` generell `inline-flex`; der 768px-Media-Query blendet nur noch die `.nav-extra`-Sekundär-Icons aus]. Bei den vielen Menüpunkten lief die Toolbar sonst auch auf mittleren/großen Screens über. NICHT deployed); 0.233.2 (Zwei Anarchy-Politur-Punkte: (1) Setup-Zug-Pfeil-Fix — der erste (automatische) Gegnerzug, der die Puzzle-Stellung aufbaut, bekommt jetzt auch den Gegnerzug-Pfeil [`showVizOpponentArrow()` im Setup-Timeout von `BasePuzzleSolver`, nach `playMove(setupMove)`]; vorher nur die Antwortzüge [advanceAfterCorrectMove/opponentRespond], sodass genau der stellungserzeugende Zug unmarkiert blieb — besonders auf dem Crazy-Brett störend. Gilt für alle Modi [+1 base-Solver-Test]. (2) Anarchy-Hints [`puzzles.anarchyHint1-3` en/de/hr] memefiziert [Holy hell / Google en passant / New response just dropped]. NICHT deployed); 0.233.1 (Mobil-Fix: Dashboard beim Anpassen wieder scrollbar — im Bearbeitungsmodus war die GANZE Kachel die Drag-Fläche [`.tile-shield` = `cdkDragHandle` über `inset:0`], sodass auf dem Touchscreen jede Berührung einen Drag auslöste statt zu scrollen. Gezogen wird jetzt nur noch am dedizierten Griff [`drag_indicator`-Icon je Kachel als `cdkDragHandle` in `.tile-controls`], der Kachel-Rumpf scrollt normal; Sortieren via ↑/↓-Pfeile bleibt. `.tile-shield` ist kein `cdkDragHandle` mehr [nur noch Klick-Fänger]. i18n `dashboard.edit.dragAria` wiederverwendet. NICHT deployed); 0.233.0 (Admin-CI-Run-Zeile aussagekräftiger: Ref (Branch/Tag-Name mit Icon; Tag-Läufe haben kein head_branch → GithubActionsService löst Tags per /tags sha→Name-Map auf, CiRunDto.Ref/IsTag), Startzeit (createdAt), Laufzeit (created→updated bzw. live bis jetzt), Titel bleibt Link zum Run (keine sichtbare URL). i18n admin.ci.started/duration. Tags-Abruf = +1 GitHub-Call/Repo (gecacht); Cache-Test angepasst); 0.232.0 (Admin-CI-Seite markiert den GitHub-Actions-Run des LAUFENDEN Frontend-Builds: Frontend-Dockerfile schreibt Commit-SHA (ARG GIT_SHA aus docker.yml --build-arg github.sha) nach /build-info.json; CI-Seite holt das + markiert den Run mit passendem head_sha grün ('live'). CiRunDto/GithubActionsService um HeadSha erweitert. Greift erst nach Deploy eines so gebauten Images; nur rookhub-Frontend meldet Build-SHA. +1 BE-Test-Assert); 0.231.0 (Repertoire-Reprocess auf denselben Stand wie Kurse gebracht + Zentralisierung: die gemeinsame Re-Fetch-Logik [Batch-Cache-Abruf 1×, kein Abbruch am Request-ct, Backoff für nicht-cachebare Kurse, Dedup, Admin-`trustOwnership`] liegt jetzt in EINER Methode `ImportReprocessService.EnqueueRefetchesAsync(candidates, isAdmin, result)` [+ `RefetchCandidate`-Record]; `ReprocessCoursesAsync`/`ReprocessRepertoiresAsync` sammeln nur noch Kandidaten. `ReprocessRepertoiresAsync` bekam `isAdmin`-Param [Default false], `IsAdmin` in `BaseApiController` hochgezogen [aus CourseController], RepertoireController reicht es durch. Vorher hing „Alle aktualisieren" bei Repertoiren [alter langsamer/blockierender Pfad]. +1 BE-Test [1152 grün]. NICHT deployed); 0.230.0 (Crazy-Modus Gegnerzug-Pfeil: der Gegnerzug wird auf dem Crazy-Brett mit einem GRÜNEN Pfeil markiert, der bis zum nächsten User-Zug stehen bleibt. BasePuzzleSolver: neuer overridebarer opponentArrowMode ['off'|'timed'|'persist']; showVizOpponentArrow setzt bei 'persist' keinen 1s-Timer, onMoveMade löscht den Pfeil beim User-Zug. Komponenten: crazy→'persist'. Board: applyVizOpponentArrow ohne visualization>0-Guard + neuer Input vizOpponentArrowBrush [crazy 'green', Viz 'red']. +2 base-Solver-Tests); 0.229.0 (Anarchy-Tipps: bei e.p.-Zwang liefern alle 3 Puzzle-Tipps drei unterschiedlich formulierte En-passant-ist-Pflicht-Hinweise statt der normalen Tipps. Zentral in BasePuzzleSolver [effectiveHints = enPassantForced && epForcedHints ? epForcedHints : availableHints; hasHints/shownHints/canShowMoreHints darauf], epForcedHints je Solver überschrieben [i18n puzzles.anarchyHint1-3 en/de/hr]. +2 base-Solver-Tests); 0.228.0 (1) Off-Path-Warnung in allen Puzzle-Modi: BasePuzzleSolver zählt off-path-Züge [offPathUserPlies], berechnet in opponentRespond aus currentEval [Weiß-Sicht] playerEvalPawns() [Spieler-Sicht, Matt=±100] und warnt EINMALIG [onOffPathWarning()-Hook → Snackbar in allen 3 Solvern] ab offPathWarnThreshold Zügen, wenn Eval < +2. Reset in setupSolver + Mouseslip-zurück-auf-Pfad. Schwelle = Pref offPathWarnMoves [lokal, Default 3, 0=nie], im Settings-Dialog. (2) Anarchy: Settings-Toggle En-passant-forciert [Pref enPassantForced, Default an, nur bei themeMode=crazy sichtbar]; enPassantForced in onSetupStart = anarchyForcedByUrl [URL ?anarchy=max] ODER (crazy && Pref). +7 base-Solver-Tests; 0.227.0 (Logging-Flag `RequestKind` [system/user]: automatische, nicht nutzer-initiierte Requests werden als `system` markiert → in Kibana filterbar. Neuer `Logging/SystemCallClassifier` [Health/Swagger, /api/client-log, /api/menu, `/count`|`-count`|`-counts`-Polls, chessable/admin/active|imports]; gesetzt via `LogContext.PushProperty("RequestKind", …)` in der Enrichment-Middleware [neben UserId/VisitorId] + im HeartbeatService [`system`]. +14 Classifier-Tests. Reine Observability. NICHT deployed); 0.226.0 (Fix „Alle aktualisieren" für Kurse tat bei Admins NICHTS: `OwnerHasCourseAsync` prüfte jeden bid gegen Chessables getHomeData, das nur einen TEIL der Bibliothek listet [User 5: 32 statt 259] → 262 übersprungen/0 eingereiht [+142s/500er]. Neu: `EnqueueReimportAsync` bekam `trustOwnership`-Param; `ReprocessCoursesAsync` setzt ihn = `isAdmin` → Admin-Reprocess überspringt die Eigentumsprüfung [Admins dürfen ohnehin alles holen]; Nicht-Admin + Repertoire-Reprocess unverändert geprüft. +3 BE-Tests. 259/262 sind gecacht → Fast-Lane. NICHT deployed); 0.225.0 (Endless-Deep-Link-Query-Parameter [`endless-puzzle.component` ngOnInit]: `?start=1`/`?autostart=1` [Auto-Start ohne Config via `maybeAutoStart()` nach Rating-Range-Load], `?themes=…`/`?tags=…` [Komma/Leerzeichen→`config.themes`, worstTags off, ODER-Filter], `?elo=…` [startElo], `?anarchy=max|max+1` [reused `parseShareViewParams` → themeMode='crazy'+enPassantForced, max+1 crazyPieceMode='square'; `[crazyPieceMode]` ans Endless-Board gebunden]. +6 FE-Tests); 0.224.1 (CSP-Fix: eigenes Inline-Script in `index.html` [PWA-`beforeinstallprompt`-Hook] wurde von der strikten CSP [`script-src 'self'`] blockiert → in externe `public/pwa-install-hook.js` ausgelagert [von `'self'` gedeckt, kein `'unsafe-inline'`/Hash]. CSP-Warnungen aus `content.js` = Browser-Extensions, korrekt geblockt, kein RookHub-Bug); 0.224.0 (Admin-Tab „CI / Actions": letzte 5 GitHub-Actions-Läufe je beteiligtem Repo [kahalm/rookhub,-chessresults_crawler,-schach-bot,-piratechess_docker,-log-watcher], Auto-Refresh alle 5 s, Status-Badge + Link. Backend: `GithubActionsService` [getypter HttpClient→api.github.com, Token aus `GitHub:Token`/Env `GitHub__Token` (PAT actions:read) pro Request, Repos `GitHub:Repos` Default=die 5, Owner `GitHub:Owner`=kahalm] + IMemoryCache-Aggregat-Cache `GitHub:CacheSeconds`=4 → 5-s-Polls rate-limit-schonend; ohne Token `Configured=false`→UI-Hinweis. `CiController` `GET /api/admin/ci/runs` [Admin]. FE: eigenständiger `AdminGithubActionsComponent` [timer(0,5000), lazy `*matTabContent`→nur bei offenem Tab], neuer Tab 'ci' [admin-tabs.ts+spec+HTML]. +4 BE-Tests. i18n en/de/hr); 0.223.0 (Neues Puzzle-Theme `enPassantPossible`: `PuzzleService.HasUnplayedEnPassant(fen, uci, firstSolverPly=1)` [pure, Gera.Chess: FEN laden, UCI-Lösung nachspielen, NUR an Löser-Zügen (Indizes 1,3,5… — moves[0]=Gegner-Setup) `board.Moves()` auf `MoveEnPassant` prüfen; e.p. verfügbar + gespielter Zug ≠ e.p. → true] + `TagEnPassantPossibleAsync` [batchweise, idempotent] taggt betroffene Standard-Puzzles im `Themes`-String UND der normalisierten `PuzzleTags`-Tabelle [get-or-create Tag]; Admin-Endpoint `POST /api/admin/puzzles/tag-en-passant` [Hintergrund-Job]. Abgegrenzt von Lichess-`enPassant` [Lösung IST e.p.]. Nur Standard-Puzzles. Kein neues DB-Feld/Migration. +7 BE-Tests); 0.222.0 (Chessable-Flut durch nicht-cachebare Kurse gestoppt — zwei rookhub-only Schutzmechanismen (keine Migration): (1) DEDUP: `ChessableImportService.EnqueueReimportAsync` legt keinen zweiten Import an, wenn für (Owner, bid) bereits einer läuft/pausiert → ein erneuter „Update all"-Klick/Retry löst keine doppelten Kurs-Downloads mehr aus (beobachtet: bid 116242 4× in Folge). (2) BACKOFF: `ImportReprocessService.ReprocessCoursesAsync` überspringt einen stale, NICHT gecachten Kurs, dessen letzter Import <24h her ist (`IncompleteRefetchBackoff`) — ein truncated Kurs wird von piratechess nie gecacht und würde sonst bei JEDEM Massen-Reprocess komplett neu von Chessable geholt (Hunderte Line-Fetches); nutzt die bereits geholte `cachedBids`-Menge + `ChessableImports.CompletedAt`. Gezielter Admin-Re-Import umgeht den Backoff. +3 BE-Tests [1114 grün]. NICHT deployed); 0.221.0 (Reprocess „Alle aktualisieren“ robust+schnell + gecachte Kurse trotz gesperrtem Bearer: (1) `ImportReprocessService.ReprocessCoursesAsync` holt den Cache-Status ALLER Kurse EINMAL per Batch [`ICourseReimporter.GetCachedBidsAsync` → piratechess `/courses/cached`] statt je Kurs den teuren `IsCourseCachedAsync` [lädt+entpackt den ganzen Blob]; `EnqueueReimportAsync` bekam `knownCached`-Param [`FullyCached = knownCached ?? IsCourseCachedAsync`]. (2) Die Einreih-Schleife läuft mit `CancellationToken.None` statt dem Request-ct → Wegnavigieren [Browser schließt Verbindung] bricht das Einreihen NICHT mehr ab [vorher nur ~20 von 268 in der Queue]. (3) `RunAsync`-Bearer-Breaker-Check `&& import.FullyCached != true` → voll-gecachte Kurse kommen aus dem Cache [kein Chessable-Abruf] und laufen trotz totem/gesperrtem Bearer durch. +4 BE-Tests [1097 grün]. NICHT deployed); 0.220.1 (Kurs-Upload-Hinweis: neuer i18n-Text `courses.upload.restriction` [en/de/hr] unter dem Upload-Panel — funktioniert nur mit Puzzle-PGNs im Chessable-Stil, sonst Admin ansprechen oder im Discord fragen [klickbarer `DISCORD_INVITE_URL`-Link aus `core/community.ts`]. Reiner Text/UI, keine Logik); 0.220.0 (Eigenes PGN als persönlicher Kurs hochladen: Kurse-Seite ist jetzt für JEDEN eingeloggten Nutzer erreichbar [`courseAccessGuard` lässt alle Eingeloggten durch, Einstieg über die Dashboard-Kachel — Navbar bleibt content-gated via `checkAccess`], auch bei leerer Liste; oben ein Upload-Panel [PGN-Datei + optionaler Name]. Backend: `CourseService.UploadPersonalCourseAsync` [reused `PgnImportService.ImportFileAsync`, pro-User-eindeutiger Dateiname `user-u{id}-{guid}.pgn`, setzt `Book.OwnerUserId`+`Kind=Study`+Tags `personal`; leeres Ergebnis → Buch wieder entfernt+400] + `DeletePersonalCourseAsync` [nur Besitzer, reused `BookAdminService.DeleteBookAsync`]; Endpoints `POST /api/courses/upload` [multipart] + `DELETE /api/courses/{bookId}`. NUR Chessable-Stil-Puzzle-PGNs [FEN+Round+Trainingsmarker] erzeugen Puzzles — normale Partie-PGNs werden verworfen. Löschen-Icon je eigenem Kurs. Kein neues DB-Feld/Migration. +6 BE-Tests [1100] +5 FE-Tests; i18n `courses.upload.*`/`courses.delete*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.219.1 (Repertoire-Trainer: alternativer (geduldeter) ODER falscher Zug bleibt jetzt ~1 s sichtbar auf dem Brett stehen, bevor er zurückgenommen wird, statt sofort zurückzuspringen. `onMove` setzt `this.fen = fenAfterPlayerMove` auch für tolerated/wrong [vorher tolerated sofort `lastMove=undefined`/wrong nur Markierung]; neue `startFen`-Erfassung am `onMove`-Anfang. Geduldet: ~1,8 s gehalten [`ADVANCE_MS.tolerated`], dann `retryCurrentCard` nimmt zurück [`fen=startFen`] + dieselbe Karte wieder spielbar. Falsch: neuer `WRONG_HOLD_MS`=1000-Timer [`scheduleWrongRevert`/`revertWrongHold`] nimmt zurück, bleibt im FEEDBACK [Mausrutscher/Lösung-zeigen-Buttons]; `clearWrongRevert` in `clearAdvance`+`mouseslip`+`showSolution`. 7 Trainer-Specs grün, PROD-Build grün); 0.219.0 (Zug-lose Kurs-Erklär-/Intro-Seiten [Kommentar, keine Züge] werden beim Buch-/Kurs-Import nicht mehr verworfen: `PgnImportService.ParsePgn(..., keepCommentOnlyAsInfo:true)` [von `ImportFileAsync` gesetzt; Wochenpost-Pfad bleibt Default false] behält sie als Info-Linie [synthetischer Fake-Zug e4 ab Grundstellung, `IsInfoOnly`, Text als Kommentar] → nur sequenziell zum Durchklicken, kein Quiz, nicht in Random/Daily, nicht im Fortschritt. `ImportPipeline.CurrentVersion` 4→5; die Seiten stehen schon im `Book.SourcePgn` → Reprocess ist hier ein reiner LOKALER Re-Import, KEIN Chessable-Re-Fetch. +4 BE-Tests [1094]); 0.218.1 (Repertoire-Trainer-Zug-Feedback gefixt: `onMove` setzte `this.fen` nie auf die Stellung NACH dem Zug → bei jedem Zug Brett-Take-back + Replay über die Folgekarte [Flackern]. Jetzt: richtig → `this.fen = fenAfterPlayerMove` [Zug bleibt stehen, kein Flackern]; geduldet/spielbar → `lastMove=undefined` + `this.fen` unverändert [Zug zurückgenommen] und neue `runAdvance`/`retryCurrentCard`: geduldet springt NICHT weiter [kein Vorspielen des Hauptzugs], sondern macht dieselbe Karte erneut spielbar, sodass der User den Hauptzug selbst zieht. `onPlayClick`+Timer rufen `runAdvance`. Spec: tolerated-Test auf Retry+Take-back umgestellt + neuer correct-„kein Flackern"-Test, 780 FE grün); 0.218.0 (Anarchy-Stufe `?anarchy=max+1`: wie max [Crazy-Brett + erzwungenes En passant] PLUS das FELD bestimmt den Figurenstil [`crazyPieceMode='square'`] — Figur nimmt das Zufallsset ihres Feldes an, beim Ziehen wechselt das Aussehen zum Zielfeld. `board-theme.util`: `paintCrazyPieces(root, mode, orientation)` + `squareOfPiece` [Feld aus `transform: translate`], 64er-Feld→Set-Map; `parseShareViewParams` matcht `max+1`/`max 1` [+-Dekodierung]. `BasePuzzleSolver.crazyPieceMode` → `@Input` am `app-puzzle-board`, `repaintCrazyPieces` mit Settle-Repaint [~230ms] nach der Zug-Animation. +5 board-theme-Specs [25]); 0.217.1 (Fix: „Letztes Puzzle"-Favoriten-Knopf zeigte zwei Herzen [mat-icon + ♥ im Label-Text der i18n `favorites.loveLast`]; ♥-Präfix aus en/de/hr entfernt, nur das Icon bleibt); 0.217.0 — 0.217.0 (Standard-Puzzle-Links beachten jetzt `?single=1` [wie Buch-Puzzles]: `puzzle.component` liest `single` aus der Query → nach dem Lösen KEIN Auto-Weiter [`handleSolved` überspringt `startSolvedCountdown`/`loadNext` bei `singlePuzzle`], der „Weiter"-Knopf [`showNext` default true] bleibt aber. +1 Spec. Der Standard-Teilen-Dialog hängt `single=1` NICHT automatisch an [nur Buch] — Link wird bei Bedarf von Hand erweitert); 0.216.0 (Geliebte Puzzles ♥: nach Lösen/Aufgeben jedes Puzzles [Standard/Endless/Kurs-Buch/Tagespuzzle] ein Herz-Button in der Status-Card [`PuzzleStatusCardComponent` `canFavorite`/`isFavorite`/`favoriteToggled`]. Der „Letztes analysieren"-Knopf ist in zwei geteilt: „Letztes analysieren" + „♥ Letztes Puzzle". Backend polymorph wie `PuzzleChallenge` [`FavoritePuzzle` Standard/Book via `PuzzleSource`, Unique (UserId,Source,PuzzleId), Migration `AddFavoritePuzzles`], `FavoriteService` [add/remove/contains idempotent, list mit Fen+Moves+Rating+Themes+Title-Enrichment + Skip verwaister Einträge], `FavoriteController` `api/favorites` [GET/`count`/`contains`/POST/DELETE], Menü-Key `favorites`. Neue Seite `/favorites` [`FavoritesComponent`, Nachspielen/Analysieren/Entlieben] + Dashboard-Kachel `favorites` [default-hidden] + Navbar. FE-Logik gekapselt in `FavoriteTracker` [optimistisch + Rollback, current/last synchron], in alle 3 Solver eingehängt. Wochenpost-Puzzles NICHT favorisierbar [keine echte BookPuzzle-Id]. +8 BE-Tests [1090] +15 FE-Tests [773]. i18n `favorites.*`/`nav.favorites`/`dashboard.favorites.*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.215.0 (Crazy-Brett würfelt jetzt JEDE Figur einzeln aus statt pro Rolle+Farbe [vorher „alle Bauern gleich"]: `board-theme.util` ersetzt das per-Rolle-`!important`-CSS [`applyCrazyPieces` entfernt] durch `paintCrazyPieces(root)` — pro `<piece>`-DOM-Element ein zufälliges Set via Inline-`background-image`, stabil über `WeakMap<Element,set>` [Figur behält ihr Set beim Ziehen; neues Puzzle/`redrawAll` würfelt neu]. `puzzle-board.component` ruft `repaintCrazyPieces()` [rAF] nach Init, nach jedem `ground.set` und nach Resize-`redrawAll`; sonst `clearCrazyPieces`. Figuren-CSS in `styles.scss` ist `.piece-set-<set>`-skopiert ohne `!important` → für `_crazy` greift nur das Inline. +5 board-theme-Specs [20]. Gilt für Crazy-Theme + `?anarchy=max`); 0.214.0 (Anarchy-Modus `?anarchy=max` an jedem Puzzle-Link [Standard + Kurs/Buch]: Crazy-Brett UND en passant verpflichtend [„en passant is forced"]. `parseShareViewParams` parst `anarchy=max`→`{themeMode:'crazy', enPassantForced:true}`; `BasePuzzleSolver.enPassantForced` an `calcDests(chess, enPassantForced)` durchgereicht → ist ein En-passant-Schlag verfügbar [chess.js-Flag `e`], enthält die dests-Map NUR diese Züge, sonst alle. `puzzle.component`+`book-puzzle.component` setzen das Flag aus dem Query. Rein transient, ändert keine gespeicherten Einstellungen. +2 Specs `puzzle-move.util` +2 `board-theme.util`. Hinweis: paralleler 0.213.1-Fix [Reprocess→Re-Fetch] liegt uncommittet in Kopie 1; daher Sprung auf 0.214.0); 0.213.1 (Reprocess-Banner-Fix für die Info-Linien-Änderung: bestehende Chessable-Kurse haben ein gecachtes Alt-PGN OHNE `[%info]`-Marker; `ImportReprocessService.GetCourseStatusAsync`/`ReprocessCoursesAsync` stuften sie als „lokal aufbereitbar" ein → lokales Reprocess hätte `ImportVersion` auf 4 hochgesetzt OHNE `IsInfoOnly` zu setzen [still falsch, Banner verschwindet]. Neu: Chessable-Kurse [`CanRefetch` = `IsChessable` + parsebare bid] werden IMMER als `Refetchable` eingestuft und per vollständigem Re-Fetch aktualisiert [Chessable-Zweig VOR dem SourcePgn-Pfad], nur Nicht-Chessable-Bücher mit Quelle laufen lokal. Matcht den v4-Doc-Kommentar [„für Chessable-Bücher ist der Reprocess ein Re-Fetch"], der vorher nur für Repertoires zutraf. 3 bestehende Tests auf Nicht-Chessable-Lokalbuch umgestellt + 1 neuer Test [`ReprocessCourses_ChessableWithSource_RefetchesInsteadOfLocalReprocess`]; 12 Reprocess-Tests/1082 BE grün. NICHT gepusht/getaggt/deployed); 0.213.0 (Frontend-Durchklick-UI für Chessable-Info-/Erklärlinien [Backend-Seite war 0.212.0]: `book-puzzle.component` bekam den Zustand `'INFO'` — `setupPuzzle` zweigt bei `puzzle.isInfoOnly` in `enterInfoReview()` ab [kein `setupSolver`, Brett view-only, `reviewMode`, ◀/▶ über die ganze Linie + Zug-Kommentare], statt Status-Card eine „Erklärlinie"-Card, Kontext-Nav zeigt „Weiter" [`courseNext`/`nextInBook`]. Board-`[visualization]` im Review/Info auf 0. i18n `book.info.title/subtitle` + `book.actions.prev/next` en/de/hr. +2 FE-Specs. HINWEIS: greift nur für aus Chessable neu importierte/abgerufene Kurse); 0.212.0 (Chessable-Info-/Erklärvarianten [von Chessable als IsInfo markiert] werden nicht mehr als Quiz abgefragt: piratechess emittiert `[%info]` im PGN [Commit `6f5ed38`], rookhub-Import setzt daraus `BookPuzzle.IsInfoOnly` [+Migration `AddBookPuzzleIsInfoOnly`, +`ImportPipeline.CurrentVersion` 3→4]. Solche Linien fallen aus ALLEN Zufalls-/Tagespuzzle-Töpfen [`BookPuzzleService`-Pool + `GetRandomInBookAsync`], zählen NICHT zum Kurs-Fortschritt [`CourseService`: `quizScope` ohne `IsInfoOnly` in `GetNextAsync`/`GetChaptersAsync`/`GetCoursesAsync`/`BuildProgressAsync`/`ComputeStatsAsync`] und sind im sequenziellen Kurs-Modus nur zum Durchklicken [seqPool nimmt Info-Linien mit, Completion bleibt quiz-basiert]. `BookPuzzleDto.IsInfoOnly` durchgereicht. HINWEIS: Marker entsteht erst beim frischen Chessable-Abruf → bestehende Kurse erst nach Re-Import/Re-Fetch. Frontend-Durchklick-UI folgt [0.213.0]. +7 BE-Tests [1081] +2 piratechess-Lib-Tests [16]); 0.211.0 (Dashboard-Customize „Standard"-Knopf + kuratierter Default: `applyDefault()` [ersetzt `resetLayout`] setzt `DEFAULT_VISIBLE` = Puzzles/Weekly/Repertoires/Kurse/Trainingsziele/Bestenlisten [in dieser Reihenfolge] sichtbar, alle übrigen Kacheln ausgeblendet [`DEFAULT_HIDDEN`], und persistiert. Dieser Default ist auch das Initial-Layout [ngOnInit: leeres gespeichertes Layout → kuratierter Default]. `DashboardLayoutService`-Key auf `rookhub_dashboard_layout_v2` erhöht → alle User starten einmalig frisch auf dem Default. Button-i18n `dashboard.edit.default` [Standard/Default/Zadano] en/de/hr. Spec-Anpassungen [Default-Layout, chessableQueue jetzt default-hidden → Edit-Modus-Checks]. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.210.0 (Visualisierungs-Umschalter unter dem Brett (0.207.0) WIEDER ENTFERNT — gefiel nicht. `VizModeSelectorComponent` + Spec gelöscht, viz-0..4-SVG-Icon-Registrierung aus `app.component` raus, Import/Imports-Array in allen drei Solvern entfernt. Stattdessen der alte Aufdecken-Knopf zurück in `.board-hint` (`@if vizPiecesHidden || visualizationMode===1`, ruft `onVizShow()`), aber als **Auge-Icon** statt Text: `vizShowPressed ? 'visibility' : 'visibility_off'` (durchgestrichen solange verdeckt, offen wenn aufgedeckt) + `.board-hint-eye`-Style; Tooltip/aria via vorhandene `*.viz.show`/`showing`-Keys. Modus-Wahl wieder nur im Einstellungsdialog. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.209.1 (Build-Fix: gerades Anführungszeichen im de-0.209.0-Changelog [`„keine Treffer"`] beendete den TS-String vorzeitig → Production-Build [CI] rot; auf typografisches `„…“` korrigiert. FALLE [[rookhub-changelog-build-gotcha]]: nach Changelog-Edit IMMER `npx ng build` [Production], nicht nur `--configuration development` — der Dev-Build lief vor dem Changelog-Edit und übersah es); 0.209.0 (Suchfeld für Kurse + Repertoires: oben in `course-list`/`repertoire-list.component` ein `mat-form-field`-Suchfeld [`[(ngModel)]="search"`, Such-Icon + Leeren-Knopf, `subscriptSizing="dynamic"`]; clientseitige Filterung über die bereits geladene Liste [Kurse nach `displayName`, Repertoires nach `name`+`description`, case-insensitive]. Kurse: neuer `filtered`-Getter, die Sektions-Getter [inProgress/public/chessable] bauen darauf auf → Filter wirkt über alle Sektionen; `courses.noMatch`-Hinweis. Repertoires: `filteredRepertoires`-Getter, `@empty`→`repertoire.list.noMatch` vs `empty`. FormsModule+MatFormField/Input ergänzt. i18n `common.clear`, `courses.searchPlaceholder`/`noMatch`, `repertoire.list.searchPlaceholder`/`noMatch` en/de/hr. +1 course-Spec +4 repertoire-Specs [756 grün]. NICHT deployed); 0.208.0 (Dashboard-Bearbeitungsmodus ändert die Ansicht NICHT mehr: das Kachelraster behält sein Layout [kein flex-column-Umbau mehr], es werden nur alle geeigneten Kacheln gezeigt [deaktivierte `tile-off` ausgegraut]. Je Kachel Overlay oben rechts: ↑/↓-Pfeile [`moveUp`/`moveDown` → `shift`/`commitOrder`] + Auge-Toggle; transparentes `.tile-shield` [zugleich `cdkDragHandle`] fängt Klicks auf den Kachel-Buttons ab [keine versehentliche Navigation] und ist die Drag-Fläche. Drag&Drop bleibt, jetzt `cdkDropListOrientation="mixed"` [rastertauglich, CDK 19]. i18n `dashboard.edit.moveUp`/`moveDown` + hint-Text en/de/hr. +2 FE-Specs [751 grün]. NICHT deployed); 0.207.0 (Visualisierungsmodi als Inline-Icon-Umschalter in der Solver-Hinweiszeile [ersetzt den „Aufdecken"-Knopf]: neue `VizModeSelectorComponent` [5 Buttons, Output `modeChange`→ruft das vorhandene `setVisualizationLevel($event)`] in `.board-hint` aller drei Solver [puzzle/book-puzzle/endless-puzzle.component.html]; aktiver Modus hervorgehoben. 5 SVG-Icons global via `MatIconRegistry.addSvgIconLiteral` in `app.component` [`viz-0`…`viz-4`, wie Discord-Logo]: 0=Bauer, 1=Bauer gedimmt+Kette, 2=schwarz/weiß-Token [fix], 3=weißes Token [fix], 4=durchgestrichenes Auge [gefüllt, Iris+Pupille via Maske]. 0/1/4 `currentColor`, 2/3 feste Farben. Aufdecken = Modus 0 wählen [`board-hint-show`+`onVizShow` aus Templates raus; Base-Logik bleibt]. i18n `puzzles.viz.modeSelectAria` en/de/hr. +3 FE-Specs [748 grün]. Versionssprung 0.206.0→0.207.0 [paralleler 0.206.0-Kollision mit Revenge-Icon ausgewichen]. NICHT deployed); 0.206.0 (Freundesliste: Revenge-a-Friend-Icon wird ROT [+ Anzahl-Badge], wenn offene Revanchen gegen den Freund existieren [Freund gescheitert+nie gelöst, ich noch nicht gelöst]. `GET /api/friends` reichert je Freund `FriendDto.OpenRevengeCount` an [`PuzzleService.GetOpenRevengeCountsAsync`, provider-sicher in-memory aggregiert]; FE `friends.component` bindet `[color]`+`matBadge`, i18n `friends.revenge.menuOpen` en/de/hr. +1 BE-Test [25] +1 FE-Test [8]); 0.205.3 (Observability: Admin-Impersonation pro Request sichtbar — die Request-Logging-Middleware [`Program.cs`] schreibt den `imp`-Claim [Admin-Id] jetzt als `LogContext`-Property `ImpersonatorId` [→ ECS `labels.ImpersonatorId`], sodass impersonierte Requests in Kibana filterbar sind; vorher nur das einmalige Impersonation-Start-Event geloggt. Reines Logging-Plumbing analog `UserId`/`UserName`; `imp`-Claim-Quelle bereits in `AuthServiceTests` getestet); 0.205.2 (Chessable-Bearer-Circuit-Breaker GEMERGT [vorher lokales v0.204.0, Commit `d6f0995`] und mit der entfernten 0.203.x-Chessable-Security-Linie versöhnt: weist Chessable einen Bearer endgültig ab [Account gesperrt/gelöscht oder Token tot — `ChessableBearerBreaker.IsBearerFatal`, NICHT bei IP-/Cloudflare-/VPN-Block], sperrt ihn [`ChessableCredential.BlockedAt`/`BlockedReason` + Migration `AddChessableCredentialBreaker`] und verwendet ihn für KEINE weitere Anfrage. Trip in `ChessableImportService.FailAsync` + `ChessableController` [Test/Courses/Admin-Courses/Estimate]; laufende Importe PAUSIEREN [`Phase="bearer-blocked"`], Lese-Abrufe/neue Importe → 400. Reset = erfolgreicher „Testen"-Klick [`ClearAndResumeAsync`]; neuer Bearer-Save schließt den Breaker. Admin-Test-Endpoint `POST admin/users/{id}/test`. FE: Sperr-Banner + 🔒/„Bearer testen" im Admin-Kursdownload. Merge-Konflikt: in `StartImportForUserAdmin` sitzt die Bearer-Sperr-Prüfung VOR der neuen `UserOwnsCourseAsync`-Eigentumsprüfung [0.203.8], damit ein toter Bearer keinen Fetch auslöst. +12 BE-Tests. Versionssprung 0.205.1→0.205.2 [0.204.0 in 0.205.2 aufgegangen]); 0.205.0 (Anpassbare Dashboard-Kacheln: Dashboard ist ein konfigurierbares Kachelraster über alle sinnvollen Module [Puzzles/Trainingsziele/Kurse/Bestenlisten/Turniere/Freunde/Repertoires/Partien/Wochenpost/Statistik/Analyse/Nachrichten + Admin-Chessable-Queue]. „Anpassen"-Knopf → Bearbeitungsmodus: Kacheln per CDK-Drag&Drop sortieren + per Augen-Knopf ein-/ausblenden, „Zurücksetzen" → Default. Persistenz pro Gerät via `DashboardLayoutService` [localStorage `rookhub_dashboard_layout`, `{order,hidden}`]; Eignung je Kachel über `MenuService.visible$` [+ `auth.isAdmin` für Chessable]; neue Kacheln werden hinten an ein bestehendes Layout angehängt. Datengetriebene `TileDef`-Definitionen [`eligible`/`subtitle`/`buttons`] + generisches Card-Template; Detail-Listen bleiben darunter. i18n en/de/hr. +5 Layout-Service-Specs +7 Dashboard-Tile-Specs. HINWEIS: Das lokale v0.204.0 [Chessable-Bearer-Circuit-Breaker, Commit `d6f0995`] ist auf diesem gepushten Stand NICHT enthalten — es kollidiert mit der entfernten 0.203.x-Chessable-Linie und braucht einen bewussten manuellen Merge; gesichert im Branch `backup-local-master`. Daher Versionssprung 0.203.13→0.205.0); 0.203.12 (piratechess VPN-Concurrency [piratechess `53e90b1`]: `VpnIpHealth._byIp`-Eviction [Cap `Vpn:IpHealthMaxEntries`]; `VpnTunnel.RequestCompleted` zählt Ausgang VOR `_inFlight`-Decrement [Block-Fehlbuchung bei Drain-Race behoben]; `_currentIp` volatile; `VpnRotationService.RotateNowAsync` sequenziell statt `Task.WhenAll` [kein Total-Stall bei manueller Rotation]. 253 Tests grün. NICHT getaggt/deployed); 0.203.11 (piratechess Parsing-Fix [piratechess `1fe1eda`, Batch 2]: `SanToMove` konstruiert gerade Bauern-Push-Umwandlungen [`e8=Q`] direkt — ChessDotNet 1.0.0 listet sie nicht in `GetValidMoves` → vorher leerer `[%tqu]`-Trainings-UCI + Abbruch der Folgezüge; relevant für Endspiel-Kurse. +10 Lib-Fixtures [SAN + softFail]. Geprüft als Nicht-Probleme: useLocalData-Oid [positionell korrekt, Oid beim Cachen nie gesetzt] + softFail-Indizierung [relativ korrekt]. 252 Tests grün. NICHT getaggt/deployed); 0.203.10 (piratechess Code-Review Resilienz+Hygiene [piratechess-Repo, Commit `5074a72`, 242 Tests grün]: ExportBackgroundService überlebt Reader-Fehler [äußere while+Backoff]; curl stdout/stderr parallel [kein Pipe-Deadlock]; ExportJobQueue bounded; Startup-Fail-fast für ConnString/Jwt; globaler ProblemDetails-Exception-Handler; Health 503 bei DB-Ausfall; Dictionary-Indexer statt Add bei doppelter Bid; bid-Format-Validierung [numerisch] → schließt BidLock-Leck. NICHT getaggt/deployed); 0.203.9 (**Security-Fix (2. Review-Pass)**: zweite Tür zum Cached-Content-Bypass geschlossen — `EnqueueReimportAsync` (Reprocess-/Re-Fetch-Choke-Point) prüft jetzt via `OwnerHasCourseAsync`, dass der `bid` in der Bibliothek des Owners liegt, sonst Skip [null]. Vorher konnte ein User ein Repertoire mit beliebigem `ChessableCourseId`/präpariertem Dateinamen anlegen [ImportVersion 0 = stale] und per `/reprocess` fremden gecachten Kursinhalt holen. +2 Tests/1051 grün); 0.203.8 (Nachzieher Import-Security: Admin-`StartImportForUserAdmin` macht denselben `UserOwnsCourseAsync`-Check gegen den Ziel-User; anonyme Tagespuzzle-Lösungen DB-seitig dedupliziert — Unique-Index `(BookPuzzleId, AnonymousSessionId)` [Migration `AddAnonBookAttemptUniqueDedup` inkl. Alt-Duplikat-Bereinigung; authentifizierte Versuche unberührt via NULL-Session], `RecordAnonymousAttemptAsync` fängt `DbUpdateException` ab → kein Doppel-Count/Webhook); 0.203.7 (**Security-Fix**: `ChessableController.StartImport` prüft via `UserOwnsCourseAsync` jetzt, dass der `bid` in der Chessable-Bibliothek des Users liegt [gecachte Liste, sonst 1× frisch laden], sonst 403. Vorher konnte jeder eingeloggte User mit Bearer JEDEN gecachten Kurs importieren — piratechess `POST direct/course` liefert gecachte Kurse aus dem geteilten Cache OHNE Eigentumsprüfung, und Chessable-Kurs-IDs sind öffentlich. Closes den Content-Bypass für alle Lanes [cached+fetch]; +1 Test/1049 grün); 0.203.6 (Doku: API-Code-Review 2026-06-30 in TODO.md festgehalten — Gefixtes [v0.202.0–0.203.5] + bewusst gegen reale Prod-Größe [49 User] zurückgestellte „unbounded read"-Funde [laden real 50–1900 Zeilen → SQL-Umbau = Pomelo-Risiko ohne Nutzen] + Import-Concurrency [bereits durch Claim+Watchdog+Lane-Gate abgedeckt]; keine Code-Änderung); 0.203.5 (Code-Review-Robustheit: `CreateChallengeBatchDto.ToUserIds` `[MaxLength(50)]` [DoS-Schutz]; BookPuzzle-Routen `{id:int}`; `RepertoireTrainingService.ReviewAsync` `DbUpdateException`-Race-Catch [Unique CardKey]; `TournamentMonitorController` `TryGetInt32` statt `GetInt32` [kein 500 bei Crawler-Typänderung]. Bereits erledigt im aktuellen Code: Buch-Versuch klemmt Zeit/Hints serverseitig + validiert SessionId; Listen-Endpoints klemmen take/page); 0.203.4 (Code Review: SavedGame-Dedup hart auf DB-Ebene — Unique-Index `(UserId, Source, ExternalId)` [Migration `AddSavedGameUniqueDedup` inkl. Alt-Duplikat-Bereinigung; NULL-ExternalId weiter erlaubt], `SaveAsync` fängt `DbUpdateException` ab und liefert die bestehende Partie); 0.203.3 (Performance [Code Review]: `AutoSubscriptionService` — frischer DbContext/Scope pro User [`CheckAllUsersAsync`, kein unbeschränktes ChangeTracker-Wachstum/Leakage über den ganzen Lauf] + Freundes-/Profil-Set je User EINMAL geladen [`LoadFavoriteProfilesAsync`, an `AutoFavoritePlayersAsync` durchgereicht] statt je Turnier neu; reine Effizienz, Verhalten unverändert); 0.203.2 (Korrektheit [Code Review]: (1) Daily-Leaderboard `AggregateScores` Competition-Ranking nach Lösezeit [zeitgleiche Löser = gleicher Rang/Bonus + alle 🥇, statt Submit-Reihenfolge bei TimeSeconds==0]; (2) `AdminMessageService.GetThreadsAsync` lädt jüngste Nachricht je Thread über `Max(Id)` statt `CreatedAt`-IN-Match [kein Cross-Thread-Mismatch bei Zeitgleichheit]; +2 Tests); 0.203.1 (Performance [Code Review]: Wochenpost-Übersicht ohne N+1 — `WeeklyPost.PuzzleCount` gecacht [Migration `AddWeeklyPostPuzzleCount`, beim Upload berechnet], `GetAllProgressAsync` lädt alle Posts in EINER Query + nutzt gecachte Anzahl statt PGN-Parse je Post; `GetTotalAsync`-Helper mit Lazy-Backfill für Alt-Datensätze auf allen Pfaden); 0.203.0 (Puzzle-Link-Query-Parameter `?crazy=1`/`?visualmode=0..4` [anderer Agent]); 0.202.2 (Security-Härtung [Code Review]: benannte Rate-Limiter `auth`/`anonymous-puzzle`/`anonymous-tournament` partitionieren jetzt pro Client-IP [`AddPolicy` mit `RateLimitPartition` statt einzelnem `AddFixedWindowLimiter`-Bucket] → kein site-weiter Login-DoS / pro-IP-Brute-Force-Drossel; Default-CORS ohne `AllowCredentials` [Bearer-only]; „Eingeloggt bleiben"-JWT 365→90 Tage); 0.202.1 (Viz-/Blind-Puzzle-Gesten gehärtet [anderer Agent]); 0.202.0 (Track-solves erfasst Tipp-Nutzung: `SharedPuzzleAttempt.HintsUsed` (0–3, Migration `AddSharedPuzzleAttemptHints`) wird beim Erstversuch mitgespeichert; `RecordSharedAttemptDto.HintsUsed` + `SharedPuzzleCountsDto.SolvedByHints[0..3]` (gelöste je Tipp-Stufe) — `GetSharedCountsAsync` aggregiert in einer GroupBy-Query; Buch-Puzzle-Solver sendet `hintLevel`. +2 BE-Tests); 0.201.0 (Kurs „Ganzes Buch" + Repertoire-Trainer Politur: (1) Kurs-Abschluss differenziert: `book-puzzle.component` neuer Getter `courseFullyDone` (= `courseSolved >= courseTotal`) + `courseRemaining`; Template zeigt Trophäe+„Kurs abgeschlossen!" nur wenn wirklich alle gelöst, sonst „Zufalls-Runde durch — noch N Aufgaben" mit „Mit den restlichen weitermachen"-Knopf. Backend bleibt unverändert (`Completed=puzzle==null` = Pool leer; war im Random-Modus immer schon irreführend für die UI). i18n `book.course.roundFinished`/`continueRemaining` en/de/hr. (2) Repertoire-Trainer `tolerated`-Feedback verrät den Repertoire-Zug nicht mehr — neuer Key `toleratedPlayable` ohne `{{move}}`. (3) `wrong`-Feedback zeigt nicht mehr sofort die Lösung: zwei Buttons „Mausrutscher" (kein Penalty, kein Server-Review, kein Re-Queue, zurück zu PLAYING; Brett unverändert) und „Lösung zeigen" (zählt erst dann als wrong, sendet `grade=0` an `RepertoireTrainingService.review`, re-queued die Karte, enthüllt `expectedDisplay`). State-Felder: `wrongRevealed`, `wrongMove`, `pendingWrongReview`. (4) Bei `wrong` Stockfish-18-Eval-Vergleich im Hintergrund: `StockfishService.getEval` (Depth 14) auf FEN nach Spielerzug + FEN nach Repertoire-Zug, Differenz aus Spieler-Sicht in Bauern; Mate-Sonderfälle (`evalMateNote` = `missed`/`allowed`). `kickOffEvalCompare` mit `evalEpoch` gegen Karten-/Zugwechsel-Race; Stockfish-Init im `ngOnInit` (Warmup). i18n `repertoireTrainer.toleratedPlayable`/`wrongNoHint`/`mouseslip`/`showSolution`/`evalLoading`/`evalWorse`/`evalEqual`/`evalMateMissed`/`evalMateAllowed` en/de/hr. +2 Spec-Cases (mouseslip kein Penalty, showSolution zählt+re-queued); 6/6 trainer-spec grün); 0.200.4 (Mobile-Fix Status-Karten: bei `max-width: 768px` setzte `puzzle.component.scss`+`book-puzzle.component.scss` zwar `flex-direction: column`, aber nur `.board-section: 100%` — `.info-section` blieb auf Content-Width, daher waren die Status-/Korrekt-Karten unter dem Brett schmaler als das Brett selbst. Beide bekommen jetzt `.board-section, .info-section { width: 100% }` wie endless-puzzle bereits seit längerem. Gilt für Standard-Puzzles und Buch-/Kurs-/Tages-/Wochen-/Geteilte-Puzzles); 0.200.3 (Dark Mode als Default: `ThemeService._preference` Default `system`→`dark`; gespeicherte Nutzerwahl [localStorage `rookhub_app_theme`] hat weiter Vorrang, Toggle unverändert); 0.200.2 (Leaderboard „Diese Woche"/„Dieser Monat" = rollierende Fenster: `LeaderboardService.WindowStart` weekly→`today.AddDays(-6)` [letzte 7 Tage], monthly→`today.AddDays(-30)` [letzte 31 Tage] statt ISO-Montag/Monatserster; reine Backend-Änderung, FE-Labels unverändert. Boundary-Test angepasst, 9 LeaderboardService-Tests grün); 0.200.1 (Track solves immer an: das Opt-in [Teilen-Dialog-Checkbox + `?track=1`-Param] entfernt; `BookPuzzleComponent.trackSolves = singlePuzzle` [also für jeden geteilten `?single=1`-Link aktiv]. SharePuzzleDialog: Checkbox/`canTrack`/`setTrack`/`&track=1`-Anhängen + MatCheckbox-Import raus, `activeUrl` liefert nur die Basis-URL; ungenutzte i18n `puzzles.share.trackSolves(+Hint)` entfernt. share-dialog-Spec umgeschrieben); 0.200.0 (Geteilte Puzzles „Track solves": Teilen-Dialog bekam Checkbox „Versuche zählen" [nur bei Buch-Einzel-Link mit `?single=1`; hängt `&track=1` an `activeUrl`]. Empfänger-Link zeigt unter dem Puzzle Gelöst-/Fehlversuch-Zähler. Neue Tabelle `SharedPuzzleAttempts` [Unique `(BookPuzzleId, IdentityKey)` → nur Erstversuch je Besucher; `u:{userId}`/`s:{sessionId}`] + Migration `AddSharedPuzzleAttempts`; Endpoints `POST/GET /api/book-puzzles/{id}/track[-counts]` [AllowAnonymous, optionale Auth via `GetUserIdOrNull`]. `BookPuzzleComponent`: Flag `trackSolves` [Query `track=1`], `recordTrack(solved)` [guard + serverseitig erstversuch-dedupliziert] aus finalizeSolve[true]/handleFailed/giveUp/resetPuzzle[false] → Reset/Aufgeben = failed; Anzeige `sharedCounts`. i18n `puzzles.share.trackSolves(+Hint)`/`book.track.solved|failed` en/de/hr. +5 BE-Tests [1036], +5 FE-Tests [neues share-dialog-Spec + book-puzzle]); 0.199.1 (Direkt geteiltes Einzel-Puzzle bleibt am Ende stehen: Teilen-Link aus dem Buch-Puzzle-Solver bekommt `?single=1` [`sharePuzzle()` url+previousUrl]; `BookPuzzleComponent` liest das in Flag `singlePuzzle` → `finalizeSolve` überspringt den Auto-Advance-Countdown, `solvedAutoNext` ist no-op, neuer Getter `browseInBook`=`standalone && !isDaily && !singlePuzzle` blendet die Nächstes/Zufällig-im-Buch-Buttons aus, und `PuzzleStatusCard` bekam `@Input() showNext` [=`!singlePuzzle`] um den „Weiter"-Knopf im Gelöst-Zustand zu verstecken. Normales Buch-Durchblättern unverändert. +3 FE-Tests [28 grün im book-puzzle-Spec; share-URL-Test auf `?single=1` angepasst]); 0.199.0 (Freundesliste zeigt ausstehende GESENDETE Anfragen: Anfragen-Tab bekam neben „Eingehend" einen Abschnitt „Gesendet (wartet auf Bestätigung)" [bisher nirgends sichtbar], je Zeile Zurückziehen-Knopf [reused `DELETE /api/friends/{id}` → `RemoveFriendAsync` erlaubt auch dem Requester], Tab-Zähler = eingehend + gesendet. BE: `FriendService.GetSentPendingRequestsAsync` [RequesterId==me && Pending, inkl. Addressee+Profile] + DTO `SentFriendRequestDto` + Endpoint `GET /api/friends/requests/sent`. FE: `FriendsService.getSentRequests` + `friends.component.sentRequests`/`withdrawRequest`; i18n `friends.requests.*`/`friends.aria.withdraw`/`friends.errors.withdrawRequest` en/de/hr [22 weitere fallen für friends-Detailkeys ohnehin auf en zurück]. +1 BE-Test [24 FriendController grün] +2 FE-Tests [7 grün]); 0.198.0 (Puzzle-an-Freund-Menü: hinter jedem Freund in Klammern, wie viele der von mir geschickten Puzzle noch OFFEN sind [Pending = noch nicht versucht], z. B. „Max (3)"; gelöste/gescheiterte zählen NICHT [erledigt], Freunde ohne offene zeigen keine Zahl. BE: `ChallengeService.GetPendingOutgoingCountsAsync` [GroupBy ToUserId, nur FromUserId==me && Pending] + Endpoint `GET /api/challenges/outgoing/pending-counts`. FE: `ChallengeService.getPendingCounts` + `ChallengeFriendsComponent.pendingCounts` [lädt beim Menü-Öffnen mit der Freundesliste, frischt nach `send()` nach]; i18n `puzzles.challenge.unsolvedTitle` en/de/hr [andere 22 Sprachen fallen für den ganzen challenge-Block ohnehin auf en zurück]. +1 BE-Test [18 ChallengeController grün], +1 FE-Test [7 grün gesamt]); 0.197.1 (Freundeszahl reaktiv: Nimmt ein Freund meine Anfrage an, aktualisierte sich die Freundeszahl bei mir erst nach Seiten-Refresh [Dashboard-`friendCount` nur in `ngOnInit`-`forkJoin` geladen, keine Reaktivität]. Fix: `InAppNotificationService` bekam ein `arrived$`-Subject [feuert, wenn `refreshCount` einen GESTIEGENEN Ungelesen-Zähler erkennt = neue Notification]; Dashboard lädt darauf die Freundeszahl nach, `friends.component` macht einen STILLEN `loadData(true)` [kein Spinner-Flackern]. Hängt am vorhandenen 60-s-Glocken-Poll → kein zusätzlicher Timer. +3 Tests [service `arrived$`, dashboard friendCount, friends quiet reload] + neue `dashboard.component.spec`); 0.197.0 (Chessable-Import-Fortschritt auf der Kursseite: neues schreibgeschütztes `ChessableImportsBannerComponent` [self-polling `getImports`, 8s] oben in `CourseListComponent` zeigt laufende/pausierte Importe mit derselben Visualisierung wie der Chessable-Tab [„hole Kurs… Kapitel 7/36 · 82/1000 Linien · noch ca. 23 Min"], `(importCompleted)`→`loadCourses()` lädt nach Abschluss nach. Fortschritts-/ETA-/Label-Logik aus `chessable.component` in geteilte `chessable-progress.util` extrahiert [`chessableStatusLabel`/`chessableQueueLabel`/`compareImportsByQueue` + `CHESSABLE_LINES_PER_MIN`/`formatDuration`/`effectiveTotalLines`/`estimateRemainingMinutes`, von der Komponente rückwärtskompatibel re-exportiert]. Chessable-Tab-Queue `activeList()` jetzt nach „#" sortiert [queuedAhead asc, dann createdAt]. +2 Spec-Dateien); 0.196.2 (Daily-💡-Badge-Fix: Tagespuzzle in Discord zeigte kein 💡 hinter Lösern mit Tipps, weil das Daily-Webhook-Payload [`SchachBotWebhookService.NotifyAttemptAsync`] pro Solver nur `name/discordId/discordUsername/timeSeconds` sendete — **`hintsUsed` fehlte** [war NIE drin, Git-Historie bestätigt; nur das Wochenpost-Payload `NotifyWeeklyAsync` sendet es]. Bot rendert es längst [`puzzle/daily_results.py:141` `s.get('hintsUsed',0)>0`]. Fix = 1 Zeile `hintsUsed = s.HintsUsed` in der Solver-Projektion [Daten lagen in `BookSolverDto.HintsUsed` bereit]. Test erweitert/6 Webhook-Tests grün. Kein Bot-Update nötig. NICHT deployed); 0.196.1 (Discord-Solver-Webhook eigene Queue: Daily-Solve [book-puzzle 19207, User 5/kahalm] erschien nicht in Discord, weil der schach-bot-Webhook-Push [`BookPuzzleService.NotifySchachBotAsync` Z.99/188, `WeeklyPostService`] sich DIESELBE `IBackgroundTaskQueue` mit dem Chessable-Import teilte — bounded cap 100 + `DropOldest` + 1 serieller Consumer [[prod-chessable-import-stall-restart]]. Nach Redeploy hatte der ResumeService ~263 minutenlange Import-Jobs in die 100er-Queue geworfen → das Webhook-Ticket wurde verdrängt, bevor es lief [heute KEIN `Notify`-Log; gestern feuerte derselbe Webhook ohne Import problemlos]. Fix: neue `IWebhookTaskQueue`/`WebhookTaskQueue` [cap 256] + eigener `WebhookTaskWorker`, registriert in Program.cs; `BookPuzzleService`+`WeeklyPostService` enqueuen Webhooks jetzt dort [`BookPuzzleController`-Hint-Gen bleibt auf der allgemeinen Queue]. Test-Stubs [NoOp/Counting/Immediate] auf `IWebhookTaskQueue` gehoben. One-Shot-Webhook für das heutige Solve manuell signiert+gefeuert [HTTP 200]. +2 Tests/1029 grün. NICHT deployed); 0.196.0 (Repertoire-Trainer ohne „Weiter"-Knopf: nach richtigem/geduldetem Zug läuft der Trainer automatisch zur nächsten Stellung weiter [`ADVANCE_MS` correct 700 ms / tolerated 1800 ms, Timer in `onMove`, abgeräumt in `ngOnDestroy`/`setColor`/`buildQueue`/`next`]; geduldet zeigt die Repertoirezug-Visualisierung länger; Tippen aufs `.play` [`onPlayClick`] überspringt die Wartezeit; FALSCH behält den expliziten „Weiter"-Knopf. Neuer i18n-Key `repertoireTrainer.tapToContinue` en/de/hr + neuer Spec `repertoire-trainer.component.spec.ts` [4 fakeAsync-Cases]); 0.195.5 (VpnIpHealth Fehlalarm-Fix [piratechess v1.0.25]: Prod-Live-Daten zeigten gesunde Exit-IPs [37.46.199.54/.70/.86, alle DE-Tunnel, ≈2–5 % Gesamt-Block-Rate] als „WIEDERHOLT SCHLECHT" geflaggt — weil einzelne kurze Stints [1 Block von 2 Requests = 50 %] über `Vpn:BadStintRate`=0.4 lagen und sich ≥2 davon ansammelten. Neu: eine Phase zählt nur ab `Vpn:BadStintMinRequests`=5 Requests als „schlecht", und „wiederholt schlecht" hängt jetzt an der KUMULATIVEN Per-IP-Block-Rate [`Vpn:BadIpBlockRate`=0.15 über `Vpn:BadIpMinRequests`=50 Requests] statt an gezählten Mini-Phasen; WARN gedrosselt [1× bei Überschreiten, danach frühestens alle 20 Stints]; `IpStat.RecurringBad`-Verdikt, Snapshot sortiert wiederholt-schlechte zuerst dann nach Block-Rate. Prod fährt seit 2026-06-29 v0.195.4+v1.0.24 [Watchdog+Resume+Cooldown live, Import-Drain verifiziert]. +3 Testfälle/214 piratechess grün. NICHT deployed); 0.195.4 (Download-Lane-Nebenläufigkeits-Fix: Chessable-Importe luden gelegentlich ZWEI Kurse gleichzeitig, weil die „serielle" Download-Lane von zwei unabhängigen Treibern bedient wird — dem einzelnen `BackgroundTaskWorker` (Queue-Tickets) UND dem `ChessableImportWatchdogService` (ruft `RunNextAsync` an der bounded Queue VORBEI direkt auf). Der atomare Claim [[prod-chessable-import-stall-restart]] verhindert nur Doppel-Verarbeitung DESSELBEN Jobs, nicht, dass jeder Treiber einen ANDEREN wartenden Job claimt und parallel zieht (beobachtet 2026-06-29: Watchdog-Drive + Queue-Worker zogen Kurs 583468e8… und 69cc61bc… gleichzeitig). Neu: prozessweites `static SemaphoreSlim(1) _downloadLaneGate` in `ChessableImportService`; `RunNextAsync` (Download-Lane) acquired per `WaitAsync(0)` → ein zweiter Drive kehrt SOFORT zurück statt parallel zu laden, Body in `DrainNextAsync` ausgelagert; Fast-Lane bleibt ungated/nebenläufig. +1 Test [`RunNextAsync_DownloadLane_GateBlocksSecondConcurrentDrive`, deterministisch via blockierten Fetch], 1027 grün. NICHT gepusht/getaggt/deployed); 0.195.3 (Per-IP-Request/Block-Tracking [piratechess]: neuer Singleton `VpnIpHealth` akkumuliert je VPN-Ausgangs-IP Requests/Blocks über alle Rotationen [„Stint" = IP-Lebensdauer zwischen Rotationen, gemeldet via `VpnTunnel.FlushIpStint` beim Rotieren; `_currentIp`/`_ipRequests`/`_ipBlocks`]; eine IP mit ≥2 schlechten Phasen [Block-Rate ≥ `Vpn:BadStintRate`=0.4] wird als „WIEDERHOLT SCHLECHT" geloggt [WARN, strukturiert→Kibana je IP gruppierbar]; Debug-Endpoint `GET /api/chessable/direct/debug/ip-health` liefert die Per-IP-Tabelle [schlechteste zuerst]. +3 Tests/211 grün. NICHT deployed); 0.195.2 (Import-Robustheit [piratechess]: (1) hartes `--max-time` je Request [`Chessable:RequestMaxTimeSec`, Default 20s] in `BuildGetArgs` → hängende IP scheitert nach ~20s statt 75–120s, leerer Body → Soft-Block → retire/rotate; DrainTimeout 60→25s. (2) Per-Tunnel-Health+Cooldown in `VpnTunnel`: gleitendes Fenster der letzten N Ausgänge [`Vpn:HealthWindow`=8], blockt ein Tunnel ≥`Vpn:HealthBlockThreshold`=5 → `Vpn:CooldownSec`=120 aus dem Pool [`TryAcquire(respectCooldown)`, `RecordOutcome` via `RequestCompleted(bool)`, `VpnLease.onComplete` jetzt `Action<bool>`]; `AcquireAsync` Zwei-Pass [gesund zuerst, Notfall ohne Cooldown → kein Verhungern]. +2 Tests/208 grün. NICHT deployed [Prod läuft noch v1.0.23]); 0.195.1 (ETA-Durchsatz `CHESSABLE_LINES_PER_MIN` 25→40 [nach Rotate-on-Block-Speedup]; admin.component nutzt jetzt die Konstante statt hartem /25 in `dlEtaMin`/`estMinFromLines`); 0.195.0 (Admin-Kursdownload „Größe schätzen": on-demand pro Kurs Gesamt-Linienzahl + grobe Zeit vor dem Import. piratechess `POST /api/chessable/direct/course/info` {bearer,bid} → `DirectCourseInfoResponse` [gecacht=Cache-Summe ohne Chessable-Call, sonst `GetCourseLineCountAsync`=1× getCourse?includeVariations]. rookhub `ChessableProxyService.GetCourseInfoAsync` + Admin-Endpoint `GET admin/users/{userId}/courses/{bid}/estimate` [Bearer des Ziel-Users]. FE: `estimateCourseForUser`, `dlEstimate`/`dlEstimates`/`estMinFromLines` [~25/min], `straighten`-Knopf je Zeile → „≈N Linien · ~M Min/gecacht", i18n estimate/estimateError/cachedInstant/min. +2 Tests; FE 630/BE 1026/piratechess 206 grün); 0.194.0 (Chessable-Import: echte Gesamt-Linienzahl + ETA. piratechess getCourse `&includeVariations=true` → `Chapter.Total`/`Variations`, `FetchCourseDataAsync` meldet Variantensumme via `onTotalLines` → `CourseFetchJob.LinesTotal`/Snapshot → `DirectCourseProgressResponse.LinesTotal`. rookhub `ChessableImport.LinesTotal` [+Migration `AddChessableImportLinesTotal`] durch alle Progress-DTOs; FE `effectiveTotalLines` bevorzugt exakten Wert, `CHESSABLE_LINES_PER_MIN` 16,7→25, i18n `chessable.fetchProgressTotal`, Linien-X/Gesamt + ETA im User-Import + admin-Kursdownload [`dlEtaMin`]; FE 630/BE 1024/piratechess 206 grün); 0.193.2 (Chessable-Line-Fetch Rotate-on-Block [piratechess-Repo]: größter Importzeit-Fresher war der FIXE 30-s-Backoff bei Soft-Block [leere `{}`-Antwort, ~6 % der Zeilen → amortisiert ~1,8 s/Zeile ≈ 77 % der Wandzeit, bei nur 187 ms echtem curl]. Neu: `VpnRotationService` hält GENAU EINEN aktiven Tunnel sticky [statt round-robin pro Request]; `VpnLease.ReportBlocked()` [aufgerufen von `ChessableHttpService.CurlGetAsync` via `IsSoftBlockedBody`] retired die IP sofort → `VpnTunnel.RetireNow()` rotiert sie drain-aware im Hintergrund, Pool wechselt auf den nächsten, bereits ausgeruhten Tunnel [Ping-Pong; Rotation dauert lt. Prod-Log nur ~4 s ≈ Abarbeiten von 10 Zeilen]; Line-Retry-Backoff 30 s→`Chessable:BlockRetryDelayMs` [Default 1500 ms]; Inter-Request-Delay-Default 1000–2000→0–200 ms [Block ist requests-pro-IP-, nicht timing-getrieben]. Erwartung ~26→~100–150 Zeilen/min. piratechess-Commit 9071793, 206 Tests grün. OFFEN: Live-ENV in /opt/stacks setzen [ParallelLineFetches=1, InterRequestDelay 0/200] + Dev-Verifikation; NICHT deployed); 0.193.1 (Chessable-Import Fast-Lane: voll-gecachte Kurse [`ChessableImport.FullyCached`, gesetzt via `IsCourseCachedAsync` beim Anlegen in allen Pfaden — StartImport/Admin/`EnqueueReimportAsync`] laufen in eigener SERIELLER, netzfreier Lane [`ChessableImportFastLaneService`, parallel zur Download-Queue] statt hinter den langsamen Downloads; `RunNextAsync(ct, fastLane)` filtert fast=`FullyCached==true` / download=`!=true` [null→Download, kann nie hängen]; Watchdog download-lane-spezifisch; altes `RunDetached` entfällt; +Migration `AddChessableImportFullyCached`, +8 Tests/1024 grün); 0.193.0 (Trainingsziele-Zeitanzeige gestuft: neue pure `formatDuration(seconds, lang)` in `training-goals.component` + Methoden `durValue`/`durUnit` → < 120 min in Minuten, < 48 h in Stunden, sonst Tage [1 Nachkommastelle, locale-Dezimaltrenner via `Intl.NumberFormat`]; ersetzt `mins()`/`minutes()` in Breakdown-Rows [heute+Periode], Tageshistory-Tabelle und Chessable-Kurs-Summen [Daily-Goal-Paarung „done/target min" bleibt bewusst in Minuten]; neue i18n-Keys `trainingGoals.hours`/`.days` in ALLEN 25 Sprachen; +6 Spec-Cases); 0.192.1 (Admin-Kursdownload zeigt „Bereits in Warteschlange": `ChessableCourseDto.Queued` + `EnrichImportStateAsync` markiert Kurse mit laufendem Import [Status=running], FE-Branch im admin.component.html `@else if (c.queued)`, i18n `admin.courseDl.alreadyQueued` en/de/hr; +1 Test); 0.192.0 (Chessable-Import-Watchdog: neuer `ChessableImportWatchdogService` (BackgroundService) draint hängende Import-Queue ohne API-Neustart. Root-Cause des „eingeschlafen"-Vorfalls: `BackgroundTaskQueue` ist bounded (cap 100, DropOldest) + single-consumer + Abschluss reiht nicht nach → großer Import-Schwung verwirft Tickets, Jobs bleiben `running/queued` liegen. Watchdog prüft periodisch `IsDrainStalledAsync` (queued>0 && kein claimed/fetching/importing) und ruft dann `RunNextAsync` DIREKT (umgeht die bounded Queue); Startverzögerung 1min/Ruhe-Takt 2min/Busy-2s; +6 Tests/1015 grün. Siehe Memory [[prod-chessable-import-stall-restart]]); 0.191.0 (Reprocess-Banner zwei Knöpfe „Alle"/„Aus Cache": `ImportReprocessService.Reprocess{Courses,Repertoires}Async` + beide POST-Endpoints bekamen `localOnly`-Flag [`[FromQuery] bool localOnly`]; localOnly=true überspringt den Chessable-Re-Fetch [`continue`], bereitet nur aus serverseitig gespeicherter Quelle auf [Courses: SourcePgn; Repertoires: Nicht-Chessable-Versions-Mark]; Banner `allCount`=reprocessableLocally+refetchable, `cachedCount`=reprocessableLocally, Cache-Knopf nur wenn cachedCount<allCount; i18n reprocess.updateAll/updateCached/+Tips en/de/hr; +4 Tests); 0.190.1 (Discord-Invite-Link auf den richtigen Server korrigiert: `discord.gg/wczc4BJtMf` statt `nKQCdC7Xff` in `core/community.ts` [Konstante → Navbar/Footer/Mobil-Menü], README + Hilfetext en/de/hr + Changelog-0.187.0-Eintrag; beide Invites resolven aktuell auf Guild „Rookhub", neuer hat kein Ablaufdatum); 0.190.0 (Offline-Auto-Cache: Kurs öffnen (online) lädt das ganze Buch im Hintergrund offline [`autoCacheCourse` in book-puzzle.component, kein manuelles ☁ nötig]; Tagespuzzle wird beim Online-Abruf automatisch gecacht [`saveDailyOffline`/`getDailyOffline`, Key `rookhub_daily_offline`, letzte 14 Tage] + Offline-Read in `loadDaily`; Default-Offline-Pool 10→30 [`DEFAULTS.puzzleCount`]; +4 Specs/621 grün); 0.189.2 (Offline-Fix: `MenuService` cacht die Menü-Sichtbarkeit in localStorage [`rookhub_menu_keys`] + seedt das `visibleSubject` daraus → Flugmodus-Kaltstart zeigt nicht mehr nur Admin+Discord, sondern das zuletzt bekannte Menü; `fetch()`-catchError gibt Cache statt leerem Set); 0.189.1 (Mobile-Navbar-Fix: Sekundär-Icons [Discord/Theme/Sprache] wandern auf Mobil ins Hamburger-Menü `nav-extra`+navMenu, damit Toolbar nicht überläuft & Profil-Icon sichtbar bleibt); 0.189.0 (Repertoire-Trainer/geduldete Züge: `ImportPipeline.CurrentVersion` 2→3 [softFail-`[%alt]` jetzt im piratechess-Export]; Reprocess-Banner auf der Repertoire-Seite bietet für Chessable-Repertoires jetzt einen echten **Re-Fetch** an statt No-op-Versions-Mark — `ImportReprocessService.GetRepertoireStatusAsync`/`ReprocessRepertoiresAsync` melden Chessable-Repertoires [bid aus `ChessableCourseId` ODER Dateiname `chessable-{bid}.pgn`] als `Refetchable` und reihen `EnqueueReimportAsync(..., target:"repertoire", targetRepertoireId)` ein; neuer `ChessableImport.TargetRepertoireId` + Migration → `ImportAsRepertoireAsync` ersetzt das PGN **in-place** im bestehenden Repertoire [Id/Trainings-Fortschritt bleiben], Nicht-Chessable nur Versions-Mark; +3 Tests/1007 grün); 0.188.0 (Discord-Link prominent in der Fußzeile [Discord-Logo via `MatIconRegistry` + Markenfarbe #5865F2, neben Hilfe/Feedback, Konstante `core/community.ts`; Footer nur Desktop, mobil im Nav-Menü]); 0.187.0 (Discord-Community-Link überall eingebunden: Discord-Button in Navbar [eingeloggt + ausgeloggt] + Mobil-Menü [SVG-Icon via `MatIconRegistry`, Link `https://discord.gg/nKQCdC7Xff`, zentrale Konstante `core/community.ts`], Einladungs-Satz im Discord-Abschnitt der Hilfeseite [en/de/hr], `nav.discord`-Tooltip in allen 25 i18n-Sprachen, READMEs aller Repos); 0.186.1 (Chessable-Rate-Limit-Fix); 0.186.0 (Repertoire-Trainer/Spaced Repetition); 0.185.0 (Admin-Benachrichtigung bei Neu-Registrierung: neuer Notification-Typ `new_user_registered` → Glocke aller Admins [`CreateManyAsync`, Link „/admin", Daten `{username}`]; `AuthService` injiziert optional `NotificationService` und benachrichtigt best-effort nach erfolgreichem Register; Icon `group_add`; i18n en/de/hr); 0.184.39 (Offline-Fix: Kurse offline startbar — erstes Puzzle aus lokalem Cache statt Server [offline gespeicherter Kurs via ☁-Knopf, sequenziell/zufällig], Versuche werden gequeued/synchronisiert; Nicht-Admins offline nicht mehr von der Kursseite ausgesperrt); 0.184.38 (Chessable-Diagnose [piratechess-Repo]: `ClassifyBlockedResponse` unterscheidet jetzt abgelaufenen/ungültigen Token [lokal am JWT-`exp`-Claim erkannt → „Bearer neu hinterlegen"] von einem Cloudflare-403-Block bei noch gültigem Token [→ „VPN-Ausgangs-IP gesperrt, IP rotieren/Server wechseln"]; `IsCloudflareBlockPage` erkennt Block-Marker; Hintergrund: M247-IPs [AS9009] werden von Chessable geblockt, Netrouting [AS6206] nicht; piratechess-Commit 32a2f83, +7 Tests/175 grün); 0.184.37 (Chessable-Fix [piratechess-Repo]: HTML-statt-JSON-Antwort [abgelaufener/ungültiger Bearer bzw. Cloudflare-Block/Proxy-Gateway → Chessable liefert eine HTML-Seite] wird in `ChessableHttpService.GetCoursesAsync`/`FetchCourseDataAsync` jetzt sauber erkannt [`LooksLikeHtml`] und als sprechender Token-Hinweis gemeldet, statt den rohen JSON-Parser-Text „'<' is an invalid start of a value" bis in die rookhub-UI durchzureichen; +JsonException-Catch ohne Leak; piratechess-Commit c1cc507, +7 Tests/168 grün); 0.184.36 (Crawler-Härtung [Crawler-Repo]: `ApiKeyMiddleware` fail-closed bei leerem `API_KEY` in Production [503 statt offen], Dev-Fallback + Liveness bleiben; Crawler-Commit 4ca4feb); 0.184.35 (BotStats-Endpoint Replay-Schutz: `GET /api/bot/player-progress` akzeptiert optionalen `X-Bot-Timestamp` [±300 s, HMAC über `<ts>.<discordId>`]; rückwärtskompatibel zur alten body-only-Signatur; Gegenstück zu Bot v2.73.0); 0.184.34 (Webhook-Timestamp-Replay-Schutz auf der rookhub-Sendeseite: alle drei Bot-Webhooks [Tagespuzzle/Wochenpost/Daily-Regenerate] signieren jetzt zusätzlich einen Zeitstempel [`X-Webhook-Timestamp`, HMAC über `<ts>.<body>`], ±300 s; Gegenstück zu Bot v2.70.0, rückwärtskompatibel); 0.184.33 (Crawler-Robustheit [Crawler-Repo]: Freilos/spielfrei nur informativ statt Warnung, defensives Response-Größenlimit `Crawler:MaxResponseBytes`, Hidden-Field-Parsing Regex→AngleSharp, Player-/Team-Upsert in DB-Transaktion; Crawler-Commits c518e74/cf6b5a9/7522f3a/bc59f31); 0.184.32 (A11y tournament-favoriten: Favoriten-Sterne [Spieler+Team, Tabelle+Mobil-Karte] in tournament-detail+public-tournament tastaturbedienbar — `role=button`/`tabindex=0`/`keydown.enter`+`space`/`aria-label`+`aria-pressed`/`:focus-visible`; i18n `tournaments.favorites.toggleAria` en/de/hr); 0.184.31 (BasePuzzleSolver-Dedup: `formatTime` → gemeinsame `puzzle-format.util.ts` [`formatPuzzleTime`], und Einzel-Stoppuhr-Timer [`elapsedSeconds`/`stopwatch`/`startTimer`/`stopTimer`] aus puzzle+book in die Basis hochgezogen; Endless erbt `elapsedSeconds`+`formatTime`, behält seine Doppel-Stoppuhren; +2 Specs, Verhalten unverändert); 0.184.30 (OnPush für 4 weitere präsentationale Puzzle-Karten: puzzle-your-turn/-status-card/-rating-card/viz-card — alle nehmen nur primitive Inputs [Eltern rebinden je CD] + EventEmitter-Outputs, in-place-Mutation geprüft = keine; +Spec); 0.184.29 (Admin-Kleinkram: chessable-Bookmarklet-`bypassSecurityTrustUrl` mit Origin-Guard+Sicherheits-Kommentar [Code rein app-konstruiert]; Admin-Mitglieder-Dropdown warnt bei `totalCount > 500` statt still abzuschneiden; `availableUsers()` war bereits memoisiert [v0.184.19]; +2 Specs); 0.184.28 (chessable.component: `activeImports`-Zeilen cachen ihr `queueLabelText` jetzt einmal je Update [`setActiveImport`] statt `queueLabel(imp)`-`translate.instant` je CD-Zyklus während des Pollings; +2 Specs); 0.184.27 (Anon-Session-IDOR-Härtung: `ValidationConstants.SessionIdPattern` Mindestlänge 1→32 [UUID-Form 32–36], erratbare Kurz-Ids können fremde anonyme Puzzle-/Endless-Stats nicht mehr claimen/überschreiben; Clients nutzen ohnehin `crypto.randomUUID()` → rückwärtskompatibel); 0.184.26 (FriendService.SearchUsersAsync: Identitäts-/Konto-Felder [Username/chess.com/Lichess/FIDE/ChessResults] präfix-anker [`StartsWith`, Username-Index nutzbar], nur DisplayName bleibt Teilstring; Länge+Take service-seitig hart gekappt); 0.184.25 (Chessable-Import: atomarer Claim beim Job-Picking via `ExecuteUpdate` „queued"→„claimed", InMemory-Re-Check-Fallback → keine Doppelverarbeitung bei Resume-Sturm/Skalierung); 0.184.24 (Schnellstart-Popup nach Register erklärt jetzt die Puzzle-Modi statt Turnier-Tipps: Zufalls-Puzzle/Endlos/Tagespuzzle/Wochenpost; `app.qs.*`-Keys in de/en/hr ersetzt, Icons 🎲/♾/📅/📰); 0.184.23 (Crawler-Robustheit gegen Redeploy/VPN-Aussetzer [Crawler-Repo]: `VpnReadinessGate` wartet vor dem ersten Crawl auf den wiederhergestellten gluetun-Tunnel [`Gluetun__WaitForReady=true` in beiden VPN-Compose-Dateien], und `ExecuteCrawlAsync` versucht reine Verbindungsfehler [`IsTransientConnectionError`, z. B. „Resource temporarily unavailable"] mit gestuftem Backoff erneut statt sofort `Failed`; Retry-Parameter via `Crawler:CrawlMaxAttempts`/`RetryDelayMs`/`CrawlRetryBackoffSeconds` konfigurierbar; behebt die Fehler-Häufung direkt nach Deploys); 0.184.22 (Frontend Service-Layer-Extraktion: `FriendsService`/`PublicTournamentService`/`ProfileService` + `RepertoireService`-Erweiterung, 9 Komponenten ohne direkten `HttpClient`; `AuthService.changePassword`; OnPush für review-nav/promotion-picker; +5 Service-Specs); 0.184.21 (Crawler-Härtung: `/api/health/ip` API-Key-pflichtig + Phantom-Runden-Clamp gegen fremde `rd=`-Links; Crawler-Repo-Commits f5071aa/052007b); 0.184.14–0.184.20 (TODO-Abarbeitung Runde 3: DataProtection-Keys konfigurierbar/anlegen/SetApplicationName · In-App-Benachrichtigung bei neuer Turnierrunde [`NotificationType.TournamentNewRound`] · Service-Extraktion Repertoire/Tournament-List/Dashboard [+Specs] · Admin-Tab in URL [`?tab=`] + availableUsers gecacht + admin.component-Spec · A11y puzzle-tags/repertoire-tree/-lines + OnPush für präsentationale Komponenten); 0.184.9–0.184.13 (Runde 2: JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source) JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source) 0.241.0 (Tipps für JEDEN Zug statt nur den ersten: die gestuften Tipps 1→3 [Stufe 1 Schach/Schlag/ruhig · Stufe 2 Figur · Stufe 3 SAN] werden jetzt on-the-fly zum AKTUELL erwarteten Löserzug erzeugt [neue Util `classifyMoveAt(fen,moves,index)`, Basis-Getter `currentMoveHint` via `classifyMoveFromFen(this.chess.fen(), solutionMoves[moveIndex])`], `hintLevel` wird pro Zug zurückgesetzt [in `advanceAfterCorrectMove`], neuer `maxHintLevel` [höchste Stufe über den GANZEN Solve] speist das `hintsUsed`-Statistikfeld aller Record-Aufrufe. Buch/Kurs zeigt die vorberechneten LLM-Tipps nur am ersten Löserzug [`atFirstSolverMove`], danach die Pro-Zug-Tipps; Standard/Endless nutzen durchgehend `currentMoveHint`. `firstMoveHint`-Felder + `classifyStandardFirstMove`-Setup-Aufrufe entfernt. +Util/Base/Book/Endless-Specs; 853 FE grün. NICHT deployed); 0.240.7 (Kurs-/Wochenpost-Puzzle: nach Abschluss steht „Naechstes Puzzle" statt „Ueberspringen" — der FAILED-Zustand nutzte bisher book.actions.skip, jetzt wie SOLVED book.actions.nextPuzzle als raised/primary-Button; „Ueberspringen" nur noch waehrend ungeloest. Reiner Template-Change book-puzzle.component.html. NICHT deployed); 0.240.6 (Live-Zug-Kommentare beim Lösen STAPELN statt ersetzen + kein Intro-Rückfall: `book-puzzle` neuer Getter `commentLines: string[]` [Template rendert je Absatz ein `<p>` in `.ctx-comment-body`], außerhalb des Reviews auf dem Lösungspfad die Kommentare ALLER bereits gespielten Lösungszüge in Reihenfolge [`commentForPlyPlayed(start..start+moveIndex−1)`, nur nicht-leere]; Züge ohne Kommentar fügen nichts hinzu, leere Liste → Block aus [KEIN Rückfall auf `puzzle.comment`, außer vor dem 1. Zug]. `displayComment` bleibt Einzel-Getter für Review/Ply-Nav. +2 FE-Tests [45]. NICHT deployed); auf dem Lösungspfad, ab dem 1. gespielten Zug, state AWAITING_USER_MOVE/THINKING — den Kommentar zum zuletzt gespielten Halbzug live zurück [`commentForPlyPlayed(startPly+moveIndex−1)`, gleiche Ply-Konvention wie Review], Fallback = Puzzle-Einleitung. Kein Template-Change [Kontext-Block ist state-unabhängig sichtbar]. Nur Buch-Puzzles [Standard/Endless haben keine moveComments]. +1 FE-Test. NICHT deployed); 0.240.2 (Reprocess-Banner (Kurse/Repertoires) auf EINEN Knopf reduziert: statt „Alle aktualisieren" + „Aus Cache" jetzt nur `reprocess.update` [voller Reprocess, localOnly-Pfad entfernt]. `ReprocessBannerComponent`: `run()` ohne Param [POST ohne ?localOnly], `working` boolean statt 'all'|'cached'|null, `cachedCount`-Getter raus; i18n `reprocess.update` mit Count + `updateTip` en/de/hr [updateAll/updateCached-Keys ungenutzt gelassen]. Spec angepasst [5 grün]. NICHT deployed); 0.240.1 (Notifications-Filter-Leiste (aus 0.238.6) erscheint jetzt auch bei nur EINER vorhandenen Kategorie: Guard in `notifications.component.ts` von `availableCategories.length > 1` auf `> 0` entschaerft. Vorher war die Leiste erst ab >=2 Kategorien sichtbar, sodass ein User mit ausschliesslich Chessable-Import-Benachrichtigungen nie einen Chip sah. Jetzt zeigt jede vorhandene Kategorie ihren Chip; Ausblenden funktioniert also immer. NICHT deployed); 0.240.0 (Chessable-Board-Annotationen + Pro-Zug-Kommentare im Kurs-Review: Import extrahiert die farbigen Pfeile [%cal] / Feld-Markierungen [%csl] je Halbzug nach `BookPuzzle.MoveShapes` [JSON {ply:[{o,d?,b}]}, +Migration `AddBookPuzzleMoveShapes`, +DTO roher JSON-String, `ExtractMoveShapes`/`ParseShapes`/`CalCslRegex` in PgnImportService], `ImportPipeline.CurrentVersion` 7->8. FE: neue `move-shapes.util.parseMoveShapes` -> `DrawShape[]`; `puzzle-board` bekam Input `reviewShapes`, das gemeinsam mit dem Gegnerzug-Pfeil in EINEM setAutoShapes gerendert wird [applyAutoShapes]; `book-puzzle` setzt `reviewShapes` je Review-Ply in solutionReviewGoTo/reviewGoTo. Zusätzlich landet `enterSolutionReview` nach dem Lösen am ERSTEN annotierten Zug [`firstAnnotatedSolutionIndex`, Kommentar ODER Pfeile] statt am Ende -> frühe Kommentare/Pfeile sofort sichtbar. +1 BE-Test [ParsePgn shapes] +1 FE-Spec [parseMoveShapes]. HINWEIS: 725d148 [0.239.0] hatte Model-Feld+Extraktion+MapToDto versehentlich mit-committet [Lock-Race]; Rest hier nachgezogen. Wirkt erst nach Kurs-Aktualisieren. NICHT deployed); 0.239.0 (Schnellere Massen-Kursimporte: die Fast-Lane (voll-gecachte Chessable-Kurse, netzfrei) läuft jetzt PARALLEL statt seriell — bis zu `MaxParallel` gleichzeitig (Config `Chessable:FastLaneParallelism`, Default 3, clamp 1–8). `ChessableImportFastLaneService`: `IsFastLaneReadyAsync` (bool, seriell) → `FreeSlotsAsync` (int = `min(MaxParallel−inflight, wartende)`); `TickAsync` stößt `slots` Drains GLEICHZEITIG an, jeder in EIGENEM DI-Scope/DbContext, Claim atomar (`ExecuteUpdate` queued→claimed) → verschiedene Jobs, keine Doppelverarbeitung; kein Gate im fastLane-Pfad. Download-Lane unverändert gedrosselt. +5 BE-Tests (FreeSlotsAsync); 1202 grün. NICHT deployed); 0.238.6 (Benachrichtigungsseite gruppiert und filtert: neue `notificationCategory(type)`-Util + `NOTIFICATION_CATEGORIES` in `core/notification-text.ts` bucketet alle Types in Kurse [chessable_*]/Freunde [friend_*]/Puzzles [challenge_*+revenge_performed]/Nachrichten [admin|user_message_received]/Turniere [tournament_new_round]/Admin [new_user_registered]/Sonstiges. `NotificationsComponent` bekam eine Filter-Chip-Leiste ueber der Liste [mat-chip-set, nur vorhandene Kategorien mit Zaehler in Klammern], Klick auf Chip schaltet Kategorie aus [durchgestrichen+dimmed] bzw. wieder an, "Alle einblenden"-Knopf sobald mindestens eine ausgeblendet ist; alle-ausgeblendet-Zustand zeigt Hinweis statt leerer Liste. Getter `availableCategories`/`visibleItems`/`counts` filtern in-memory ueber die bereits geladenen Items. Ausgeblendete Kategorien in localStorage `rookhub_notifications_hidden_categories` persistiert [`readHiddenCategories`/`persistHiddenCategories`, still bei Storage-Fehler]. +8 FE-Specs. i18n `notifications.filter.*`/`notifications.category.*` en/de/hr. NICHT deployed); 0.238.5 (Kurs<->Repertoire-Umwandlung VERSCHIEBT jetzt statt zu kopieren: das Original verschwindet nach Erfolg. `CourseService.ConvertToRepertoireAsync` loescht den Original-Kurs via `BookAdminService.DeleteBookAsync` NUR wenn eigener [`Book.OwnerUserId==userId`; geteilte Gruppen-/Admin-Kurse bleiben]; `RepertoireController.ConvertToCourse` loescht das Repertoire per `RepertoireService.DeleteAsync` NACH erfolgreicher Kurs-Erstellung [400 = kein Puzzle-PGN -> nichts geloescht]. FE: course-list/repertoire-list entfernen das Original aus der Liste [Kurs nur bei isOwned]. +3 BE-Tests [CourseConversionTests owned-deleted/shared-kept, RepertoireControllerTests convert-deletes/400-keeps]. NICHT deployed); 0.238.4 (Chessable-Bearer-Feld akzeptiert jetzt auch den kompletten "Bearer eyJ..."-String, nicht nur den reinen JWT: neue exportierte Util stripBearerPrefix() strippt ein fuehrendes Bearer-Praefix (case-insensitiv, ``-verankert, beliebige Whitespace-Menge) und trimmt den Rest; chessable.component.save() nutzt es vor saveCredentials(), und der Speichern-Knopf haengt an einem neuen Getter hasBearerInput = stripBearerPrefix(bearerInput).length > 0 statt an bearerInput.trim(). +5 FE-Specs im chessable.component.spec. NICHT deployed); 0.238.3 (Eigenes PGN als persoenlichen Kurs hochladen laeuft jetzt wie das Repertoire-Anlegen: kleiner "PGN hochladen"-Knopf oben rechts auf der Kurseite (.header analog repertoire-list) oeffnet einen neuen UploadCourseDialogComponent (Datei-Picker + optionaler Kursname + Discord-Hinweis, dialogRef.close({file,name})), statt das inline Panel oben in der Liste. CourseListComponent bekam openUploadDialog() (oeffnet Dialog, ruft bei Bestaetigung uploadCourseFile(file, name) - die Upload-Logik ist aus onFileSelected in einen Public-Helper extrahiert, weiter idempotent + notifyAccessChanged); uploadName/discordUrl/onFileSelected/upload-panel-CSS entfernt, MatDialog injiziert. i18n courses.upload.choosePgn en/de/hr. +2 FE-Specs (Dialog-Flow + Abbrechen), bestehender uploadCourseFile-Test angepasst. NICHT deployed); 0.238.2 (Fehlende Kurs-Linien behoben: Chessable-Kapitel-Einleitungen als NULL-Zug [`{[%info]} 1. -- {Text}`] wurden beim Import verworfen [`--` ergibt keine UCI-Züge, und die Info-Behalten-Logik verlangte einen nicht-leeren ERSTEN Kommentar — der ist hier nur der leere `[%info]`-Marker, der Text folgt im Zug-Kommentar]. Fix in `PgnImportService.ParsePgn`: `isInfoOnly` vor die uci-Prüfung, Keep-Zweig greift auch bei `[%info]`-Linien, Text = erster NICHT-leerer Kommentar [neuer Helfer `FirstNonEmptyComment`]. `ImportPipeline.CurrentVersion` 6→7. Linien stehen bereits im `Book.SourcePgn` → lokal reprozessierbar; Chessable-Kurse via Re-Fetch. Beispiel Buch 16: 583 PGN- vs. 562 importierte Linien = 21 verworfene NULL-Zug-Intros. +2 BE-Tests; 1200 grün. NICHT deployed); 0.238.1 (Lange Kurs-Kommentare nicht mehr gekappt + Kommentar-Kasten scrollbar: `BookPuzzle.Comment` varchar(5000)->LONGTEXT [Migration `WidenBookPuzzleComment`], Import-Kappung 5000->100.000 [`PgnImportService.MaxCommentLength`, ExtractFirstComment+ExtractMoveComments]. Lange Chessable-Erklaer-/Intro-Texte [z. B. Introduction #2 in 100 Tactical Patterns, ~6500 Zeichen] wurden bei 5000 abgeschnitten. `ImportPipeline.CurrentVersion` 5->6 [Reprocess-Banner; voller Text steht im SourcePgn -> lokal aufbereitbar, Chessable-Buecher via Re-Fetch]. CSS `.ctx-comment p`: max-height 40vh + overflow-y:auto + min-width:0/overflow-wrap [laeuft nicht mehr ueber die Seite]. +1 BE-Test [ParsePgn_KeepsLongComment, 169 Import/Course gruen] +Migration. NICHT deployed [zum Fuellen des Alt-Textes Kurs aktualisieren]); 0.238.0 (Kurs ↔ Repertoire umwandeln: `POST /api/courses/{bookId}/convert-to-repertoire` [`CourseService.ConvertToRepertoireAsync` → neues `RepertoireService.CreateFromPgnAsync`, legt Repertoire aus dem Kurs-PGN an, `UseForExtension=false`, Original-Kurs bleibt] + `POST /api/repertoires/{id}/convert-to-course` [reused `UploadPersonalCourseAsync`; nur Puzzle-PGN im Chessable-Stil, reines Eröffnungs-Repertoire → 400]. `CourseService` bekam `RepertoireService`, `RepertoireController` bekam `CourseService` [beide azyklisch]. FE: 📚-Icon-Knopf in `course-list` + „In Kurs"-Knopf in `repertoire-list` [400→klarer Hinweis], Service-Methoden + i18n en/de/hr. +4 BE-Tests [`CourseConversionTests`], Ctor-Anpassungen in 5 Testklassen; 1197 grün. NICHT deployed); 0.237.6 (Dashboard-Snapshot lokal cachen [neuer `DashboardCacheService`, per-User localStorage-Key `rookhub_dashboard_cache_v1_u{userId}`]: `dashboard.component.ngOnInit` liest den Snapshot [Repertoire-/Kurs-Zaehler, angepinnte Kurse, Abos, Freundeszahl, Favoriten, Puzzle-Solved/Accuracy/Elo] SYNCHRON und wendet ihn sofort an, danach ueberschreibt der bestehende `forkJoin`-Fetch mit frischen Werten UND schreibt den Snapshot zurueck. Beim Neuladen keine leeren Kacheln mehr. Fremde User-Snapshots werden nicht angezeigt [Auth-`userId` im Key]; kaputter/leerer Cache = still `null`. +5 FE-Tests [`dashboard-cache.service.spec` 5 cases + 2 neue dashboard cache-Behavior-Tests, 30 gruen]. NICHT deployed); 0.237.5 (Kurs-/Buch-Puzzles zeigen den Kommentar NACH dem letzten Zug (Abschlusstext) + kein Auto-Weiter dabei: `BookPuzzleComponent.enterSolutionReview` springt jetzt via `solutionReviewGoTo(reviewTotal)` ans Ende UND setzt `moveComment` auf den Kommentar des letzten Zugs [vorher nur reviewIndex → displayComment fiel auf die Einleitung zurueck, Schlusstext verborgen]; `finalizeSolve` bricht bei vorhandenem Abschlusstext [neuer Getter `hasTrailingSolutionComment` = commentForPlyPlayed(letzter Ply)] den Auto-Advance-Countdown ab, der manuelle Weiter-Knopf bleibt. Beispiel: Dziuba-Erwich-Linie in „100 Tactical Patterns (Round 002.003, MoveComments key 0). +5 FE-Tests [43 book-puzzle gruen]. NICHT deployed); 0.237.4 (Admin-CI: die per Push gemeldete laufende Version eines Stacks [z. B. log-watcher via `POST /api/ci/build-report`] ist jetzt PERSISTENT in der DB [neue Tabelle `CiBuildReports`, +Migration `AddCiBuildReport`; `GithubActionsService.ReportBuildAsync` upsertet, `ResolveRunningBuildsAsync` liest daraus; `GithubActionsService` bekam `AppDbContext`, `CiBuildReportController.Report` async]. Vorher war es ein prozessweiter statischer `_reportedBuilds`-Cache → jeder rookhub-api-Neustart/Deploy löschte ihn, log-watchers Version verschwand bis zum nächsten Push [bis zu 10 min] aus der Übersicht. Der GitHub-`workflow_run`-Push-Cache [`_pushedRuns`] bleibt bewusst In-Memory [self-healend]. +1 BE-Test [`ReportedBuild_PersistsAcrossServiceInstances`, geteilte InMemory-DB simuliert Neustart], Ctor-Anpassungen in 2 Testklassen. NICHT deployed); 0.237.0 (Kurse fürs Dashboard anpinnbar + direkt startbar: neue per-User-Tabelle `CoursePin` [UserId+BookId unique, +Migration `AddCoursePins`, Cascade User/Book, Cleanup in `BookAdminService.DeleteBookAsync`], `CourseListItemDto.IsPinned` [in `GetCoursesAsync` via Pin-Set gesetzt], Endpoints `POST/DELETE /api/courses/{bookId}/pin` [`CourseService.PinCourseAsync`/`UnpinCourseAsync`, idempotent, EnsureAccess]. FE: 📌-Toggle je Kurs in der Kursliste [optimistisch+Rollback], neue Dashboard-Kachel `pinnedCourses` [in DEFAULT_VISIBLE vor `courses`; eligible nur wenn ≥1 Pin bzw. im Edit-Modus] mit je Kurs zwei Start-Buttons Sequenziell/Zufällig; Dashboard nutzt das ohnehin geladene `/api/courses` [jetzt typisiert `DashboardCourse` inkl. `isPinned`] mit. i18n `courses.pin*`/`dashboard.pinnedCourses.*` en/de/hr. NICHT deployed); 0.236.18 (Admin-CI Push statt Pull: neuer Endpoint `POST /api/ci/gh-webhook` [`CiBuildReportController.GithubWebhook`, AllowAnonymous, verifiziert GitHubs `X-Hub-Signature-256` HMAC gegen `CI:GithubWebhookSecret` ODER Fallback `CI:BuildReportSecret`] empfängt GitHub-`workflow_run`-Events → `GithubActionsService.ReportRun` [statischer `_pushedRuns`-Cache je Repo, ~20 jüngste]. `OverlayPushedRuns`/`OverlayPushedRepo` legt die Push-Läufe bei JEDEM Overview-Abruf LIVE über die (gecachten) GitHub-Daten [Merge per Run-Id, Push gewinnt, Workflow-Filter, neueste 5] — auch im Einzel-Repo-Abruf [👁]. FE `NORMAL_MS` 2min→20s [RookHub-Cache statt GitHub]. Webhook je Repo unter Settings→Webhooks eintragen [Event „Workflow runs", Secret = CI_BUILD_REPORT_SECRET-Wert]. +5 BE-Tests [Overlay + Webhook-Sig; GithubActionsServiceTests+CiBuildReportControllerTests in Collection „CiStaticState" serialisiert wg. statischer Caches]. NICHT deployed); 0.236.17 (Perf: `CourseService.GetCoursesAsync` — die Puzzle-Anzahl je Buch [`PuzzleCount`] lief als korrelierte Unterabfrage pro Buch [`b.Puzzles.Count(!IsInfoOnly)` im Select] → bei Admins mit ~260 Büchern sekundenlang. Jetzt EINE gruppierte Query [`BookPuzzles.Where(!IsInfoOnly).GroupBy(BookId)` → Dict]; Mapping zieht die Zahl aus dem Dict. Verhalten/Zahlen unverändert [67 Course-Tests grün]. NICHT deployed); 0.236.16 (Chessable-Importe sind jetzt standardmäßig NICHT von der RepCheck-Extension nutzbar: `ChessableImportService.ImportAsRepertoireAsync` setzt beim Anlegen `UseForExtension=false` [`CreateRepertoireDto`]. Manuell angelegte Repertoires unberührt [Create-Dialog-Default bleibt true]. +1 BE-Test-Assert. Prod-Datenfix separat: kahalms 133 Repertoires alle auf `UseForExtension=0`. NICHT deployed); 0.236.15 (Admin-CI Poll-Umbau: normaler Voll-Abruf jetzt alle 2 min statt 5 s [`NORMAL_MS`], gegen GitHub-Rate-Limit. Neuer per-Repo-👁-Knopf [`toggleWatch`/`isWatching`, `.watch-btn`]: schnell-pollt NUR dieses eine Repo alle 10 s [`WATCH_MS`] für 1 min [`WATCH_WINDOW_MS`], bei laufender Aktion rollend bis zu deren Ende [`WATCH_RUNNING_EXTEND_MS`, `watchTick`]. Neuer BE-Endpoint `GET /api/admin/ci/runs/{repo}` [`GithubActionsService.GetRepoAsync`, ungecacht, Repo-Whitelist; Merge-Logik in `MergeRunningAsync` extrahiert, von Voll- + Einzel-Abruf geteilt]; FE `loadRepo` mergt das Einzel-Ergebnis in die Übersicht. i18n `admin.ci.watchStart/watchStop` + Subtitle en/de/hr. +2 BE-Tests. NICHT deployed); 0.236.14 (Admin-CI-Seite: die „Deployt: …"-Zusammenfassungszeile im Kopf [gelbe Chips je Stack, `.ci-deployed`] entfernt — inkl. `deployed`-Feld + dessen Berechnung in `recomputeEta` + CSS; die gelbe Zeilen-Hervorhebung des laufenden Builds [`.run-deployed`/`isRunningBuild`] bleibt. NICHT deployed); 0.236.13 (CI-Fix: flakiger `GithubActionsServiceTests.LogWatcher_FiltersOutTestRuns`-Test [aus dem 0.236.11-Feature], der je nach Testreihenfolge den ganzen `test-rookhub`-Job rot machte → kein `:latest`-Image. Ursache: prozessweiter **statischer** `_reportedBuilds`-Cache leckte über Testklassen [`CiBuildReportControllerTests` meldet „log-watcher" → im Overview-Test feuerte der „6.-Zeile-nachladen"-Pfad und der naive Stub lieferte den Run doppelt → `Assert.Single` sah 2]. Fix: interner `ResetReportedBuildsForTests()`-Hook, im `GithubActionsServiceTests`-Ctor vor jedem Test geleert. Produktcode unverändert [`FetchRunByShaAsync` filtert in Prod echt nach `head_sha`]. NICHT deployed); 0.236.12 (RepCheck „Remember line": echter Kursname über den Chessable-Bearer. Neues `RememberedPosition.CourseName` [+Migration `AddRememberedPositionCourseName`], `RememberLineInputDto`/`RememberedPositionDto` um `CourseName`. `RememberedPositionService` löst den Namen auf: von der Extension mitgeliefert [Vorrang] → sonst cache-first aus `ChessableCredential.CachedCoursesJson` → sonst best-effort Live-`GetCoursesAsync` [try/catch, Merken scheitert nie am Kursnamen]. `ListAsync` backfillt Alt-Einträge ohne Namen aus dem Cache [rein in-memory]. +7 BE-Tests [RememberedPositionServiceTests] + ExtensionControllerTests-Ctor angepasst. Extension-Seite [repcheck ≥1.19.0] löst den Namen via erfasstem Bearer + Chessable-getHomeData auf. NICHT deployed); 0.236.11 (Admin-CI: Workflow-Filter je Repo [`GithubActionsService`]: `DefaultWorkflowFilter` [log-watcher→„Build & Push"], überschreibbar via `GitHub:WorkflowFilter:<repo>`; `FetchRepoAsync` lädt `RawRunsPerRepo`=20 statt 5 und filtert per `MatchesWorkflowFilter` [Substring, case-insensitiv] vor `Take(5)`, `FetchRunByShaAsync` ebenso [dieselbe SHA triggert bei log-watcher „Tests" UND „Build & Push"] → log-watcher zeigt nur noch Build-Läufe, keine Test-Läufe. +1 BE-Test [GithubActionsServiceTests]. NICHT deployed); 0.236.10 (Admin-CI: der deployte Build-Lauf ist wieder mit GELBEM Zeilen-Hintergrund markiert [`.run.run-deployed`, `[class.run-deployed]="isRunningBuild(run,repo)"` + gelber Border-Left], nicht nur dem Icon-Badge — 0.236.5 hatte die Zeilenfarbe entfernt, User wollte aber nur ein anderes Icon, nicht den Wegfall des Highlights. NICHT deployed); 0.236.9 (Admin-CI, drei Ergänzungen: (1) laufender Build zeigt prominenten Live-Countdown im Panel-Kopf [`.ci-eta`, BLAUE Plakette `timer`-Icon, z. B. „schach-bot · fertig in ~2m 10s"]; ETA = `avgDurationSec` [Mittel der abgeschlossenen der letzten 5 Läufe je Repo, created→updated] − verstrichene Zeit; 1-s-Ticker [`nowMs`] treibt den Countdown, `recomputeEta` [ETA+deployed] bei 1-s-Tick + 5-s-Refresh. (2) prominente „Deployt"-Übersicht im Kopf [`.ci-deployed`, GELBE Chips je Stack = Repo + Ref des via `isRunningBuild` erkannten laufenden Runs]. (3) Farbschema: BLAU=baut [`.run`-Badge + ETA], GRÜN=fertig gebaut [`.ok`], GELB=deployed [`.run-live-tag` + `.ci-deployed`]; Repo-Icon `folder_open`→`code`. i18n `admin.ci.etaIn/etaSoon/etaUnknown/etaTooltip/deployed/deployedTooltip` en/de/hr. NICHT deployed); 0.236.8 (Dashboard-Kurse-Kachel zeigt jetzt die Kurs-Anzahl [dashboard.courses.count "{{count}} Kurse"] wie die Repertoires-Kachel: neuer courseCount, DashboardService.getCourses [GET /api/courses], im forkJoin geladen. +2 FE-Tests, i18n en/de/hr. NICHT deployed); 0.236.7 (Admin-CI: „live"-Badge des laufenden Builds klar vom grünen Erfolgs-Häkchen abgehoben — `.run-live-tag` jetzt BLAU (#1565c0) + Icon `fiber_manual_record` (live-Punkt) statt grün/`play_circle` [`admin-github-actions.component`]. Rein visuell. FALLE: zeigt die CI-Seite bei ALLEN Repos „HTTP 403", liegt das am GitHub-PAT [`GITHUB_TOKEN` in der rookhub-.env] — abgelaufen oder ohne `Actions: Read-only`-Permission —, NICHT am Code [UA/Accept/Api-Version im HttpClient sind gesetzt]. NICHT deployed); 0.236.6 (Repertoire-Reprocess: (a) nach erfolgreichem Chessable-Re-Fetch werden ALLE Geschwister-Repertoires desselben Users mit derselben bid mit-aktualisiert [ChessableImportService.BumpSiblingRepertoiresAsync in ImportAsRepertoireAsync] — vorher blieben Kurs-Duplikate veraltet, weil der (Owner,bid)-Dedup den Kurs nur 1x holt (Banner ging nie weg). (b) GetRepertoireStatusAsync/ReprocessRepertoiresAsync sind jetzt admin-weit [isAdmin -> alle User statt nur eigene; RefetchCandidate.OwnerId = r.UserId -> je Owner-Bearer]; RepertoireController reicht IsAdmin durch. +2 BE-Tests. NICHT deployed); 0.236.5 (0.236.5 (Admin-CI: laufender Build nicht mehr per Zeilenfarbe markiert, nur noch eigenes live-Icon-Badge [run-live-tag; .run-live-Fill entfernt]; faellt der laufende Build aus den Top-5 raus, wird er per GitHub ?head_sha= nachgeladen + als Zusatzzeile angehaengt [GithubActionsService.FetchRunByShaAsync + RunMatchesBuild + MapRun extrahiert]. Server ermittelt jetzt auch rookhubs eigene laufende SHA aus der Frontend-build-info [http://rookhub:8080/build-info.json, Frontend:BuildInfoUrl], isRunningBuild bevorzugt repo.runningSha [client-buildSha nur Fallback]. + Push-Modell-Empfangsseite: POST /api/ci/build-report [CiBuildReportController, AllowAnonymous + X-Build-Report-Key==CI:BuildReportSecret] -> GithubActionsService.ReportBuild [statischer _reportedBuilds-Cache, in ResolveRunningBuilds gemergt]. +3 BE-Tests. OFFEN: log-watcher-Client (Push). NICHT deployed); 0.236.4 (0.236.4 (Repertoire-Trainer falscher Zug: (1) "Losung zeigen" spielt den erwarteten Zug jetzt auf dem Brett [fen nach expected + lastMove-Markierung], statt ihn nur im Text zu nennen; (2) nach falschem Zug SOFORT erneut ziehbar - onMove akzeptiert einen Retry im wrong-FEEDBACK, Zug wird sofort auf startFen zurueckgenommen, Brett bleibt spielbar [viewOnly nur wenn nicht wrong-unrevealed], kein Mausrutscher-Klick noetig; Mouseslip-Button entfernt [Retry ersetzt ihn]. +Specs angepasst. NICHT deployed); 0.236.3 (0.236.3 (Admin-Kursdownload: Schalter "Geladene Kurse ausblenden" [dlHideLoaded + Getter dlVisibleCourses] blendet Kurse aus, die schon als Repertoire/Buch importiert sind [ChessableCourseDto.ImportedRepertoire/ImportedBook, bereits vorhanden]; mat-slide-toggle + i18n admin.courseDl.hideLoaded/allLoaded en/de/hr. +1 FE-Test. NICHT deployed); 0.236.2 (0.236.2 (Frontend erkennt neue Versionen schnell: app.component sucht aktiv beim Start, alle 15 min und bei visibilitychange->sichtbar via swUpdate.checkForUpdate() [neue Methode checkForAppUpdate, fehlertolerant] - lange offener Tab/PWA haengt nicht mehr auf altem Build bis manuellem Reload; VERSION_READY-Neu-laden-Snackbar wie gehabt. Spec: SwUpdate-Mock um checkForUpdate ergaenzt. + Bot v2.77.0: Monats-Endstand am 1. im Tagespuzzle-Thread statt Standalone-Post. NICHT deployed); 0.236.1 (0.236.1 (Zwei Prod-Log-Fixes nach Watcher-Alerts: (1) Weniger Fehlalarme: der anonyme Endless-Progress-Upsert [`EndlessProgressService.SaveAnonymousProgressAsync`] läuft bei gleichzeitigen Saves in eine `IX_EndlessProgresses_AnonymousSessionId`-Unique-Race — war bereits abgefangen+retried, aber EF Core loggte `SaveChangesFailed` trotzdem auf Error → triggerte den Log-Watcher grundlos. Jetzt `ConfigureWarnings` in Program.cs: `CoreEventId.SaveChangesFailed`→Information [genuine, NICHT abgefangene Fehler bleiben übers HTTP-500-Request-Log sichtbar; betrifft alle bewusst abgefangenen DbUpdateException-Races: CoursePuzzleResult/CourseInfoView/CourseProgress/…]. (2) `POST /api/courses|repertoires/reprocess` lief bei vielen Chessable-Kursen über das ~60-s-Request-Timeout → 500 „operation was canceled". Neu: `IReprocessLauncher`/`ReprocessLauncher` [Singleton, eigener DI-Scope, `Task.Run` mit `CancellationToken.None`, Fehler nur geloggt] startet den Reprocess im Hintergrund; beide Endpoints geben sofort `202 Accepted {started:true}` zurück [nicht mehr synchron/`ct`-gebunden]. FE `ReprocessBannerComponent.run` erwartet keine Counts mehr → Snackbar `reprocess.started` [en/de/hr] + Status-Refresh nach 2,5 s. +Test-Doppel `RecordingReprocessLauncher`, reprocess-banner-Spec auf fakeAsync/202 umgestellt. NICHT deployed); 0.236.0 (Admin-CI markiert das laufende Image jetzt auch für crawler/piratechess/bot, nicht nur rookhub-Frontend: jeder Dienst hat einen build-info-Endpoint [crawler `GET /api/health/build-info`, piratechess `GET /api/chessable/direct/build-info` (ServiceKey), bot `GET /webhook/build-info`], gespeist aus CI-Build-Args `GIT_SHA`/`GIT_REF`→ENV. `GithubActionsService.ResolveRunningBuildsAsync` pollt sie parallel [best-effort, 4s-Timeout, im 4s-Overview-Cache], `CiRepoDto.RunningSha/RunningRef`; FE `isRunningBuild(run, repo)` nutzt für Fremd-Stacks repo.runningSha/ref [rookhub weiter client-seitig], Match per SHA+Ref → :dev=master-Run, :prod=Tag-Run. Nicht erreichbar/altes Image = kein Marker. +2 BE-Tests [GithubActionsService], +1 FE-Test [Fremd-Stack], +Crawler-/piratechess-/Bot-Endpoint-Tests. NICHT deployed); 0.235.1 (0.235.1 (Admin-CI markiert genau den EINEN Run, der das laufende Image baute: build-info.json trägt jetzt zusätzlich den Build-Ref [Branch bei :dev, Tag-Name bei :prod; docker.yml `GIT_REF=github.ref_name`, Dockerfile `ARG GIT_REF`, `{sha,ref}`]. master-Push + gleichnamiger Tag haben dieselbe SHA → reines SHA-Matching markierte beide; `isRunningBuild` verlangt jetzt SHA UND [sofern gemeldet] Ref-Gleichheit → :dev hebt master-Run, :prod den Tag-Run hervor. Altes Image ohne Ref = SHA-only-Fallback. +4 FE-Tests [neues admin-github-actions.component.spec]. NICHT deployed); 0.235.0 (Zwei Kurs-/Buch-Verbesserungen: (1) Sequenziell durchgeklickte Info-/Erklärlinien werden gemerkt [neue Tabelle `CourseInfoView` + Migration `AddCourseInfoView`, Endpoint `POST /api/courses/{bookId}/info-seen`, `CourseService.MarkInfoSeenAsync`; seqPool überspringt gesehene Info-Linien via `CourseInfoViews` statt sie IMMER zu zeigen; FE `courseNext` meldet die aktuelle Info-Linie + merkt sie offline in `offlineCourseSolvedIds`; beim Reset geleert; Cleanup in `BookAdminService.DeleteBookAsync`]. (2) Doppelte Kommentar-Anzeige entdoppelt: `puzzle.comment`-Kopfzeile [Template Z.88] UND `move-comment-card` [unten] ersetzt durch EINEN `ctx-comment`-Block in der Kontext-Card ÜBER der Fortschrittsanzeige, gespeist vom neuen Getter `displayComment` [Review/Info→moveComment mit Fallback auf puzzle.comment, sonst puzzle.comment] — gilt für alle Modi, nicht nur Info-Linien. +4 BE-Tests [1156] +5 FE-Tests. NICHT deployed); 0.234.1 (piratechess-Fix [Repo bb74686]: TryGetChessableErrorMessage fällt bei leerem error.message auf userFriendlyErrorMessage zurück → nicht besessene Kurse melden ehrlich 'You do not own this course.' (BOOK_NOT_OWNED) statt fälschlich 'Course has no chapters'. +1 Regressionstest. [Analyse: die 4 'no chapters'-Kurse des Re-Fetchs gehören dem aktuellen Bearer-Account uid 790927 nicht]); 0.234.0 (Navigation läuft jetzt IMMER über das Hamburger-Menü — auf allen Auflösungen, nicht nur mobil [`navbar.component`: `.nav-links` generell `display:none`, `.mobile-menu-btn` generell `inline-flex`; der 768px-Media-Query blendet nur noch die `.nav-extra`-Sekundär-Icons aus]. Bei den vielen Menüpunkten lief die Toolbar sonst auch auf mittleren/großen Screens über. NICHT deployed); 0.233.2 (Zwei Anarchy-Politur-Punkte: (1) Setup-Zug-Pfeil-Fix — der erste (automatische) Gegnerzug, der die Puzzle-Stellung aufbaut, bekommt jetzt auch den Gegnerzug-Pfeil [`showVizOpponentArrow()` im Setup-Timeout von `BasePuzzleSolver`, nach `playMove(setupMove)`]; vorher nur die Antwortzüge [advanceAfterCorrectMove/opponentRespond], sodass genau der stellungserzeugende Zug unmarkiert blieb — besonders auf dem Crazy-Brett störend. Gilt für alle Modi [+1 base-Solver-Test]. (2) Anarchy-Hints [`puzzles.anarchyHint1-3` en/de/hr] memefiziert [Holy hell / Google en passant / New response just dropped]. NICHT deployed); 0.233.1 (Mobil-Fix: Dashboard beim Anpassen wieder scrollbar — im Bearbeitungsmodus war die GANZE Kachel die Drag-Fläche [`.tile-shield` = `cdkDragHandle` über `inset:0`], sodass auf dem Touchscreen jede Berührung einen Drag auslöste statt zu scrollen. Gezogen wird jetzt nur noch am dedizierten Griff [`drag_indicator`-Icon je Kachel als `cdkDragHandle` in `.tile-controls`], der Kachel-Rumpf scrollt normal; Sortieren via ↑/↓-Pfeile bleibt. `.tile-shield` ist kein `cdkDragHandle` mehr [nur noch Klick-Fänger]. i18n `dashboard.edit.dragAria` wiederverwendet. NICHT deployed); 0.233.0 (Admin-CI-Run-Zeile aussagekräftiger: Ref (Branch/Tag-Name mit Icon; Tag-Läufe haben kein head_branch → GithubActionsService löst Tags per /tags sha→Name-Map auf, CiRunDto.Ref/IsTag), Startzeit (createdAt), Laufzeit (created→updated bzw. live bis jetzt), Titel bleibt Link zum Run (keine sichtbare URL). i18n admin.ci.started/duration. Tags-Abruf = +1 GitHub-Call/Repo (gecacht); Cache-Test angepasst); 0.232.0 (Admin-CI-Seite markiert den GitHub-Actions-Run des LAUFENDEN Frontend-Builds: Frontend-Dockerfile schreibt Commit-SHA (ARG GIT_SHA aus docker.yml --build-arg github.sha) nach /build-info.json; CI-Seite holt das + markiert den Run mit passendem head_sha grün ('live'). CiRunDto/GithubActionsService um HeadSha erweitert. Greift erst nach Deploy eines so gebauten Images; nur rookhub-Frontend meldet Build-SHA. +1 BE-Test-Assert); 0.231.0 (Repertoire-Reprocess auf denselben Stand wie Kurse gebracht + Zentralisierung: die gemeinsame Re-Fetch-Logik [Batch-Cache-Abruf 1×, kein Abbruch am Request-ct, Backoff für nicht-cachebare Kurse, Dedup, Admin-`trustOwnership`] liegt jetzt in EINER Methode `ImportReprocessService.EnqueueRefetchesAsync(candidates, isAdmin, result)` [+ `RefetchCandidate`-Record]; `ReprocessCoursesAsync`/`ReprocessRepertoiresAsync` sammeln nur noch Kandidaten. `ReprocessRepertoiresAsync` bekam `isAdmin`-Param [Default false], `IsAdmin` in `BaseApiController` hochgezogen [aus CourseController], RepertoireController reicht es durch. Vorher hing „Alle aktualisieren" bei Repertoiren [alter langsamer/blockierender Pfad]. +1 BE-Test [1152 grün]. NICHT deployed); 0.230.0 (Crazy-Modus Gegnerzug-Pfeil: der Gegnerzug wird auf dem Crazy-Brett mit einem GRÜNEN Pfeil markiert, der bis zum nächsten User-Zug stehen bleibt. BasePuzzleSolver: neuer overridebarer opponentArrowMode ['off'|'timed'|'persist']; showVizOpponentArrow setzt bei 'persist' keinen 1s-Timer, onMoveMade löscht den Pfeil beim User-Zug. Komponenten: crazy→'persist'. Board: applyVizOpponentArrow ohne visualization>0-Guard + neuer Input vizOpponentArrowBrush [crazy 'green', Viz 'red']. +2 base-Solver-Tests); 0.229.0 (Anarchy-Tipps: bei e.p.-Zwang liefern alle 3 Puzzle-Tipps drei unterschiedlich formulierte En-passant-ist-Pflicht-Hinweise statt der normalen Tipps. Zentral in BasePuzzleSolver [effectiveHints = enPassantForced && epForcedHints ? epForcedHints : availableHints; hasHints/shownHints/canShowMoreHints darauf], epForcedHints je Solver überschrieben [i18n puzzles.anarchyHint1-3 en/de/hr]. +2 base-Solver-Tests); 0.228.0 (1) Off-Path-Warnung in allen Puzzle-Modi: BasePuzzleSolver zählt off-path-Züge [offPathUserPlies], berechnet in opponentRespond aus currentEval [Weiß-Sicht] playerEvalPawns() [Spieler-Sicht, Matt=±100] und warnt EINMALIG [onOffPathWarning()-Hook → Snackbar in allen 3 Solvern] ab offPathWarnThreshold Zügen, wenn Eval < +2. Reset in setupSolver + Mouseslip-zurück-auf-Pfad. Schwelle = Pref offPathWarnMoves [lokal, Default 3, 0=nie], im Settings-Dialog. (2) Anarchy: Settings-Toggle En-passant-forciert [Pref enPassantForced, Default an, nur bei themeMode=crazy sichtbar]; enPassantForced in onSetupStart = anarchyForcedByUrl [URL ?anarchy=max] ODER (crazy && Pref). +7 base-Solver-Tests; 0.227.0 (Logging-Flag `RequestKind` [system/user]: automatische, nicht nutzer-initiierte Requests werden als `system` markiert → in Kibana filterbar. Neuer `Logging/SystemCallClassifier` [Health/Swagger, /api/client-log, /api/menu, `/count`|`-count`|`-counts`-Polls, chessable/admin/active|imports]; gesetzt via `LogContext.PushProperty("RequestKind", …)` in der Enrichment-Middleware [neben UserId/VisitorId] + im HeartbeatService [`system`]. +14 Classifier-Tests. Reine Observability. NICHT deployed); 0.226.0 (Fix „Alle aktualisieren" für Kurse tat bei Admins NICHTS: `OwnerHasCourseAsync` prüfte jeden bid gegen Chessables getHomeData, das nur einen TEIL der Bibliothek listet [User 5: 32 statt 259] → 262 übersprungen/0 eingereiht [+142s/500er]. Neu: `EnqueueReimportAsync` bekam `trustOwnership`-Param; `ReprocessCoursesAsync` setzt ihn = `isAdmin` → Admin-Reprocess überspringt die Eigentumsprüfung [Admins dürfen ohnehin alles holen]; Nicht-Admin + Repertoire-Reprocess unverändert geprüft. +3 BE-Tests. 259/262 sind gecacht → Fast-Lane. NICHT deployed); 0.225.0 (Endless-Deep-Link-Query-Parameter [`endless-puzzle.component` ngOnInit]: `?start=1`/`?autostart=1` [Auto-Start ohne Config via `maybeAutoStart()` nach Rating-Range-Load], `?themes=…`/`?tags=…` [Komma/Leerzeichen→`config.themes`, worstTags off, ODER-Filter], `?elo=…` [startElo], `?anarchy=max|max+1` [reused `parseShareViewParams` → themeMode='crazy'+enPassantForced, max+1 crazyPieceMode='square'; `[crazyPieceMode]` ans Endless-Board gebunden]. +6 FE-Tests); 0.224.1 (CSP-Fix: eigenes Inline-Script in `index.html` [PWA-`beforeinstallprompt`-Hook] wurde von der strikten CSP [`script-src 'self'`] blockiert → in externe `public/pwa-install-hook.js` ausgelagert [von `'self'` gedeckt, kein `'unsafe-inline'`/Hash]. CSP-Warnungen aus `content.js` = Browser-Extensions, korrekt geblockt, kein RookHub-Bug); 0.224.0 (Admin-Tab „CI / Actions": letzte 5 GitHub-Actions-Läufe je beteiligtem Repo [kahalm/rookhub,-chessresults_crawler,-schach-bot,-piratechess_docker,-log-watcher], Auto-Refresh alle 5 s, Status-Badge + Link. Backend: `GithubActionsService` [getypter HttpClient→api.github.com, Token aus `GitHub:Token`/Env `GitHub__Token` (PAT actions:read) pro Request, Repos `GitHub:Repos` Default=die 5, Owner `GitHub:Owner`=kahalm] + IMemoryCache-Aggregat-Cache `GitHub:CacheSeconds`=4 → 5-s-Polls rate-limit-schonend; ohne Token `Configured=false`→UI-Hinweis. `CiController` `GET /api/admin/ci/runs` [Admin]. FE: eigenständiger `AdminGithubActionsComponent` [timer(0,5000), lazy `*matTabContent`→nur bei offenem Tab], neuer Tab 'ci' [admin-tabs.ts+spec+HTML]. +4 BE-Tests. i18n en/de/hr); 0.223.0 (Neues Puzzle-Theme `enPassantPossible`: `PuzzleService.HasUnplayedEnPassant(fen, uci, firstSolverPly=1)` [pure, Gera.Chess: FEN laden, UCI-Lösung nachspielen, NUR an Löser-Zügen (Indizes 1,3,5… — moves[0]=Gegner-Setup) `board.Moves()` auf `MoveEnPassant` prüfen; e.p. verfügbar + gespielter Zug ≠ e.p. → true] + `TagEnPassantPossibleAsync` [batchweise, idempotent] taggt betroffene Standard-Puzzles im `Themes`-String UND der normalisierten `PuzzleTags`-Tabelle [get-or-create Tag]; Admin-Endpoint `POST /api/admin/puzzles/tag-en-passant` [Hintergrund-Job]. Abgegrenzt von Lichess-`enPassant` [Lösung IST e.p.]. Nur Standard-Puzzles. Kein neues DB-Feld/Migration. +7 BE-Tests); 0.222.0 (Chessable-Flut durch nicht-cachebare Kurse gestoppt — zwei rookhub-only Schutzmechanismen (keine Migration): (1) DEDUP: `ChessableImportService.EnqueueReimportAsync` legt keinen zweiten Import an, wenn für (Owner, bid) bereits einer läuft/pausiert → ein erneuter „Update all"-Klick/Retry löst keine doppelten Kurs-Downloads mehr aus (beobachtet: bid 116242 4× in Folge). (2) BACKOFF: `ImportReprocessService.ReprocessCoursesAsync` überspringt einen stale, NICHT gecachten Kurs, dessen letzter Import <24h her ist (`IncompleteRefetchBackoff`) — ein truncated Kurs wird von piratechess nie gecacht und würde sonst bei JEDEM Massen-Reprocess komplett neu von Chessable geholt (Hunderte Line-Fetches); nutzt die bereits geholte `cachedBids`-Menge + `ChessableImports.CompletedAt`. Gezielter Admin-Re-Import umgeht den Backoff. +3 BE-Tests [1114 grün]. NICHT deployed); 0.221.0 (Reprocess „Alle aktualisieren“ robust+schnell + gecachte Kurse trotz gesperrtem Bearer: (1) `ImportReprocessService.ReprocessCoursesAsync` holt den Cache-Status ALLER Kurse EINMAL per Batch [`ICourseReimporter.GetCachedBidsAsync` → piratechess `/courses/cached`] statt je Kurs den teuren `IsCourseCachedAsync` [lädt+entpackt den ganzen Blob]; `EnqueueReimportAsync` bekam `knownCached`-Param [`FullyCached = knownCached ?? IsCourseCachedAsync`]. (2) Die Einreih-Schleife läuft mit `CancellationToken.None` statt dem Request-ct → Wegnavigieren [Browser schließt Verbindung] bricht das Einreihen NICHT mehr ab [vorher nur ~20 von 268 in der Queue]. (3) `RunAsync`-Bearer-Breaker-Check `&& import.FullyCached != true` → voll-gecachte Kurse kommen aus dem Cache [kein Chessable-Abruf] und laufen trotz totem/gesperrtem Bearer durch. +4 BE-Tests [1097 grün]. NICHT deployed); 0.220.1 (Kurs-Upload-Hinweis: neuer i18n-Text `courses.upload.restriction` [en/de/hr] unter dem Upload-Panel — funktioniert nur mit Puzzle-PGNs im Chessable-Stil, sonst Admin ansprechen oder im Discord fragen [klickbarer `DISCORD_INVITE_URL`-Link aus `core/community.ts`]. Reiner Text/UI, keine Logik); 0.220.0 (Eigenes PGN als persönlicher Kurs hochladen: Kurse-Seite ist jetzt für JEDEN eingeloggten Nutzer erreichbar [`courseAccessGuard` lässt alle Eingeloggten durch, Einstieg über die Dashboard-Kachel — Navbar bleibt content-gated via `checkAccess`], auch bei leerer Liste; oben ein Upload-Panel [PGN-Datei + optionaler Name]. Backend: `CourseService.UploadPersonalCourseAsync` [reused `PgnImportService.ImportFileAsync`, pro-User-eindeutiger Dateiname `user-u{id}-{guid}.pgn`, setzt `Book.OwnerUserId`+`Kind=Study`+Tags `personal`; leeres Ergebnis → Buch wieder entfernt+400] + `DeletePersonalCourseAsync` [nur Besitzer, reused `BookAdminService.DeleteBookAsync`]; Endpoints `POST /api/courses/upload` [multipart] + `DELETE /api/courses/{bookId}`. NUR Chessable-Stil-Puzzle-PGNs [FEN+Round+Trainingsmarker] erzeugen Puzzles — normale Partie-PGNs werden verworfen. Löschen-Icon je eigenem Kurs. Kein neues DB-Feld/Migration. +6 BE-Tests [1100] +5 FE-Tests; i18n `courses.upload.*`/`courses.delete*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.219.1 (Repertoire-Trainer: alternativer (geduldeter) ODER falscher Zug bleibt jetzt ~1 s sichtbar auf dem Brett stehen, bevor er zurückgenommen wird, statt sofort zurückzuspringen. `onMove` setzt `this.fen = fenAfterPlayerMove` auch für tolerated/wrong [vorher tolerated sofort `lastMove=undefined`/wrong nur Markierung]; neue `startFen`-Erfassung am `onMove`-Anfang. Geduldet: ~1,8 s gehalten [`ADVANCE_MS.tolerated`], dann `retryCurrentCard` nimmt zurück [`fen=startFen`] + dieselbe Karte wieder spielbar. Falsch: neuer `WRONG_HOLD_MS`=1000-Timer [`scheduleWrongRevert`/`revertWrongHold`] nimmt zurück, bleibt im FEEDBACK [Mausrutscher/Lösung-zeigen-Buttons]; `clearWrongRevert` in `clearAdvance`+`mouseslip`+`showSolution`. 7 Trainer-Specs grün, PROD-Build grün); 0.219.0 (Zug-lose Kurs-Erklär-/Intro-Seiten [Kommentar, keine Züge] werden beim Buch-/Kurs-Import nicht mehr verworfen: `PgnImportService.ParsePgn(..., keepCommentOnlyAsInfo:true)` [von `ImportFileAsync` gesetzt; Wochenpost-Pfad bleibt Default false] behält sie als Info-Linie [synthetischer Fake-Zug e4 ab Grundstellung, `IsInfoOnly`, Text als Kommentar] → nur sequenziell zum Durchklicken, kein Quiz, nicht in Random/Daily, nicht im Fortschritt. `ImportPipeline.CurrentVersion` 4→5; die Seiten stehen schon im `Book.SourcePgn` → Reprocess ist hier ein reiner LOKALER Re-Import, KEIN Chessable-Re-Fetch. +4 BE-Tests [1094]); 0.218.1 (Repertoire-Trainer-Zug-Feedback gefixt: `onMove` setzte `this.fen` nie auf die Stellung NACH dem Zug → bei jedem Zug Brett-Take-back + Replay über die Folgekarte [Flackern]. Jetzt: richtig → `this.fen = fenAfterPlayerMove` [Zug bleibt stehen, kein Flackern]; geduldet/spielbar → `lastMove=undefined` + `this.fen` unverändert [Zug zurückgenommen] und neue `runAdvance`/`retryCurrentCard`: geduldet springt NICHT weiter [kein Vorspielen des Hauptzugs], sondern macht dieselbe Karte erneut spielbar, sodass der User den Hauptzug selbst zieht. `onPlayClick`+Timer rufen `runAdvance`. Spec: tolerated-Test auf Retry+Take-back umgestellt + neuer correct-„kein Flackern"-Test, 780 FE grün); 0.218.0 (Anarchy-Stufe `?anarchy=max+1`: wie max [Crazy-Brett + erzwungenes En passant] PLUS das FELD bestimmt den Figurenstil [`crazyPieceMode='square'`] — Figur nimmt das Zufallsset ihres Feldes an, beim Ziehen wechselt das Aussehen zum Zielfeld. `board-theme.util`: `paintCrazyPieces(root, mode, orientation)` + `squareOfPiece` [Feld aus `transform: translate`], 64er-Feld→Set-Map; `parseShareViewParams` matcht `max+1`/`max 1` [+-Dekodierung]. `BasePuzzleSolver.crazyPieceMode` → `@Input` am `app-puzzle-board`, `repaintCrazyPieces` mit Settle-Repaint [~230ms] nach der Zug-Animation. +5 board-theme-Specs [25]); 0.217.1 (Fix: „Letztes Puzzle"-Favoriten-Knopf zeigte zwei Herzen [mat-icon + ♥ im Label-Text der i18n `favorites.loveLast`]; ♥-Präfix aus en/de/hr entfernt, nur das Icon bleibt); 0.217.0 — 0.217.0 (Standard-Puzzle-Links beachten jetzt `?single=1` [wie Buch-Puzzles]: `puzzle.component` liest `single` aus der Query → nach dem Lösen KEIN Auto-Weiter [`handleSolved` überspringt `startSolvedCountdown`/`loadNext` bei `singlePuzzle`], der „Weiter"-Knopf [`showNext` default true] bleibt aber. +1 Spec. Der Standard-Teilen-Dialog hängt `single=1` NICHT automatisch an [nur Buch] — Link wird bei Bedarf von Hand erweitert); 0.216.0 (Geliebte Puzzles ♥: nach Lösen/Aufgeben jedes Puzzles [Standard/Endless/Kurs-Buch/Tagespuzzle] ein Herz-Button in der Status-Card [`PuzzleStatusCardComponent` `canFavorite`/`isFavorite`/`favoriteToggled`]. Der „Letztes analysieren"-Knopf ist in zwei geteilt: „Letztes analysieren" + „♥ Letztes Puzzle". Backend polymorph wie `PuzzleChallenge` [`FavoritePuzzle` Standard/Book via `PuzzleSource`, Unique (UserId,Source,PuzzleId), Migration `AddFavoritePuzzles`], `FavoriteService` [add/remove/contains idempotent, list mit Fen+Moves+Rating+Themes+Title-Enrichment + Skip verwaister Einträge], `FavoriteController` `api/favorites` [GET/`count`/`contains`/POST/DELETE], Menü-Key `favorites`. Neue Seite `/favorites` [`FavoritesComponent`, Nachspielen/Analysieren/Entlieben] + Dashboard-Kachel `favorites` [default-hidden] + Navbar. FE-Logik gekapselt in `FavoriteTracker` [optimistisch + Rollback, current/last synchron], in alle 3 Solver eingehängt. Wochenpost-Puzzles NICHT favorisierbar [keine echte BookPuzzle-Id]. +8 BE-Tests [1090] +15 FE-Tests [773]. i18n `favorites.*`/`nav.favorites`/`dashboard.favorites.*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.215.0 (Crazy-Brett würfelt jetzt JEDE Figur einzeln aus statt pro Rolle+Farbe [vorher „alle Bauern gleich"]: `board-theme.util` ersetzt das per-Rolle-`!important`-CSS [`applyCrazyPieces` entfernt] durch `paintCrazyPieces(root)` — pro `<piece>`-DOM-Element ein zufälliges Set via Inline-`background-image`, stabil über `WeakMap<Element,set>` [Figur behält ihr Set beim Ziehen; neues Puzzle/`redrawAll` würfelt neu]. `puzzle-board.component` ruft `repaintCrazyPieces()` [rAF] nach Init, nach jedem `ground.set` und nach Resize-`redrawAll`; sonst `clearCrazyPieces`. Figuren-CSS in `styles.scss` ist `.piece-set-<set>`-skopiert ohne `!important` → für `_crazy` greift nur das Inline. +5 board-theme-Specs [20]. Gilt für Crazy-Theme + `?anarchy=max`); 0.214.0 (Anarchy-Modus `?anarchy=max` an jedem Puzzle-Link [Standard + Kurs/Buch]: Crazy-Brett UND en passant verpflichtend [„en passant is forced"]. `parseShareViewParams` parst `anarchy=max`→`{themeMode:'crazy', enPassantForced:true}`; `BasePuzzleSolver.enPassantForced` an `calcDests(chess, enPassantForced)` durchgereicht → ist ein En-passant-Schlag verfügbar [chess.js-Flag `e`], enthält die dests-Map NUR diese Züge, sonst alle. `puzzle.component`+`book-puzzle.component` setzen das Flag aus dem Query. Rein transient, ändert keine gespeicherten Einstellungen. +2 Specs `puzzle-move.util` +2 `board-theme.util`. Hinweis: paralleler 0.213.1-Fix [Reprocess→Re-Fetch] liegt uncommittet in Kopie 1; daher Sprung auf 0.214.0); 0.213.1 (Reprocess-Banner-Fix für die Info-Linien-Änderung: bestehende Chessable-Kurse haben ein gecachtes Alt-PGN OHNE `[%info]`-Marker; `ImportReprocessService.GetCourseStatusAsync`/`ReprocessCoursesAsync` stuften sie als „lokal aufbereitbar" ein → lokales Reprocess hätte `ImportVersion` auf 4 hochgesetzt OHNE `IsInfoOnly` zu setzen [still falsch, Banner verschwindet]. Neu: Chessable-Kurse [`CanRefetch` = `IsChessable` + parsebare bid] werden IMMER als `Refetchable` eingestuft und per vollständigem Re-Fetch aktualisiert [Chessable-Zweig VOR dem SourcePgn-Pfad], nur Nicht-Chessable-Bücher mit Quelle laufen lokal. Matcht den v4-Doc-Kommentar [„für Chessable-Bücher ist der Reprocess ein Re-Fetch"], der vorher nur für Repertoires zutraf. 3 bestehende Tests auf Nicht-Chessable-Lokalbuch umgestellt + 1 neuer Test [`ReprocessCourses_ChessableWithSource_RefetchesInsteadOfLocalReprocess`]; 12 Reprocess-Tests/1082 BE grün. NICHT gepusht/getaggt/deployed); 0.213.0 (Frontend-Durchklick-UI für Chessable-Info-/Erklärlinien [Backend-Seite war 0.212.0]: `book-puzzle.component` bekam den Zustand `'INFO'` — `setupPuzzle` zweigt bei `puzzle.isInfoOnly` in `enterInfoReview()` ab [kein `setupSolver`, Brett view-only, `reviewMode`, ◀/▶ über die ganze Linie + Zug-Kommentare], statt Status-Card eine „Erklärlinie"-Card, Kontext-Nav zeigt „Weiter" [`courseNext`/`nextInBook`]. Board-`[visualization]` im Review/Info auf 0. i18n `book.info.title/subtitle` + `book.actions.prev/next` en/de/hr. +2 FE-Specs. HINWEIS: greift nur für aus Chessable neu importierte/abgerufene Kurse); 0.212.0 (Chessable-Info-/Erklärvarianten [von Chessable als IsInfo markiert] werden nicht mehr als Quiz abgefragt: piratechess emittiert `[%info]` im PGN [Commit `6f5ed38`], rookhub-Import setzt daraus `BookPuzzle.IsInfoOnly` [+Migration `AddBookPuzzleIsInfoOnly`, +`ImportPipeline.CurrentVersion` 3→4]. Solche Linien fallen aus ALLEN Zufalls-/Tagespuzzle-Töpfen [`BookPuzzleService`-Pool + `GetRandomInBookAsync`], zählen NICHT zum Kurs-Fortschritt [`CourseService`: `quizScope` ohne `IsInfoOnly` in `GetNextAsync`/`GetChaptersAsync`/`GetCoursesAsync`/`BuildProgressAsync`/`ComputeStatsAsync`] und sind im sequenziellen Kurs-Modus nur zum Durchklicken [seqPool nimmt Info-Linien mit, Completion bleibt quiz-basiert]. `BookPuzzleDto.IsInfoOnly` durchgereicht. HINWEIS: Marker entsteht erst beim frischen Chessable-Abruf → bestehende Kurse erst nach Re-Import/Re-Fetch. Frontend-Durchklick-UI folgt [0.213.0]. +7 BE-Tests [1081] +2 piratechess-Lib-Tests [16]); 0.211.0 (Dashboard-Customize „Standard"-Knopf + kuratierter Default: `applyDefault()` [ersetzt `resetLayout`] setzt `DEFAULT_VISIBLE` = Puzzles/Weekly/Repertoires/Kurse/Trainingsziele/Bestenlisten [in dieser Reihenfolge] sichtbar, alle übrigen Kacheln ausgeblendet [`DEFAULT_HIDDEN`], und persistiert. Dieser Default ist auch das Initial-Layout [ngOnInit: leeres gespeichertes Layout → kuratierter Default]. `DashboardLayoutService`-Key auf `rookhub_dashboard_layout_v2` erhöht → alle User starten einmalig frisch auf dem Default. Button-i18n `dashboard.edit.default` [Standard/Default/Zadano] en/de/hr. Spec-Anpassungen [Default-Layout, chessableQueue jetzt default-hidden → Edit-Modus-Checks]. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.210.0 (Visualisierungs-Umschalter unter dem Brett (0.207.0) WIEDER ENTFERNT — gefiel nicht. `VizModeSelectorComponent` + Spec gelöscht, viz-0..4-SVG-Icon-Registrierung aus `app.component` raus, Import/Imports-Array in allen drei Solvern entfernt. Stattdessen der alte Aufdecken-Knopf zurück in `.board-hint` (`@if vizPiecesHidden || visualizationMode===1`, ruft `onVizShow()`), aber als **Auge-Icon** statt Text: `vizShowPressed ? 'visibility' : 'visibility_off'` (durchgestrichen solange verdeckt, offen wenn aufgedeckt) + `.board-hint-eye`-Style; Tooltip/aria via vorhandene `*.viz.show`/`showing`-Keys. Modus-Wahl wieder nur im Einstellungsdialog. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.209.1 (Build-Fix: gerades Anführungszeichen im de-0.209.0-Changelog [`„keine Treffer"`] beendete den TS-String vorzeitig → Production-Build [CI] rot; auf typografisches `„…“` korrigiert. FALLE [[rookhub-changelog-build-gotcha]]: nach Changelog-Edit IMMER `npx ng build` [Production], nicht nur `--configuration development` — der Dev-Build lief vor dem Changelog-Edit und übersah es); 0.209.0 (Suchfeld für Kurse + Repertoires: oben in `course-list`/`repertoire-list.component` ein `mat-form-field`-Suchfeld [`[(ngModel)]="search"`, Such-Icon + Leeren-Knopf, `subscriptSizing="dynamic"`]; clientseitige Filterung über die bereits geladene Liste [Kurse nach `displayName`, Repertoires nach `name`+`description`, case-insensitive]. Kurse: neuer `filtered`-Getter, die Sektions-Getter [inProgress/public/chessable] bauen darauf auf → Filter wirkt über alle Sektionen; `courses.noMatch`-Hinweis. Repertoires: `filteredRepertoires`-Getter, `@empty`→`repertoire.list.noMatch` vs `empty`. FormsModule+MatFormField/Input ergänzt. i18n `common.clear`, `courses.searchPlaceholder`/`noMatch`, `repertoire.list.searchPlaceholder`/`noMatch` en/de/hr. +1 course-Spec +4 repertoire-Specs [756 grün]. NICHT deployed); 0.208.0 (Dashboard-Bearbeitungsmodus ändert die Ansicht NICHT mehr: das Kachelraster behält sein Layout [kein flex-column-Umbau mehr], es werden nur alle geeigneten Kacheln gezeigt [deaktivierte `tile-off` ausgegraut]. Je Kachel Overlay oben rechts: ↑/↓-Pfeile [`moveUp`/`moveDown` → `shift`/`commitOrder`] + Auge-Toggle; transparentes `.tile-shield` [zugleich `cdkDragHandle`] fängt Klicks auf den Kachel-Buttons ab [keine versehentliche Navigation] und ist die Drag-Fläche. Drag&Drop bleibt, jetzt `cdkDropListOrientation="mixed"` [rastertauglich, CDK 19]. i18n `dashboard.edit.moveUp`/`moveDown` + hint-Text en/de/hr. +2 FE-Specs [751 grün]. NICHT deployed); 0.207.0 (Visualisierungsmodi als Inline-Icon-Umschalter in der Solver-Hinweiszeile [ersetzt den „Aufdecken"-Knopf]: neue `VizModeSelectorComponent` [5 Buttons, Output `modeChange`→ruft das vorhandene `setVisualizationLevel($event)`] in `.board-hint` aller drei Solver [puzzle/book-puzzle/endless-puzzle.component.html]; aktiver Modus hervorgehoben. 5 SVG-Icons global via `MatIconRegistry.addSvgIconLiteral` in `app.component` [`viz-0`…`viz-4`, wie Discord-Logo]: 0=Bauer, 1=Bauer gedimmt+Kette, 2=schwarz/weiß-Token [fix], 3=weißes Token [fix], 4=durchgestrichenes Auge [gefüllt, Iris+Pupille via Maske]. 0/1/4 `currentColor`, 2/3 feste Farben. Aufdecken = Modus 0 wählen [`board-hint-show`+`onVizShow` aus Templates raus; Base-Logik bleibt]. i18n `puzzles.viz.modeSelectAria` en/de/hr. +3 FE-Specs [748 grün]. Versionssprung 0.206.0→0.207.0 [paralleler 0.206.0-Kollision mit Revenge-Icon ausgewichen]. NICHT deployed); 0.206.0 (Freundesliste: Revenge-a-Friend-Icon wird ROT [+ Anzahl-Badge], wenn offene Revanchen gegen den Freund existieren [Freund gescheitert+nie gelöst, ich noch nicht gelöst]. `GET /api/friends` reichert je Freund `FriendDto.OpenRevengeCount` an [`PuzzleService.GetOpenRevengeCountsAsync`, provider-sicher in-memory aggregiert]; FE `friends.component` bindet `[color]`+`matBadge`, i18n `friends.revenge.menuOpen` en/de/hr. +1 BE-Test [25] +1 FE-Test [8]); 0.205.3 (Observability: Admin-Impersonation pro Request sichtbar — die Request-Logging-Middleware [`Program.cs`] schreibt den `imp`-Claim [Admin-Id] jetzt als `LogContext`-Property `ImpersonatorId` [→ ECS `labels.ImpersonatorId`], sodass impersonierte Requests in Kibana filterbar sind; vorher nur das einmalige Impersonation-Start-Event geloggt. Reines Logging-Plumbing analog `UserId`/`UserName`; `imp`-Claim-Quelle bereits in `AuthServiceTests` getestet); 0.205.2 (Chessable-Bearer-Circuit-Breaker GEMERGT [vorher lokales v0.204.0, Commit `d6f0995`] und mit der entfernten 0.203.x-Chessable-Security-Linie versöhnt: weist Chessable einen Bearer endgültig ab [Account gesperrt/gelöscht oder Token tot — `ChessableBearerBreaker.IsBearerFatal`, NICHT bei IP-/Cloudflare-/VPN-Block], sperrt ihn [`ChessableCredential.BlockedAt`/`BlockedReason` + Migration `AddChessableCredentialBreaker`] und verwendet ihn für KEINE weitere Anfrage. Trip in `ChessableImportService.FailAsync` + `ChessableController` [Test/Courses/Admin-Courses/Estimate]; laufende Importe PAUSIEREN [`Phase="bearer-blocked"`], Lese-Abrufe/neue Importe → 400. Reset = erfolgreicher „Testen"-Klick [`ClearAndResumeAsync`]; neuer Bearer-Save schließt den Breaker. Admin-Test-Endpoint `POST admin/users/{id}/test`. FE: Sperr-Banner + 🔒/„Bearer testen" im Admin-Kursdownload. Merge-Konflikt: in `StartImportForUserAdmin` sitzt die Bearer-Sperr-Prüfung VOR der neuen `UserOwnsCourseAsync`-Eigentumsprüfung [0.203.8], damit ein toter Bearer keinen Fetch auslöst. +12 BE-Tests. Versionssprung 0.205.1→0.205.2 [0.204.0 in 0.205.2 aufgegangen]); 0.205.0 (Anpassbare Dashboard-Kacheln: Dashboard ist ein konfigurierbares Kachelraster über alle sinnvollen Module [Puzzles/Trainingsziele/Kurse/Bestenlisten/Turniere/Freunde/Repertoires/Partien/Wochenpost/Statistik/Analyse/Nachrichten + Admin-Chessable-Queue]. „Anpassen"-Knopf → Bearbeitungsmodus: Kacheln per CDK-Drag&Drop sortieren + per Augen-Knopf ein-/ausblenden, „Zurücksetzen" → Default. Persistenz pro Gerät via `DashboardLayoutService` [localStorage `rookhub_dashboard_layout`, `{order,hidden}`]; Eignung je Kachel über `MenuService.visible$` [+ `auth.isAdmin` für Chessable]; neue Kacheln werden hinten an ein bestehendes Layout angehängt. Datengetriebene `TileDef`-Definitionen [`eligible`/`subtitle`/`buttons`] + generisches Card-Template; Detail-Listen bleiben darunter. i18n en/de/hr. +5 Layout-Service-Specs +7 Dashboard-Tile-Specs. HINWEIS: Das lokale v0.204.0 [Chessable-Bearer-Circuit-Breaker, Commit `d6f0995`] ist auf diesem gepushten Stand NICHT enthalten — es kollidiert mit der entfernten 0.203.x-Chessable-Linie und braucht einen bewussten manuellen Merge; gesichert im Branch `backup-local-master`. Daher Versionssprung 0.203.13→0.205.0); 0.203.12 (piratechess VPN-Concurrency [piratechess `53e90b1`]: `VpnIpHealth._byIp`-Eviction [Cap `Vpn:IpHealthMaxEntries`]; `VpnTunnel.RequestCompleted` zählt Ausgang VOR `_inFlight`-Decrement [Block-Fehlbuchung bei Drain-Race behoben]; `_currentIp` volatile; `VpnRotationService.RotateNowAsync` sequenziell statt `Task.WhenAll` [kein Total-Stall bei manueller Rotation]. 253 Tests grün. NICHT getaggt/deployed); 0.203.11 (piratechess Parsing-Fix [piratechess `1fe1eda`, Batch 2]: `SanToMove` konstruiert gerade Bauern-Push-Umwandlungen [`e8=Q`] direkt — ChessDotNet 1.0.0 listet sie nicht in `GetValidMoves` → vorher leerer `[%tqu]`-Trainings-UCI + Abbruch der Folgezüge; relevant für Endspiel-Kurse. +10 Lib-Fixtures [SAN + softFail]. Geprüft als Nicht-Probleme: useLocalData-Oid [positionell korrekt, Oid beim Cachen nie gesetzt] + softFail-Indizierung [relativ korrekt]. 252 Tests grün. NICHT getaggt/deployed); 0.203.10 (piratechess Code-Review Resilienz+Hygiene [piratechess-Repo, Commit `5074a72`, 242 Tests grün]: ExportBackgroundService überlebt Reader-Fehler [äußere while+Backoff]; curl stdout/stderr parallel [kein Pipe-Deadlock]; ExportJobQueue bounded; Startup-Fail-fast für ConnString/Jwt; globaler ProblemDetails-Exception-Handler; Health 503 bei DB-Ausfall; Dictionary-Indexer statt Add bei doppelter Bid; bid-Format-Validierung [numerisch] → schließt BidLock-Leck. NICHT getaggt/deployed); 0.203.9 (**Security-Fix (2. Review-Pass)**: zweite Tür zum Cached-Content-Bypass geschlossen — `EnqueueReimportAsync` (Reprocess-/Re-Fetch-Choke-Point) prüft jetzt via `OwnerHasCourseAsync`, dass der `bid` in der Bibliothek des Owners liegt, sonst Skip [null]. Vorher konnte ein User ein Repertoire mit beliebigem `ChessableCourseId`/präpariertem Dateinamen anlegen [ImportVersion 0 = stale] und per `/reprocess` fremden gecachten Kursinhalt holen. +2 Tests/1051 grün); 0.203.8 (Nachzieher Import-Security: Admin-`StartImportForUserAdmin` macht denselben `UserOwnsCourseAsync`-Check gegen den Ziel-User; anonyme Tagespuzzle-Lösungen DB-seitig dedupliziert — Unique-Index `(BookPuzzleId, AnonymousSessionId)` [Migration `AddAnonBookAttemptUniqueDedup` inkl. Alt-Duplikat-Bereinigung; authentifizierte Versuche unberührt via NULL-Session], `RecordAnonymousAttemptAsync` fängt `DbUpdateException` ab → kein Doppel-Count/Webhook); 0.203.7 (**Security-Fix**: `ChessableController.StartImport` prüft via `UserOwnsCourseAsync` jetzt, dass der `bid` in der Chessable-Bibliothek des Users liegt [gecachte Liste, sonst 1× frisch laden], sonst 403. Vorher konnte jeder eingeloggte User mit Bearer JEDEN gecachten Kurs importieren — piratechess `POST direct/course` liefert gecachte Kurse aus dem geteilten Cache OHNE Eigentumsprüfung, und Chessable-Kurs-IDs sind öffentlich. Closes den Content-Bypass für alle Lanes [cached+fetch]; +1 Test/1049 grün); 0.203.6 (Doku: API-Code-Review 2026-06-30 in TODO.md festgehalten — Gefixtes [v0.202.0–0.203.5] + bewusst gegen reale Prod-Größe [49 User] zurückgestellte „unbounded read"-Funde [laden real 50–1900 Zeilen → SQL-Umbau = Pomelo-Risiko ohne Nutzen] + Import-Concurrency [bereits durch Claim+Watchdog+Lane-Gate abgedeckt]; keine Code-Änderung); 0.203.5 (Code-Review-Robustheit: `CreateChallengeBatchDto.ToUserIds` `[MaxLength(50)]` [DoS-Schutz]; BookPuzzle-Routen `{id:int}`; `RepertoireTrainingService.ReviewAsync` `DbUpdateException`-Race-Catch [Unique CardKey]; `TournamentMonitorController` `TryGetInt32` statt `GetInt32` [kein 500 bei Crawler-Typänderung]. Bereits erledigt im aktuellen Code: Buch-Versuch klemmt Zeit/Hints serverseitig + validiert SessionId; Listen-Endpoints klemmen take/page); 0.203.4 (Code Review: SavedGame-Dedup hart auf DB-Ebene — Unique-Index `(UserId, Source, ExternalId)` [Migration `AddSavedGameUniqueDedup` inkl. Alt-Duplikat-Bereinigung; NULL-ExternalId weiter erlaubt], `SaveAsync` fängt `DbUpdateException` ab und liefert die bestehende Partie); 0.203.3 (Performance [Code Review]: `AutoSubscriptionService` — frischer DbContext/Scope pro User [`CheckAllUsersAsync`, kein unbeschränktes ChangeTracker-Wachstum/Leakage über den ganzen Lauf] + Freundes-/Profil-Set je User EINMAL geladen [`LoadFavoriteProfilesAsync`, an `AutoFavoritePlayersAsync` durchgereicht] statt je Turnier neu; reine Effizienz, Verhalten unverändert); 0.203.2 (Korrektheit [Code Review]: (1) Daily-Leaderboard `AggregateScores` Competition-Ranking nach Lösezeit [zeitgleiche Löser = gleicher Rang/Bonus + alle 🥇, statt Submit-Reihenfolge bei TimeSeconds==0]; (2) `AdminMessageService.GetThreadsAsync` lädt jüngste Nachricht je Thread über `Max(Id)` statt `CreatedAt`-IN-Match [kein Cross-Thread-Mismatch bei Zeitgleichheit]; +2 Tests); 0.203.1 (Performance [Code Review]: Wochenpost-Übersicht ohne N+1 — `WeeklyPost.PuzzleCount` gecacht [Migration `AddWeeklyPostPuzzleCount`, beim Upload berechnet], `GetAllProgressAsync` lädt alle Posts in EINER Query + nutzt gecachte Anzahl statt PGN-Parse je Post; `GetTotalAsync`-Helper mit Lazy-Backfill für Alt-Datensätze auf allen Pfaden); 0.203.0 (Puzzle-Link-Query-Parameter `?crazy=1`/`?visualmode=0..4` [anderer Agent]); 0.202.2 (Security-Härtung [Code Review]: benannte Rate-Limiter `auth`/`anonymous-puzzle`/`anonymous-tournament` partitionieren jetzt pro Client-IP [`AddPolicy` mit `RateLimitPartition` statt einzelnem `AddFixedWindowLimiter`-Bucket] → kein site-weiter Login-DoS / pro-IP-Brute-Force-Drossel; Default-CORS ohne `AllowCredentials` [Bearer-only]; „Eingeloggt bleiben"-JWT 365→90 Tage); 0.202.1 (Viz-/Blind-Puzzle-Gesten gehärtet [anderer Agent]); 0.202.0 (Track-solves erfasst Tipp-Nutzung: `SharedPuzzleAttempt.HintsUsed` (0–3, Migration `AddSharedPuzzleAttemptHints`) wird beim Erstversuch mitgespeichert; `RecordSharedAttemptDto.HintsUsed` + `SharedPuzzleCountsDto.SolvedByHints[0..3]` (gelöste je Tipp-Stufe) — `GetSharedCountsAsync` aggregiert in einer GroupBy-Query; Buch-Puzzle-Solver sendet `hintLevel`. +2 BE-Tests); 0.201.0 (Kurs „Ganzes Buch" + Repertoire-Trainer Politur: (1) Kurs-Abschluss differenziert: `book-puzzle.component` neuer Getter `courseFullyDone` (= `courseSolved >= courseTotal`) + `courseRemaining`; Template zeigt Trophäe+„Kurs abgeschlossen!" nur wenn wirklich alle gelöst, sonst „Zufalls-Runde durch — noch N Aufgaben" mit „Mit den restlichen weitermachen"-Knopf. Backend bleibt unverändert (`Completed=puzzle==null` = Pool leer; war im Random-Modus immer schon irreführend für die UI). i18n `book.course.roundFinished`/`continueRemaining` en/de/hr. (2) Repertoire-Trainer `tolerated`-Feedback verrät den Repertoire-Zug nicht mehr — neuer Key `toleratedPlayable` ohne `{{move}}`. (3) `wrong`-Feedback zeigt nicht mehr sofort die Lösung: zwei Buttons „Mausrutscher" (kein Penalty, kein Server-Review, kein Re-Queue, zurück zu PLAYING; Brett unverändert) und „Lösung zeigen" (zählt erst dann als wrong, sendet `grade=0` an `RepertoireTrainingService.review`, re-queued die Karte, enthüllt `expectedDisplay`). State-Felder: `wrongRevealed`, `wrongMove`, `pendingWrongReview`. (4) Bei `wrong` Stockfish-18-Eval-Vergleich im Hintergrund: `StockfishService.getEval` (Depth 14) auf FEN nach Spielerzug + FEN nach Repertoire-Zug, Differenz aus Spieler-Sicht in Bauern; Mate-Sonderfälle (`evalMateNote` = `missed`/`allowed`). `kickOffEvalCompare` mit `evalEpoch` gegen Karten-/Zugwechsel-Race; Stockfish-Init im `ngOnInit` (Warmup). i18n `repertoireTrainer.toleratedPlayable`/`wrongNoHint`/`mouseslip`/`showSolution`/`evalLoading`/`evalWorse`/`evalEqual`/`evalMateMissed`/`evalMateAllowed` en/de/hr. +2 Spec-Cases (mouseslip kein Penalty, showSolution zählt+re-queued); 6/6 trainer-spec grün); 0.200.4 (Mobile-Fix Status-Karten: bei `max-width: 768px` setzte `puzzle.component.scss`+`book-puzzle.component.scss` zwar `flex-direction: column`, aber nur `.board-section: 100%` — `.info-section` blieb auf Content-Width, daher waren die Status-/Korrekt-Karten unter dem Brett schmaler als das Brett selbst. Beide bekommen jetzt `.board-section, .info-section { width: 100% }` wie endless-puzzle bereits seit längerem. Gilt für Standard-Puzzles und Buch-/Kurs-/Tages-/Wochen-/Geteilte-Puzzles); 0.200.3 (Dark Mode als Default: `ThemeService._preference` Default `system`→`dark`; gespeicherte Nutzerwahl [localStorage `rookhub_app_theme`] hat weiter Vorrang, Toggle unverändert); 0.200.2 (Leaderboard „Diese Woche"/„Dieser Monat" = rollierende Fenster: `LeaderboardService.WindowStart` weekly→`today.AddDays(-6)` [letzte 7 Tage], monthly→`today.AddDays(-30)` [letzte 31 Tage] statt ISO-Montag/Monatserster; reine Backend-Änderung, FE-Labels unverändert. Boundary-Test angepasst, 9 LeaderboardService-Tests grün); 0.200.1 (Track solves immer an: das Opt-in [Teilen-Dialog-Checkbox + `?track=1`-Param] entfernt; `BookPuzzleComponent.trackSolves = singlePuzzle` [also für jeden geteilten `?single=1`-Link aktiv]. SharePuzzleDialog: Checkbox/`canTrack`/`setTrack`/`&track=1`-Anhängen + MatCheckbox-Import raus, `activeUrl` liefert nur die Basis-URL; ungenutzte i18n `puzzles.share.trackSolves(+Hint)` entfernt. share-dialog-Spec umgeschrieben); 0.200.0 (Geteilte Puzzles „Track solves": Teilen-Dialog bekam Checkbox „Versuche zählen" [nur bei Buch-Einzel-Link mit `?single=1`; hängt `&track=1` an `activeUrl`]. Empfänger-Link zeigt unter dem Puzzle Gelöst-/Fehlversuch-Zähler. Neue Tabelle `SharedPuzzleAttempts` [Unique `(BookPuzzleId, IdentityKey)` → nur Erstversuch je Besucher; `u:{userId}`/`s:{sessionId}`] + Migration `AddSharedPuzzleAttempts`; Endpoints `POST/GET /api/book-puzzles/{id}/track[-counts]` [AllowAnonymous, optionale Auth via `GetUserIdOrNull`]. `BookPuzzleComponent`: Flag `trackSolves` [Query `track=1`], `recordTrack(solved)` [guard + serverseitig erstversuch-dedupliziert] aus finalizeSolve[true]/handleFailed/giveUp/resetPuzzle[false] → Reset/Aufgeben = failed; Anzeige `sharedCounts`. i18n `puzzles.share.trackSolves(+Hint)`/`book.track.solved|failed` en/de/hr. +5 BE-Tests [1036], +5 FE-Tests [neues share-dialog-Spec + book-puzzle]); 0.199.1 (Direkt geteiltes Einzel-Puzzle bleibt am Ende stehen: Teilen-Link aus dem Buch-Puzzle-Solver bekommt `?single=1` [`sharePuzzle()` url+previousUrl]; `BookPuzzleComponent` liest das in Flag `singlePuzzle` → `finalizeSolve` überspringt den Auto-Advance-Countdown, `solvedAutoNext` ist no-op, neuer Getter `browseInBook`=`standalone && !isDaily && !singlePuzzle` blendet die Nächstes/Zufällig-im-Buch-Buttons aus, und `PuzzleStatusCard` bekam `@Input() showNext` [=`!singlePuzzle`] um den „Weiter"-Knopf im Gelöst-Zustand zu verstecken. Normales Buch-Durchblättern unverändert. +3 FE-Tests [28 grün im book-puzzle-Spec; share-URL-Test auf `?single=1` angepasst]); 0.199.0 (Freundesliste zeigt ausstehende GESENDETE Anfragen: Anfragen-Tab bekam neben „Eingehend" einen Abschnitt „Gesendet (wartet auf Bestätigung)" [bisher nirgends sichtbar], je Zeile Zurückziehen-Knopf [reused `DELETE /api/friends/{id}` → `RemoveFriendAsync` erlaubt auch dem Requester], Tab-Zähler = eingehend + gesendet. BE: `FriendService.GetSentPendingRequestsAsync` [RequesterId==me && Pending, inkl. Addressee+Profile] + DTO `SentFriendRequestDto` + Endpoint `GET /api/friends/requests/sent`. FE: `FriendsService.getSentRequests` + `friends.component.sentRequests`/`withdrawRequest`; i18n `friends.requests.*`/`friends.aria.withdraw`/`friends.errors.withdrawRequest` en/de/hr [22 weitere fallen für friends-Detailkeys ohnehin auf en zurück]. +1 BE-Test [24 FriendController grün] +2 FE-Tests [7 grün]); 0.198.0 (Puzzle-an-Freund-Menü: hinter jedem Freund in Klammern, wie viele der von mir geschickten Puzzle noch OFFEN sind [Pending = noch nicht versucht], z. B. „Max (3)"; gelöste/gescheiterte zählen NICHT [erledigt], Freunde ohne offene zeigen keine Zahl. BE: `ChallengeService.GetPendingOutgoingCountsAsync` [GroupBy ToUserId, nur FromUserId==me && Pending] + Endpoint `GET /api/challenges/outgoing/pending-counts`. FE: `ChallengeService.getPendingCounts` + `ChallengeFriendsComponent.pendingCounts` [lädt beim Menü-Öffnen mit der Freundesliste, frischt nach `send()` nach]; i18n `puzzles.challenge.unsolvedTitle` en/de/hr [andere 22 Sprachen fallen für den ganzen challenge-Block ohnehin auf en zurück]. +1 BE-Test [18 ChallengeController grün], +1 FE-Test [7 grün gesamt]); 0.197.1 (Freundeszahl reaktiv: Nimmt ein Freund meine Anfrage an, aktualisierte sich die Freundeszahl bei mir erst nach Seiten-Refresh [Dashboard-`friendCount` nur in `ngOnInit`-`forkJoin` geladen, keine Reaktivität]. Fix: `InAppNotificationService` bekam ein `arrived$`-Subject [feuert, wenn `refreshCount` einen GESTIEGENEN Ungelesen-Zähler erkennt = neue Notification]; Dashboard lädt darauf die Freundeszahl nach, `friends.component` macht einen STILLEN `loadData(true)` [kein Spinner-Flackern]. Hängt am vorhandenen 60-s-Glocken-Poll → kein zusätzlicher Timer. +3 Tests [service `arrived$`, dashboard friendCount, friends quiet reload] + neue `dashboard.component.spec`); 0.197.0 (Chessable-Import-Fortschritt auf der Kursseite: neues schreibgeschütztes `ChessableImportsBannerComponent` [self-polling `getImports`, 8s] oben in `CourseListComponent` zeigt laufende/pausierte Importe mit derselben Visualisierung wie der Chessable-Tab [„hole Kurs… Kapitel 7/36 · 82/1000 Linien · noch ca. 23 Min"], `(importCompleted)`→`loadCourses()` lädt nach Abschluss nach. Fortschritts-/ETA-/Label-Logik aus `chessable.component` in geteilte `chessable-progress.util` extrahiert [`chessableStatusLabel`/`chessableQueueLabel`/`compareImportsByQueue` + `CHESSABLE_LINES_PER_MIN`/`formatDuration`/`effectiveTotalLines`/`estimateRemainingMinutes`, von der Komponente rückwärtskompatibel re-exportiert]. Chessable-Tab-Queue `activeList()` jetzt nach „#" sortiert [queuedAhead asc, dann createdAt]. +2 Spec-Dateien); 0.196.2 (Daily-💡-Badge-Fix: Tagespuzzle in Discord zeigte kein 💡 hinter Lösern mit Tipps, weil das Daily-Webhook-Payload [`SchachBotWebhookService.NotifyAttemptAsync`] pro Solver nur `name/discordId/discordUsername/timeSeconds` sendete — **`hintsUsed` fehlte** [war NIE drin, Git-Historie bestätigt; nur das Wochenpost-Payload `NotifyWeeklyAsync` sendet es]. Bot rendert es längst [`puzzle/daily_results.py:141` `s.get('hintsUsed',0)>0`]. Fix = 1 Zeile `hintsUsed = s.HintsUsed` in der Solver-Projektion [Daten lagen in `BookSolverDto.HintsUsed` bereit]. Test erweitert/6 Webhook-Tests grün. Kein Bot-Update nötig. NICHT deployed); 0.196.1 (Discord-Solver-Webhook eigene Queue: Daily-Solve [book-puzzle 19207, User 5/kahalm] erschien nicht in Discord, weil der schach-bot-Webhook-Push [`BookPuzzleService.NotifySchachBotAsync` Z.99/188, `WeeklyPostService`] sich DIESELBE `IBackgroundTaskQueue` mit dem Chessable-Import teilte — bounded cap 100 + `DropOldest` + 1 serieller Consumer [[prod-chessable-import-stall-restart]]. Nach Redeploy hatte der ResumeService ~263 minutenlange Import-Jobs in die 100er-Queue geworfen → das Webhook-Ticket wurde verdrängt, bevor es lief [heute KEIN `Notify`-Log; gestern feuerte derselbe Webhook ohne Import problemlos]. Fix: neue `IWebhookTaskQueue`/`WebhookTaskQueue` [cap 256] + eigener `WebhookTaskWorker`, registriert in Program.cs; `BookPuzzleService`+`WeeklyPostService` enqueuen Webhooks jetzt dort [`BookPuzzleController`-Hint-Gen bleibt auf der allgemeinen Queue]. Test-Stubs [NoOp/Counting/Immediate] auf `IWebhookTaskQueue` gehoben. One-Shot-Webhook für das heutige Solve manuell signiert+gefeuert [HTTP 200]. +2 Tests/1029 grün. NICHT deployed); 0.196.0 (Repertoire-Trainer ohne „Weiter"-Knopf: nach richtigem/geduldetem Zug läuft der Trainer automatisch zur nächsten Stellung weiter [`ADVANCE_MS` correct 700 ms / tolerated 1800 ms, Timer in `onMove`, abgeräumt in `ngOnDestroy`/`setColor`/`buildQueue`/`next`]; geduldet zeigt die Repertoirezug-Visualisierung länger; Tippen aufs `.play` [`onPlayClick`] überspringt die Wartezeit; FALSCH behält den expliziten „Weiter"-Knopf. Neuer i18n-Key `repertoireTrainer.tapToContinue` en/de/hr + neuer Spec `repertoire-trainer.component.spec.ts` [4 fakeAsync-Cases]); 0.195.5 (VpnIpHealth Fehlalarm-Fix [piratechess v1.0.25]: Prod-Live-Daten zeigten gesunde Exit-IPs [37.46.199.54/.70/.86, alle DE-Tunnel, ≈2–5 % Gesamt-Block-Rate] als „WIEDERHOLT SCHLECHT" geflaggt — weil einzelne kurze Stints [1 Block von 2 Requests = 50 %] über `Vpn:BadStintRate`=0.4 lagen und sich ≥2 davon ansammelten. Neu: eine Phase zählt nur ab `Vpn:BadStintMinRequests`=5 Requests als „schlecht", und „wiederholt schlecht" hängt jetzt an der KUMULATIVEN Per-IP-Block-Rate [`Vpn:BadIpBlockRate`=0.15 über `Vpn:BadIpMinRequests`=50 Requests] statt an gezählten Mini-Phasen; WARN gedrosselt [1× bei Überschreiten, danach frühestens alle 20 Stints]; `IpStat.RecurringBad`-Verdikt, Snapshot sortiert wiederholt-schlechte zuerst dann nach Block-Rate. Prod fährt seit 2026-06-29 v0.195.4+v1.0.24 [Watchdog+Resume+Cooldown live, Import-Drain verifiziert]. +3 Testfälle/214 piratechess grün. NICHT deployed); 0.195.4 (Download-Lane-Nebenläufigkeits-Fix: Chessable-Importe luden gelegentlich ZWEI Kurse gleichzeitig, weil die „serielle" Download-Lane von zwei unabhängigen Treibern bedient wird — dem einzelnen `BackgroundTaskWorker` (Queue-Tickets) UND dem `ChessableImportWatchdogService` (ruft `RunNextAsync` an der bounded Queue VORBEI direkt auf). Der atomare Claim [[prod-chessable-import-stall-restart]] verhindert nur Doppel-Verarbeitung DESSELBEN Jobs, nicht, dass jeder Treiber einen ANDEREN wartenden Job claimt und parallel zieht (beobachtet 2026-06-29: Watchdog-Drive + Queue-Worker zogen Kurs 583468e8… und 69cc61bc… gleichzeitig). Neu: prozessweites `static SemaphoreSlim(1) _downloadLaneGate` in `ChessableImportService`; `RunNextAsync` (Download-Lane) acquired per `WaitAsync(0)` → ein zweiter Drive kehrt SOFORT zurück statt parallel zu laden, Body in `DrainNextAsync` ausgelagert; Fast-Lane bleibt ungated/nebenläufig. +1 Test [`RunNextAsync_DownloadLane_GateBlocksSecondConcurrentDrive`, deterministisch via blockierten Fetch], 1027 grün. NICHT gepusht/getaggt/deployed); 0.195.3 (Per-IP-Request/Block-Tracking [piratechess]: neuer Singleton `VpnIpHealth` akkumuliert je VPN-Ausgangs-IP Requests/Blocks über alle Rotationen [„Stint" = IP-Lebensdauer zwischen Rotationen, gemeldet via `VpnTunnel.FlushIpStint` beim Rotieren; `_currentIp`/`_ipRequests`/`_ipBlocks`]; eine IP mit ≥2 schlechten Phasen [Block-Rate ≥ `Vpn:BadStintRate`=0.4] wird als „WIEDERHOLT SCHLECHT" geloggt [WARN, strukturiert→Kibana je IP gruppierbar]; Debug-Endpoint `GET /api/chessable/direct/debug/ip-health` liefert die Per-IP-Tabelle [schlechteste zuerst]. +3 Tests/211 grün. NICHT deployed); 0.195.2 (Import-Robustheit [piratechess]: (1) hartes `--max-time` je Request [`Chessable:RequestMaxTimeSec`, Default 20s] in `BuildGetArgs` → hängende IP scheitert nach ~20s statt 75–120s, leerer Body → Soft-Block → retire/rotate; DrainTimeout 60→25s. (2) Per-Tunnel-Health+Cooldown in `VpnTunnel`: gleitendes Fenster der letzten N Ausgänge [`Vpn:HealthWindow`=8], blockt ein Tunnel ≥`Vpn:HealthBlockThreshold`=5 → `Vpn:CooldownSec`=120 aus dem Pool [`TryAcquire(respectCooldown)`, `RecordOutcome` via `RequestCompleted(bool)`, `VpnLease.onComplete` jetzt `Action<bool>`]; `AcquireAsync` Zwei-Pass [gesund zuerst, Notfall ohne Cooldown → kein Verhungern]. +2 Tests/208 grün. NICHT deployed [Prod läuft noch v1.0.23]); 0.195.1 (ETA-Durchsatz `CHESSABLE_LINES_PER_MIN` 25→40 [nach Rotate-on-Block-Speedup]; admin.component nutzt jetzt die Konstante statt hartem /25 in `dlEtaMin`/`estMinFromLines`); 0.195.0 (Admin-Kursdownload „Größe schätzen": on-demand pro Kurs Gesamt-Linienzahl + grobe Zeit vor dem Import. piratechess `POST /api/chessable/direct/course/info` {bearer,bid} → `DirectCourseInfoResponse` [gecacht=Cache-Summe ohne Chessable-Call, sonst `GetCourseLineCountAsync`=1× getCourse?includeVariations]. rookhub `ChessableProxyService.GetCourseInfoAsync` + Admin-Endpoint `GET admin/users/{userId}/courses/{bid}/estimate` [Bearer des Ziel-Users]. FE: `estimateCourseForUser`, `dlEstimate`/`dlEstimates`/`estMinFromLines` [~25/min], `straighten`-Knopf je Zeile → „≈N Linien · ~M Min/gecacht", i18n estimate/estimateError/cachedInstant/min. +2 Tests; FE 630/BE 1026/piratechess 206 grün); 0.194.0 (Chessable-Import: echte Gesamt-Linienzahl + ETA. piratechess getCourse `&includeVariations=true` → `Chapter.Total`/`Variations`, `FetchCourseDataAsync` meldet Variantensumme via `onTotalLines` → `CourseFetchJob.LinesTotal`/Snapshot → `DirectCourseProgressResponse.LinesTotal`. rookhub `ChessableImport.LinesTotal` [+Migration `AddChessableImportLinesTotal`] durch alle Progress-DTOs; FE `effectiveTotalLines` bevorzugt exakten Wert, `CHESSABLE_LINES_PER_MIN` 16,7→25, i18n `chessable.fetchProgressTotal`, Linien-X/Gesamt + ETA im User-Import + admin-Kursdownload [`dlEtaMin`]; FE 630/BE 1024/piratechess 206 grün); 0.193.2 (Chessable-Line-Fetch Rotate-on-Block [piratechess-Repo]: größter Importzeit-Fresher war der FIXE 30-s-Backoff bei Soft-Block [leere `{}`-Antwort, ~6 % der Zeilen → amortisiert ~1,8 s/Zeile ≈ 77 % der Wandzeit, bei nur 187 ms echtem curl]. Neu: `VpnRotationService` hält GENAU EINEN aktiven Tunnel sticky [statt round-robin pro Request]; `VpnLease.ReportBlocked()` [aufgerufen von `ChessableHttpService.CurlGetAsync` via `IsSoftBlockedBody`] retired die IP sofort → `VpnTunnel.RetireNow()` rotiert sie drain-aware im Hintergrund, Pool wechselt auf den nächsten, bereits ausgeruhten Tunnel [Ping-Pong; Rotation dauert lt. Prod-Log nur ~4 s ≈ Abarbeiten von 10 Zeilen]; Line-Retry-Backoff 30 s→`Chessable:BlockRetryDelayMs` [Default 1500 ms]; Inter-Request-Delay-Default 1000–2000→0–200 ms [Block ist requests-pro-IP-, nicht timing-getrieben]. Erwartung ~26→~100–150 Zeilen/min. piratechess-Commit 9071793, 206 Tests grün. OFFEN: Live-ENV in /opt/stacks setzen [ParallelLineFetches=1, InterRequestDelay 0/200] + Dev-Verifikation; NICHT deployed); 0.193.1 (Chessable-Import Fast-Lane: voll-gecachte Kurse [`ChessableImport.FullyCached`, gesetzt via `IsCourseCachedAsync` beim Anlegen in allen Pfaden — StartImport/Admin/`EnqueueReimportAsync`] laufen in eigener SERIELLER, netzfreier Lane [`ChessableImportFastLaneService`, parallel zur Download-Queue] statt hinter den langsamen Downloads; `RunNextAsync(ct, fastLane)` filtert fast=`FullyCached==true` / download=`!=true` [null→Download, kann nie hängen]; Watchdog download-lane-spezifisch; altes `RunDetached` entfällt; +Migration `AddChessableImportFullyCached`, +8 Tests/1024 grün); 0.193.0 (Trainingsziele-Zeitanzeige gestuft: neue pure `formatDuration(seconds, lang)` in `training-goals.component` + Methoden `durValue`/`durUnit` → < 120 min in Minuten, < 48 h in Stunden, sonst Tage [1 Nachkommastelle, locale-Dezimaltrenner via `Intl.NumberFormat`]; ersetzt `mins()`/`minutes()` in Breakdown-Rows [heute+Periode], Tageshistory-Tabelle und Chessable-Kurs-Summen [Daily-Goal-Paarung „done/target min" bleibt bewusst in Minuten]; neue i18n-Keys `trainingGoals.hours`/`.days` in ALLEN 25 Sprachen; +6 Spec-Cases); 0.192.1 (Admin-Kursdownload zeigt „Bereits in Warteschlange": `ChessableCourseDto.Queued` + `EnrichImportStateAsync` markiert Kurse mit laufendem Import [Status=running], FE-Branch im admin.component.html `@else if (c.queued)`, i18n `admin.courseDl.alreadyQueued` en/de/hr; +1 Test); 0.192.0 (Chessable-Import-Watchdog: neuer `ChessableImportWatchdogService` (BackgroundService) draint hängende Import-Queue ohne API-Neustart. Root-Cause des „eingeschlafen"-Vorfalls: `BackgroundTaskQueue` ist bounded (cap 100, DropOldest) + single-consumer + Abschluss reiht nicht nach → großer Import-Schwung verwirft Tickets, Jobs bleiben `running/queued` liegen. Watchdog prüft periodisch `IsDrainStalledAsync` (queued>0 && kein claimed/fetching/importing) und ruft dann `RunNextAsync` DIREKT (umgeht die bounded Queue); Startverzögerung 1min/Ruhe-Takt 2min/Busy-2s; +6 Tests/1015 grün. Siehe Memory [[prod-chessable-import-stall-restart]]); 0.191.0 (Reprocess-Banner zwei Knöpfe „Alle"/„Aus Cache": `ImportReprocessService.Reprocess{Courses,Repertoires}Async` + beide POST-Endpoints bekamen `localOnly`-Flag [`[FromQuery] bool localOnly`]; localOnly=true überspringt den Chessable-Re-Fetch [`continue`], bereitet nur aus serverseitig gespeicherter Quelle auf [Courses: SourcePgn; Repertoires: Nicht-Chessable-Versions-Mark]; Banner `allCount`=reprocessableLocally+refetchable, `cachedCount`=reprocessableLocally, Cache-Knopf nur wenn cachedCount<allCount; i18n reprocess.updateAll/updateCached/+Tips en/de/hr; +4 Tests); 0.190.1 (Discord-Invite-Link auf den richtigen Server korrigiert: `discord.gg/wczc4BJtMf` statt `nKQCdC7Xff` in `core/community.ts` [Konstante → Navbar/Footer/Mobil-Menü], README + Hilfetext en/de/hr + Changelog-0.187.0-Eintrag; beide Invites resolven aktuell auf Guild „Rookhub", neuer hat kein Ablaufdatum); 0.190.0 (Offline-Auto-Cache: Kurs öffnen (online) lädt das ganze Buch im Hintergrund offline [`autoCacheCourse` in book-puzzle.component, kein manuelles ☁ nötig]; Tagespuzzle wird beim Online-Abruf automatisch gecacht [`saveDailyOffline`/`getDailyOffline`, Key `rookhub_daily_offline`, letzte 14 Tage] + Offline-Read in `loadDaily`; Default-Offline-Pool 10→30 [`DEFAULTS.puzzleCount`]; +4 Specs/621 grün); 0.189.2 (Offline-Fix: `MenuService` cacht die Menü-Sichtbarkeit in localStorage [`rookhub_menu_keys`] + seedt das `visibleSubject` daraus → Flugmodus-Kaltstart zeigt nicht mehr nur Admin+Discord, sondern das zuletzt bekannte Menü; `fetch()`-catchError gibt Cache statt leerem Set); 0.189.1 (Mobile-Navbar-Fix: Sekundär-Icons [Discord/Theme/Sprache] wandern auf Mobil ins Hamburger-Menü `nav-extra`+navMenu, damit Toolbar nicht überläuft & Profil-Icon sichtbar bleibt); 0.189.0 (Repertoire-Trainer/geduldete Züge: `ImportPipeline.CurrentVersion` 2→3 [softFail-`[%alt]` jetzt im piratechess-Export]; Reprocess-Banner auf der Repertoire-Seite bietet für Chessable-Repertoires jetzt einen echten **Re-Fetch** an statt No-op-Versions-Mark — `ImportReprocessService.GetRepertoireStatusAsync`/`ReprocessRepertoiresAsync` melden Chessable-Repertoires [bid aus `ChessableCourseId` ODER Dateiname `chessable-{bid}.pgn`] als `Refetchable` und reihen `EnqueueReimportAsync(..., target:"repertoire", targetRepertoireId)` ein; neuer `ChessableImport.TargetRepertoireId` + Migration → `ImportAsRepertoireAsync` ersetzt das PGN **in-place** im bestehenden Repertoire [Id/Trainings-Fortschritt bleiben], Nicht-Chessable nur Versions-Mark; +3 Tests/1007 grün); 0.188.0 (Discord-Link prominent in der Fußzeile [Discord-Logo via `MatIconRegistry` + Markenfarbe #5865F2, neben Hilfe/Feedback, Konstante `core/community.ts`; Footer nur Desktop, mobil im Nav-Menü]); 0.187.0 (Discord-Community-Link überall eingebunden: Discord-Button in Navbar [eingeloggt + ausgeloggt] + Mobil-Menü [SVG-Icon via `MatIconRegistry`, Link `https://discord.gg/nKQCdC7Xff`, zentrale Konstante `core/community.ts`], Einladungs-Satz im Discord-Abschnitt der Hilfeseite [en/de/hr], `nav.discord`-Tooltip in allen 25 i18n-Sprachen, READMEs aller Repos); 0.186.1 (Chessable-Rate-Limit-Fix); 0.186.0 (Repertoire-Trainer/Spaced Repetition); 0.185.0 (Admin-Benachrichtigung bei Neu-Registrierung: neuer Notification-Typ `new_user_registered` → Glocke aller Admins [`CreateManyAsync`, Link „/admin", Daten `{username}`]; `AuthService` injiziert optional `NotificationService` und benachrichtigt best-effort nach erfolgreichem Register; Icon `group_add`; i18n en/de/hr); 0.184.39 (Offline-Fix: Kurse offline startbar — erstes Puzzle aus lokalem Cache statt Server [offline gespeicherter Kurs via ☁-Knopf, sequenziell/zufällig], Versuche werden gequeued/synchronisiert; Nicht-Admins offline nicht mehr von der Kursseite ausgesperrt); 0.184.38 (Chessable-Diagnose [piratechess-Repo]: `ClassifyBlockedResponse` unterscheidet jetzt abgelaufenen/ungültigen Token [lokal am JWT-`exp`-Claim erkannt → „Bearer neu hinterlegen"] von einem Cloudflare-403-Block bei noch gültigem Token [→ „VPN-Ausgangs-IP gesperrt, IP rotieren/Server wechseln"]; `IsCloudflareBlockPage` erkennt Block-Marker; Hintergrund: M247-IPs [AS9009] werden von Chessable geblockt, Netrouting [AS6206] nicht; piratechess-Commit 32a2f83, +7 Tests/175 grün); 0.184.37 (Chessable-Fix [piratechess-Repo]: HTML-statt-JSON-Antwort [abgelaufener/ungültiger Bearer bzw. Cloudflare-Block/Proxy-Gateway → Chessable liefert eine HTML-Seite] wird in `ChessableHttpService.GetCoursesAsync`/`FetchCourseDataAsync` jetzt sauber erkannt [`LooksLikeHtml`] und als sprechender Token-Hinweis gemeldet, statt den rohen JSON-Parser-Text „'<' is an invalid start of a value" bis in die rookhub-UI durchzureichen; +JsonException-Catch ohne Leak; piratechess-Commit c1cc507, +7 Tests/168 grün); 0.184.36 (Crawler-Härtung [Crawler-Repo]: `ApiKeyMiddleware` fail-closed bei leerem `API_KEY` in Production [503 statt offen], Dev-Fallback + Liveness bleiben; Crawler-Commit 4ca4feb); 0.184.35 (BotStats-Endpoint Replay-Schutz: `GET /api/bot/player-progress` akzeptiert optionalen `X-Bot-Timestamp` [±300 s, HMAC über `<ts>.<discordId>`]; rückwärtskompatibel zur alten body-only-Signatur; Gegenstück zu Bot v2.73.0); 0.184.34 (Webhook-Timestamp-Replay-Schutz auf der rookhub-Sendeseite: alle drei Bot-Webhooks [Tagespuzzle/Wochenpost/Daily-Regenerate] signieren jetzt zusätzlich einen Zeitstempel [`X-Webhook-Timestamp`, HMAC über `<ts>.<body>`], ±300 s; Gegenstück zu Bot v2.70.0, rückwärtskompatibel); 0.184.33 (Crawler-Robustheit [Crawler-Repo]: Freilos/spielfrei nur informativ statt Warnung, defensives Response-Größenlimit `Crawler:MaxResponseBytes`, Hidden-Field-Parsing Regex→AngleSharp, Player-/Team-Upsert in DB-Transaktion; Crawler-Commits c518e74/cf6b5a9/7522f3a/bc59f31); 0.184.32 (A11y tournament-favoriten: Favoriten-Sterne [Spieler+Team, Tabelle+Mobil-Karte] in tournament-detail+public-tournament tastaturbedienbar — `role=button`/`tabindex=0`/`keydown.enter`+`space`/`aria-label`+`aria-pressed`/`:focus-visible`; i18n `tournaments.favorites.toggleAria` en/de/hr); 0.184.31 (BasePuzzleSolver-Dedup: `formatTime` → gemeinsame `puzzle-format.util.ts` [`formatPuzzleTime`], und Einzel-Stoppuhr-Timer [`elapsedSeconds`/`stopwatch`/`startTimer`/`stopTimer`] aus puzzle+book in die Basis hochgezogen; Endless erbt `elapsedSeconds`+`formatTime`, behält seine Doppel-Stoppuhren; +2 Specs, Verhalten unverändert); 0.184.30 (OnPush für 4 weitere präsentationale Puzzle-Karten: puzzle-your-turn/-status-card/-rating-card/viz-card — alle nehmen nur primitive Inputs [Eltern rebinden je CD] + EventEmitter-Outputs, in-place-Mutation geprüft = keine; +Spec); 0.184.29 (Admin-Kleinkram: chessable-Bookmarklet-`bypassSecurityTrustUrl` mit Origin-Guard+Sicherheits-Kommentar [Code rein app-konstruiert]; Admin-Mitglieder-Dropdown warnt bei `totalCount > 500` statt still abzuschneiden; `availableUsers()` war bereits memoisiert [v0.184.19]; +2 Specs); 0.184.28 (chessable.component: `activeImports`-Zeilen cachen ihr `queueLabelText` jetzt einmal je Update [`setActiveImport`] statt `queueLabel(imp)`-`translate.instant` je CD-Zyklus während des Pollings; +2 Specs); 0.184.27 (Anon-Session-IDOR-Härtung: `ValidationConstants.SessionIdPattern` Mindestlänge 1→32 [UUID-Form 32–36], erratbare Kurz-Ids können fremde anonyme Puzzle-/Endless-Stats nicht mehr claimen/überschreiben; Clients nutzen ohnehin `crypto.randomUUID()` → rückwärtskompatibel); 0.184.26 (FriendService.SearchUsersAsync: Identitäts-/Konto-Felder [Username/chess.com/Lichess/FIDE/ChessResults] präfix-anker [`StartsWith`, Username-Index nutzbar], nur DisplayName bleibt Teilstring; Länge+Take service-seitig hart gekappt); 0.184.25 (Chessable-Import: atomarer Claim beim Job-Picking via `ExecuteUpdate` „queued"→„claimed", InMemory-Re-Check-Fallback → keine Doppelverarbeitung bei Resume-Sturm/Skalierung); 0.184.24 (Schnellstart-Popup nach Register erklärt jetzt die Puzzle-Modi statt Turnier-Tipps: Zufalls-Puzzle/Endlos/Tagespuzzle/Wochenpost; `app.qs.*`-Keys in de/en/hr ersetzt, Icons 🎲/♾/📅/📰); 0.184.23 (Crawler-Robustheit gegen Redeploy/VPN-Aussetzer [Crawler-Repo]: `VpnReadinessGate` wartet vor dem ersten Crawl auf den wiederhergestellten gluetun-Tunnel [`Gluetun__WaitForReady=true` in beiden VPN-Compose-Dateien], und `ExecuteCrawlAsync` versucht reine Verbindungsfehler [`IsTransientConnectionError`, z. B. „Resource temporarily unavailable"] mit gestuftem Backoff erneut statt sofort `Failed`; Retry-Parameter via `Crawler:CrawlMaxAttempts`/`RetryDelayMs`/`CrawlRetryBackoffSeconds` konfigurierbar; behebt die Fehler-Häufung direkt nach Deploys); 0.184.22 (Frontend Service-Layer-Extraktion: `FriendsService`/`PublicTournamentService`/`ProfileService` + `RepertoireService`-Erweiterung, 9 Komponenten ohne direkten `HttpClient`; `AuthService.changePassword`; OnPush für review-nav/promotion-picker; +5 Service-Specs); 0.184.21 (Crawler-Härtung: `/api/health/ip` API-Key-pflichtig + Phantom-Runden-Clamp gegen fremde `rd=`-Links; Crawler-Repo-Commits f5071aa/052007b); 0.184.14–0.184.20 (TODO-Abarbeitung Runde 3: DataProtection-Keys konfigurierbar/anlegen/SetApplicationName · In-App-Benachrichtigung bei neuer Turnierrunde [`NotificationType.TournamentNewRound`] · Service-Extraktion Repertoire/Tournament-List/Dashboard [+Specs] · Admin-Tab in URL [`?tab=`] + availableUsers gecacht + admin.component-Spec · A11y puzzle-tags/repertoire-tree/-lines + OnPush für präsentationale Komponenten); 0.184.9–0.184.13 (Runde 2: JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source) JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source); 0.237.3 (Dashboard-Edit-Steuerpille [Ziehen/Hoch/Runter/Auge] richtig dimensioniert: die sichtbaren Buttons waren auf 30 px geklemmt, aber Angular Material 19s mat-icon-button hält intern via MDC einen 48-px-State-Layer/Touch-Target — die Klick-Flächen lagen über die Icons hinaus und überlappten die Nachbarknöpfe. Neu: die Material-3-MDC-Custom-Properties --mdc-icon-button-state-layer-size:32px + --mdc-icon-button-icon-size:18px gesetzt statt nur width/height [Pille 30→32 px, padding:3px, dezenter 1-px-Border]; deaktivierte Pfeile bekommen opacity:.35, Griff-Icon opacity:.7. NICHT deployed); 0.237.2 (Kurs-Lesereihenfolge nach `Round` statt `Id`: Intro-/Info-Linien, die durch einen spaeteren Re-Fetch ergaenzt wurden [hohe DB-Id, aber kleines Chessable-Round, z. B. Intro von 100 Tactical Patterns Round 002.002], erschienen hinter den ersten Puzzles statt ganz vorne. `CourseService` sortiert Kapitel [`GetOrderedChapterNamesAsync` MinRound], Sequenz-Cursor [`GetNextAsync` cursor-sicher in-memory ueber (Round,Id)], `GetChaptersAsync`, `GetAllPuzzlesAsync` und den PGN-Export-Fallback jetzt nach `Round` [dann Id]; `BookPuzzleService.GetNextInBookAsync` ebenso. SQL macht nur ORDER BY/MIN(Round) [Standard], String-Cursor-Vergleich in-memory. Kein Migration/Re-Import noetig, wirkt fuer alle Kurse. +4 BE-Tests [CourseServiceReadingOrderTests, 189 Course/BookPuzzle gruen]. NICHT deployed [Round-SQL-Sortierung auf Dev gegen MariaDB verifizieren]); 0.237.1 (Dashboard-Kachel „Angepinnte Kurse" aufgehübscht: je Kurs `mat-progress-bar` [wie Kurse-Seite], Sequenziell = `mat-flat-button color=primary` [primär], Zufällig = `mat-stroked-button` [sekundär, Rahmen], beide `flex:1` volle Breite; bei 100 % gelöst 🏆-Icon neben `progressPercent`; größere Zeilen-Trennlinien+Gaps [0.6→0.9rem/0.25→0.5rem]. `MatProgressBarModule` importiert. Nur Template+Styles+kleine i18n-lose Umbenennung `.pc-info`→`.pc-header`. NICHT deployed); 0.237.0 (Kurse fürs Dashboard anpinnbar + direkt startbar: neue per-User-Tabelle `CoursePin` [UserId+BookId unique, +Migration `AddCoursePins`, Cascade User/Book, Cleanup in `BookAdminService.DeleteBookAsync`], `CourseListItemDto.IsPinned` [in `GetCoursesAsync` via Pin-Set gesetzt], Endpoints `POST/DELETE /api/courses/{bookId}/pin` [`CourseService.PinCourseAsync`/`UnpinCourseAsync`, idempotent, EnsureAccess]. FE: 📌-Toggle je Kurs in der Kursliste [optimistisch+Rollback], neue Dashboard-Kachel `pinnedCourses` [in DEFAULT_VISIBLE vor `courses`; eligible nur wenn ≥1 Pin bzw. im Edit-Modus] mit je Kurs zwei Start-Buttons Sequenziell/Zufällig; Dashboard nutzt das ohnehin geladene `/api/courses` [jetzt typisiert `DashboardCourse` inkl. `isPinned`] mit. i18n `courses.pin*`/`dashboard.pinnedCourses.*` en/de/hr. NICHT deployed); 0.236.18 (Admin-CI Push statt Pull: neuer Endpoint `POST /api/ci/gh-webhook` [`CiBuildReportController.GithubWebhook`, AllowAnonymous, verifiziert GitHubs `X-Hub-Signature-256` HMAC gegen `CI:GithubWebhookSecret` ODER Fallback `CI:BuildReportSecret`] empfängt GitHub-`workflow_run`-Events → `GithubActionsService.ReportRun` [statischer `_pushedRuns`-Cache je Repo, ~20 jüngste]. `OverlayPushedRuns`/`OverlayPushedRepo` legt die Push-Läufe bei JEDEM Overview-Abruf LIVE über die (gecachten) GitHub-Daten [Merge per Run-Id, Push gewinnt, Workflow-Filter, neueste 5] — auch im Einzel-Repo-Abruf [👁]. FE `NORMAL_MS` 2min→20s [RookHub-Cache statt GitHub]. Webhook je Repo unter Settings→Webhooks eintragen [Event „Workflow runs", Secret = CI_BUILD_REPORT_SECRET-Wert]. +5 BE-Tests [Overlay + Webhook-Sig; GithubActionsServiceTests+CiBuildReportControllerTests in Collection „CiStaticState" serialisiert wg. statischer Caches]. NICHT deployed); 0.236.17 (Perf: `CourseService.GetCoursesAsync` — die Puzzle-Anzahl je Buch [`PuzzleCount`] lief als korrelierte Unterabfrage pro Buch [`b.Puzzles.Count(!IsInfoOnly)` im Select] → bei Admins mit ~260 Büchern sekundenlang. Jetzt EINE gruppierte Query [`BookPuzzles.Where(!IsInfoOnly).GroupBy(BookId)` → Dict]; Mapping zieht die Zahl aus dem Dict. Verhalten/Zahlen unverändert [67 Course-Tests grün]. NICHT deployed); 0.236.16 (Chessable-Importe sind jetzt standardmäßig NICHT von der RepCheck-Extension nutzbar: `ChessableImportService.ImportAsRepertoireAsync` setzt beim Anlegen `UseForExtension=false` [`CreateRepertoireDto`]. Manuell angelegte Repertoires unberührt [Create-Dialog-Default bleibt true]. +1 BE-Test-Assert. Prod-Datenfix separat: kahalms 133 Repertoires alle auf `UseForExtension=0`. NICHT deployed); 0.236.15 (Admin-CI Poll-Umbau: normaler Voll-Abruf jetzt alle 2 min statt 5 s [`NORMAL_MS`], gegen GitHub-Rate-Limit. Neuer per-Repo-👁-Knopf [`toggleWatch`/`isWatching`, `.watch-btn`]: schnell-pollt NUR dieses eine Repo alle 10 s [`WATCH_MS`] für 1 min [`WATCH_WINDOW_MS`], bei laufender Aktion rollend bis zu deren Ende [`WATCH_RUNNING_EXTEND_MS`, `watchTick`]. Neuer BE-Endpoint `GET /api/admin/ci/runs/{repo}` [`GithubActionsService.GetRepoAsync`, ungecacht, Repo-Whitelist; Merge-Logik in `MergeRunningAsync` extrahiert, von Voll- + Einzel-Abruf geteilt]; FE `loadRepo` mergt das Einzel-Ergebnis in die Übersicht. i18n `admin.ci.watchStart/watchStop` + Subtitle en/de/hr. +2 BE-Tests. NICHT deployed); 0.236.14 (Admin-CI-Seite: die „Deployt: …"-Zusammenfassungszeile im Kopf [gelbe Chips je Stack, `.ci-deployed`] entfernt — inkl. `deployed`-Feld + dessen Berechnung in `recomputeEta` + CSS; die gelbe Zeilen-Hervorhebung des laufenden Builds [`.run-deployed`/`isRunningBuild`] bleibt. NICHT deployed); 0.236.13 (CI-Fix: flakiger `GithubActionsServiceTests.LogWatcher_FiltersOutTestRuns`-Test [aus dem 0.236.11-Feature], der je nach Testreihenfolge den ganzen `test-rookhub`-Job rot machte → kein `:latest`-Image. Ursache: prozessweiter **statischer** `_reportedBuilds`-Cache leckte über Testklassen [`CiBuildReportControllerTests` meldet „log-watcher" → im Overview-Test feuerte der „6.-Zeile-nachladen"-Pfad und der naive Stub lieferte den Run doppelt → `Assert.Single` sah 2]. Fix: interner `ResetReportedBuildsForTests()`-Hook, im `GithubActionsServiceTests`-Ctor vor jedem Test geleert. Produktcode unverändert [`FetchRunByShaAsync` filtert in Prod echt nach `head_sha`]. NICHT deployed); 0.236.12 (RepCheck „Remember line": echter Kursname über den Chessable-Bearer. Neues `RememberedPosition.CourseName` [+Migration `AddRememberedPositionCourseName`], `RememberLineInputDto`/`RememberedPositionDto` um `CourseName`. `RememberedPositionService` löst den Namen auf: von der Extension mitgeliefert [Vorrang] → sonst cache-first aus `ChessableCredential.CachedCoursesJson` → sonst best-effort Live-`GetCoursesAsync` [try/catch, Merken scheitert nie am Kursnamen]. `ListAsync` backfillt Alt-Einträge ohne Namen aus dem Cache [rein in-memory]. +7 BE-Tests [RememberedPositionServiceTests] + ExtensionControllerTests-Ctor angepasst. Extension-Seite [repcheck ≥1.19.0] löst den Namen via erfasstem Bearer + Chessable-getHomeData auf. NICHT deployed); 0.236.11 (Admin-CI: Workflow-Filter je Repo [`GithubActionsService`]: `DefaultWorkflowFilter` [log-watcher→„Build & Push"], überschreibbar via `GitHub:WorkflowFilter:<repo>`; `FetchRepoAsync` lädt `RawRunsPerRepo`=20 statt 5 und filtert per `MatchesWorkflowFilter` [Substring, case-insensitiv] vor `Take(5)`, `FetchRunByShaAsync` ebenso [dieselbe SHA triggert bei log-watcher „Tests" UND „Build & Push"] → log-watcher zeigt nur noch Build-Läufe, keine Test-Läufe. +1 BE-Test [GithubActionsServiceTests]. NICHT deployed); 0.236.10 (Admin-CI: der deployte Build-Lauf ist wieder mit GELBEM Zeilen-Hintergrund markiert [`.run.run-deployed`, `[class.run-deployed]="isRunningBuild(run,repo)"` + gelber Border-Left], nicht nur dem Icon-Badge — 0.236.5 hatte die Zeilenfarbe entfernt, User wollte aber nur ein anderes Icon, nicht den Wegfall des Highlights. NICHT deployed); 0.236.9 (Admin-CI, drei Ergänzungen: (1) laufender Build zeigt prominenten Live-Countdown im Panel-Kopf [`.ci-eta`, BLAUE Plakette `timer`-Icon, z. B. „schach-bot · fertig in ~2m 10s"]; ETA = `avgDurationSec` [Mittel der abgeschlossenen der letzten 5 Läufe je Repo, created→updated] − verstrichene Zeit; 1-s-Ticker [`nowMs`] treibt den Countdown, `recomputeEta` [ETA+deployed] bei 1-s-Tick + 5-s-Refresh. (2) prominente „Deployt"-Übersicht im Kopf [`.ci-deployed`, GELBE Chips je Stack = Repo + Ref des via `isRunningBuild` erkannten laufenden Runs]. (3) Farbschema: BLAU=baut [`.run`-Badge + ETA], GRÜN=fertig gebaut [`.ok`], GELB=deployed [`.run-live-tag` + `.ci-deployed`]; Repo-Icon `folder_open`→`code`. i18n `admin.ci.etaIn/etaSoon/etaUnknown/etaTooltip/deployed/deployedTooltip` en/de/hr. NICHT deployed); 0.236.8 (Dashboard-Kurse-Kachel zeigt jetzt die Kurs-Anzahl [dashboard.courses.count "{{count}} Kurse"] wie die Repertoires-Kachel: neuer courseCount, DashboardService.getCourses [GET /api/courses], im forkJoin geladen. +2 FE-Tests, i18n en/de/hr. NICHT deployed); 0.236.7 (Admin-CI: „live"-Badge des laufenden Builds klar vom grünen Erfolgs-Häkchen abgehoben — `.run-live-tag` jetzt BLAU (#1565c0) + Icon `fiber_manual_record` (live-Punkt) statt grün/`play_circle` [`admin-github-actions.component`]. Rein visuell. FALLE: zeigt die CI-Seite bei ALLEN Repos „HTTP 403", liegt das am GitHub-PAT [`GITHUB_TOKEN` in der rookhub-.env] — abgelaufen oder ohne `Actions: Read-only`-Permission —, NICHT am Code [UA/Accept/Api-Version im HttpClient sind gesetzt]. NICHT deployed); 0.236.6 (Repertoire-Reprocess: (a) nach erfolgreichem Chessable-Re-Fetch werden ALLE Geschwister-Repertoires desselben Users mit derselben bid mit-aktualisiert [ChessableImportService.BumpSiblingRepertoiresAsync in ImportAsRepertoireAsync] — vorher blieben Kurs-Duplikate veraltet, weil der (Owner,bid)-Dedup den Kurs nur 1x holt (Banner ging nie weg). (b) GetRepertoireStatusAsync/ReprocessRepertoiresAsync sind jetzt admin-weit [isAdmin -> alle User statt nur eigene; RefetchCandidate.OwnerId = r.UserId -> je Owner-Bearer]; RepertoireController reicht IsAdmin durch. +2 BE-Tests. NICHT deployed); 0.236.5 (0.236.5 (Admin-CI: laufender Build nicht mehr per Zeilenfarbe markiert, nur noch eigenes live-Icon-Badge [run-live-tag; .run-live-Fill entfernt]; faellt der laufende Build aus den Top-5 raus, wird er per GitHub ?head_sha= nachgeladen + als Zusatzzeile angehaengt [GithubActionsService.FetchRunByShaAsync + RunMatchesBuild + MapRun extrahiert]. Server ermittelt jetzt auch rookhubs eigene laufende SHA aus der Frontend-build-info [http://rookhub:8080/build-info.json, Frontend:BuildInfoUrl], isRunningBuild bevorzugt repo.runningSha [client-buildSha nur Fallback]. + Push-Modell-Empfangsseite: POST /api/ci/build-report [CiBuildReportController, AllowAnonymous + X-Build-Report-Key==CI:BuildReportSecret] -> GithubActionsService.ReportBuild [statischer _reportedBuilds-Cache, in ResolveRunningBuilds gemergt]. +3 BE-Tests. OFFEN: log-watcher-Client (Push). NICHT deployed); 0.236.4 (0.236.4 (Repertoire-Trainer falscher Zug: (1) "Losung zeigen" spielt den erwarteten Zug jetzt auf dem Brett [fen nach expected + lastMove-Markierung], statt ihn nur im Text zu nennen; (2) nach falschem Zug SOFORT erneut ziehbar - onMove akzeptiert einen Retry im wrong-FEEDBACK, Zug wird sofort auf startFen zurueckgenommen, Brett bleibt spielbar [viewOnly nur wenn nicht wrong-unrevealed], kein Mausrutscher-Klick noetig; Mouseslip-Button entfernt [Retry ersetzt ihn]. +Specs angepasst. NICHT deployed); 0.236.3 (0.236.3 (Admin-Kursdownload: Schalter "Geladene Kurse ausblenden" [dlHideLoaded + Getter dlVisibleCourses] blendet Kurse aus, die schon als Repertoire/Buch importiert sind [ChessableCourseDto.ImportedRepertoire/ImportedBook, bereits vorhanden]; mat-slide-toggle + i18n admin.courseDl.hideLoaded/allLoaded en/de/hr. +1 FE-Test. NICHT deployed); 0.236.2 (0.236.2 (Frontend erkennt neue Versionen schnell: app.component sucht aktiv beim Start, alle 15 min und bei visibilitychange->sichtbar via swUpdate.checkForUpdate() [neue Methode checkForAppUpdate, fehlertolerant] - lange offener Tab/PWA haengt nicht mehr auf altem Build bis manuellem Reload; VERSION_READY-Neu-laden-Snackbar wie gehabt. Spec: SwUpdate-Mock um checkForUpdate ergaenzt. + Bot v2.77.0: Monats-Endstand am 1. im Tagespuzzle-Thread statt Standalone-Post. NICHT deployed); 0.236.1 (0.236.1 (Zwei Prod-Log-Fixes nach Watcher-Alerts: (1) Weniger Fehlalarme: der anonyme Endless-Progress-Upsert [`EndlessProgressService.SaveAnonymousProgressAsync`] läuft bei gleichzeitigen Saves in eine `IX_EndlessProgresses_AnonymousSessionId`-Unique-Race — war bereits abgefangen+retried, aber EF Core loggte `SaveChangesFailed` trotzdem auf Error → triggerte den Log-Watcher grundlos. Jetzt `ConfigureWarnings` in Program.cs: `CoreEventId.SaveChangesFailed`→Information [genuine, NICHT abgefangene Fehler bleiben übers HTTP-500-Request-Log sichtbar; betrifft alle bewusst abgefangenen DbUpdateException-Races: CoursePuzzleResult/CourseInfoView/CourseProgress/…]. (2) `POST /api/courses|repertoires/reprocess` lief bei vielen Chessable-Kursen über das ~60-s-Request-Timeout → 500 „operation was canceled". Neu: `IReprocessLauncher`/`ReprocessLauncher` [Singleton, eigener DI-Scope, `Task.Run` mit `CancellationToken.None`, Fehler nur geloggt] startet den Reprocess im Hintergrund; beide Endpoints geben sofort `202 Accepted {started:true}` zurück [nicht mehr synchron/`ct`-gebunden]. FE `ReprocessBannerComponent.run` erwartet keine Counts mehr → Snackbar `reprocess.started` [en/de/hr] + Status-Refresh nach 2,5 s. +Test-Doppel `RecordingReprocessLauncher`, reprocess-banner-Spec auf fakeAsync/202 umgestellt. NICHT deployed); 0.236.0 (Admin-CI markiert das laufende Image jetzt auch für crawler/piratechess/bot, nicht nur rookhub-Frontend: jeder Dienst hat einen build-info-Endpoint [crawler `GET /api/health/build-info`, piratechess `GET /api/chessable/direct/build-info` (ServiceKey), bot `GET /webhook/build-info`], gespeist aus CI-Build-Args `GIT_SHA`/`GIT_REF`→ENV. `GithubActionsService.ResolveRunningBuildsAsync` pollt sie parallel [best-effort, 4s-Timeout, im 4s-Overview-Cache], `CiRepoDto.RunningSha/RunningRef`; FE `isRunningBuild(run, repo)` nutzt für Fremd-Stacks repo.runningSha/ref [rookhub weiter client-seitig], Match per SHA+Ref → :dev=master-Run, :prod=Tag-Run. Nicht erreichbar/altes Image = kein Marker. +2 BE-Tests [GithubActionsService], +1 FE-Test [Fremd-Stack], +Crawler-/piratechess-/Bot-Endpoint-Tests. NICHT deployed); 0.235.1 (0.235.1 (Admin-CI markiert genau den EINEN Run, der das laufende Image baute: build-info.json trägt jetzt zusätzlich den Build-Ref [Branch bei :dev, Tag-Name bei :prod; docker.yml `GIT_REF=github.ref_name`, Dockerfile `ARG GIT_REF`, `{sha,ref}`]. master-Push + gleichnamiger Tag haben dieselbe SHA → reines SHA-Matching markierte beide; `isRunningBuild` verlangt jetzt SHA UND [sofern gemeldet] Ref-Gleichheit → :dev hebt master-Run, :prod den Tag-Run hervor. Altes Image ohne Ref = SHA-only-Fallback. +4 FE-Tests [neues admin-github-actions.component.spec]. NICHT deployed); 0.235.0 (Zwei Kurs-/Buch-Verbesserungen: (1) Sequenziell durchgeklickte Info-/Erklärlinien werden gemerkt [neue Tabelle `CourseInfoView` + Migration `AddCourseInfoView`, Endpoint `POST /api/courses/{bookId}/info-seen`, `CourseService.MarkInfoSeenAsync`; seqPool überspringt gesehene Info-Linien via `CourseInfoViews` statt sie IMMER zu zeigen; FE `courseNext` meldet die aktuelle Info-Linie + merkt sie offline in `offlineCourseSolvedIds`; beim Reset geleert; Cleanup in `BookAdminService.DeleteBookAsync`]. (2) Doppelte Kommentar-Anzeige entdoppelt: `puzzle.comment`-Kopfzeile [Template Z.88] UND `move-comment-card` [unten] ersetzt durch EINEN `ctx-comment`-Block in der Kontext-Card ÜBER der Fortschrittsanzeige, gespeist vom neuen Getter `displayComment` [Review/Info→moveComment mit Fallback auf puzzle.comment, sonst puzzle.comment] — gilt für alle Modi, nicht nur Info-Linien. +4 BE-Tests [1156] +5 FE-Tests. NICHT deployed); 0.234.1 (piratechess-Fix [Repo bb74686]: TryGetChessableErrorMessage fällt bei leerem error.message auf userFriendlyErrorMessage zurück → nicht besessene Kurse melden ehrlich 'You do not own this course.' (BOOK_NOT_OWNED) statt fälschlich 'Course has no chapters'. +1 Regressionstest. [Analyse: die 4 'no chapters'-Kurse des Re-Fetchs gehören dem aktuellen Bearer-Account uid 790927 nicht]); 0.234.0 (Navigation läuft jetzt IMMER über das Hamburger-Menü — auf allen Auflösungen, nicht nur mobil [`navbar.component`: `.nav-links` generell `display:none`, `.mobile-menu-btn` generell `inline-flex`; der 768px-Media-Query blendet nur noch die `.nav-extra`-Sekundär-Icons aus]. Bei den vielen Menüpunkten lief die Toolbar sonst auch auf mittleren/großen Screens über. NICHT deployed); 0.233.2 (Zwei Anarchy-Politur-Punkte: (1) Setup-Zug-Pfeil-Fix — der erste (automatische) Gegnerzug, der die Puzzle-Stellung aufbaut, bekommt jetzt auch den Gegnerzug-Pfeil [`showVizOpponentArrow()` im Setup-Timeout von `BasePuzzleSolver`, nach `playMove(setupMove)`]; vorher nur die Antwortzüge [advanceAfterCorrectMove/opponentRespond], sodass genau der stellungserzeugende Zug unmarkiert blieb — besonders auf dem Crazy-Brett störend. Gilt für alle Modi [+1 base-Solver-Test]. (2) Anarchy-Hints [`puzzles.anarchyHint1-3` en/de/hr] memefiziert [Holy hell / Google en passant / New response just dropped]. NICHT deployed); 0.233.1 (Mobil-Fix: Dashboard beim Anpassen wieder scrollbar — im Bearbeitungsmodus war die GANZE Kachel die Drag-Fläche [`.tile-shield` = `cdkDragHandle` über `inset:0`], sodass auf dem Touchscreen jede Berührung einen Drag auslöste statt zu scrollen. Gezogen wird jetzt nur noch am dedizierten Griff [`drag_indicator`-Icon je Kachel als `cdkDragHandle` in `.tile-controls`], der Kachel-Rumpf scrollt normal; Sortieren via ↑/↓-Pfeile bleibt. `.tile-shield` ist kein `cdkDragHandle` mehr [nur noch Klick-Fänger]. i18n `dashboard.edit.dragAria` wiederverwendet. NICHT deployed); 0.233.0 (Admin-CI-Run-Zeile aussagekräftiger: Ref (Branch/Tag-Name mit Icon; Tag-Läufe haben kein head_branch → GithubActionsService löst Tags per /tags sha→Name-Map auf, CiRunDto.Ref/IsTag), Startzeit (createdAt), Laufzeit (created→updated bzw. live bis jetzt), Titel bleibt Link zum Run (keine sichtbare URL). i18n admin.ci.started/duration. Tags-Abruf = +1 GitHub-Call/Repo (gecacht); Cache-Test angepasst); 0.232.0 (Admin-CI-Seite markiert den GitHub-Actions-Run des LAUFENDEN Frontend-Builds: Frontend-Dockerfile schreibt Commit-SHA (ARG GIT_SHA aus docker.yml --build-arg github.sha) nach /build-info.json; CI-Seite holt das + markiert den Run mit passendem head_sha grün ('live'). CiRunDto/GithubActionsService um HeadSha erweitert. Greift erst nach Deploy eines so gebauten Images; nur rookhub-Frontend meldet Build-SHA. +1 BE-Test-Assert); 0.231.0 (Repertoire-Reprocess auf denselben Stand wie Kurse gebracht + Zentralisierung: die gemeinsame Re-Fetch-Logik [Batch-Cache-Abruf 1×, kein Abbruch am Request-ct, Backoff für nicht-cachebare Kurse, Dedup, Admin-`trustOwnership`] liegt jetzt in EINER Methode `ImportReprocessService.EnqueueRefetchesAsync(candidates, isAdmin, result)` [+ `RefetchCandidate`-Record]; `ReprocessCoursesAsync`/`ReprocessRepertoiresAsync` sammeln nur noch Kandidaten. `ReprocessRepertoiresAsync` bekam `isAdmin`-Param [Default false], `IsAdmin` in `BaseApiController` hochgezogen [aus CourseController], RepertoireController reicht es durch. Vorher hing „Alle aktualisieren" bei Repertoiren [alter langsamer/blockierender Pfad]. +1 BE-Test [1152 grün]. NICHT deployed); 0.230.0 (Crazy-Modus Gegnerzug-Pfeil: der Gegnerzug wird auf dem Crazy-Brett mit einem GRÜNEN Pfeil markiert, der bis zum nächsten User-Zug stehen bleibt. BasePuzzleSolver: neuer overridebarer opponentArrowMode ['off'|'timed'|'persist']; showVizOpponentArrow setzt bei 'persist' keinen 1s-Timer, onMoveMade löscht den Pfeil beim User-Zug. Komponenten: crazy→'persist'. Board: applyVizOpponentArrow ohne visualization>0-Guard + neuer Input vizOpponentArrowBrush [crazy 'green', Viz 'red']. +2 base-Solver-Tests); 0.229.0 (Anarchy-Tipps: bei e.p.-Zwang liefern alle 3 Puzzle-Tipps drei unterschiedlich formulierte En-passant-ist-Pflicht-Hinweise statt der normalen Tipps. Zentral in BasePuzzleSolver [effectiveHints = enPassantForced && epForcedHints ? epForcedHints : availableHints; hasHints/shownHints/canShowMoreHints darauf], epForcedHints je Solver überschrieben [i18n puzzles.anarchyHint1-3 en/de/hr]. +2 base-Solver-Tests); 0.228.0 (1) Off-Path-Warnung in allen Puzzle-Modi: BasePuzzleSolver zählt off-path-Züge [offPathUserPlies], berechnet in opponentRespond aus currentEval [Weiß-Sicht] playerEvalPawns() [Spieler-Sicht, Matt=±100] und warnt EINMALIG [onOffPathWarning()-Hook → Snackbar in allen 3 Solvern] ab offPathWarnThreshold Zügen, wenn Eval < +2. Reset in setupSolver + Mouseslip-zurück-auf-Pfad. Schwelle = Pref offPathWarnMoves [lokal, Default 3, 0=nie], im Settings-Dialog. (2) Anarchy: Settings-Toggle En-passant-forciert [Pref enPassantForced, Default an, nur bei themeMode=crazy sichtbar]; enPassantForced in onSetupStart = anarchyForcedByUrl [URL ?anarchy=max] ODER (crazy && Pref). +7 base-Solver-Tests; 0.227.0 (Logging-Flag `RequestKind` [system/user]: automatische, nicht nutzer-initiierte Requests werden als `system` markiert → in Kibana filterbar. Neuer `Logging/SystemCallClassifier` [Health/Swagger, /api/client-log, /api/menu, `/count`|`-count`|`-counts`-Polls, chessable/admin/active|imports]; gesetzt via `LogContext.PushProperty("RequestKind", …)` in der Enrichment-Middleware [neben UserId/VisitorId] + im HeartbeatService [`system`]. +14 Classifier-Tests. Reine Observability. NICHT deployed); 0.226.0 (Fix „Alle aktualisieren" für Kurse tat bei Admins NICHTS: `OwnerHasCourseAsync` prüfte jeden bid gegen Chessables getHomeData, das nur einen TEIL der Bibliothek listet [User 5: 32 statt 259] → 262 übersprungen/0 eingereiht [+142s/500er]. Neu: `EnqueueReimportAsync` bekam `trustOwnership`-Param; `ReprocessCoursesAsync` setzt ihn = `isAdmin` → Admin-Reprocess überspringt die Eigentumsprüfung [Admins dürfen ohnehin alles holen]; Nicht-Admin + Repertoire-Reprocess unverändert geprüft. +3 BE-Tests. 259/262 sind gecacht → Fast-Lane. NICHT deployed); 0.225.0 (Endless-Deep-Link-Query-Parameter [`endless-puzzle.component` ngOnInit]: `?start=1`/`?autostart=1` [Auto-Start ohne Config via `maybeAutoStart()` nach Rating-Range-Load], `?themes=…`/`?tags=…` [Komma/Leerzeichen→`config.themes`, worstTags off, ODER-Filter], `?elo=…` [startElo], `?anarchy=max|max+1` [reused `parseShareViewParams` → themeMode='crazy'+enPassantForced, max+1 crazyPieceMode='square'; `[crazyPieceMode]` ans Endless-Board gebunden]. +6 FE-Tests); 0.224.1 (CSP-Fix: eigenes Inline-Script in `index.html` [PWA-`beforeinstallprompt`-Hook] wurde von der strikten CSP [`script-src 'self'`] blockiert → in externe `public/pwa-install-hook.js` ausgelagert [von `'self'` gedeckt, kein `'unsafe-inline'`/Hash]. CSP-Warnungen aus `content.js` = Browser-Extensions, korrekt geblockt, kein RookHub-Bug); 0.224.0 (Admin-Tab „CI / Actions": letzte 5 GitHub-Actions-Läufe je beteiligtem Repo [kahalm/rookhub,-chessresults_crawler,-schach-bot,-piratechess_docker,-log-watcher], Auto-Refresh alle 5 s, Status-Badge + Link. Backend: `GithubActionsService` [getypter HttpClient→api.github.com, Token aus `GitHub:Token`/Env `GitHub__Token` (PAT actions:read) pro Request, Repos `GitHub:Repos` Default=die 5, Owner `GitHub:Owner`=kahalm] + IMemoryCache-Aggregat-Cache `GitHub:CacheSeconds`=4 → 5-s-Polls rate-limit-schonend; ohne Token `Configured=false`→UI-Hinweis. `CiController` `GET /api/admin/ci/runs` [Admin]. FE: eigenständiger `AdminGithubActionsComponent` [timer(0,5000), lazy `*matTabContent`→nur bei offenem Tab], neuer Tab 'ci' [admin-tabs.ts+spec+HTML]. +4 BE-Tests. i18n en/de/hr); 0.223.0 (Neues Puzzle-Theme `enPassantPossible`: `PuzzleService.HasUnplayedEnPassant(fen, uci, firstSolverPly=1)` [pure, Gera.Chess: FEN laden, UCI-Lösung nachspielen, NUR an Löser-Zügen (Indizes 1,3,5… — moves[0]=Gegner-Setup) `board.Moves()` auf `MoveEnPassant` prüfen; e.p. verfügbar + gespielter Zug ≠ e.p. → true] + `TagEnPassantPossibleAsync` [batchweise, idempotent] taggt betroffene Standard-Puzzles im `Themes`-String UND der normalisierten `PuzzleTags`-Tabelle [get-or-create Tag]; Admin-Endpoint `POST /api/admin/puzzles/tag-en-passant` [Hintergrund-Job]. Abgegrenzt von Lichess-`enPassant` [Lösung IST e.p.]. Nur Standard-Puzzles. Kein neues DB-Feld/Migration. +7 BE-Tests); 0.222.0 (Chessable-Flut durch nicht-cachebare Kurse gestoppt — zwei rookhub-only Schutzmechanismen (keine Migration): (1) DEDUP: `ChessableImportService.EnqueueReimportAsync` legt keinen zweiten Import an, wenn für (Owner, bid) bereits einer läuft/pausiert → ein erneuter „Update all"-Klick/Retry löst keine doppelten Kurs-Downloads mehr aus (beobachtet: bid 116242 4× in Folge). (2) BACKOFF: `ImportReprocessService.ReprocessCoursesAsync` überspringt einen stale, NICHT gecachten Kurs, dessen letzter Import <24h her ist (`IncompleteRefetchBackoff`) — ein truncated Kurs wird von piratechess nie gecacht und würde sonst bei JEDEM Massen-Reprocess komplett neu von Chessable geholt (Hunderte Line-Fetches); nutzt die bereits geholte `cachedBids`-Menge + `ChessableImports.CompletedAt`. Gezielter Admin-Re-Import umgeht den Backoff. +3 BE-Tests [1114 grün]. NICHT deployed); 0.221.0 (Reprocess „Alle aktualisieren“ robust+schnell + gecachte Kurse trotz gesperrtem Bearer: (1) `ImportReprocessService.ReprocessCoursesAsync` holt den Cache-Status ALLER Kurse EINMAL per Batch [`ICourseReimporter.GetCachedBidsAsync` → piratechess `/courses/cached`] statt je Kurs den teuren `IsCourseCachedAsync` [lädt+entpackt den ganzen Blob]; `EnqueueReimportAsync` bekam `knownCached`-Param [`FullyCached = knownCached ?? IsCourseCachedAsync`]. (2) Die Einreih-Schleife läuft mit `CancellationToken.None` statt dem Request-ct → Wegnavigieren [Browser schließt Verbindung] bricht das Einreihen NICHT mehr ab [vorher nur ~20 von 268 in der Queue]. (3) `RunAsync`-Bearer-Breaker-Check `&& import.FullyCached != true` → voll-gecachte Kurse kommen aus dem Cache [kein Chessable-Abruf] und laufen trotz totem/gesperrtem Bearer durch. +4 BE-Tests [1097 grün]. NICHT deployed); 0.220.1 (Kurs-Upload-Hinweis: neuer i18n-Text `courses.upload.restriction` [en/de/hr] unter dem Upload-Panel — funktioniert nur mit Puzzle-PGNs im Chessable-Stil, sonst Admin ansprechen oder im Discord fragen [klickbarer `DISCORD_INVITE_URL`-Link aus `core/community.ts`]. Reiner Text/UI, keine Logik); 0.220.0 (Eigenes PGN als persönlicher Kurs hochladen: Kurse-Seite ist jetzt für JEDEN eingeloggten Nutzer erreichbar [`courseAccessGuard` lässt alle Eingeloggten durch, Einstieg über die Dashboard-Kachel — Navbar bleibt content-gated via `checkAccess`], auch bei leerer Liste; oben ein Upload-Panel [PGN-Datei + optionaler Name]. Backend: `CourseService.UploadPersonalCourseAsync` [reused `PgnImportService.ImportFileAsync`, pro-User-eindeutiger Dateiname `user-u{id}-{guid}.pgn`, setzt `Book.OwnerUserId`+`Kind=Study`+Tags `personal`; leeres Ergebnis → Buch wieder entfernt+400] + `DeletePersonalCourseAsync` [nur Besitzer, reused `BookAdminService.DeleteBookAsync`]; Endpoints `POST /api/courses/upload` [multipart] + `DELETE /api/courses/{bookId}`. NUR Chessable-Stil-Puzzle-PGNs [FEN+Round+Trainingsmarker] erzeugen Puzzles — normale Partie-PGNs werden verworfen. Löschen-Icon je eigenem Kurs. Kein neues DB-Feld/Migration. +6 BE-Tests [1100] +5 FE-Tests; i18n `courses.upload.*`/`courses.delete*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.219.1 (Repertoire-Trainer: alternativer (geduldeter) ODER falscher Zug bleibt jetzt ~1 s sichtbar auf dem Brett stehen, bevor er zurückgenommen wird, statt sofort zurückzuspringen. `onMove` setzt `this.fen = fenAfterPlayerMove` auch für tolerated/wrong [vorher tolerated sofort `lastMove=undefined`/wrong nur Markierung]; neue `startFen`-Erfassung am `onMove`-Anfang. Geduldet: ~1,8 s gehalten [`ADVANCE_MS.tolerated`], dann `retryCurrentCard` nimmt zurück [`fen=startFen`] + dieselbe Karte wieder spielbar. Falsch: neuer `WRONG_HOLD_MS`=1000-Timer [`scheduleWrongRevert`/`revertWrongHold`] nimmt zurück, bleibt im FEEDBACK [Mausrutscher/Lösung-zeigen-Buttons]; `clearWrongRevert` in `clearAdvance`+`mouseslip`+`showSolution`. 7 Trainer-Specs grün, PROD-Build grün); 0.219.0 (Zug-lose Kurs-Erklär-/Intro-Seiten [Kommentar, keine Züge] werden beim Buch-/Kurs-Import nicht mehr verworfen: `PgnImportService.ParsePgn(..., keepCommentOnlyAsInfo:true)` [von `ImportFileAsync` gesetzt; Wochenpost-Pfad bleibt Default false] behält sie als Info-Linie [synthetischer Fake-Zug e4 ab Grundstellung, `IsInfoOnly`, Text als Kommentar] → nur sequenziell zum Durchklicken, kein Quiz, nicht in Random/Daily, nicht im Fortschritt. `ImportPipeline.CurrentVersion` 4→5; die Seiten stehen schon im `Book.SourcePgn` → Reprocess ist hier ein reiner LOKALER Re-Import, KEIN Chessable-Re-Fetch. +4 BE-Tests [1094]); 0.218.1 (Repertoire-Trainer-Zug-Feedback gefixt: `onMove` setzte `this.fen` nie auf die Stellung NACH dem Zug → bei jedem Zug Brett-Take-back + Replay über die Folgekarte [Flackern]. Jetzt: richtig → `this.fen = fenAfterPlayerMove` [Zug bleibt stehen, kein Flackern]; geduldet/spielbar → `lastMove=undefined` + `this.fen` unverändert [Zug zurückgenommen] und neue `runAdvance`/`retryCurrentCard`: geduldet springt NICHT weiter [kein Vorspielen des Hauptzugs], sondern macht dieselbe Karte erneut spielbar, sodass der User den Hauptzug selbst zieht. `onPlayClick`+Timer rufen `runAdvance`. Spec: tolerated-Test auf Retry+Take-back umgestellt + neuer correct-„kein Flackern"-Test, 780 FE grün); 0.218.0 (Anarchy-Stufe `?anarchy=max+1`: wie max [Crazy-Brett + erzwungenes En passant] PLUS das FELD bestimmt den Figurenstil [`crazyPieceMode='square'`] — Figur nimmt das Zufallsset ihres Feldes an, beim Ziehen wechselt das Aussehen zum Zielfeld. `board-theme.util`: `paintCrazyPieces(root, mode, orientation)` + `squareOfPiece` [Feld aus `transform: translate`], 64er-Feld→Set-Map; `parseShareViewParams` matcht `max+1`/`max 1` [+-Dekodierung]. `BasePuzzleSolver.crazyPieceMode` → `@Input` am `app-puzzle-board`, `repaintCrazyPieces` mit Settle-Repaint [~230ms] nach der Zug-Animation. +5 board-theme-Specs [25]); 0.217.1 (Fix: „Letztes Puzzle"-Favoriten-Knopf zeigte zwei Herzen [mat-icon + ♥ im Label-Text der i18n `favorites.loveLast`]; ♥-Präfix aus en/de/hr entfernt, nur das Icon bleibt); 0.217.0 — 0.217.0 (Standard-Puzzle-Links beachten jetzt `?single=1` [wie Buch-Puzzles]: `puzzle.component` liest `single` aus der Query → nach dem Lösen KEIN Auto-Weiter [`handleSolved` überspringt `startSolvedCountdown`/`loadNext` bei `singlePuzzle`], der „Weiter"-Knopf [`showNext` default true] bleibt aber. +1 Spec. Der Standard-Teilen-Dialog hängt `single=1` NICHT automatisch an [nur Buch] — Link wird bei Bedarf von Hand erweitert); 0.216.0 (Geliebte Puzzles ♥: nach Lösen/Aufgeben jedes Puzzles [Standard/Endless/Kurs-Buch/Tagespuzzle] ein Herz-Button in der Status-Card [`PuzzleStatusCardComponent` `canFavorite`/`isFavorite`/`favoriteToggled`]. Der „Letztes analysieren"-Knopf ist in zwei geteilt: „Letztes analysieren" + „♥ Letztes Puzzle". Backend polymorph wie `PuzzleChallenge` [`FavoritePuzzle` Standard/Book via `PuzzleSource`, Unique (UserId,Source,PuzzleId), Migration `AddFavoritePuzzles`], `FavoriteService` [add/remove/contains idempotent, list mit Fen+Moves+Rating+Themes+Title-Enrichment + Skip verwaister Einträge], `FavoriteController` `api/favorites` [GET/`count`/`contains`/POST/DELETE], Menü-Key `favorites`. Neue Seite `/favorites` [`FavoritesComponent`, Nachspielen/Analysieren/Entlieben] + Dashboard-Kachel `favorites` [default-hidden] + Navbar. FE-Logik gekapselt in `FavoriteTracker` [optimistisch + Rollback, current/last synchron], in alle 3 Solver eingehängt. Wochenpost-Puzzles NICHT favorisierbar [keine echte BookPuzzle-Id]. +8 BE-Tests [1090] +15 FE-Tests [773]. i18n `favorites.*`/`nav.favorites`/`dashboard.favorites.*` en/de/hr. NICHT gepusht/getaggt/deployed); 0.215.0 (Crazy-Brett würfelt jetzt JEDE Figur einzeln aus statt pro Rolle+Farbe [vorher „alle Bauern gleich"]: `board-theme.util` ersetzt das per-Rolle-`!important`-CSS [`applyCrazyPieces` entfernt] durch `paintCrazyPieces(root)` — pro `<piece>`-DOM-Element ein zufälliges Set via Inline-`background-image`, stabil über `WeakMap<Element,set>` [Figur behält ihr Set beim Ziehen; neues Puzzle/`redrawAll` würfelt neu]. `puzzle-board.component` ruft `repaintCrazyPieces()` [rAF] nach Init, nach jedem `ground.set` und nach Resize-`redrawAll`; sonst `clearCrazyPieces`. Figuren-CSS in `styles.scss` ist `.piece-set-<set>`-skopiert ohne `!important` → für `_crazy` greift nur das Inline. +5 board-theme-Specs [20]. Gilt für Crazy-Theme + `?anarchy=max`); 0.214.0 (Anarchy-Modus `?anarchy=max` an jedem Puzzle-Link [Standard + Kurs/Buch]: Crazy-Brett UND en passant verpflichtend [„en passant is forced"]. `parseShareViewParams` parst `anarchy=max`→`{themeMode:'crazy', enPassantForced:true}`; `BasePuzzleSolver.enPassantForced` an `calcDests(chess, enPassantForced)` durchgereicht → ist ein En-passant-Schlag verfügbar [chess.js-Flag `e`], enthält die dests-Map NUR diese Züge, sonst alle. `puzzle.component`+`book-puzzle.component` setzen das Flag aus dem Query. Rein transient, ändert keine gespeicherten Einstellungen. +2 Specs `puzzle-move.util` +2 `board-theme.util`. Hinweis: paralleler 0.213.1-Fix [Reprocess→Re-Fetch] liegt uncommittet in Kopie 1; daher Sprung auf 0.214.0); 0.213.1 (Reprocess-Banner-Fix für die Info-Linien-Änderung: bestehende Chessable-Kurse haben ein gecachtes Alt-PGN OHNE `[%info]`-Marker; `ImportReprocessService.GetCourseStatusAsync`/`ReprocessCoursesAsync` stuften sie als „lokal aufbereitbar" ein → lokales Reprocess hätte `ImportVersion` auf 4 hochgesetzt OHNE `IsInfoOnly` zu setzen [still falsch, Banner verschwindet]. Neu: Chessable-Kurse [`CanRefetch` = `IsChessable` + parsebare bid] werden IMMER als `Refetchable` eingestuft und per vollständigem Re-Fetch aktualisiert [Chessable-Zweig VOR dem SourcePgn-Pfad], nur Nicht-Chessable-Bücher mit Quelle laufen lokal. Matcht den v4-Doc-Kommentar [„für Chessable-Bücher ist der Reprocess ein Re-Fetch"], der vorher nur für Repertoires zutraf. 3 bestehende Tests auf Nicht-Chessable-Lokalbuch umgestellt + 1 neuer Test [`ReprocessCourses_ChessableWithSource_RefetchesInsteadOfLocalReprocess`]; 12 Reprocess-Tests/1082 BE grün. NICHT gepusht/getaggt/deployed); 0.213.0 (Frontend-Durchklick-UI für Chessable-Info-/Erklärlinien [Backend-Seite war 0.212.0]: `book-puzzle.component` bekam den Zustand `'INFO'` — `setupPuzzle` zweigt bei `puzzle.isInfoOnly` in `enterInfoReview()` ab [kein `setupSolver`, Brett view-only, `reviewMode`, ◀/▶ über die ganze Linie + Zug-Kommentare], statt Status-Card eine „Erklärlinie"-Card, Kontext-Nav zeigt „Weiter" [`courseNext`/`nextInBook`]. Board-`[visualization]` im Review/Info auf 0. i18n `book.info.title/subtitle` + `book.actions.prev/next` en/de/hr. +2 FE-Specs. HINWEIS: greift nur für aus Chessable neu importierte/abgerufene Kurse); 0.212.0 (Chessable-Info-/Erklärvarianten [von Chessable als IsInfo markiert] werden nicht mehr als Quiz abgefragt: piratechess emittiert `[%info]` im PGN [Commit `6f5ed38`], rookhub-Import setzt daraus `BookPuzzle.IsInfoOnly` [+Migration `AddBookPuzzleIsInfoOnly`, +`ImportPipeline.CurrentVersion` 3→4]. Solche Linien fallen aus ALLEN Zufalls-/Tagespuzzle-Töpfen [`BookPuzzleService`-Pool + `GetRandomInBookAsync`], zählen NICHT zum Kurs-Fortschritt [`CourseService`: `quizScope` ohne `IsInfoOnly` in `GetNextAsync`/`GetChaptersAsync`/`GetCoursesAsync`/`BuildProgressAsync`/`ComputeStatsAsync`] und sind im sequenziellen Kurs-Modus nur zum Durchklicken [seqPool nimmt Info-Linien mit, Completion bleibt quiz-basiert]. `BookPuzzleDto.IsInfoOnly` durchgereicht. HINWEIS: Marker entsteht erst beim frischen Chessable-Abruf → bestehende Kurse erst nach Re-Import/Re-Fetch. Frontend-Durchklick-UI folgt [0.213.0]. +7 BE-Tests [1081] +2 piratechess-Lib-Tests [16]); 0.211.0 (Dashboard-Customize „Standard"-Knopf + kuratierter Default: `applyDefault()` [ersetzt `resetLayout`] setzt `DEFAULT_VISIBLE` = Puzzles/Weekly/Repertoires/Kurse/Trainingsziele/Bestenlisten [in dieser Reihenfolge] sichtbar, alle übrigen Kacheln ausgeblendet [`DEFAULT_HIDDEN`], und persistiert. Dieser Default ist auch das Initial-Layout [ngOnInit: leeres gespeichertes Layout → kuratierter Default]. `DashboardLayoutService`-Key auf `rookhub_dashboard_layout_v2` erhöht → alle User starten einmalig frisch auf dem Default. Button-i18n `dashboard.edit.default` [Standard/Default/Zadano] en/de/hr. Spec-Anpassungen [Default-Layout, chessableQueue jetzt default-hidden → Edit-Modus-Checks]. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.210.0 (Visualisierungs-Umschalter unter dem Brett (0.207.0) WIEDER ENTFERNT — gefiel nicht. `VizModeSelectorComponent` + Spec gelöscht, viz-0..4-SVG-Icon-Registrierung aus `app.component` raus, Import/Imports-Array in allen drei Solvern entfernt. Stattdessen der alte Aufdecken-Knopf zurück in `.board-hint` (`@if vizPiecesHidden || visualizationMode===1`, ruft `onVizShow()`), aber als **Auge-Icon** statt Text: `vizShowPressed ? 'visibility' : 'visibility_off'` (durchgestrichen solange verdeckt, offen wenn aufgedeckt) + `.board-hint-eye`-Style; Tooltip/aria via vorhandene `*.viz.show`/`showing`-Keys. Modus-Wahl wieder nur im Einstellungsdialog. PROD-Build grün, 753 FE-Tests. NICHT deployed); 0.209.1 (Build-Fix: gerades Anführungszeichen im de-0.209.0-Changelog [`„keine Treffer"`] beendete den TS-String vorzeitig → Production-Build [CI] rot; auf typografisches `„…“` korrigiert. FALLE [[rookhub-changelog-build-gotcha]]: nach Changelog-Edit IMMER `npx ng build` [Production], nicht nur `--configuration development` — der Dev-Build lief vor dem Changelog-Edit und übersah es); 0.209.0 (Suchfeld für Kurse + Repertoires: oben in `course-list`/`repertoire-list.component` ein `mat-form-field`-Suchfeld [`[(ngModel)]="search"`, Such-Icon + Leeren-Knopf, `subscriptSizing="dynamic"`]; clientseitige Filterung über die bereits geladene Liste [Kurse nach `displayName`, Repertoires nach `name`+`description`, case-insensitive]. Kurse: neuer `filtered`-Getter, die Sektions-Getter [inProgress/public/chessable] bauen darauf auf → Filter wirkt über alle Sektionen; `courses.noMatch`-Hinweis. Repertoires: `filteredRepertoires`-Getter, `@empty`→`repertoire.list.noMatch` vs `empty`. FormsModule+MatFormField/Input ergänzt. i18n `common.clear`, `courses.searchPlaceholder`/`noMatch`, `repertoire.list.searchPlaceholder`/`noMatch` en/de/hr. +1 course-Spec +4 repertoire-Specs [756 grün]. NICHT deployed); 0.208.0 (Dashboard-Bearbeitungsmodus ändert die Ansicht NICHT mehr: das Kachelraster behält sein Layout [kein flex-column-Umbau mehr], es werden nur alle geeigneten Kacheln gezeigt [deaktivierte `tile-off` ausgegraut]. Je Kachel Overlay oben rechts: ↑/↓-Pfeile [`moveUp`/`moveDown` → `shift`/`commitOrder`] + Auge-Toggle; transparentes `.tile-shield` [zugleich `cdkDragHandle`] fängt Klicks auf den Kachel-Buttons ab [keine versehentliche Navigation] und ist die Drag-Fläche. Drag&Drop bleibt, jetzt `cdkDropListOrientation="mixed"` [rastertauglich, CDK 19]. i18n `dashboard.edit.moveUp`/`moveDown` + hint-Text en/de/hr. +2 FE-Specs [751 grün]. NICHT deployed); 0.207.0 (Visualisierungsmodi als Inline-Icon-Umschalter in der Solver-Hinweiszeile [ersetzt den „Aufdecken"-Knopf]: neue `VizModeSelectorComponent` [5 Buttons, Output `modeChange`→ruft das vorhandene `setVisualizationLevel($event)`] in `.board-hint` aller drei Solver [puzzle/book-puzzle/endless-puzzle.component.html]; aktiver Modus hervorgehoben. 5 SVG-Icons global via `MatIconRegistry.addSvgIconLiteral` in `app.component` [`viz-0`…`viz-4`, wie Discord-Logo]: 0=Bauer, 1=Bauer gedimmt+Kette, 2=schwarz/weiß-Token [fix], 3=weißes Token [fix], 4=durchgestrichenes Auge [gefüllt, Iris+Pupille via Maske]. 0/1/4 `currentColor`, 2/3 feste Farben. Aufdecken = Modus 0 wählen [`board-hint-show`+`onVizShow` aus Templates raus; Base-Logik bleibt]. i18n `puzzles.viz.modeSelectAria` en/de/hr. +3 FE-Specs [748 grün]. Versionssprung 0.206.0→0.207.0 [paralleler 0.206.0-Kollision mit Revenge-Icon ausgewichen]. NICHT deployed); 0.206.0 (Freundesliste: Revenge-a-Friend-Icon wird ROT [+ Anzahl-Badge], wenn offene Revanchen gegen den Freund existieren [Freund gescheitert+nie gelöst, ich noch nicht gelöst]. `GET /api/friends` reichert je Freund `FriendDto.OpenRevengeCount` an [`PuzzleService.GetOpenRevengeCountsAsync`, provider-sicher in-memory aggregiert]; FE `friends.component` bindet `[color]`+`matBadge`, i18n `friends.revenge.menuOpen` en/de/hr. +1 BE-Test [25] +1 FE-Test [8]); 0.205.3 (Observability: Admin-Impersonation pro Request sichtbar — die Request-Logging-Middleware [`Program.cs`] schreibt den `imp`-Claim [Admin-Id] jetzt als `LogContext`-Property `ImpersonatorId` [→ ECS `labels.ImpersonatorId`], sodass impersonierte Requests in Kibana filterbar sind; vorher nur das einmalige Impersonation-Start-Event geloggt. Reines Logging-Plumbing analog `UserId`/`UserName`; `imp`-Claim-Quelle bereits in `AuthServiceTests` getestet); 0.205.2 (Chessable-Bearer-Circuit-Breaker GEMERGT [vorher lokales v0.204.0, Commit `d6f0995`] und mit der entfernten 0.203.x-Chessable-Security-Linie versöhnt: weist Chessable einen Bearer endgültig ab [Account gesperrt/gelöscht oder Token tot — `ChessableBearerBreaker.IsBearerFatal`, NICHT bei IP-/Cloudflare-/VPN-Block], sperrt ihn [`ChessableCredential.BlockedAt`/`BlockedReason` + Migration `AddChessableCredentialBreaker`] und verwendet ihn für KEINE weitere Anfrage. Trip in `ChessableImportService.FailAsync` + `ChessableController` [Test/Courses/Admin-Courses/Estimate]; laufende Importe PAUSIEREN [`Phase="bearer-blocked"`], Lese-Abrufe/neue Importe → 400. Reset = erfolgreicher „Testen"-Klick [`ClearAndResumeAsync`]; neuer Bearer-Save schließt den Breaker. Admin-Test-Endpoint `POST admin/users/{id}/test`. FE: Sperr-Banner + 🔒/„Bearer testen" im Admin-Kursdownload. Merge-Konflikt: in `StartImportForUserAdmin` sitzt die Bearer-Sperr-Prüfung VOR der neuen `UserOwnsCourseAsync`-Eigentumsprüfung [0.203.8], damit ein toter Bearer keinen Fetch auslöst. +12 BE-Tests. Versionssprung 0.205.1→0.205.2 [0.204.0 in 0.205.2 aufgegangen]); 0.205.0 (Anpassbare Dashboard-Kacheln: Dashboard ist ein konfigurierbares Kachelraster über alle sinnvollen Module [Puzzles/Trainingsziele/Kurse/Bestenlisten/Turniere/Freunde/Repertoires/Partien/Wochenpost/Statistik/Analyse/Nachrichten + Admin-Chessable-Queue]. „Anpassen"-Knopf → Bearbeitungsmodus: Kacheln per CDK-Drag&Drop sortieren + per Augen-Knopf ein-/ausblenden, „Zurücksetzen" → Default. Persistenz pro Gerät via `DashboardLayoutService` [localStorage `rookhub_dashboard_layout`, `{order,hidden}`]; Eignung je Kachel über `MenuService.visible$` [+ `auth.isAdmin` für Chessable]; neue Kacheln werden hinten an ein bestehendes Layout angehängt. Datengetriebene `TileDef`-Definitionen [`eligible`/`subtitle`/`buttons`] + generisches Card-Template; Detail-Listen bleiben darunter. i18n en/de/hr. +5 Layout-Service-Specs +7 Dashboard-Tile-Specs. HINWEIS: Das lokale v0.204.0 [Chessable-Bearer-Circuit-Breaker, Commit `d6f0995`] ist auf diesem gepushten Stand NICHT enthalten — es kollidiert mit der entfernten 0.203.x-Chessable-Linie und braucht einen bewussten manuellen Merge; gesichert im Branch `backup-local-master`. Daher Versionssprung 0.203.13→0.205.0); 0.203.12 (piratechess VPN-Concurrency [piratechess `53e90b1`]: `VpnIpHealth._byIp`-Eviction [Cap `Vpn:IpHealthMaxEntries`]; `VpnTunnel.RequestCompleted` zählt Ausgang VOR `_inFlight`-Decrement [Block-Fehlbuchung bei Drain-Race behoben]; `_currentIp` volatile; `VpnRotationService.RotateNowAsync` sequenziell statt `Task.WhenAll` [kein Total-Stall bei manueller Rotation]. 253 Tests grün. NICHT getaggt/deployed); 0.203.11 (piratechess Parsing-Fix [piratechess `1fe1eda`, Batch 2]: `SanToMove` konstruiert gerade Bauern-Push-Umwandlungen [`e8=Q`] direkt — ChessDotNet 1.0.0 listet sie nicht in `GetValidMoves` → vorher leerer `[%tqu]`-Trainings-UCI + Abbruch der Folgezüge; relevant für Endspiel-Kurse. +10 Lib-Fixtures [SAN + softFail]. Geprüft als Nicht-Probleme: useLocalData-Oid [positionell korrekt, Oid beim Cachen nie gesetzt] + softFail-Indizierung [relativ korrekt]. 252 Tests grün. NICHT getaggt/deployed); 0.203.10 (piratechess Code-Review Resilienz+Hygiene [piratechess-Repo, Commit `5074a72`, 242 Tests grün]: ExportBackgroundService überlebt Reader-Fehler [äußere while+Backoff]; curl stdout/stderr parallel [kein Pipe-Deadlock]; ExportJobQueue bounded; Startup-Fail-fast für ConnString/Jwt; globaler ProblemDetails-Exception-Handler; Health 503 bei DB-Ausfall; Dictionary-Indexer statt Add bei doppelter Bid; bid-Format-Validierung [numerisch] → schließt BidLock-Leck. NICHT getaggt/deployed); 0.203.9 (**Security-Fix (2. Review-Pass)**: zweite Tür zum Cached-Content-Bypass geschlossen — `EnqueueReimportAsync` (Reprocess-/Re-Fetch-Choke-Point) prüft jetzt via `OwnerHasCourseAsync`, dass der `bid` in der Bibliothek des Owners liegt, sonst Skip [null]. Vorher konnte ein User ein Repertoire mit beliebigem `ChessableCourseId`/präpariertem Dateinamen anlegen [ImportVersion 0 = stale] und per `/reprocess` fremden gecachten Kursinhalt holen. +2 Tests/1051 grün); 0.203.8 (Nachzieher Import-Security: Admin-`StartImportForUserAdmin` macht denselben `UserOwnsCourseAsync`-Check gegen den Ziel-User; anonyme Tagespuzzle-Lösungen DB-seitig dedupliziert — Unique-Index `(BookPuzzleId, AnonymousSessionId)` [Migration `AddAnonBookAttemptUniqueDedup` inkl. Alt-Duplikat-Bereinigung; authentifizierte Versuche unberührt via NULL-Session], `RecordAnonymousAttemptAsync` fängt `DbUpdateException` ab → kein Doppel-Count/Webhook); 0.203.7 (**Security-Fix**: `ChessableController.StartImport` prüft via `UserOwnsCourseAsync` jetzt, dass der `bid` in der Chessable-Bibliothek des Users liegt [gecachte Liste, sonst 1× frisch laden], sonst 403. Vorher konnte jeder eingeloggte User mit Bearer JEDEN gecachten Kurs importieren — piratechess `POST direct/course` liefert gecachte Kurse aus dem geteilten Cache OHNE Eigentumsprüfung, und Chessable-Kurs-IDs sind öffentlich. Closes den Content-Bypass für alle Lanes [cached+fetch]; +1 Test/1049 grün); 0.203.6 (Doku: API-Code-Review 2026-06-30 in TODO.md festgehalten — Gefixtes [v0.202.0–0.203.5] + bewusst gegen reale Prod-Größe [49 User] zurückgestellte „unbounded read"-Funde [laden real 50–1900 Zeilen → SQL-Umbau = Pomelo-Risiko ohne Nutzen] + Import-Concurrency [bereits durch Claim+Watchdog+Lane-Gate abgedeckt]; keine Code-Änderung); 0.203.5 (Code-Review-Robustheit: `CreateChallengeBatchDto.ToUserIds` `[MaxLength(50)]` [DoS-Schutz]; BookPuzzle-Routen `{id:int}`; `RepertoireTrainingService.ReviewAsync` `DbUpdateException`-Race-Catch [Unique CardKey]; `TournamentMonitorController` `TryGetInt32` statt `GetInt32` [kein 500 bei Crawler-Typänderung]. Bereits erledigt im aktuellen Code: Buch-Versuch klemmt Zeit/Hints serverseitig + validiert SessionId; Listen-Endpoints klemmen take/page); 0.203.4 (Code Review: SavedGame-Dedup hart auf DB-Ebene — Unique-Index `(UserId, Source, ExternalId)` [Migration `AddSavedGameUniqueDedup` inkl. Alt-Duplikat-Bereinigung; NULL-ExternalId weiter erlaubt], `SaveAsync` fängt `DbUpdateException` ab und liefert die bestehende Partie); 0.203.3 (Performance [Code Review]: `AutoSubscriptionService` — frischer DbContext/Scope pro User [`CheckAllUsersAsync`, kein unbeschränktes ChangeTracker-Wachstum/Leakage über den ganzen Lauf] + Freundes-/Profil-Set je User EINMAL geladen [`LoadFavoriteProfilesAsync`, an `AutoFavoritePlayersAsync` durchgereicht] statt je Turnier neu; reine Effizienz, Verhalten unverändert); 0.203.2 (Korrektheit [Code Review]: (1) Daily-Leaderboard `AggregateScores` Competition-Ranking nach Lösezeit [zeitgleiche Löser = gleicher Rang/Bonus + alle 🥇, statt Submit-Reihenfolge bei TimeSeconds==0]; (2) `AdminMessageService.GetThreadsAsync` lädt jüngste Nachricht je Thread über `Max(Id)` statt `CreatedAt`-IN-Match [kein Cross-Thread-Mismatch bei Zeitgleichheit]; +2 Tests); 0.203.1 (Performance [Code Review]: Wochenpost-Übersicht ohne N+1 — `WeeklyPost.PuzzleCount` gecacht [Migration `AddWeeklyPostPuzzleCount`, beim Upload berechnet], `GetAllProgressAsync` lädt alle Posts in EINER Query + nutzt gecachte Anzahl statt PGN-Parse je Post; `GetTotalAsync`-Helper mit Lazy-Backfill für Alt-Datensätze auf allen Pfaden); 0.203.0 (Puzzle-Link-Query-Parameter `?crazy=1`/`?visualmode=0..4` [anderer Agent]); 0.202.2 (Security-Härtung [Code Review]: benannte Rate-Limiter `auth`/`anonymous-puzzle`/`anonymous-tournament` partitionieren jetzt pro Client-IP [`AddPolicy` mit `RateLimitPartition` statt einzelnem `AddFixedWindowLimiter`-Bucket] → kein site-weiter Login-DoS / pro-IP-Brute-Force-Drossel; Default-CORS ohne `AllowCredentials` [Bearer-only]; „Eingeloggt bleiben"-JWT 365→90 Tage); 0.202.1 (Viz-/Blind-Puzzle-Gesten gehärtet [anderer Agent]); 0.202.0 (Track-solves erfasst Tipp-Nutzung: `SharedPuzzleAttempt.HintsUsed` (0–3, Migration `AddSharedPuzzleAttemptHints`) wird beim Erstversuch mitgespeichert; `RecordSharedAttemptDto.HintsUsed` + `SharedPuzzleCountsDto.SolvedByHints[0..3]` (gelöste je Tipp-Stufe) — `GetSharedCountsAsync` aggregiert in einer GroupBy-Query; Buch-Puzzle-Solver sendet `hintLevel`. +2 BE-Tests); 0.201.0 (Kurs „Ganzes Buch" + Repertoire-Trainer Politur: (1) Kurs-Abschluss differenziert: `book-puzzle.component` neuer Getter `courseFullyDone` (= `courseSolved >= courseTotal`) + `courseRemaining`; Template zeigt Trophäe+„Kurs abgeschlossen!" nur wenn wirklich alle gelöst, sonst „Zufalls-Runde durch — noch N Aufgaben" mit „Mit den restlichen weitermachen"-Knopf. Backend bleibt unverändert (`Completed=puzzle==null` = Pool leer; war im Random-Modus immer schon irreführend für die UI). i18n `book.course.roundFinished`/`continueRemaining` en/de/hr. (2) Repertoire-Trainer `tolerated`-Feedback verrät den Repertoire-Zug nicht mehr — neuer Key `toleratedPlayable` ohne `{{move}}`. (3) `wrong`-Feedback zeigt nicht mehr sofort die Lösung: zwei Buttons „Mausrutscher" (kein Penalty, kein Server-Review, kein Re-Queue, zurück zu PLAYING; Brett unverändert) und „Lösung zeigen" (zählt erst dann als wrong, sendet `grade=0` an `RepertoireTrainingService.review`, re-queued die Karte, enthüllt `expectedDisplay`). State-Felder: `wrongRevealed`, `wrongMove`, `pendingWrongReview`. (4) Bei `wrong` Stockfish-18-Eval-Vergleich im Hintergrund: `StockfishService.getEval` (Depth 14) auf FEN nach Spielerzug + FEN nach Repertoire-Zug, Differenz aus Spieler-Sicht in Bauern; Mate-Sonderfälle (`evalMateNote` = `missed`/`allowed`). `kickOffEvalCompare` mit `evalEpoch` gegen Karten-/Zugwechsel-Race; Stockfish-Init im `ngOnInit` (Warmup). i18n `repertoireTrainer.toleratedPlayable`/`wrongNoHint`/`mouseslip`/`showSolution`/`evalLoading`/`evalWorse`/`evalEqual`/`evalMateMissed`/`evalMateAllowed` en/de/hr. +2 Spec-Cases (mouseslip kein Penalty, showSolution zählt+re-queued); 6/6 trainer-spec grün); 0.200.4 (Mobile-Fix Status-Karten: bei `max-width: 768px` setzte `puzzle.component.scss`+`book-puzzle.component.scss` zwar `flex-direction: column`, aber nur `.board-section: 100%` — `.info-section` blieb auf Content-Width, daher waren die Status-/Korrekt-Karten unter dem Brett schmaler als das Brett selbst. Beide bekommen jetzt `.board-section, .info-section { width: 100% }` wie endless-puzzle bereits seit längerem. Gilt für Standard-Puzzles und Buch-/Kurs-/Tages-/Wochen-/Geteilte-Puzzles); 0.200.3 (Dark Mode als Default: `ThemeService._preference` Default `system`→`dark`; gespeicherte Nutzerwahl [localStorage `rookhub_app_theme`] hat weiter Vorrang, Toggle unverändert); 0.200.2 (Leaderboard „Diese Woche"/„Dieser Monat" = rollierende Fenster: `LeaderboardService.WindowStart` weekly→`today.AddDays(-6)` [letzte 7 Tage], monthly→`today.AddDays(-30)` [letzte 31 Tage] statt ISO-Montag/Monatserster; reine Backend-Änderung, FE-Labels unverändert. Boundary-Test angepasst, 9 LeaderboardService-Tests grün); 0.200.1 (Track solves immer an: das Opt-in [Teilen-Dialog-Checkbox + `?track=1`-Param] entfernt; `BookPuzzleComponent.trackSolves = singlePuzzle` [also für jeden geteilten `?single=1`-Link aktiv]. SharePuzzleDialog: Checkbox/`canTrack`/`setTrack`/`&track=1`-Anhängen + MatCheckbox-Import raus, `activeUrl` liefert nur die Basis-URL; ungenutzte i18n `puzzles.share.trackSolves(+Hint)` entfernt. share-dialog-Spec umgeschrieben); 0.200.0 (Geteilte Puzzles „Track solves": Teilen-Dialog bekam Checkbox „Versuche zählen" [nur bei Buch-Einzel-Link mit `?single=1`; hängt `&track=1` an `activeUrl`]. Empfänger-Link zeigt unter dem Puzzle Gelöst-/Fehlversuch-Zähler. Neue Tabelle `SharedPuzzleAttempts` [Unique `(BookPuzzleId, IdentityKey)` → nur Erstversuch je Besucher; `u:{userId}`/`s:{sessionId}`] + Migration `AddSharedPuzzleAttempts`; Endpoints `POST/GET /api/book-puzzles/{id}/track[-counts]` [AllowAnonymous, optionale Auth via `GetUserIdOrNull`]. `BookPuzzleComponent`: Flag `trackSolves` [Query `track=1`], `recordTrack(solved)` [guard + serverseitig erstversuch-dedupliziert] aus finalizeSolve[true]/handleFailed/giveUp/resetPuzzle[false] → Reset/Aufgeben = failed; Anzeige `sharedCounts`. i18n `puzzles.share.trackSolves(+Hint)`/`book.track.solved|failed` en/de/hr. +5 BE-Tests [1036], +5 FE-Tests [neues share-dialog-Spec + book-puzzle]); 0.199.1 (Direkt geteiltes Einzel-Puzzle bleibt am Ende stehen: Teilen-Link aus dem Buch-Puzzle-Solver bekommt `?single=1` [`sharePuzzle()` url+previousUrl]; `BookPuzzleComponent` liest das in Flag `singlePuzzle` → `finalizeSolve` überspringt den Auto-Advance-Countdown, `solvedAutoNext` ist no-op, neuer Getter `browseInBook`=`standalone && !isDaily && !singlePuzzle` blendet die Nächstes/Zufällig-im-Buch-Buttons aus, und `PuzzleStatusCard` bekam `@Input() showNext` [=`!singlePuzzle`] um den „Weiter"-Knopf im Gelöst-Zustand zu verstecken. Normales Buch-Durchblättern unverändert. +3 FE-Tests [28 grün im book-puzzle-Spec; share-URL-Test auf `?single=1` angepasst]); 0.199.0 (Freundesliste zeigt ausstehende GESENDETE Anfragen: Anfragen-Tab bekam neben „Eingehend" einen Abschnitt „Gesendet (wartet auf Bestätigung)" [bisher nirgends sichtbar], je Zeile Zurückziehen-Knopf [reused `DELETE /api/friends/{id}` → `RemoveFriendAsync` erlaubt auch dem Requester], Tab-Zähler = eingehend + gesendet. BE: `FriendService.GetSentPendingRequestsAsync` [RequesterId==me && Pending, inkl. Addressee+Profile] + DTO `SentFriendRequestDto` + Endpoint `GET /api/friends/requests/sent`. FE: `FriendsService.getSentRequests` + `friends.component.sentRequests`/`withdrawRequest`; i18n `friends.requests.*`/`friends.aria.withdraw`/`friends.errors.withdrawRequest` en/de/hr [22 weitere fallen für friends-Detailkeys ohnehin auf en zurück]. +1 BE-Test [24 FriendController grün] +2 FE-Tests [7 grün]); 0.198.0 (Puzzle-an-Freund-Menü: hinter jedem Freund in Klammern, wie viele der von mir geschickten Puzzle noch OFFEN sind [Pending = noch nicht versucht], z. B. „Max (3)"; gelöste/gescheiterte zählen NICHT [erledigt], Freunde ohne offene zeigen keine Zahl. BE: `ChallengeService.GetPendingOutgoingCountsAsync` [GroupBy ToUserId, nur FromUserId==me && Pending] + Endpoint `GET /api/challenges/outgoing/pending-counts`. FE: `ChallengeService.getPendingCounts` + `ChallengeFriendsComponent.pendingCounts` [lädt beim Menü-Öffnen mit der Freundesliste, frischt nach `send()` nach]; i18n `puzzles.challenge.unsolvedTitle` en/de/hr [andere 22 Sprachen fallen für den ganzen challenge-Block ohnehin auf en zurück]. +1 BE-Test [18 ChallengeController grün], +1 FE-Test [7 grün gesamt]); 0.197.1 (Freundeszahl reaktiv: Nimmt ein Freund meine Anfrage an, aktualisierte sich die Freundeszahl bei mir erst nach Seiten-Refresh [Dashboard-`friendCount` nur in `ngOnInit`-`forkJoin` geladen, keine Reaktivität]. Fix: `InAppNotificationService` bekam ein `arrived$`-Subject [feuert, wenn `refreshCount` einen GESTIEGENEN Ungelesen-Zähler erkennt = neue Notification]; Dashboard lädt darauf die Freundeszahl nach, `friends.component` macht einen STILLEN `loadData(true)` [kein Spinner-Flackern]. Hängt am vorhandenen 60-s-Glocken-Poll → kein zusätzlicher Timer. +3 Tests [service `arrived$`, dashboard friendCount, friends quiet reload] + neue `dashboard.component.spec`); 0.197.0 (Chessable-Import-Fortschritt auf der Kursseite: neues schreibgeschütztes `ChessableImportsBannerComponent` [self-polling `getImports`, 8s] oben in `CourseListComponent` zeigt laufende/pausierte Importe mit derselben Visualisierung wie der Chessable-Tab [„hole Kurs… Kapitel 7/36 · 82/1000 Linien · noch ca. 23 Min"], `(importCompleted)`→`loadCourses()` lädt nach Abschluss nach. Fortschritts-/ETA-/Label-Logik aus `chessable.component` in geteilte `chessable-progress.util` extrahiert [`chessableStatusLabel`/`chessableQueueLabel`/`compareImportsByQueue` + `CHESSABLE_LINES_PER_MIN`/`formatDuration`/`effectiveTotalLines`/`estimateRemainingMinutes`, von der Komponente rückwärtskompatibel re-exportiert]. Chessable-Tab-Queue `activeList()` jetzt nach „#" sortiert [queuedAhead asc, dann createdAt]. +2 Spec-Dateien); 0.196.2 (Daily-💡-Badge-Fix: Tagespuzzle in Discord zeigte kein 💡 hinter Lösern mit Tipps, weil das Daily-Webhook-Payload [`SchachBotWebhookService.NotifyAttemptAsync`] pro Solver nur `name/discordId/discordUsername/timeSeconds` sendete — **`hintsUsed` fehlte** [war NIE drin, Git-Historie bestätigt; nur das Wochenpost-Payload `NotifyWeeklyAsync` sendet es]. Bot rendert es längst [`puzzle/daily_results.py:141` `s.get('hintsUsed',0)>0`]. Fix = 1 Zeile `hintsUsed = s.HintsUsed` in der Solver-Projektion [Daten lagen in `BookSolverDto.HintsUsed` bereit]. Test erweitert/6 Webhook-Tests grün. Kein Bot-Update nötig. NICHT deployed); 0.196.1 (Discord-Solver-Webhook eigene Queue: Daily-Solve [book-puzzle 19207, User 5/kahalm] erschien nicht in Discord, weil der schach-bot-Webhook-Push [`BookPuzzleService.NotifySchachBotAsync` Z.99/188, `WeeklyPostService`] sich DIESELBE `IBackgroundTaskQueue` mit dem Chessable-Import teilte — bounded cap 100 + `DropOldest` + 1 serieller Consumer [[prod-chessable-import-stall-restart]]. Nach Redeploy hatte der ResumeService ~263 minutenlange Import-Jobs in die 100er-Queue geworfen → das Webhook-Ticket wurde verdrängt, bevor es lief [heute KEIN `Notify`-Log; gestern feuerte derselbe Webhook ohne Import problemlos]. Fix: neue `IWebhookTaskQueue`/`WebhookTaskQueue` [cap 256] + eigener `WebhookTaskWorker`, registriert in Program.cs; `BookPuzzleService`+`WeeklyPostService` enqueuen Webhooks jetzt dort [`BookPuzzleController`-Hint-Gen bleibt auf der allgemeinen Queue]. Test-Stubs [NoOp/Counting/Immediate] auf `IWebhookTaskQueue` gehoben. One-Shot-Webhook für das heutige Solve manuell signiert+gefeuert [HTTP 200]. +2 Tests/1029 grün. NICHT deployed); 0.196.0 (Repertoire-Trainer ohne „Weiter"-Knopf: nach richtigem/geduldetem Zug läuft der Trainer automatisch zur nächsten Stellung weiter [`ADVANCE_MS` correct 700 ms / tolerated 1800 ms, Timer in `onMove`, abgeräumt in `ngOnDestroy`/`setColor`/`buildQueue`/`next`]; geduldet zeigt die Repertoirezug-Visualisierung länger; Tippen aufs `.play` [`onPlayClick`] überspringt die Wartezeit; FALSCH behält den expliziten „Weiter"-Knopf. Neuer i18n-Key `repertoireTrainer.tapToContinue` en/de/hr + neuer Spec `repertoire-trainer.component.spec.ts` [4 fakeAsync-Cases]); 0.195.5 (VpnIpHealth Fehlalarm-Fix [piratechess v1.0.25]: Prod-Live-Daten zeigten gesunde Exit-IPs [37.46.199.54/.70/.86, alle DE-Tunnel, ≈2–5 % Gesamt-Block-Rate] als „WIEDERHOLT SCHLECHT" geflaggt — weil einzelne kurze Stints [1 Block von 2 Requests = 50 %] über `Vpn:BadStintRate`=0.4 lagen und sich ≥2 davon ansammelten. Neu: eine Phase zählt nur ab `Vpn:BadStintMinRequests`=5 Requests als „schlecht", und „wiederholt schlecht" hängt jetzt an der KUMULATIVEN Per-IP-Block-Rate [`Vpn:BadIpBlockRate`=0.15 über `Vpn:BadIpMinRequests`=50 Requests] statt an gezählten Mini-Phasen; WARN gedrosselt [1× bei Überschreiten, danach frühestens alle 20 Stints]; `IpStat.RecurringBad`-Verdikt, Snapshot sortiert wiederholt-schlechte zuerst dann nach Block-Rate. Prod fährt seit 2026-06-29 v0.195.4+v1.0.24 [Watchdog+Resume+Cooldown live, Import-Drain verifiziert]. +3 Testfälle/214 piratechess grün. NICHT deployed); 0.195.4 (Download-Lane-Nebenläufigkeits-Fix: Chessable-Importe luden gelegentlich ZWEI Kurse gleichzeitig, weil die „serielle" Download-Lane von zwei unabhängigen Treibern bedient wird — dem einzelnen `BackgroundTaskWorker` (Queue-Tickets) UND dem `ChessableImportWatchdogService` (ruft `RunNextAsync` an der bounded Queue VORBEI direkt auf). Der atomare Claim [[prod-chessable-import-stall-restart]] verhindert nur Doppel-Verarbeitung DESSELBEN Jobs, nicht, dass jeder Treiber einen ANDEREN wartenden Job claimt und parallel zieht (beobachtet 2026-06-29: Watchdog-Drive + Queue-Worker zogen Kurs 583468e8… und 69cc61bc… gleichzeitig). Neu: prozessweites `static SemaphoreSlim(1) _downloadLaneGate` in `ChessableImportService`; `RunNextAsync` (Download-Lane) acquired per `WaitAsync(0)` → ein zweiter Drive kehrt SOFORT zurück statt parallel zu laden, Body in `DrainNextAsync` ausgelagert; Fast-Lane bleibt ungated/nebenläufig. +1 Test [`RunNextAsync_DownloadLane_GateBlocksSecondConcurrentDrive`, deterministisch via blockierten Fetch], 1027 grün. NICHT gepusht/getaggt/deployed); 0.195.3 (Per-IP-Request/Block-Tracking [piratechess]: neuer Singleton `VpnIpHealth` akkumuliert je VPN-Ausgangs-IP Requests/Blocks über alle Rotationen [„Stint" = IP-Lebensdauer zwischen Rotationen, gemeldet via `VpnTunnel.FlushIpStint` beim Rotieren; `_currentIp`/`_ipRequests`/`_ipBlocks`]; eine IP mit ≥2 schlechten Phasen [Block-Rate ≥ `Vpn:BadStintRate`=0.4] wird als „WIEDERHOLT SCHLECHT" geloggt [WARN, strukturiert→Kibana je IP gruppierbar]; Debug-Endpoint `GET /api/chessable/direct/debug/ip-health` liefert die Per-IP-Tabelle [schlechteste zuerst]. +3 Tests/211 grün. NICHT deployed); 0.195.2 (Import-Robustheit [piratechess]: (1) hartes `--max-time` je Request [`Chessable:RequestMaxTimeSec`, Default 20s] in `BuildGetArgs` → hängende IP scheitert nach ~20s statt 75–120s, leerer Body → Soft-Block → retire/rotate; DrainTimeout 60→25s. (2) Per-Tunnel-Health+Cooldown in `VpnTunnel`: gleitendes Fenster der letzten N Ausgänge [`Vpn:HealthWindow`=8], blockt ein Tunnel ≥`Vpn:HealthBlockThreshold`=5 → `Vpn:CooldownSec`=120 aus dem Pool [`TryAcquire(respectCooldown)`, `RecordOutcome` via `RequestCompleted(bool)`, `VpnLease.onComplete` jetzt `Action<bool>`]; `AcquireAsync` Zwei-Pass [gesund zuerst, Notfall ohne Cooldown → kein Verhungern]. +2 Tests/208 grün. NICHT deployed [Prod läuft noch v1.0.23]); 0.195.1 (ETA-Durchsatz `CHESSABLE_LINES_PER_MIN` 25→40 [nach Rotate-on-Block-Speedup]; admin.component nutzt jetzt die Konstante statt hartem /25 in `dlEtaMin`/`estMinFromLines`); 0.195.0 (Admin-Kursdownload „Größe schätzen": on-demand pro Kurs Gesamt-Linienzahl + grobe Zeit vor dem Import. piratechess `POST /api/chessable/direct/course/info` {bearer,bid} → `DirectCourseInfoResponse` [gecacht=Cache-Summe ohne Chessable-Call, sonst `GetCourseLineCountAsync`=1× getCourse?includeVariations]. rookhub `ChessableProxyService.GetCourseInfoAsync` + Admin-Endpoint `GET admin/users/{userId}/courses/{bid}/estimate` [Bearer des Ziel-Users]. FE: `estimateCourseForUser`, `dlEstimate`/`dlEstimates`/`estMinFromLines` [~25/min], `straighten`-Knopf je Zeile → „≈N Linien · ~M Min/gecacht", i18n estimate/estimateError/cachedInstant/min. +2 Tests; FE 630/BE 1026/piratechess 206 grün); 0.194.0 (Chessable-Import: echte Gesamt-Linienzahl + ETA. piratechess getCourse `&includeVariations=true` → `Chapter.Total`/`Variations`, `FetchCourseDataAsync` meldet Variantensumme via `onTotalLines` → `CourseFetchJob.LinesTotal`/Snapshot → `DirectCourseProgressResponse.LinesTotal`. rookhub `ChessableImport.LinesTotal` [+Migration `AddChessableImportLinesTotal`] durch alle Progress-DTOs; FE `effectiveTotalLines` bevorzugt exakten Wert, `CHESSABLE_LINES_PER_MIN` 16,7→25, i18n `chessable.fetchProgressTotal`, Linien-X/Gesamt + ETA im User-Import + admin-Kursdownload [`dlEtaMin`]; FE 630/BE 1024/piratechess 206 grün); 0.193.2 (Chessable-Line-Fetch Rotate-on-Block [piratechess-Repo]: größter Importzeit-Fresher war der FIXE 30-s-Backoff bei Soft-Block [leere `{}`-Antwort, ~6 % der Zeilen → amortisiert ~1,8 s/Zeile ≈ 77 % der Wandzeit, bei nur 187 ms echtem curl]. Neu: `VpnRotationService` hält GENAU EINEN aktiven Tunnel sticky [statt round-robin pro Request]; `VpnLease.ReportBlocked()` [aufgerufen von `ChessableHttpService.CurlGetAsync` via `IsSoftBlockedBody`] retired die IP sofort → `VpnTunnel.RetireNow()` rotiert sie drain-aware im Hintergrund, Pool wechselt auf den nächsten, bereits ausgeruhten Tunnel [Ping-Pong; Rotation dauert lt. Prod-Log nur ~4 s ≈ Abarbeiten von 10 Zeilen]; Line-Retry-Backoff 30 s→`Chessable:BlockRetryDelayMs` [Default 1500 ms]; Inter-Request-Delay-Default 1000–2000→0–200 ms [Block ist requests-pro-IP-, nicht timing-getrieben]. Erwartung ~26→~100–150 Zeilen/min. piratechess-Commit 9071793, 206 Tests grün. OFFEN: Live-ENV in /opt/stacks setzen [ParallelLineFetches=1, InterRequestDelay 0/200] + Dev-Verifikation; NICHT deployed); 0.193.1 (Chessable-Import Fast-Lane: voll-gecachte Kurse [`ChessableImport.FullyCached`, gesetzt via `IsCourseCachedAsync` beim Anlegen in allen Pfaden — StartImport/Admin/`EnqueueReimportAsync`] laufen in eigener SERIELLER, netzfreier Lane [`ChessableImportFastLaneService`, parallel zur Download-Queue] statt hinter den langsamen Downloads; `RunNextAsync(ct, fastLane)` filtert fast=`FullyCached==true` / download=`!=true` [null→Download, kann nie hängen]; Watchdog download-lane-spezifisch; altes `RunDetached` entfällt; +Migration `AddChessableImportFullyCached`, +8 Tests/1024 grün); 0.193.0 (Trainingsziele-Zeitanzeige gestuft: neue pure `formatDuration(seconds, lang)` in `training-goals.component` + Methoden `durValue`/`durUnit` → < 120 min in Minuten, < 48 h in Stunden, sonst Tage [1 Nachkommastelle, locale-Dezimaltrenner via `Intl.NumberFormat`]; ersetzt `mins()`/`minutes()` in Breakdown-Rows [heute+Periode], Tageshistory-Tabelle und Chessable-Kurs-Summen [Daily-Goal-Paarung „done/target min" bleibt bewusst in Minuten]; neue i18n-Keys `trainingGoals.hours`/`.days` in ALLEN 25 Sprachen; +6 Spec-Cases); 0.192.1 (Admin-Kursdownload zeigt „Bereits in Warteschlange": `ChessableCourseDto.Queued` + `EnrichImportStateAsync` markiert Kurse mit laufendem Import [Status=running], FE-Branch im admin.component.html `@else if (c.queued)`, i18n `admin.courseDl.alreadyQueued` en/de/hr; +1 Test); 0.192.0 (Chessable-Import-Watchdog: neuer `ChessableImportWatchdogService` (BackgroundService) draint hängende Import-Queue ohne API-Neustart. Root-Cause des „eingeschlafen"-Vorfalls: `BackgroundTaskQueue` ist bounded (cap 100, DropOldest) + single-consumer + Abschluss reiht nicht nach → großer Import-Schwung verwirft Tickets, Jobs bleiben `running/queued` liegen. Watchdog prüft periodisch `IsDrainStalledAsync` (queued>0 && kein claimed/fetching/importing) und ruft dann `RunNextAsync` DIREKT (umgeht die bounded Queue); Startverzögerung 1min/Ruhe-Takt 2min/Busy-2s; +6 Tests/1015 grün. Siehe Memory [[prod-chessable-import-stall-restart]]); 0.191.0 (Reprocess-Banner zwei Knöpfe „Alle"/„Aus Cache": `ImportReprocessService.Reprocess{Courses,Repertoires}Async` + beide POST-Endpoints bekamen `localOnly`-Flag [`[FromQuery] bool localOnly`]; localOnly=true überspringt den Chessable-Re-Fetch [`continue`], bereitet nur aus serverseitig gespeicherter Quelle auf [Courses: SourcePgn; Repertoires: Nicht-Chessable-Versions-Mark]; Banner `allCount`=reprocessableLocally+refetchable, `cachedCount`=reprocessableLocally, Cache-Knopf nur wenn cachedCount<allCount; i18n reprocess.updateAll/updateCached/+Tips en/de/hr; +4 Tests); 0.190.1 (Discord-Invite-Link auf den richtigen Server korrigiert: `discord.gg/wczc4BJtMf` statt `nKQCdC7Xff` in `core/community.ts` [Konstante → Navbar/Footer/Mobil-Menü], README + Hilfetext en/de/hr + Changelog-0.187.0-Eintrag; beide Invites resolven aktuell auf Guild „Rookhub", neuer hat kein Ablaufdatum); 0.190.0 (Offline-Auto-Cache: Kurs öffnen (online) lädt das ganze Buch im Hintergrund offline [`autoCacheCourse` in book-puzzle.component, kein manuelles ☁ nötig]; Tagespuzzle wird beim Online-Abruf automatisch gecacht [`saveDailyOffline`/`getDailyOffline`, Key `rookhub_daily_offline`, letzte 14 Tage] + Offline-Read in `loadDaily`; Default-Offline-Pool 10→30 [`DEFAULTS.puzzleCount`]; +4 Specs/621 grün); 0.189.2 (Offline-Fix: `MenuService` cacht die Menü-Sichtbarkeit in localStorage [`rookhub_menu_keys`] + seedt das `visibleSubject` daraus → Flugmodus-Kaltstart zeigt nicht mehr nur Admin+Discord, sondern das zuletzt bekannte Menü; `fetch()`-catchError gibt Cache statt leerem Set); 0.189.1 (Mobile-Navbar-Fix: Sekundär-Icons [Discord/Theme/Sprache] wandern auf Mobil ins Hamburger-Menü `nav-extra`+navMenu, damit Toolbar nicht überläuft & Profil-Icon sichtbar bleibt); 0.189.0 (Repertoire-Trainer/geduldete Züge: `ImportPipeline.CurrentVersion` 2→3 [softFail-`[%alt]` jetzt im piratechess-Export]; Reprocess-Banner auf der Repertoire-Seite bietet für Chessable-Repertoires jetzt einen echten **Re-Fetch** an statt No-op-Versions-Mark — `ImportReprocessService.GetRepertoireStatusAsync`/`ReprocessRepertoiresAsync` melden Chessable-Repertoires [bid aus `ChessableCourseId` ODER Dateiname `chessable-{bid}.pgn`] als `Refetchable` und reihen `EnqueueReimportAsync(..., target:"repertoire", targetRepertoireId)` ein; neuer `ChessableImport.TargetRepertoireId` + Migration → `ImportAsRepertoireAsync` ersetzt das PGN **in-place** im bestehenden Repertoire [Id/Trainings-Fortschritt bleiben], Nicht-Chessable nur Versions-Mark; +3 Tests/1007 grün); 0.188.0 (Discord-Link prominent in der Fußzeile [Discord-Logo via `MatIconRegistry` + Markenfarbe #5865F2, neben Hilfe/Feedback, Konstante `core/community.ts`; Footer nur Desktop, mobil im Nav-Menü]); 0.187.0 (Discord-Community-Link überall eingebunden: Discord-Button in Navbar [eingeloggt + ausgeloggt] + Mobil-Menü [SVG-Icon via `MatIconRegistry`, Link `https://discord.gg/nKQCdC7Xff`, zentrale Konstante `core/community.ts`], Einladungs-Satz im Discord-Abschnitt der Hilfeseite [en/de/hr], `nav.discord`-Tooltip in allen 25 i18n-Sprachen, READMEs aller Repos); 0.186.1 (Chessable-Rate-Limit-Fix); 0.186.0 (Repertoire-Trainer/Spaced Repetition); 0.185.0 (Admin-Benachrichtigung bei Neu-Registrierung: neuer Notification-Typ `new_user_registered` → Glocke aller Admins [`CreateManyAsync`, Link „/admin", Daten `{username}`]; `AuthService` injiziert optional `NotificationService` und benachrichtigt best-effort nach erfolgreichem Register; Icon `group_add`; i18n en/de/hr); 0.184.39 (Offline-Fix: Kurse offline startbar — erstes Puzzle aus lokalem Cache statt Server [offline gespeicherter Kurs via ☁-Knopf, sequenziell/zufällig], Versuche werden gequeued/synchronisiert; Nicht-Admins offline nicht mehr von der Kursseite ausgesperrt); 0.184.38 (Chessable-Diagnose [piratechess-Repo]: `ClassifyBlockedResponse` unterscheidet jetzt abgelaufenen/ungültigen Token [lokal am JWT-`exp`-Claim erkannt → „Bearer neu hinterlegen"] von einem Cloudflare-403-Block bei noch gültigem Token [→ „VPN-Ausgangs-IP gesperrt, IP rotieren/Server wechseln"]; `IsCloudflareBlockPage` erkennt Block-Marker; Hintergrund: M247-IPs [AS9009] werden von Chessable geblockt, Netrouting [AS6206] nicht; piratechess-Commit 32a2f83, +7 Tests/175 grün); 0.184.37 (Chessable-Fix [piratechess-Repo]: HTML-statt-JSON-Antwort [abgelaufener/ungültiger Bearer bzw. Cloudflare-Block/Proxy-Gateway → Chessable liefert eine HTML-Seite] wird in `ChessableHttpService.GetCoursesAsync`/`FetchCourseDataAsync` jetzt sauber erkannt [`LooksLikeHtml`] und als sprechender Token-Hinweis gemeldet, statt den rohen JSON-Parser-Text „'<' is an invalid start of a value" bis in die rookhub-UI durchzureichen; +JsonException-Catch ohne Leak; piratechess-Commit c1cc507, +7 Tests/168 grün); 0.184.36 (Crawler-Härtung [Crawler-Repo]: `ApiKeyMiddleware` fail-closed bei leerem `API_KEY` in Production [503 statt offen], Dev-Fallback + Liveness bleiben; Crawler-Commit 4ca4feb); 0.184.35 (BotStats-Endpoint Replay-Schutz: `GET /api/bot/player-progress` akzeptiert optionalen `X-Bot-Timestamp` [±300 s, HMAC über `<ts>.<discordId>`]; rückwärtskompatibel zur alten body-only-Signatur; Gegenstück zu Bot v2.73.0); 0.184.34 (Webhook-Timestamp-Replay-Schutz auf der rookhub-Sendeseite: alle drei Bot-Webhooks [Tagespuzzle/Wochenpost/Daily-Regenerate] signieren jetzt zusätzlich einen Zeitstempel [`X-Webhook-Timestamp`, HMAC über `<ts>.<body>`], ±300 s; Gegenstück zu Bot v2.70.0, rückwärtskompatibel); 0.184.33 (Crawler-Robustheit [Crawler-Repo]: Freilos/spielfrei nur informativ statt Warnung, defensives Response-Größenlimit `Crawler:MaxResponseBytes`, Hidden-Field-Parsing Regex→AngleSharp, Player-/Team-Upsert in DB-Transaktion; Crawler-Commits c518e74/cf6b5a9/7522f3a/bc59f31); 0.184.32 (A11y tournament-favoriten: Favoriten-Sterne [Spieler+Team, Tabelle+Mobil-Karte] in tournament-detail+public-tournament tastaturbedienbar — `role=button`/`tabindex=0`/`keydown.enter`+`space`/`aria-label`+`aria-pressed`/`:focus-visible`; i18n `tournaments.favorites.toggleAria` en/de/hr); 0.184.31 (BasePuzzleSolver-Dedup: `formatTime` → gemeinsame `puzzle-format.util.ts` [`formatPuzzleTime`], und Einzel-Stoppuhr-Timer [`elapsedSeconds`/`stopwatch`/`startTimer`/`stopTimer`] aus puzzle+book in die Basis hochgezogen; Endless erbt `elapsedSeconds`+`formatTime`, behält seine Doppel-Stoppuhren; +2 Specs, Verhalten unverändert); 0.184.30 (OnPush für 4 weitere präsentationale Puzzle-Karten: puzzle-your-turn/-status-card/-rating-card/viz-card — alle nehmen nur primitive Inputs [Eltern rebinden je CD] + EventEmitter-Outputs, in-place-Mutation geprüft = keine; +Spec); 0.184.29 (Admin-Kleinkram: chessable-Bookmarklet-`bypassSecurityTrustUrl` mit Origin-Guard+Sicherheits-Kommentar [Code rein app-konstruiert]; Admin-Mitglieder-Dropdown warnt bei `totalCount > 500` statt still abzuschneiden; `availableUsers()` war bereits memoisiert [v0.184.19]; +2 Specs); 0.184.28 (chessable.component: `activeImports`-Zeilen cachen ihr `queueLabelText` jetzt einmal je Update [`setActiveImport`] statt `queueLabel(imp)`-`translate.instant` je CD-Zyklus während des Pollings; +2 Specs); 0.184.27 (Anon-Session-IDOR-Härtung: `ValidationConstants.SessionIdPattern` Mindestlänge 1→32 [UUID-Form 32–36], erratbare Kurz-Ids können fremde anonyme Puzzle-/Endless-Stats nicht mehr claimen/überschreiben; Clients nutzen ohnehin `crypto.randomUUID()` → rückwärtskompatibel); 0.184.26 (FriendService.SearchUsersAsync: Identitäts-/Konto-Felder [Username/chess.com/Lichess/FIDE/ChessResults] präfix-anker [`StartsWith`, Username-Index nutzbar], nur DisplayName bleibt Teilstring; Länge+Take service-seitig hart gekappt); 0.184.25 (Chessable-Import: atomarer Claim beim Job-Picking via `ExecuteUpdate` „queued"→„claimed", InMemory-Re-Check-Fallback → keine Doppelverarbeitung bei Resume-Sturm/Skalierung); 0.184.24 (Schnellstart-Popup nach Register erklärt jetzt die Puzzle-Modi statt Turnier-Tipps: Zufalls-Puzzle/Endlos/Tagespuzzle/Wochenpost; `app.qs.*`-Keys in de/en/hr ersetzt, Icons 🎲/♾/📅/📰); 0.184.23 (Crawler-Robustheit gegen Redeploy/VPN-Aussetzer [Crawler-Repo]: `VpnReadinessGate` wartet vor dem ersten Crawl auf den wiederhergestellten gluetun-Tunnel [`Gluetun__WaitForReady=true` in beiden VPN-Compose-Dateien], und `ExecuteCrawlAsync` versucht reine Verbindungsfehler [`IsTransientConnectionError`, z. B. „Resource temporarily unavailable"] mit gestuftem Backoff erneut statt sofort `Failed`; Retry-Parameter via `Crawler:CrawlMaxAttempts`/`RetryDelayMs`/`CrawlRetryBackoffSeconds` konfigurierbar; behebt die Fehler-Häufung direkt nach Deploys); 0.184.22 (Frontend Service-Layer-Extraktion: `FriendsService`/`PublicTournamentService`/`ProfileService` + `RepertoireService`-Erweiterung, 9 Komponenten ohne direkten `HttpClient`; `AuthService.changePassword`; OnPush für review-nav/promotion-picker; +5 Service-Specs); 0.184.21 (Crawler-Härtung: `/api/health/ip` API-Key-pflichtig + Phantom-Runden-Clamp gegen fremde `rd=`-Links; Crawler-Repo-Commits f5071aa/052007b); 0.184.14–0.184.20 (TODO-Abarbeitung Runde 3: DataProtection-Keys konfigurierbar/anlegen/SetApplicationName · In-App-Benachrichtigung bei neuer Turnierrunde [`NotificationType.TournamentNewRound`] · Service-Extraktion Repertoire/Tournament-List/Dashboard [+Specs] · Admin-Tab in URL [`?tab=`] + availableUsers gecacht + admin.component-Spec · A11y puzzle-tags/repertoire-tree/-lines + OnPush für präsentationale Komponenten); 0.184.9–0.184.13 (Runde 2: JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source) JWT-Invalidierung bei PW-Reset/-Änderung via `AppUser.SecurityStamp`+`sstamp`-Claim+Migration · Kapitel-Spoiler-Stripping für Puzzle-Bücher [ImportPipeline.CurrentVersion 1→2] · Specs für menu/preferences/chessable/admin-Service + profile.component · api-tokens-Subscribes abgeflacht · A11y Theme-Chips+Endless-Verlaufskarten tastaturbedienbar); 0.184.1–0.184.8 (Runde 1, 10 Punkte: JWT-ClockSkew 1 min · Reset-Link-Logging nur in Dev · ApiToken-LastUsedAt-Drossel · Impersonation-Guard für destruktive Aktionen · Challenge-„gelöst" serverseitig bestätigt · Retry-Interceptor Exponential-Backoff · Glocken-Badge-Flackern · Chessable-Label-Caching · dlImport-paused-Polling · loadAllUsers/acceptDisclaimer-Politur); 0.184.0 (Logging/Observability: ECS-`LogTags` an client-log [`clientlog`/`engine`] + Chessable-Import-Lifecycle [`import,chessable`] für Kibana-Filter); 0.183.0 (Endless: Themen-Schnellauswahl/Preset-Chips `puzzle-theme-presets.ts`, Klick setzt `config.themes`-Bündel, ODER-Filter); 0.182.0 (Puzzle-Lösezeit zählt nur bei aktivem Tab: `VisibilityStopwatch` pausiert bei verstecktem Tab; alle 3 Solver + Endless-Session-Timer; 5-Min-`LongSolveService`-Nachfrage bleibt); 0.181.5 (Build-Fix Buchtitel-Anzeige); 0.181.3/4 (Tages-/Kurs-Buchtitel + on-the-fly „dumme Tipps"-Flag); vollständiger Verlauf ausschließlich in `src/frontend/app/src/environments/changelog.ts` (Single Source)
- `environment.ts` (dev) UND `environment.prod.ts` (prod-Build via fileReplacements) importieren beide aus `changelog.ts` — Footer zeigt in jedem Build dieselbe Version. **Nur `changelog.ts` editieren**, nie die Environment-Dateien
- Angezeigt im Footer der Desktop-Version (Klick öffnet Changelog-Overlay)
- **Jeder Fix/jedes Feature MUSS die Version erhöhen**: Patch für Fixes (0.0.x), Minor für Features (0.x.0)
- **Changelog pflegen**: Jeden Eintrag im `CHANGELOG`-Array in `changelog.ts` vermerken (Version, Datum, Liste der Änderungen). **Jeder Änderungstext gehört ZWEISPRACHIG hin** — pro Eintrag `changes: { en, de }[]` (Englisch = Default/Fallback, Deutsch). Der Footer zeigt die Variante der aktiven UI-Sprache (`changeText()` in `app.component`; `hr` fällt auf `en` zurück). Neue Einträge also IMMER mit `en` UND `de` anlegen, nicht nur eine Sprache
- **Gilt auch für Änderungen im Crawler-Repo** (`C:/git/chessresults_crawler`): Features/Fixes dort müssen ebenfalls hier Version + Changelog erhöhen und committet werden
- **Parallel-Arbeit**: Wegen der zwei Stack-Kopien (siehe Lock-Block oben) können Versionssprünge nicht-monoton wirken — beim Commit immer den **aktuellen** `APP_VERSION`-Wert aus `changelog.ts` als Basis nehmen, nicht den Commit-Subject-Wert

### Checkliste vor JEDEM Commit (beide Projekte)
1. [ ] Tests vorhanden für die Änderung?
2. [ ] `APP_VERSION` + `CHANGELOG`-Eintrag in `src/frontend/app/src/environments/changelog.ts` aktualisiert? (gilt automatisch für dev + prod-Build)
3. [ ] `Aktuelle Version` in diesem Abschnitt angepasst?
4. [ ] Versionsänderung committet?
5. [ ] **Nach jedem Commit dem User die aktuelle Version mitteilen** (z.B. "Version: 0.95.2")

**NIEMALS committen ohne diese Checkliste abzuarbeiten.** Auch reine Test- oder Doku-Änderungen erhöhen die Patch-Version.

## Screenshots

- Screenshots liegen in `C:/git/screenshot/` (z.B. `Screenshot.jpg`)
- Diesen Pfad nutzen um visuelle Prüfungen durchzuführen

## Wichtige Konventionen

- **Import-/Aufbereitungs-Pipeline versionieren** – Ändert sich die Transformation Roh-PGN → gespeicherte `BookPuzzles` (bzw. abgeleitete Repertoire-Daten) so, dass BEREITS importierte Datensätze unvollständig/veraltet werden (Beispiel: nachträgliche Pro-Zug-Kommentar-Extraktion), MUSS `ImportPipeline.CurrentVersion` (in `Services/ImportPipeline.cs`) um 1 erhöht und die Versionshistorie im Doc-Kommentar ergänzt werden. Bücher/Repertoires mit kleinerer `ImportVersion` gelten dann als „veraltet" und werden über den „Aktualisieren (N)"-Knopf (Sektion Kurse/Repertoires, `ReprocessBannerComponent` → `/api/courses|repertoires/reprocess`) neu aufbereitet — **in-place per LineId** (Fortschritt/Statistik-FKs bleiben erhalten), Quelle ist `Book.SourcePgn` (bzw. Chessable-Re-Fetch). `ImportFileAsync` aktualisiert bestehende Linien NUR, wenn das Buch veraltet ist; sonst überspringt es sie (idempotenter Resume).
- **Puzzle-Modi konsistent halten** – Standard (`puzzle.component`), Endless (`endless-puzzle.component`) und Book/Course/Weekly/Daily (`book-puzzle.component` – ist selbst schon Mehr-Modus-Template) sollen optisch + funktional so ähnlich wie möglich bleiben. Wenn ein Modus eine UI-/UX-Erweiterung bekommt (z. B. „Tags ausklappbar", „Eval-Button", „Viz-Pfeil"), **immer kurz nachfragen**, ob das nicht auch in den anderen zwei Modi sinnvoll wäre. Gemeinsame Bausteine in dedizierte Komponenten (`PuzzleTagsComponent`, `VizCardComponent`, `ReviewNavComponent`, `ThemePickerComponent`) auslagern statt 3-fach kopieren; die Solver-Mechanik liegt in `BasePuzzleSolver`.
- **Keine Default-Werte in Compose-Example-Dateien** – `compose.yml.example` und `compose.vpn.example` verwenden `${VAR}` ohne `:-default`. Alle Werte müssen explizit in der `.env`-Datei gesetzt werden.
- **i18n-Validierung**: Nach jeder Änderung an `src/frontend/app/src/assets/i18n/*.json` alle 25 Sprachdateien mit `JSON.parse` validieren — Trailing-Comma-Fehler bricht ngx-translate komplett, UI zeigt dann nur noch Schlüssel statt Texte
- **Literal-Routen vor Parameter-Routen**: z.B. `GET /api/weekly-posts/progress` MUSS vor `GET /api/weekly-posts/{id}` deklariert sein, sonst matcht der Router „progress" als ID
- Crawler-Proxy-Endpoints müssen mit tatsächlichen Crawler-Routen übereinstimmen
- Angular nutzt lazy-loaded standalone components (kein NgModule)
- JWT-Claims: `ClaimTypes.NameIdentifier` = UserId, `ClaimTypes.Name` = Username
- PGN-Upload-Limit: 10 MB pro Datei (in `RepertoireService`)
- Alle Controller holen UserId via `User.FindFirstValue(ClaimTypes.NameIdentifier)`
- Friendship-Status ist eine State Machine: Pending → Accepted/Declined; nur der Addressee kann Accept/Decline ausführen
- Stockfish-WASM **NICHT** über Service-Worker cachen außer in eigener assetGroup `engine` (installMode prefetch) — der Glue muss bei `instantiateStreaming`-Fehler auf `instantiate(arrayBuffer)` zurückfallen, sonst hängt die Analyse
- HMAC-Webhooks zum Bot: gleiches Secret-Pattern (`SchachBot:WebhookSecret` für Tagespuzzle/Wochenpost, `SchachBot:StatsSecret` für Bot-Stats-Pull) — `ComputeHmacHex` aus `SchachBotWebhookService` wiederverwenden
